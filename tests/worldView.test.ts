import { readFileSync } from "node:fs";
import { resolve } from "node:path";
import { describe, expect, test } from "vitest";
import {
  MARKER_PALETTE,
  MARKER_RING_OPACITY,
  muteBadgeY,
  possessedBadgeY,
} from "../src/render/markers";
import {
  HOSTILE_COLOR,
  PLAYER_COLOR,
  PLAYER_HEAD_COLOR,
  PLAYER_HEAD_EMISSIVE,
  POSSESSED_COLOR,
  POSSESSED_HEAD_COLOR,
  POSSESSED_HEAD_EMISSIVE,
  WARM_LIGHT_AMBER_B,
  WARM_LIGHT_AMBER_B_GAIN,
  WARM_LIGHT_AMBER_G,
  WARM_LIGHT_AMBER_G_GAIN,
  WARM_LIGHT_COLOR,
  WARM_LIGHT_DISTANCE_BASE,
  WARM_LIGHT_DISTANCE_GAIN,
  WARM_LIGHT_INTENSITY_MUL,
  WARM_LIGHT_DECAY,
  WARM_LIGHT_VISIBLE_EPS,
  WARM_LIGHT_Y,
  MUZZLE_FLASH_COLOR,
  MUZZLE_LIGHT_COLOR,
  MUZZLE_LIGHT_DECAY,
  IMPACT_SPARK_COLOR,
  IMPACT_SPARK_LIGHT_COLOR,
  IMPACT_SPARK_LIGHT_DECAY,
  TRACER_COLOR,
  TRACER_FLASH_COLOR,
  TRACER_FLASH_DECAY,
  TRACER_FLASH_DISTANCE,
  TRACER_FLASH_INTENSITY,
  TRACER_FLASH_Y_OFFSET,
  FLASHLIGHT_FILL_COLOR,
  FLASHLIGHT_FILL_DECAY,
  FLASHLIGHT_FILL_DISTANCE_BASE,
  FLASHLIGHT_FILL_DISTANCE_GAIN,
  FLASHLIGHT_FILL_Y,
  FLASHLIGHT_SPOT_DECAY,
  FLASHLIGHT_SPOT_Y,
  FLASHLIGHT_SPOT_DISTANCE_EXTRA,
  FLASHLIGHT_SPOT_DISTANCE_GAIN,
  FLASHLIGHT_SPOT_TARGET_Y,
  MUZZLE_FORWARD,
  CHEVRON_Y,
  CHEVRON_TILT,
  NOISE_RING_Y,
  MARKER_RING_Y,
  MARKER_ICON_Y,
  MARKER_ICON_SIZE,
  MARKER_BADGE_RADIUS,
  MARKER_BADGE_TILT,
  PLAYER_BODY_BASE_Y,
  PLAYER_HEAD_BASE_Y,
  HOSTILE_BODY_BASE_Y,
  HOSTILE_HEAD_BASE_Y,
  HOSTILE_MUTE_XZ_SCALE,
} from "../src/render/worldView";

describe("worldView player head mesh", () => {
  test("player head emissive 0x102030 × 1.15/canal → 0x122537; intensity/palette/badge-Y iguales", () => {
    expect(PLAYER_HEAD_EMISSIVE).toBe(0x122537);
    const r = (PLAYER_HEAD_EMISSIVE >> 16) & 0xff;
    const g = (PLAYER_HEAD_EMISSIVE >> 8) & 0xff;
    const b = PLAYER_HEAD_EMISSIVE & 0xff;
    expect(r).toBe(0x12);
    expect(g).toBe(0x25);
    expect(b).toBe(0x37);
    expect(Math.round((0x10 * 115) / 100)).toBe(r);
    expect(Math.round((0x20 * 115) / 100)).toBe(g);
    expect(Math.round((0x30 * 115) / 100)).toBe(b);
    const src = readFileSync(
      resolve(process.cwd(), "src/render/worldView.ts"),
      "utf8",
    );
    expect(src).toContain("emissive: PLAYER_HEAD_EMISSIVE");
    expect(src).toContain("color: PLAYER_HEAD_COLOR");
    expect(src).toContain("emissiveIntensity: 0.22");
    expect(src).not.toMatch(/emissive:\s*0x102030/);
    expect(MARKER_PALETTE.player.emissive).toBe(0x1e4a6e);
    expect(MARKER_PALETTE.player.badge).toBe(0x91d1ff);
    expect(MARKER_PALETTE.player.ring).toBe(0x4392f4);
    expect(MARKER_RING_OPACITY).toBe(0.6877);
    expect(muteBadgeY).toBe(2.3);
    expect(possessedBadgeY).toBe(2.3);
  });

  test("player head color 0x7eb6ef × 1.15/canal (b clamp) → 0x91d1ff; emissive/intensity/palette/badge-Y iguales", () => {
    expect(PLAYER_HEAD_COLOR).toBe(0x91d1ff);
    const r = (PLAYER_HEAD_COLOR >> 16) & 0xff;
    const g = (PLAYER_HEAD_COLOR >> 8) & 0xff;
    const b = PLAYER_HEAD_COLOR & 0xff;
    expect(r).toBe(0x91);
    expect(g).toBe(0xd1);
    expect(b).toBe(0xff);
    expect(Math.round((0x7e * 115) / 100)).toBe(r);
    expect(Math.round((0xb6 * 115) / 100)).toBe(g);
    expect(Math.min(0xff, Math.round((0xef * 115) / 100))).toBe(b);
    const src = readFileSync(
      resolve(process.cwd(), "src/render/worldView.ts"),
      "utf8",
    );
    expect(src).toContain("color: PLAYER_HEAD_COLOR");
    expect(src).toContain("emissive: PLAYER_HEAD_EMISSIVE");
    expect(src).toContain("emissiveIntensity: 0.22");
    expect(src).not.toMatch(/color:\s*0x7eb6ef/);
    expect(PLAYER_HEAD_EMISSIVE).toBe(0x122537);
    expect(MARKER_PALETTE.player.badge).toBe(0x91d1ff);
    expect(MARKER_PALETTE.player.emissive).toBe(0x1e4a6e);
    expect(MARKER_PALETTE.player.ring).toBe(0x4392f4);
    expect(MARKER_RING_OPACITY).toBe(0.6877);
    expect(muteBadgeY).toBe(2.3);
    expect(possessedBadgeY).toBe(2.3);
  });
});

describe("worldView player body mesh", () => {
  test("player body color 0x4a8fd4 × 1.15/canal → 0x55a4f4; head/emissive/intensity/palette/badge-Y iguales", () => {
    expect(PLAYER_COLOR).toBe(0x55a4f4);
    const r = (PLAYER_COLOR >> 16) & 0xff;
    const g = (PLAYER_COLOR >> 8) & 0xff;
    const b = PLAYER_COLOR & 0xff;
    expect(r).toBe(0x55);
    expect(g).toBe(0xa4);
    expect(b).toBe(0xf4);
    expect(Math.round((0x4a * 115) / 100)).toBe(r);
    expect(Math.round((0x8f * 115) / 100)).toBe(g);
    expect(Math.round((0xd4 * 115) / 100)).toBe(b);
    const src = readFileSync(
      resolve(process.cwd(), "src/render/worldView.ts"),
      "utf8",
    );
    expect(src).toContain("color: PLAYER_COLOR");
    expect(src).toContain("color: PLAYER_HEAD_COLOR");
    expect(src).toContain("emissive: PLAYER_HEAD_EMISSIVE");
    expect(src).toContain("emissiveIntensity: 0.22");
    expect(src).not.toMatch(/color:\s*0x4a8fd4/);
    expect(PLAYER_HEAD_COLOR).toBe(0x91d1ff);
    expect(PLAYER_HEAD_EMISSIVE).toBe(0x122537);
    expect(MARKER_PALETTE.player.badge).toBe(0x91d1ff);
    expect(MARKER_PALETTE.player.emissive).toBe(0x1e4a6e);
    expect(MARKER_PALETTE.player.ring).toBe(0x4392f4);
    expect(MARKER_RING_OPACITY).toBe(0.6877);
    expect(muteBadgeY).toBe(2.3);
    expect(possessedBadgeY).toBe(2.3);
  });
});

describe("worldView mute/hostile fallback body mesh", () => {
  test("hostile body color 0x6b1a1a × 1.15/canal → 0x7b1e1e; player/head/emissive/intensity/palette/badge-Y iguales", () => {
    expect(HOSTILE_COLOR).toBe(0x7b1e1e);
    const r = (HOSTILE_COLOR >> 16) & 0xff;
    const g = (HOSTILE_COLOR >> 8) & 0xff;
    const b = HOSTILE_COLOR & 0xff;
    expect(r).toBe(0x7b);
    expect(g).toBe(0x1e);
    expect(b).toBe(0x1e);
    expect(Math.round((0x6b * 115) / 100)).toBe(r);
    expect(Math.round((0x1a * 115) / 100)).toBe(g);
    expect(Math.round((0x1a * 115) / 100)).toBe(b);
    const src = readFileSync(
      resolve(process.cwd(), "src/render/worldView.ts"),
      "utf8",
    );
    expect(src).toContain("color: HOSTILE_COLOR");
    expect(src).toContain("color: PLAYER_COLOR");
    expect(src).toContain("color: PLAYER_HEAD_COLOR");
    expect(src).toContain("emissive: PLAYER_HEAD_EMISSIVE");
    expect(src).toContain("emissiveIntensity: 0.22");
    expect(src).not.toMatch(/color:\s*0x6b1a1a/);
    expect(src).toContain("const POSSESSED_COLOR = 0x68347b");
    expect(PLAYER_COLOR).toBe(0x55a4f4);
    expect(PLAYER_HEAD_COLOR).toBe(0x91d1ff);
    expect(PLAYER_HEAD_EMISSIVE).toBe(0x122537);
    expect(MARKER_PALETTE.player.badge).toBe(0x91d1ff);
    expect(MARKER_PALETTE.player.emissive).toBe(0x1e4a6e);
    expect(MARKER_PALETTE.player.ring).toBe(0x4392f4);
    expect(MARKER_RING_OPACITY).toBe(0.6877);
    expect(muteBadgeY).toBe(2.3);
    expect(possessedBadgeY).toBe(2.3);
  });
});

describe("worldView possessed fallback body mesh", () => {
  test("possessed body color 0x5a2d6b × 1.15/canal → 0x68347b; hostile/player/head/emissive/intensity/palette/badge-Y iguales", () => {
    expect(POSSESSED_COLOR).toBe(0x68347b);
    const r = (POSSESSED_COLOR >> 16) & 0xff;
    const g = (POSSESSED_COLOR >> 8) & 0xff;
    const b = POSSESSED_COLOR & 0xff;
    expect(r).toBe(0x68);
    expect(g).toBe(0x34);
    expect(b).toBe(0x7b);
    expect(Math.round((0x5a * 115) / 100)).toBe(r);
    expect(Math.round((0x2d * 115) / 100)).toBe(g);
    expect(Math.round((0x6b * 115) / 100)).toBe(b);
    const src = readFileSync(
      resolve(process.cwd(), "src/render/worldView.ts"),
      "utf8",
    );
    expect(src).toContain("color: POSSESSED_COLOR");
    expect(src).toContain("emissive: POSSESSED_EMISSIVE");
    expect(src).toContain("color: HOSTILE_COLOR");
    expect(src).toContain("color: PLAYER_COLOR");
    expect(src).toContain("color: PLAYER_HEAD_COLOR");
    expect(src).toContain("emissive: PLAYER_HEAD_EMISSIVE");
    expect(src).toContain("emissiveIntensity: 0.22");
    expect(src).not.toMatch(/color:\s*0x5a2d6b/);
    expect(src).toContain("const POSSESSED_EMISSIVE = 0x1e0925");
    expect(src).toContain("color: POSSESSED_HEAD_COLOR");
    expect(src).toContain("emissive: POSSESSED_HEAD_EMISSIVE");
    expect(POSSESSED_HEAD_COLOR).toBe(0x8c469f);
    expect(POSSESSED_HEAD_EMISSIVE).toBe(0x30124a);
    expect(HOSTILE_COLOR).toBe(0x7b1e1e);
    expect(PLAYER_COLOR).toBe(0x55a4f4);
    expect(PLAYER_HEAD_COLOR).toBe(0x91d1ff);
    expect(PLAYER_HEAD_EMISSIVE).toBe(0x122537);
    expect(MARKER_PALETTE.player.badge).toBe(0x91d1ff);
    expect(MARKER_PALETTE.player.emissive).toBe(0x1e4a6e);
    expect(MARKER_PALETTE.player.ring).toBe(0x4392f4);
    expect(MARKER_RING_OPACITY).toBe(0.6877);
    expect(muteBadgeY).toBe(2.3);
    expect(possessedBadgeY).toBe(2.3);
  });
});

describe("worldView possessed fallback body emissive", () => {
  test("possessed body emissive 0x1a0820 × 1.15/canal → 0x1e0925; color/hostile/player/head/intensity/palette/badge-Y iguales", () => {
    const src = readFileSync(
      resolve(process.cwd(), "src/render/worldView.ts"),
      "utf8",
    );
    expect(src).toContain("const POSSESSED_EMISSIVE = 0x1e0925");
    expect(src).toContain("emissive: POSSESSED_EMISSIVE");
    expect(src).toContain("color: POSSESSED_COLOR");
    expect(src).toContain("color: POSSESSED_HEAD_COLOR");
    expect(src).toContain("emissive: POSSESSED_HEAD_EMISSIVE");
    expect(src).not.toMatch(/const POSSESSED_EMISSIVE = 0x1a0820/);
    const r = 0x1e;
    const g = 0x09;
    const b = 0x25;
    expect(Math.round((0x1a * 115) / 100)).toBe(r);
    expect(Math.round((0x08 * 115) / 100)).toBe(g);
    expect(Math.round((0x20 * 115) / 100)).toBe(b);
    expect(src).toContain("emissiveIntensity: 0.22");
    expect(src).toContain("color: HOSTILE_COLOR");
    expect(src).toContain("color: PLAYER_COLOR");
    expect(src).toContain("color: PLAYER_HEAD_COLOR");
    expect(src).toContain("emissive: PLAYER_HEAD_EMISSIVE");
    expect(POSSESSED_COLOR).toBe(0x68347b);
    expect(POSSESSED_HEAD_COLOR).toBe(0x8c469f);
    expect(POSSESSED_HEAD_EMISSIVE).toBe(0x30124a);
    expect(HOSTILE_COLOR).toBe(0x7b1e1e);
    expect(PLAYER_COLOR).toBe(0x55a4f4);
    expect(PLAYER_HEAD_COLOR).toBe(0x91d1ff);
    expect(PLAYER_HEAD_EMISSIVE).toBe(0x122537);
    expect(MARKER_PALETTE.player.badge).toBe(0x91d1ff);
    expect(MARKER_PALETTE.player.emissive).toBe(0x1e4a6e);
    expect(MARKER_PALETTE.player.ring).toBe(0x4392f4);
    expect(MARKER_RING_OPACITY).toBe(0.6877);
    expect(muteBadgeY).toBe(2.3);
    expect(possessedBadgeY).toBe(2.3);
  });
});

describe("worldView possessed fallback head mesh", () => {
  test("possessed head color 0x7a3d8a × 1.15/canal → 0x8c469f; emissive/color/hostile/player/intensity/palette/badge-Y iguales", () => {
    expect(POSSESSED_HEAD_COLOR).toBe(0x8c469f);
    const r = (POSSESSED_HEAD_COLOR >> 16) & 0xff;
    const g = (POSSESSED_HEAD_COLOR >> 8) & 0xff;
    const b = POSSESSED_HEAD_COLOR & 0xff;
    expect(r).toBe(0x8c);
    expect(g).toBe(0x46);
    expect(b).toBe(0x9f);
    expect(Math.round((0x7a * 115) / 100)).toBe(r);
    expect(Math.round((0x3d * 115) / 100)).toBe(g);
    expect(Math.round((0x8a * 115) / 100)).toBe(b);
    const src = readFileSync(
      resolve(process.cwd(), "src/render/worldView.ts"),
      "utf8",
    );
    expect(src).toContain("export const POSSESSED_HEAD_COLOR = 0x8c469f");
    expect(src).toContain("color: POSSESSED_HEAD_COLOR");
    expect(src).toContain("emissive: POSSESSED_HEAD_EMISSIVE");
    expect(src).toContain("export const POSSESSED_HEAD_EMISSIVE = 0x30124a");
    expect(src).toContain("emissive: POSSESSED_EMISSIVE");
    expect(src).toContain("color: POSSESSED_COLOR");
    expect(src).toContain("const POSSESSED_EMISSIVE = 0x1e0925");
    expect(src).not.toMatch(/color:\s*0x7a3d8a/);
    expect(src).toContain("emissiveIntensity: 0.22");
    expect(src).toContain("color: HOSTILE_COLOR");
    expect(src).toContain("color: PLAYER_COLOR");
    expect(src).toContain("color: PLAYER_HEAD_COLOR");
    expect(src).toContain("emissive: PLAYER_HEAD_EMISSIVE");
    expect(POSSESSED_HEAD_EMISSIVE).toBe(0x30124a);
    expect(POSSESSED_COLOR).toBe(0x68347b);
    expect(HOSTILE_COLOR).toBe(0x7b1e1e);
    expect(PLAYER_COLOR).toBe(0x55a4f4);
    expect(PLAYER_HEAD_COLOR).toBe(0x91d1ff);
    expect(PLAYER_HEAD_EMISSIVE).toBe(0x122537);
    expect(MARKER_PALETTE.player.badge).toBe(0x91d1ff);
    expect(MARKER_PALETTE.player.emissive).toBe(0x1e4a6e);
    expect(MARKER_PALETTE.player.ring).toBe(0x4392f4);
    expect(MARKER_RING_OPACITY).toBe(0.6877);
    expect(muteBadgeY).toBe(2.3);
    expect(possessedBadgeY).toBe(2.3);
  });
});

describe("worldView possessed fallback head emissive", () => {
  test("possessed head emissive 0x2a1040 × 1.15/canal → 0x30124a; color/hostile/player/intensity/palette/badge-Y iguales", () => {
    expect(POSSESSED_HEAD_EMISSIVE).toBe(0x30124a);
    const r = (POSSESSED_HEAD_EMISSIVE >> 16) & 0xff;
    const g = (POSSESSED_HEAD_EMISSIVE >> 8) & 0xff;
    const b = POSSESSED_HEAD_EMISSIVE & 0xff;
    expect(r).toBe(0x30);
    expect(g).toBe(0x12);
    expect(b).toBe(0x4a);
    expect(Math.round((0x2a * 115) / 100)).toBe(r);
    expect(Math.round((0x10 * 115) / 100)).toBe(g);
    expect(Math.round((0x40 * 115) / 100)).toBe(b);
    const src = readFileSync(
      resolve(process.cwd(), "src/render/worldView.ts"),
      "utf8",
    );
    expect(src).toContain("export const POSSESSED_HEAD_EMISSIVE = 0x30124a");
    expect(src).toContain("emissive: POSSESSED_HEAD_EMISSIVE");
    expect(src).toContain("color: POSSESSED_HEAD_COLOR");
    expect(src).toContain("export const POSSESSED_HEAD_COLOR = 0x8c469f");
    expect(src).toContain("emissive: POSSESSED_EMISSIVE");
    expect(src).toContain("color: POSSESSED_COLOR");
    expect(src).toContain("const POSSESSED_EMISSIVE = 0x1e0925");
    expect(src).not.toMatch(/emissive:\s*0x2a1040/);
    expect(src).toContain("export const WARM_LIGHT_COLOR = 0xffca81");
    expect(src).toContain("new THREE.PointLight(WARM_LIGHT_COLOR, 0, 7.5, WARM_LIGHT_DECAY)");
    expect(src).toContain("emissiveIntensity: 0.22");
    expect(src).toContain("color: HOSTILE_COLOR");
    expect(src).toContain("color: PLAYER_COLOR");
    expect(src).toContain("color: PLAYER_HEAD_COLOR");
    expect(src).toContain("emissive: PLAYER_HEAD_EMISSIVE");
    expect(POSSESSED_HEAD_COLOR).toBe(0x8c469f);
    expect(POSSESSED_COLOR).toBe(0x68347b);
    expect(HOSTILE_COLOR).toBe(0x7b1e1e);
    expect(PLAYER_COLOR).toBe(0x55a4f4);
    expect(PLAYER_HEAD_COLOR).toBe(0x91d1ff);
    expect(PLAYER_HEAD_EMISSIVE).toBe(0x122537);
    expect(MARKER_PALETTE.possessed.emissive).toBe(0x30124a);
    expect(MARKER_PALETTE.player.badge).toBe(0x91d1ff);
    expect(MARKER_PALETTE.player.emissive).toBe(0x1e4a6e);
    expect(MARKER_PALETTE.player.ring).toBe(0x4392f4);
    expect(MARKER_RING_OPACITY).toBe(0.6877);
    expect(muteBadgeY).toBe(2.3);
    expect(possessedBadgeY).toBe(2.3);
  });
});

describe("worldView indoor night warm light", () => {
  test("warm light 0xffb070 × 1.15/canal (r clamp) → 0xffca81; possessed/hostile/player/intensity/palette/badge-Y iguales", () => {
    expect(WARM_LIGHT_COLOR).toBe(0xffca81);
    const r = (WARM_LIGHT_COLOR >> 16) & 0xff;
    const g = (WARM_LIGHT_COLOR >> 8) & 0xff;
    const b = WARM_LIGHT_COLOR & 0xff;
    expect(r).toBe(0xff);
    expect(g).toBe(0xca);
    expect(b).toBe(0x81);
    expect(Math.min(0xff, Math.round((0xff * 115) / 100))).toBe(r);
    expect(Math.round((0xb0 * 115) / 100)).toBe(g);
    expect(Math.round((0x70 * 115) / 100)).toBe(b);
    const src = readFileSync(
      resolve(process.cwd(), "src/render/worldView.ts"),
      "utf8",
    );
    expect(src).toContain("export const WARM_LIGHT_COLOR = 0xffca81");
    expect(src).toContain("new THREE.PointLight(WARM_LIGHT_COLOR, 0, 7.5, WARM_LIGHT_DECAY)");
    expect(src).not.toMatch(/PointLight\(0xffb070/);
    expect(src).toContain("export const MUZZLE_FLASH_COLOR = 0xffffdd");
    expect(src).toContain("color: MUZZLE_FLASH_COLOR");
    expect(src).toContain("export const MUZZLE_LIGHT_COLOR = 0xffffb8");
    expect(src).toContain("new THREE.PointLight(\n    MUZZLE_LIGHT_COLOR,");
    expect(src).toContain("export const POSSESSED_HEAD_EMISSIVE = 0x30124a");
    expect(src).toContain("emissive: POSSESSED_HEAD_EMISSIVE");
    expect(src).toContain("color: POSSESSED_HEAD_COLOR");
    expect(src).toContain("export const POSSESSED_HEAD_COLOR = 0x8c469f");
    expect(src).toContain("emissive: POSSESSED_EMISSIVE");
    expect(src).toContain("color: POSSESSED_COLOR");
    expect(src).toContain("const POSSESSED_EMISSIVE = 0x1e0925");
    expect(src).toContain("emissiveIntensity: 0.22");
    expect(src).toContain("color: HOSTILE_COLOR");
    expect(src).toContain("color: PLAYER_COLOR");
    expect(src).toContain("color: PLAYER_HEAD_COLOR");
    expect(src).toContain("emissive: PLAYER_HEAD_EMISSIVE");
    expect(POSSESSED_HEAD_EMISSIVE).toBe(0x30124a);
    expect(POSSESSED_HEAD_COLOR).toBe(0x8c469f);
    expect(POSSESSED_COLOR).toBe(0x68347b);
    expect(HOSTILE_COLOR).toBe(0x7b1e1e);
    expect(PLAYER_COLOR).toBe(0x55a4f4);
    expect(PLAYER_HEAD_COLOR).toBe(0x91d1ff);
    expect(PLAYER_HEAD_EMISSIVE).toBe(0x122537);
    expect(MARKER_PALETTE.possessed.emissive).toBe(0x30124a);
    expect(MARKER_PALETTE.player.badge).toBe(0x91d1ff);
    expect(MARKER_PALETTE.player.emissive).toBe(0x1e4a6e);
    expect(MARKER_PALETTE.player.ring).toBe(0x4392f4);
    expect(MARKER_RING_OPACITY).toBe(0.6877);
    expect(muteBadgeY).toBe(2.3);
    expect(possessedBadgeY).toBe(2.3);
  });
});

describe("worldView muzzle flash mesh", () => {
  test("muzzle flash mesh 0xfff2c0 × 1.15/canal (r/g clamp) → 0xffffdd; warmLight/possessed/hostile/player/intensity/palette/badge-Y iguales", () => {
    expect(MUZZLE_FLASH_COLOR).toBe(0xffffdd);
    const r = (MUZZLE_FLASH_COLOR >> 16) & 0xff;
    const g = (MUZZLE_FLASH_COLOR >> 8) & 0xff;
    const b = MUZZLE_FLASH_COLOR & 0xff;
    expect(r).toBe(0xff);
    expect(g).toBe(0xff);
    expect(b).toBe(0xdd);
    expect(Math.min(0xff, Math.round((0xff * 115) / 100))).toBe(r);
    expect(Math.min(0xff, Math.round((0xf2 * 115) / 100))).toBe(g);
    expect(Math.round((0xc0 * 115) / 100)).toBe(b);
    const src = readFileSync(
      resolve(process.cwd(), "src/render/worldView.ts"),
      "utf8",
    );
    expect(src).toContain("export const MUZZLE_FLASH_COLOR = 0xffffdd");
    expect(src).toContain("color: MUZZLE_FLASH_COLOR");
    expect(src).not.toMatch(/color:\s*0xfff2c0/);
    expect(src).toContain("export const MUZZLE_LIGHT_COLOR = 0xffffb8");
    expect(src).toContain("new THREE.PointLight(\n    MUZZLE_LIGHT_COLOR,");
    expect(src).toContain("export const WARM_LIGHT_COLOR = 0xffca81");
    expect(src).toContain("new THREE.PointLight(WARM_LIGHT_COLOR, 0, 7.5, WARM_LIGHT_DECAY)");
    expect(src).toContain("export const POSSESSED_HEAD_EMISSIVE = 0x30124a");
    expect(src).toContain("emissive: POSSESSED_HEAD_EMISSIVE");
    expect(src).toContain("color: POSSESSED_HEAD_COLOR");
    expect(src).toContain("export const POSSESSED_HEAD_COLOR = 0x8c469f");
    expect(src).toContain("emissive: POSSESSED_EMISSIVE");
    expect(src).toContain("color: POSSESSED_COLOR");
    expect(src).toContain("const POSSESSED_EMISSIVE = 0x1e0925");
    expect(src).toContain("emissiveIntensity: 0.22");
    expect(src).toContain("color: HOSTILE_COLOR");
    expect(src).toContain("color: PLAYER_COLOR");
    expect(src).toContain("color: PLAYER_HEAD_COLOR");
    expect(src).toContain("emissive: PLAYER_HEAD_EMISSIVE");
    expect(WARM_LIGHT_COLOR).toBe(0xffca81);
    expect(POSSESSED_HEAD_EMISSIVE).toBe(0x30124a);
    expect(POSSESSED_HEAD_COLOR).toBe(0x8c469f);
    expect(POSSESSED_COLOR).toBe(0x68347b);
    expect(HOSTILE_COLOR).toBe(0x7b1e1e);
    expect(PLAYER_COLOR).toBe(0x55a4f4);
    expect(PLAYER_HEAD_COLOR).toBe(0x91d1ff);
    expect(PLAYER_HEAD_EMISSIVE).toBe(0x122537);
    expect(MARKER_PALETTE.possessed.emissive).toBe(0x30124a);
    expect(MARKER_PALETTE.player.badge).toBe(0x91d1ff);
    expect(MARKER_PALETTE.player.emissive).toBe(0x1e4a6e);
    expect(MARKER_PALETTE.player.ring).toBe(0x4392f4);
    expect(MARKER_RING_OPACITY).toBe(0.6877);
    expect(muteBadgeY).toBe(2.3);
    expect(possessedBadgeY).toBe(2.3);
  });
});

describe("worldView muzzle PointLight", () => {
  test("muzzle PointLight 0xffe8a0 × 1.15/canal (r/g clamp) → 0xffffb8; flash/warmLight/possessed/hostile/player/intensity/palette/badge-Y iguales", () => {
    expect(MUZZLE_LIGHT_COLOR).toBe(0xffffb8);
    const r = (MUZZLE_LIGHT_COLOR >> 16) & 0xff;
    const g = (MUZZLE_LIGHT_COLOR >> 8) & 0xff;
    const b = MUZZLE_LIGHT_COLOR & 0xff;
    expect(r).toBe(0xff);
    expect(g).toBe(0xff);
    expect(b).toBe(0xb8);
    expect(Math.min(0xff, Math.round((0xff * 115) / 100))).toBe(r);
    expect(Math.min(0xff, Math.round((0xe8 * 115) / 100))).toBe(g);
    expect(Math.round((0xa0 * 115) / 100)).toBe(b);
    const src = readFileSync(
      resolve(process.cwd(), "src/render/worldView.ts"),
      "utf8",
    );
    expect(src).toContain("export const MUZZLE_LIGHT_COLOR = 0xffffb8");
    expect(src).toContain("new THREE.PointLight(\n    MUZZLE_LIGHT_COLOR,");
    expect(src).not.toMatch(/new THREE\.PointLight\(\s*0xffe8a0/);
    expect(src).toContain("export const MUZZLE_FLASH_COLOR = 0xffffdd");
    expect(src).toContain("color: MUZZLE_FLASH_COLOR");
    expect(src).toContain("export const WARM_LIGHT_COLOR = 0xffca81");
    expect(src).toContain("new THREE.PointLight(WARM_LIGHT_COLOR, 0, 7.5, WARM_LIGHT_DECAY)");
    expect(src).toContain("export const POSSESSED_HEAD_EMISSIVE = 0x30124a");
    expect(src).toContain("emissive: POSSESSED_HEAD_EMISSIVE");
    expect(src).toContain("color: POSSESSED_HEAD_COLOR");
    expect(src).toContain("export const POSSESSED_HEAD_COLOR = 0x8c469f");
    expect(src).toContain("emissive: POSSESSED_EMISSIVE");
    expect(src).toContain("color: POSSESSED_COLOR");
    expect(src).toContain("const POSSESSED_EMISSIVE = 0x1e0925");
    expect(src).toContain("emissiveIntensity: 0.22");
    expect(src).toContain("color: HOSTILE_COLOR");
    expect(src).toContain("color: PLAYER_COLOR");
    expect(src).toContain("color: PLAYER_HEAD_COLOR");
    expect(src).toContain("emissive: PLAYER_HEAD_EMISSIVE");
    expect(MUZZLE_FLASH_COLOR).toBe(0xffffdd);
    expect(WARM_LIGHT_COLOR).toBe(0xffca81);
    expect(POSSESSED_HEAD_EMISSIVE).toBe(0x30124a);
    expect(POSSESSED_HEAD_COLOR).toBe(0x8c469f);
    expect(POSSESSED_COLOR).toBe(0x68347b);
    expect(HOSTILE_COLOR).toBe(0x7b1e1e);
    expect(PLAYER_COLOR).toBe(0x55a4f4);
    expect(PLAYER_HEAD_COLOR).toBe(0x91d1ff);
    expect(PLAYER_HEAD_EMISSIVE).toBe(0x122537);
    expect(MARKER_PALETTE.possessed.emissive).toBe(0x30124a);
    expect(MARKER_PALETTE.player.badge).toBe(0x91d1ff);
    expect(MARKER_PALETTE.player.emissive).toBe(0x1e4a6e);
    expect(MARKER_PALETTE.player.ring).toBe(0x4392f4);
    expect(MARKER_RING_OPACITY).toBe(0.6877);
    expect(muteBadgeY).toBe(2.3);
    expect(possessedBadgeY).toBe(2.3);
  });
});

describe("worldView impact spark mesh", () => {
  test("impact spark mesh 0xffd080 × 1.15/canal (r clamp) → 0xffef93; light/muzzle/warmLight/possessed/hostile/player/intensity/palette/badge-Y iguales", () => {
    expect(IMPACT_SPARK_COLOR).toBe(0xffef93);
    const r = (IMPACT_SPARK_COLOR >> 16) & 0xff;
    const g = (IMPACT_SPARK_COLOR >> 8) & 0xff;
    const b = IMPACT_SPARK_COLOR & 0xff;
    expect(r).toBe(0xff);
    expect(g).toBe(0xef);
    expect(b).toBe(0x93);
    expect(Math.min(0xff, Math.round((0xff * 115) / 100))).toBe(r);
    expect(Math.round((0xd0 * 115) / 100)).toBe(g);
    expect(Math.round((0x80 * 115) / 100)).toBe(b);
    const src = readFileSync(
      resolve(process.cwd(), "src/render/worldView.ts"),
      "utf8",
    );
    expect(src).toContain("export const IMPACT_SPARK_COLOR = 0xffef93");
    expect(src).toContain("color: IMPACT_SPARK_COLOR");
    expect(src).not.toMatch(/color:\s*0xffd080/);
    expect(src).toContain("export const IMPACT_SPARK_LIGHT_COLOR = 0xffef93");
    expect(src).toContain("new THREE.PointLight(\n    IMPACT_SPARK_LIGHT_COLOR,");
    expect(src).toContain("export const MUZZLE_LIGHT_COLOR = 0xffffb8");
    expect(src).toContain("new THREE.PointLight(\n    MUZZLE_LIGHT_COLOR,");
    expect(src).toContain("export const MUZZLE_FLASH_COLOR = 0xffffdd");
    expect(src).toContain("color: MUZZLE_FLASH_COLOR");
    expect(src).toContain("export const WARM_LIGHT_COLOR = 0xffca81");
    expect(src).toContain("new THREE.PointLight(WARM_LIGHT_COLOR, 0, 7.5, WARM_LIGHT_DECAY)");
    expect(src).toContain("export const POSSESSED_HEAD_EMISSIVE = 0x30124a");
    expect(src).toContain("emissive: POSSESSED_HEAD_EMISSIVE");
    expect(src).toContain("color: POSSESSED_HEAD_COLOR");
    expect(src).toContain("export const POSSESSED_HEAD_COLOR = 0x8c469f");
    expect(src).toContain("emissive: POSSESSED_EMISSIVE");
    expect(src).toContain("color: POSSESSED_COLOR");
    expect(src).toContain("const POSSESSED_EMISSIVE = 0x1e0925");
    expect(src).toContain("emissiveIntensity: 0.22");
    expect(src).toContain("color: HOSTILE_COLOR");
    expect(src).toContain("color: PLAYER_COLOR");
    expect(src).toContain("color: PLAYER_HEAD_COLOR");
    expect(src).toContain("emissive: PLAYER_HEAD_EMISSIVE");
    expect(IMPACT_SPARK_LIGHT_COLOR).toBe(0xffef93);
    expect(MUZZLE_LIGHT_COLOR).toBe(0xffffb8);
    expect(MUZZLE_FLASH_COLOR).toBe(0xffffdd);
    expect(WARM_LIGHT_COLOR).toBe(0xffca81);
    expect(POSSESSED_HEAD_EMISSIVE).toBe(0x30124a);
    expect(POSSESSED_HEAD_COLOR).toBe(0x8c469f);
    expect(POSSESSED_COLOR).toBe(0x68347b);
    expect(HOSTILE_COLOR).toBe(0x7b1e1e);
    expect(PLAYER_COLOR).toBe(0x55a4f4);
    expect(PLAYER_HEAD_COLOR).toBe(0x91d1ff);
    expect(PLAYER_HEAD_EMISSIVE).toBe(0x122537);
    expect(MARKER_PALETTE.possessed.emissive).toBe(0x30124a);
    expect(MARKER_PALETTE.player.badge).toBe(0x91d1ff);
    expect(MARKER_PALETTE.player.emissive).toBe(0x1e4a6e);
    expect(MARKER_PALETTE.player.ring).toBe(0x4392f4);
    expect(MARKER_RING_OPACITY).toBe(0.6877);
    expect(muteBadgeY).toBe(2.3);
    expect(possessedBadgeY).toBe(2.3);
  });
});

describe("worldView impact spark PointLight", () => {
  test("impact spark PointLight 0xffd080 × 1.15/canal (r clamp) → 0xffef93; mesh/muzzle/warmLight/possessed/hostile/player/intensity/palette/badge-Y iguales", () => {
    expect(IMPACT_SPARK_LIGHT_COLOR).toBe(0xffef93);
    const r = (IMPACT_SPARK_LIGHT_COLOR >> 16) & 0xff;
    const g = (IMPACT_SPARK_LIGHT_COLOR >> 8) & 0xff;
    const b = IMPACT_SPARK_LIGHT_COLOR & 0xff;
    expect(r).toBe(0xff);
    expect(g).toBe(0xef);
    expect(b).toBe(0x93);
    expect(Math.min(0xff, Math.round((0xff * 115) / 100))).toBe(r);
    expect(Math.round((0xd0 * 115) / 100)).toBe(g);
    expect(Math.round((0x80 * 115) / 100)).toBe(b);
    const src = readFileSync(
      resolve(process.cwd(), "src/render/worldView.ts"),
      "utf8",
    );
    expect(src).toContain("export const IMPACT_SPARK_LIGHT_COLOR = 0xffef93");
    expect(src).toContain("new THREE.PointLight(\n    IMPACT_SPARK_LIGHT_COLOR,");
    expect(src).not.toMatch(/new THREE\.PointLight\(\s*0xffd080/);
    expect(src).toContain("export const IMPACT_SPARK_COLOR = 0xffef93");
    expect(src).toContain("color: IMPACT_SPARK_COLOR");
    expect(src).toContain("export const MUZZLE_LIGHT_COLOR = 0xffffb8");
    expect(src).toContain("new THREE.PointLight(\n    MUZZLE_LIGHT_COLOR,");
    expect(src).toContain("export const MUZZLE_FLASH_COLOR = 0xffffdd");
    expect(src).toContain("color: MUZZLE_FLASH_COLOR");
    expect(src).toContain("export const WARM_LIGHT_COLOR = 0xffca81");
    expect(src).toContain("new THREE.PointLight(WARM_LIGHT_COLOR, 0, 7.5, WARM_LIGHT_DECAY)");
    expect(src).toContain("export const POSSESSED_HEAD_EMISSIVE = 0x30124a");
    expect(src).toContain("emissive: POSSESSED_HEAD_EMISSIVE");
    expect(src).toContain("color: POSSESSED_HEAD_COLOR");
    expect(src).toContain("export const POSSESSED_HEAD_COLOR = 0x8c469f");
    expect(src).toContain("emissive: POSSESSED_EMISSIVE");
    expect(src).toContain("color: POSSESSED_COLOR");
    expect(src).toContain("const POSSESSED_EMISSIVE = 0x1e0925");
    expect(src).toContain("emissiveIntensity: 0.22");
    expect(src).toContain("color: HOSTILE_COLOR");
    expect(src).toContain("color: PLAYER_COLOR");
    expect(src).toContain("color: PLAYER_HEAD_COLOR");
    expect(src).toContain("emissive: PLAYER_HEAD_EMISSIVE");
    expect(src).toContain("export const TRACER_COLOR = 0xffffb8");
    expect(src).toContain("color: TRACER_COLOR");
    expect(src).toContain("export const TRACER_FLASH_COLOR = 0xffdd6e");
    expect(src).toContain("new THREE.PointLight(TRACER_FLASH_COLOR, TRACER_FLASH_INTENSITY, TRACER_FLASH_DISTANCE, TRACER_FLASH_DECAY)");
    expect(TRACER_COLOR).toBe(0xffffb8);
    expect(TRACER_FLASH_COLOR).toBe(0xffdd6e);
    expect(IMPACT_SPARK_COLOR).toBe(0xffef93);
    expect(MUZZLE_LIGHT_COLOR).toBe(0xffffb8);
    expect(MUZZLE_FLASH_COLOR).toBe(0xffffdd);
    expect(WARM_LIGHT_COLOR).toBe(0xffca81);
    expect(POSSESSED_HEAD_EMISSIVE).toBe(0x30124a);
    expect(POSSESSED_HEAD_COLOR).toBe(0x8c469f);
    expect(POSSESSED_COLOR).toBe(0x68347b);
    expect(HOSTILE_COLOR).toBe(0x7b1e1e);
    expect(PLAYER_COLOR).toBe(0x55a4f4);
    expect(PLAYER_HEAD_COLOR).toBe(0x91d1ff);
    expect(PLAYER_HEAD_EMISSIVE).toBe(0x122537);
    expect(MARKER_PALETTE.possessed.emissive).toBe(0x30124a);
    expect(MARKER_PALETTE.player.badge).toBe(0x91d1ff);
    expect(MARKER_PALETTE.player.emissive).toBe(0x1e4a6e);
    expect(MARKER_PALETTE.player.ring).toBe(0x4392f4);
    expect(MARKER_RING_OPACITY).toBe(0.6877);
    expect(muteBadgeY).toBe(2.3);
    expect(possessedBadgeY).toBe(2.3);
  });
});

describe("worldView tracer mesh", () => {
  test("tracer mesh 0xffe8a0 × 1.15/canal (r/g clamp) → 0xffffb8; flash/impact/muzzle/warmLight/possessed/hostile/player/intensity/palette/badge-Y iguales", () => {
    expect(TRACER_COLOR).toBe(0xffffb8);
    const r = (TRACER_COLOR >> 16) & 0xff;
    const g = (TRACER_COLOR >> 8) & 0xff;
    const b = TRACER_COLOR & 0xff;
    expect(r).toBe(0xff);
    expect(g).toBe(0xff);
    expect(b).toBe(0xb8);
    expect(Math.min(0xff, Math.round((0xff * 115) / 100))).toBe(r);
    expect(Math.min(0xff, Math.round((0xe8 * 115) / 100))).toBe(g);
    expect(Math.round((0xa0 * 115) / 100)).toBe(b);
    const src = readFileSync(
      resolve(process.cwd(), "src/render/worldView.ts"),
      "utf8",
    );
    expect(src).toContain("export const TRACER_COLOR = 0xffffb8");
    expect(src).toContain("color: TRACER_COLOR");
    expect(src).not.toMatch(/color:\s*0xffe8a0/);
    expect(src).toContain("export const TRACER_FLASH_COLOR = 0xffdd6e");
    expect(src).toContain("new THREE.PointLight(TRACER_FLASH_COLOR, TRACER_FLASH_INTENSITY, TRACER_FLASH_DISTANCE, TRACER_FLASH_DECAY)");
    expect(TRACER_FLASH_COLOR).toBe(0xffdd6e);
    expect(src).toContain("export const IMPACT_SPARK_LIGHT_COLOR = 0xffef93");
    expect(src).toContain("new THREE.PointLight(\n    IMPACT_SPARK_LIGHT_COLOR,");
    expect(src).toContain("export const IMPACT_SPARK_COLOR = 0xffef93");
    expect(src).toContain("color: IMPACT_SPARK_COLOR");
    expect(src).toContain("export const MUZZLE_LIGHT_COLOR = 0xffffb8");
    expect(src).toContain("new THREE.PointLight(\n    MUZZLE_LIGHT_COLOR,");
    expect(src).toContain("export const MUZZLE_FLASH_COLOR = 0xffffdd");
    expect(src).toContain("color: MUZZLE_FLASH_COLOR");
    expect(src).toContain("export const WARM_LIGHT_COLOR = 0xffca81");
    expect(src).toContain("new THREE.PointLight(WARM_LIGHT_COLOR, 0, 7.5, WARM_LIGHT_DECAY)");
    expect(src).toContain("export const POSSESSED_HEAD_EMISSIVE = 0x30124a");
    expect(src).toContain("emissive: POSSESSED_HEAD_EMISSIVE");
    expect(src).toContain("color: POSSESSED_HEAD_COLOR");
    expect(src).toContain("export const POSSESSED_HEAD_COLOR = 0x8c469f");
    expect(src).toContain("emissive: POSSESSED_EMISSIVE");
    expect(src).toContain("color: POSSESSED_COLOR");
    expect(src).toContain("const POSSESSED_EMISSIVE = 0x1e0925");
    expect(src).toContain("emissiveIntensity: 0.22");
    expect(src).toContain("color: HOSTILE_COLOR");
    expect(src).toContain("color: PLAYER_COLOR");
    expect(src).toContain("color: PLAYER_HEAD_COLOR");
    expect(src).toContain("emissive: PLAYER_HEAD_EMISSIVE");
    expect(IMPACT_SPARK_LIGHT_COLOR).toBe(0xffef93);
    expect(IMPACT_SPARK_COLOR).toBe(0xffef93);
    expect(MUZZLE_LIGHT_COLOR).toBe(0xffffb8);
    expect(MUZZLE_FLASH_COLOR).toBe(0xffffdd);
    expect(WARM_LIGHT_COLOR).toBe(0xffca81);
    expect(POSSESSED_HEAD_EMISSIVE).toBe(0x30124a);
    expect(POSSESSED_HEAD_COLOR).toBe(0x8c469f);
    expect(POSSESSED_COLOR).toBe(0x68347b);
    expect(HOSTILE_COLOR).toBe(0x7b1e1e);
    expect(PLAYER_COLOR).toBe(0x55a4f4);
    expect(PLAYER_HEAD_COLOR).toBe(0x91d1ff);
    expect(PLAYER_HEAD_EMISSIVE).toBe(0x122537);
    expect(MARKER_PALETTE.possessed.emissive).toBe(0x30124a);
    expect(MARKER_PALETTE.player.badge).toBe(0x91d1ff);
    expect(MARKER_PALETTE.player.emissive).toBe(0x1e4a6e);
    expect(MARKER_PALETTE.player.ring).toBe(0x4392f4);
    expect(MARKER_RING_OPACITY).toBe(0.6877);
    expect(muteBadgeY).toBe(2.3);
    expect(possessedBadgeY).toBe(2.3);
  });
});

describe("worldView tracer flash", () => {
  test("tracer flash 0xffc060 × 1.15/canal (r clamp) → 0xffdd6e; mesh/impact/muzzle/warmLight/possessed/hostile/player/intensity/palette/badge-Y iguales", () => {
    expect(TRACER_FLASH_COLOR).toBe(0xffdd6e);
    const r = (TRACER_FLASH_COLOR >> 16) & 0xff;
    const g = (TRACER_FLASH_COLOR >> 8) & 0xff;
    const b = TRACER_FLASH_COLOR & 0xff;
    expect(r).toBe(0xff);
    expect(g).toBe(0xdd);
    expect(b).toBe(0x6e);
    expect(Math.min(0xff, Math.round((0xff * 115) / 100))).toBe(r);
    expect(Math.round((0xc0 * 115) / 100)).toBe(g);
    expect(Math.round((0x60 * 115) / 100)).toBe(b);
    const src = readFileSync(
      resolve(process.cwd(), "src/render/worldView.ts"),
      "utf8",
    );
    expect(src).toContain("export const TRACER_FLASH_COLOR = 0xffdd6e");
    expect(src).toContain("new THREE.PointLight(TRACER_FLASH_COLOR, TRACER_FLASH_INTENSITY, TRACER_FLASH_DISTANCE, TRACER_FLASH_DECAY)");
    expect(src).not.toMatch(/new THREE\.PointLight\(0xffc060/);
    expect(src).toContain("export const TRACER_COLOR = 0xffffb8");
    expect(src).toContain("color: TRACER_COLOR");
    expect(src).toContain("export const IMPACT_SPARK_LIGHT_COLOR = 0xffef93");
    expect(src).toContain("new THREE.PointLight(\n    IMPACT_SPARK_LIGHT_COLOR,");
    expect(src).toContain("export const IMPACT_SPARK_COLOR = 0xffef93");
    expect(src).toContain("color: IMPACT_SPARK_COLOR");
    expect(src).toContain("export const MUZZLE_LIGHT_COLOR = 0xffffb8");
    expect(src).toContain("new THREE.PointLight(\n    MUZZLE_LIGHT_COLOR,");
    expect(src).toContain("export const MUZZLE_FLASH_COLOR = 0xffffdd");
    expect(src).toContain("color: MUZZLE_FLASH_COLOR");
    expect(src).toContain("export const WARM_LIGHT_COLOR = 0xffca81");
    expect(src).toContain("new THREE.PointLight(WARM_LIGHT_COLOR, 0, 7.5, WARM_LIGHT_DECAY)");
    expect(src).toContain("export const POSSESSED_HEAD_EMISSIVE = 0x30124a");
    expect(src).toContain("emissive: POSSESSED_HEAD_EMISSIVE");
    expect(src).toContain("color: POSSESSED_HEAD_COLOR");
    expect(src).toContain("export const POSSESSED_HEAD_COLOR = 0x8c469f");
    expect(src).toContain("emissive: POSSESSED_EMISSIVE");
    expect(src).toContain("color: POSSESSED_COLOR");
    expect(src).toContain("const POSSESSED_EMISSIVE = 0x1e0925");
    expect(src).toContain("emissiveIntensity: 0.22");
    expect(src).toContain("color: HOSTILE_COLOR");
    expect(src).toContain("color: PLAYER_COLOR");
    expect(src).toContain("color: PLAYER_HEAD_COLOR");
    expect(src).toContain("emissive: PLAYER_HEAD_EMISSIVE");
    expect(TRACER_COLOR).toBe(0xffffb8);
    expect(IMPACT_SPARK_LIGHT_COLOR).toBe(0xffef93);
    expect(IMPACT_SPARK_COLOR).toBe(0xffef93);
    expect(MUZZLE_LIGHT_COLOR).toBe(0xffffb8);
    expect(MUZZLE_FLASH_COLOR).toBe(0xffffdd);
    expect(WARM_LIGHT_COLOR).toBe(0xffca81);
    expect(POSSESSED_HEAD_EMISSIVE).toBe(0x30124a);
    expect(POSSESSED_HEAD_COLOR).toBe(0x8c469f);
    expect(POSSESSED_COLOR).toBe(0x68347b);
    expect(HOSTILE_COLOR).toBe(0x7b1e1e);
    expect(PLAYER_COLOR).toBe(0x55a4f4);
    expect(PLAYER_HEAD_COLOR).toBe(0x91d1ff);
    expect(PLAYER_HEAD_EMISSIVE).toBe(0x122537);
    expect(MARKER_PALETTE.possessed.emissive).toBe(0x30124a);
    expect(MARKER_PALETTE.player.badge).toBe(0x91d1ff);
    expect(MARKER_PALETTE.player.emissive).toBe(0x1e4a6e);
    expect(MARKER_PALETTE.player.ring).toBe(0x4392f4);
    expect(MARKER_RING_OPACITY).toBe(0.6877);
    expect(muteBadgeY).toBe(2.3);
    expect(possessedBadgeY).toBe(2.3);
  });
});

describe("worldView flashlight fill", () => {
  test("flashlight fill 0xb0d0ff × 1.15/canal (b clamp) → 0xcaefff; flash/mesh/impact/muzzle/warmLight/possessed/hostile/player/intensity/palette/badge-Y iguales", () => {
    expect(FLASHLIGHT_FILL_COLOR).toBe(0xcaefff);
    expect(FLASHLIGHT_FILL_DECAY).toBe(1.74);
    const r = (FLASHLIGHT_FILL_COLOR >> 16) & 0xff;
    const g = (FLASHLIGHT_FILL_COLOR >> 8) & 0xff;
    const b = FLASHLIGHT_FILL_COLOR & 0xff;
    expect(r).toBe(0xca);
    expect(g).toBe(0xef);
    expect(b).toBe(0xff);
    expect(Math.round((0xb0 * 115) / 100)).toBe(r);
    expect(Math.round((0xd0 * 115) / 100)).toBe(g);
    expect(Math.min(0xff, Math.round((0xff * 115) / 100))).toBe(b);
    const src = readFileSync(
      resolve(process.cwd(), "src/render/worldView.ts"),
      "utf8",
    );
    expect(src).toContain("export const FLASHLIGHT_FILL_COLOR = 0xcaefff");
    expect(src).toContain("export const FLASHLIGHT_FILL_DECAY = 1.74");
    expect(src).toContain("new THREE.PointLight(FLASHLIGHT_FILL_COLOR, 0, 10, FLASHLIGHT_FILL_DECAY)");
    expect(src).not.toMatch(/new THREE\.PointLight\(FLASHLIGHT_FILL_COLOR, 0, 10, 2\)/);
    expect(src).toContain("torchLight.color.setHex(FLASHLIGHT_FILL_COLOR)");
    expect(src).not.toMatch(/new THREE\.PointLight\(0xb0d0ff/);
    expect(src).not.toMatch(/torchLight\.color\.setHex\(0xb0d0ff\)/);
    expect(src).toContain("export const TRACER_FLASH_COLOR = 0xffdd6e");
    expect(src).toContain("new THREE.PointLight(TRACER_FLASH_COLOR, TRACER_FLASH_INTENSITY, TRACER_FLASH_DISTANCE, TRACER_FLASH_DECAY)");
    expect(src).toContain("export const TRACER_COLOR = 0xffffb8");
    expect(src).toContain("color: TRACER_COLOR");
    expect(src).toContain("export const IMPACT_SPARK_LIGHT_COLOR = 0xffef93");
    expect(src).toContain("new THREE.PointLight(\n    IMPACT_SPARK_LIGHT_COLOR,");
    expect(src).toContain("export const IMPACT_SPARK_COLOR = 0xffef93");
    expect(src).toContain("color: IMPACT_SPARK_COLOR");
    expect(src).toContain("export const MUZZLE_LIGHT_COLOR = 0xffffb8");
    expect(src).toContain("new THREE.PointLight(\n    MUZZLE_LIGHT_COLOR,");
    expect(src).toContain("export const MUZZLE_FLASH_COLOR = 0xffffdd");
    expect(src).toContain("color: MUZZLE_FLASH_COLOR");
    expect(src).toContain("export const WARM_LIGHT_COLOR = 0xffca81");
    expect(src).toContain("new THREE.PointLight(WARM_LIGHT_COLOR, 0, 7.5, WARM_LIGHT_DECAY)");
    expect(src).toContain("export const POSSESSED_HEAD_EMISSIVE = 0x30124a");
    expect(src).toContain("emissive: POSSESSED_HEAD_EMISSIVE");
    expect(src).toContain("color: POSSESSED_HEAD_COLOR");
    expect(src).toContain("export const POSSESSED_HEAD_COLOR = 0x8c469f");
    expect(src).toContain("emissive: POSSESSED_EMISSIVE");
    expect(src).toContain("color: POSSESSED_COLOR");
    expect(src).toContain("const POSSESSED_EMISSIVE = 0x1e0925");
    expect(src).toContain("emissiveIntensity: 0.22");
    expect(src).toContain("color: HOSTILE_COLOR");
    expect(src).toContain("color: PLAYER_COLOR");
    expect(src).toContain("color: PLAYER_HEAD_COLOR");
    expect(src).toContain("emissive: PLAYER_HEAD_EMISSIVE");
    expect(TRACER_FLASH_COLOR).toBe(0xffdd6e);
    expect(TRACER_COLOR).toBe(0xffffb8);
    expect(IMPACT_SPARK_LIGHT_COLOR).toBe(0xffef93);
    expect(IMPACT_SPARK_COLOR).toBe(0xffef93);
    expect(MUZZLE_LIGHT_COLOR).toBe(0xffffb8);
    expect(MUZZLE_FLASH_COLOR).toBe(0xffffdd);
    expect(WARM_LIGHT_COLOR).toBe(0xffca81);
    expect(POSSESSED_HEAD_EMISSIVE).toBe(0x30124a);
    expect(POSSESSED_HEAD_COLOR).toBe(0x8c469f);
    expect(POSSESSED_COLOR).toBe(0x68347b);
    expect(HOSTILE_COLOR).toBe(0x7b1e1e);
    expect(PLAYER_COLOR).toBe(0x55a4f4);
    expect(PLAYER_HEAD_COLOR).toBe(0x91d1ff);
    expect(PLAYER_HEAD_EMISSIVE).toBe(0x122537);
    expect(MARKER_PALETTE.possessed.emissive).toBe(0x30124a);
    expect(MARKER_PALETTE.player.badge).toBe(0x91d1ff);
    expect(MARKER_PALETTE.player.emissive).toBe(0x1e4a6e);
    expect(MARKER_PALETTE.player.ring).toBe(0x4392f4);
    expect(MARKER_RING_OPACITY).toBe(0.6877);
    expect(muteBadgeY).toBe(2.3);
    expect(possessedBadgeY).toBe(2.3);
  });
});

describe("worldView flashlight fill distance", () => {
  test("flashlight fill distance base 7 × 1.15 → 8.05; color/gain/flash/mesh/impact/muzzle/warmLight/possessed/hostile/player/intensity/palette/badge-Y iguales", () => {
    expect(FLASHLIGHT_FILL_DISTANCE_BASE).toBe(8.05);
    expect(FLASHLIGHT_FILL_DISTANCE_BASE).toBeCloseTo(7 * 1.15, 10);
    expect(FLASHLIGHT_FILL_DISTANCE_GAIN).toBe(4.025);
    expect(FLASHLIGHT_FILL_COLOR).toBe(0xcaefff);
    expect(FLASHLIGHT_FILL_DECAY).toBe(1.74);
    const src = readFileSync(
      resolve(process.cwd(), "src/render/worldView.ts"),
      "utf8",
    );
    expect(src).toContain("export const FLASHLIGHT_FILL_DISTANCE_BASE = 8.05");
    expect(src).toContain("export const FLASHLIGHT_FILL_DISTANCE_GAIN = 4.025");
    expect(src).toContain(
      "torchLight.distance = FLASHLIGHT_FILL_DISTANCE_BASE + i * FLASHLIGHT_FILL_DISTANCE_GAIN",
    );
    expect(src).not.toMatch(/torchLight\.distance = 7 \+ i \* 3\.5/);
    expect(src).toContain("export const FLASHLIGHT_FILL_COLOR = 0xcaefff");
    expect(src).toContain("export const FLASHLIGHT_FILL_DECAY = 1.74");
    expect(src).toContain("new THREE.PointLight(FLASHLIGHT_FILL_COLOR, 0, 10, FLASHLIGHT_FILL_DECAY)");
    expect(src).not.toMatch(/new THREE\.PointLight\(FLASHLIGHT_FILL_COLOR, 0, 10, 2\)/);
    expect(src).toContain("torchLight.color.setHex(FLASHLIGHT_FILL_COLOR)");
    expect(src).toContain("export const TRACER_FLASH_COLOR = 0xffdd6e");
    expect(src).toContain("new THREE.PointLight(TRACER_FLASH_COLOR, TRACER_FLASH_INTENSITY, TRACER_FLASH_DISTANCE, TRACER_FLASH_DECAY)");
    expect(src).toContain("export const TRACER_COLOR = 0xffffb8");
    expect(src).toContain("color: TRACER_COLOR");
    expect(src).toContain("export const IMPACT_SPARK_LIGHT_COLOR = 0xffef93");
    expect(src).toContain("new THREE.PointLight(\n    IMPACT_SPARK_LIGHT_COLOR,");
    expect(src).toContain("export const IMPACT_SPARK_COLOR = 0xffef93");
    expect(src).toContain("color: IMPACT_SPARK_COLOR");
    expect(src).toContain("export const MUZZLE_LIGHT_COLOR = 0xffffb8");
    expect(src).toContain("new THREE.PointLight(\n    MUZZLE_LIGHT_COLOR,");
    expect(src).toContain("export const MUZZLE_FLASH_COLOR = 0xffffdd");
    expect(src).toContain("color: MUZZLE_FLASH_COLOR");
    expect(src).toContain("export const WARM_LIGHT_COLOR = 0xffca81");
    expect(src).toContain("new THREE.PointLight(WARM_LIGHT_COLOR, 0, 7.5, WARM_LIGHT_DECAY)");
    expect(src).toContain("export const POSSESSED_HEAD_EMISSIVE = 0x30124a");
    expect(src).toContain("emissive: POSSESSED_HEAD_EMISSIVE");
    expect(src).toContain("color: POSSESSED_HEAD_COLOR");
    expect(src).toContain("export const POSSESSED_HEAD_COLOR = 0x8c469f");
    expect(src).toContain("emissive: POSSESSED_EMISSIVE");
    expect(src).toContain("color: POSSESSED_COLOR");
    expect(src).toContain("const POSSESSED_EMISSIVE = 0x1e0925");
    expect(src).toContain("emissiveIntensity: 0.22");
    expect(src).toContain("color: HOSTILE_COLOR");
    expect(src).toContain("color: PLAYER_COLOR");
    expect(src).toContain("color: PLAYER_HEAD_COLOR");
    expect(src).toContain("emissive: PLAYER_HEAD_EMISSIVE");
    expect(TRACER_FLASH_COLOR).toBe(0xffdd6e);
    expect(TRACER_COLOR).toBe(0xffffb8);
    expect(IMPACT_SPARK_LIGHT_COLOR).toBe(0xffef93);
    expect(IMPACT_SPARK_COLOR).toBe(0xffef93);
    expect(MUZZLE_LIGHT_COLOR).toBe(0xffffb8);
    expect(MUZZLE_FLASH_COLOR).toBe(0xffffdd);
    expect(WARM_LIGHT_COLOR).toBe(0xffca81);
    expect(POSSESSED_HEAD_EMISSIVE).toBe(0x30124a);
    expect(POSSESSED_HEAD_COLOR).toBe(0x8c469f);
    expect(POSSESSED_COLOR).toBe(0x68347b);
    expect(HOSTILE_COLOR).toBe(0x7b1e1e);
    expect(PLAYER_COLOR).toBe(0x55a4f4);
    expect(PLAYER_HEAD_COLOR).toBe(0x91d1ff);
    expect(PLAYER_HEAD_EMISSIVE).toBe(0x122537);
    expect(MARKER_PALETTE.possessed.emissive).toBe(0x30124a);
    expect(MARKER_PALETTE.player.badge).toBe(0x91d1ff);
    expect(MARKER_PALETTE.player.emissive).toBe(0x1e4a6e);
    expect(MARKER_PALETTE.player.ring).toBe(0x4392f4);
    expect(MARKER_RING_OPACITY).toBe(0.6877);
    expect(muteBadgeY).toBe(2.3);
    expect(possessedBadgeY).toBe(2.3);
  });
});

describe("worldView flashlight fill distance gain", () => {
  test("flashlight fill distance gain 3.5 × 1.15 → 4.025; base/color/flash/mesh/impact/muzzle/warmLight/possessed/hostile/player/intensity/palette/badge-Y iguales", () => {
    expect(FLASHLIGHT_FILL_DISTANCE_GAIN).toBe(4.025);
    expect(FLASHLIGHT_FILL_DISTANCE_GAIN).toBeCloseTo(3.5 * 1.15, 10);
    expect(FLASHLIGHT_FILL_DISTANCE_BASE).toBe(8.05);
    expect(FLASHLIGHT_FILL_COLOR).toBe(0xcaefff);
    expect(FLASHLIGHT_FILL_DECAY).toBe(1.74);
    const src = readFileSync(
      resolve(process.cwd(), "src/render/worldView.ts"),
      "utf8",
    );
    expect(src).toContain("export const FLASHLIGHT_FILL_DISTANCE_GAIN = 4.025");
    expect(src).toContain("export const FLASHLIGHT_FILL_DISTANCE_BASE = 8.05");
    expect(src).toContain(
      "torchLight.distance = FLASHLIGHT_FILL_DISTANCE_BASE + i * FLASHLIGHT_FILL_DISTANCE_GAIN",
    );
    expect(src).not.toMatch(/export const FLASHLIGHT_FILL_DISTANCE_GAIN = 3\.5/);
    expect(src).toContain("export const FLASHLIGHT_FILL_COLOR = 0xcaefff");
    expect(src).toContain("export const FLASHLIGHT_FILL_DECAY = 1.74");
    expect(src).toContain("new THREE.PointLight(FLASHLIGHT_FILL_COLOR, 0, 10, FLASHLIGHT_FILL_DECAY)");
    expect(src).not.toMatch(/new THREE\.PointLight\(FLASHLIGHT_FILL_COLOR, 0, 10, 2\)/);
    expect(src).toContain("torchLight.color.setHex(FLASHLIGHT_FILL_COLOR)");
    expect(src).toContain("export const TRACER_FLASH_COLOR = 0xffdd6e");
    expect(src).toContain("new THREE.PointLight(TRACER_FLASH_COLOR, TRACER_FLASH_INTENSITY, TRACER_FLASH_DISTANCE, TRACER_FLASH_DECAY)");
    expect(src).toContain("export const TRACER_COLOR = 0xffffb8");
    expect(src).toContain("color: TRACER_COLOR");
    expect(src).toContain("export const IMPACT_SPARK_LIGHT_COLOR = 0xffef93");
    expect(src).toContain("new THREE.PointLight(\n    IMPACT_SPARK_LIGHT_COLOR,");
    expect(src).toContain("export const IMPACT_SPARK_COLOR = 0xffef93");
    expect(src).toContain("color: IMPACT_SPARK_COLOR");
    expect(src).toContain("export const MUZZLE_LIGHT_COLOR = 0xffffb8");
    expect(src).toContain("new THREE.PointLight(\n    MUZZLE_LIGHT_COLOR,");
    expect(src).toContain("export const MUZZLE_FLASH_COLOR = 0xffffdd");
    expect(src).toContain("color: MUZZLE_FLASH_COLOR");
    expect(src).toContain("export const WARM_LIGHT_COLOR = 0xffca81");
    expect(src).toContain("new THREE.PointLight(WARM_LIGHT_COLOR, 0, 7.5, WARM_LIGHT_DECAY)");
    expect(src).toContain("export const POSSESSED_HEAD_EMISSIVE = 0x30124a");
    expect(src).toContain("emissive: POSSESSED_HEAD_EMISSIVE");
    expect(src).toContain("color: POSSESSED_HEAD_COLOR");
    expect(src).toContain("export const POSSESSED_HEAD_COLOR = 0x8c469f");
    expect(src).toContain("emissive: POSSESSED_EMISSIVE");
    expect(src).toContain("color: POSSESSED_COLOR");
    expect(src).toContain("const POSSESSED_EMISSIVE = 0x1e0925");
    expect(src).toContain("emissiveIntensity: 0.22");
    expect(src).toContain("color: HOSTILE_COLOR");
    expect(src).toContain("color: PLAYER_COLOR");
    expect(src).toContain("color: PLAYER_HEAD_COLOR");
    expect(src).toContain("emissive: PLAYER_HEAD_EMISSIVE");
    expect(TRACER_FLASH_COLOR).toBe(0xffdd6e);
    expect(TRACER_COLOR).toBe(0xffffb8);
    expect(IMPACT_SPARK_LIGHT_COLOR).toBe(0xffef93);
    expect(IMPACT_SPARK_COLOR).toBe(0xffef93);
    expect(MUZZLE_LIGHT_COLOR).toBe(0xffffb8);
    expect(MUZZLE_FLASH_COLOR).toBe(0xffffdd);
    expect(WARM_LIGHT_COLOR).toBe(0xffca81);
    expect(POSSESSED_HEAD_EMISSIVE).toBe(0x30124a);
    expect(POSSESSED_HEAD_COLOR).toBe(0x8c469f);
    expect(POSSESSED_COLOR).toBe(0x68347b);
    expect(HOSTILE_COLOR).toBe(0x7b1e1e);
    expect(PLAYER_COLOR).toBe(0x55a4f4);
    expect(PLAYER_HEAD_COLOR).toBe(0x91d1ff);
    expect(PLAYER_HEAD_EMISSIVE).toBe(0x122537);
    expect(MARKER_PALETTE.possessed.emissive).toBe(0x30124a);
    expect(MARKER_PALETTE.player.badge).toBe(0x91d1ff);
    expect(MARKER_PALETTE.player.emissive).toBe(0x1e4a6e);
    expect(MARKER_PALETTE.player.ring).toBe(0x4392f4);
    expect(MARKER_RING_OPACITY).toBe(0.6877);
    expect(muteBadgeY).toBe(2.3);
    expect(possessedBadgeY).toBe(2.3);
  });
});

describe("worldView flashlight fill Y", () => {
  test("flashlight fill Y 1.35 × 1.15 → 1.5525; gain/base/color/flash/mesh/impact/muzzle/warmLight/possessed/hostile/player/intensity/palette/badge-Y iguales", () => {
    expect(FLASHLIGHT_FILL_Y).toBe(1.5525);
    expect(FLASHLIGHT_FILL_Y).toBeCloseTo(1.35 * 1.15, 10);
    expect(FLASHLIGHT_FILL_DISTANCE_GAIN).toBe(4.025);
    expect(FLASHLIGHT_FILL_DISTANCE_BASE).toBe(8.05);
    expect(FLASHLIGHT_FILL_COLOR).toBe(0xcaefff);
    expect(FLASHLIGHT_FILL_DECAY).toBe(1.74);
    const src = readFileSync(
      resolve(process.cwd(), "src/render/worldView.ts"),
      "utf8",
    );
    expect(src).toContain("export const FLASHLIGHT_FILL_Y = 1.5525");
    expect(src).toContain("torchLight.position.set(0, FLASHLIGHT_FILL_Y, 0)");
    expect(src).toContain("torchLight.position.set(wx, FLASHLIGHT_FILL_Y, wy)");
    expect(src).not.toMatch(/export const FLASHLIGHT_FILL_Y = 1\.35/);
    expect(src).not.toMatch(/torchLight\.position\.set\(0, 1\.35, 0\)/);
    expect(src).not.toMatch(/torchLight\.position\.set\(wx, 1\.35, wy\)/);
    expect(src).toContain("export const FLASHLIGHT_FILL_DISTANCE_GAIN = 4.025");
    expect(src).toContain("export const FLASHLIGHT_FILL_DISTANCE_BASE = 8.05");
    expect(src).toContain(
      "torchLight.distance = FLASHLIGHT_FILL_DISTANCE_BASE + i * FLASHLIGHT_FILL_DISTANCE_GAIN",
    );
    expect(src).toContain("export const FLASHLIGHT_FILL_COLOR = 0xcaefff");
    expect(src).toContain("export const FLASHLIGHT_FILL_DECAY = 1.74");
    expect(src).toContain("new THREE.PointLight(FLASHLIGHT_FILL_COLOR, 0, 10, FLASHLIGHT_FILL_DECAY)");
    expect(src).not.toMatch(/new THREE\.PointLight\(FLASHLIGHT_FILL_COLOR, 0, 10, 2\)/);
    expect(src).toContain("torchLight.color.setHex(FLASHLIGHT_FILL_COLOR)");
    expect(src).toContain("export const TRACER_FLASH_COLOR = 0xffdd6e");
    expect(src).toContain("new THREE.PointLight(TRACER_FLASH_COLOR, TRACER_FLASH_INTENSITY, TRACER_FLASH_DISTANCE, TRACER_FLASH_DECAY)");
    expect(src).toContain("export const TRACER_COLOR = 0xffffb8");
    expect(src).toContain("color: TRACER_COLOR");
    expect(src).toContain("export const IMPACT_SPARK_LIGHT_COLOR = 0xffef93");
    expect(src).toContain("new THREE.PointLight(\n    IMPACT_SPARK_LIGHT_COLOR,");
    expect(src).toContain("export const IMPACT_SPARK_COLOR = 0xffef93");
    expect(src).toContain("color: IMPACT_SPARK_COLOR");
    expect(src).toContain("export const MUZZLE_LIGHT_COLOR = 0xffffb8");
    expect(src).toContain("new THREE.PointLight(\n    MUZZLE_LIGHT_COLOR,");
    expect(src).toContain("export const MUZZLE_FLASH_COLOR = 0xffffdd");
    expect(src).toContain("color: MUZZLE_FLASH_COLOR");
    expect(src).toContain("export const WARM_LIGHT_COLOR = 0xffca81");
    expect(src).toContain("new THREE.PointLight(WARM_LIGHT_COLOR, 0, 7.5, WARM_LIGHT_DECAY)");
    expect(src).toContain("export const POSSESSED_HEAD_EMISSIVE = 0x30124a");
    expect(src).toContain("emissive: POSSESSED_HEAD_EMISSIVE");
    expect(src).toContain("color: POSSESSED_HEAD_COLOR");
    expect(src).toContain("export const POSSESSED_HEAD_COLOR = 0x8c469f");
    expect(src).toContain("emissive: POSSESSED_EMISSIVE");
    expect(src).toContain("color: POSSESSED_COLOR");
    expect(src).toContain("const POSSESSED_EMISSIVE = 0x1e0925");
    expect(src).toContain("emissiveIntensity: 0.22");
    expect(src).toContain("color: HOSTILE_COLOR");
    expect(src).toContain("color: PLAYER_COLOR");
    expect(src).toContain("color: PLAYER_HEAD_COLOR");
    expect(src).toContain("emissive: PLAYER_HEAD_EMISSIVE");
    expect(TRACER_FLASH_COLOR).toBe(0xffdd6e);
    expect(TRACER_COLOR).toBe(0xffffb8);
    expect(IMPACT_SPARK_LIGHT_COLOR).toBe(0xffef93);
    expect(IMPACT_SPARK_COLOR).toBe(0xffef93);
    expect(MUZZLE_LIGHT_COLOR).toBe(0xffffb8);
    expect(MUZZLE_FLASH_COLOR).toBe(0xffffdd);
    expect(WARM_LIGHT_COLOR).toBe(0xffca81);
    expect(POSSESSED_HEAD_EMISSIVE).toBe(0x30124a);
    expect(POSSESSED_HEAD_COLOR).toBe(0x8c469f);
    expect(POSSESSED_COLOR).toBe(0x68347b);
    expect(HOSTILE_COLOR).toBe(0x7b1e1e);
    expect(PLAYER_COLOR).toBe(0x55a4f4);
    expect(PLAYER_HEAD_COLOR).toBe(0x91d1ff);
    expect(PLAYER_HEAD_EMISSIVE).toBe(0x122537);
    expect(MARKER_PALETTE.possessed.emissive).toBe(0x30124a);
    expect(MARKER_PALETTE.player.badge).toBe(0x91d1ff);
    expect(MARKER_PALETTE.player.emissive).toBe(0x1e4a6e);
    expect(MARKER_PALETTE.player.ring).toBe(0x4392f4);
    expect(MARKER_RING_OPACITY).toBe(0.6877);
    expect(muteBadgeY).toBe(2.3);
    expect(possessedBadgeY).toBe(2.3);
  });
});

describe("worldView flashlight spot Y", () => {
  test("flashlight spot Y 1.55 × 1.15 → 1.7825; fill-Y/gain/base/color/flash/mesh/impact/muzzle/warmLight/possessed/hostile/player/intensity/palette/badge-Y iguales", () => {
    expect(FLASHLIGHT_SPOT_Y).toBe(1.7825);
    expect(FLASHLIGHT_SPOT_Y).toBeCloseTo(1.55 * 1.15, 10);
    expect(FLASHLIGHT_FILL_Y).toBe(1.5525);
    expect(FLASHLIGHT_FILL_DISTANCE_GAIN).toBe(4.025);
    expect(FLASHLIGHT_FILL_DISTANCE_BASE).toBe(8.05);
    expect(FLASHLIGHT_FILL_COLOR).toBe(0xcaefff);
    expect(FLASHLIGHT_FILL_DECAY).toBe(1.74);
    const src = readFileSync(
      resolve(process.cwd(), "src/render/worldView.ts"),
      "utf8",
    );
    expect(src).toContain("export const FLASHLIGHT_SPOT_Y = 1.7825");
    expect(src).toContain("torchSpot.position.set(0, FLASHLIGHT_SPOT_Y, 0)");
    expect(src).toContain("torchSpot.position.set(wx, FLASHLIGHT_SPOT_Y, wy)");
    expect(src).not.toMatch(/export const FLASHLIGHT_SPOT_Y = 1\.55/);
    expect(src).not.toMatch(/torchSpot\.position\.set\(0, 1\.55, 0\)/);
    expect(src).not.toMatch(/torchSpot\.position\.set\(wx, 1\.55, wy\)/);
    expect(src).toContain("export const FLASHLIGHT_FILL_Y = 1.5525");
    expect(src).toContain("torchLight.position.set(0, FLASHLIGHT_FILL_Y, 0)");
    expect(src).toContain("torchLight.position.set(wx, FLASHLIGHT_FILL_Y, wy)");
    expect(src).toContain("export const WARM_LIGHT_Y = 1.7825");
    expect(src).toContain("warmLight.position.set(wx, WARM_LIGHT_Y, wy)");
    expect(src).not.toMatch(/warmLight\.position\.set\(wx, 1\.55, wy\)/);
    expect(WARM_LIGHT_Y).toBe(1.7825);
    expect(src).toContain("export const WARM_LIGHT_INTENSITY_MUL = 1.7825");
    expect(src).toContain("warmLight.intensity = i * WARM_LIGHT_INTENSITY_MUL");
    expect(src).not.toMatch(/warmLight\.intensity = i \* 1\.55/);
    expect(WARM_LIGHT_INTENSITY_MUL).toBe(1.7825);
    expect(src).toContain("export const FLASHLIGHT_FILL_DISTANCE_GAIN = 4.025");
    expect(src).toContain("export const FLASHLIGHT_FILL_DISTANCE_BASE = 8.05");
    expect(src).toContain(
      "torchLight.distance = FLASHLIGHT_FILL_DISTANCE_BASE + i * FLASHLIGHT_FILL_DISTANCE_GAIN",
    );
    expect(src).toContain("export const FLASHLIGHT_FILL_COLOR = 0xcaefff");
    expect(src).toContain("export const FLASHLIGHT_FILL_DECAY = 1.74");
    expect(src).toContain("new THREE.PointLight(FLASHLIGHT_FILL_COLOR, 0, 10, FLASHLIGHT_FILL_DECAY)");
    expect(src).not.toMatch(/new THREE\.PointLight\(FLASHLIGHT_FILL_COLOR, 0, 10, 2\)/);
    expect(src).toContain("torchLight.color.setHex(FLASHLIGHT_FILL_COLOR)");
    expect(src).toContain("export const TRACER_FLASH_COLOR = 0xffdd6e");
    expect(src).toContain("new THREE.PointLight(TRACER_FLASH_COLOR, TRACER_FLASH_INTENSITY, TRACER_FLASH_DISTANCE, TRACER_FLASH_DECAY)");
    expect(src).toContain("export const TRACER_COLOR = 0xffffb8");
    expect(src).toContain("color: TRACER_COLOR");
    expect(src).toContain("export const IMPACT_SPARK_LIGHT_COLOR = 0xffef93");
    expect(src).toContain("new THREE.PointLight(\n    IMPACT_SPARK_LIGHT_COLOR,");
    expect(src).toContain("export const IMPACT_SPARK_COLOR = 0xffef93");
    expect(src).toContain("color: IMPACT_SPARK_COLOR");
    expect(src).toContain("export const MUZZLE_LIGHT_COLOR = 0xffffb8");
    expect(src).toContain("new THREE.PointLight(\n    MUZZLE_LIGHT_COLOR,");
    expect(src).toContain("export const MUZZLE_FLASH_COLOR = 0xffffdd");
    expect(src).toContain("color: MUZZLE_FLASH_COLOR");
    expect(src).toContain("export const WARM_LIGHT_COLOR = 0xffca81");
    expect(src).toContain("new THREE.PointLight(WARM_LIGHT_COLOR, 0, 7.5, WARM_LIGHT_DECAY)");
    expect(src).toContain("export const POSSESSED_HEAD_EMISSIVE = 0x30124a");
    expect(src).toContain("emissive: POSSESSED_HEAD_EMISSIVE");
    expect(src).toContain("color: POSSESSED_HEAD_COLOR");
    expect(src).toContain("export const POSSESSED_HEAD_COLOR = 0x8c469f");
    expect(src).toContain("emissive: POSSESSED_EMISSIVE");
    expect(src).toContain("color: POSSESSED_COLOR");
    expect(src).toContain("const POSSESSED_EMISSIVE = 0x1e0925");
    expect(src).toContain("emissiveIntensity: 0.22");
    expect(src).toContain("color: HOSTILE_COLOR");
    expect(src).toContain("color: PLAYER_COLOR");
    expect(src).toContain("color: PLAYER_HEAD_COLOR");
    expect(src).toContain("emissive: PLAYER_HEAD_EMISSIVE");
    expect(TRACER_FLASH_COLOR).toBe(0xffdd6e);
    expect(TRACER_COLOR).toBe(0xffffb8);
    expect(IMPACT_SPARK_LIGHT_COLOR).toBe(0xffef93);
    expect(IMPACT_SPARK_COLOR).toBe(0xffef93);
    expect(MUZZLE_LIGHT_COLOR).toBe(0xffffb8);
    expect(MUZZLE_FLASH_COLOR).toBe(0xffffdd);
    expect(WARM_LIGHT_COLOR).toBe(0xffca81);
    expect(POSSESSED_HEAD_EMISSIVE).toBe(0x30124a);
    expect(POSSESSED_HEAD_COLOR).toBe(0x8c469f);
    expect(POSSESSED_COLOR).toBe(0x68347b);
    expect(HOSTILE_COLOR).toBe(0x7b1e1e);
    expect(PLAYER_COLOR).toBe(0x55a4f4);
    expect(PLAYER_HEAD_COLOR).toBe(0x91d1ff);
    expect(PLAYER_HEAD_EMISSIVE).toBe(0x122537);
    expect(MARKER_PALETTE.possessed.emissive).toBe(0x30124a);
    expect(MARKER_PALETTE.player.badge).toBe(0x91d1ff);
    expect(MARKER_PALETTE.player.emissive).toBe(0x1e4a6e);
    expect(MARKER_PALETTE.player.ring).toBe(0x4392f4);
    expect(MARKER_RING_OPACITY).toBe(0.6877);
    expect(muteBadgeY).toBe(2.3);
    expect(possessedBadgeY).toBe(2.3);
  });
});

describe("worldView flashlight spot distance extra", () => {
  test("flashlight spot distance extra 1.6 × 1.15 → 1.84; spot-Y/fill-Y/gain/base/color/flash/mesh/impact/muzzle/warmLight/possessed/hostile/player/intensity/palette/badge-Y iguales", () => {
    expect(FLASHLIGHT_SPOT_DISTANCE_EXTRA).toBe(1.84);
    expect(FLASHLIGHT_SPOT_DISTANCE_EXTRA).toBeCloseTo(1.6 * 1.15, 10);
    expect(FLASHLIGHT_SPOT_DISTANCE_GAIN).toBe(2.3);
    expect(FLASHLIGHT_SPOT_Y).toBe(1.7825);
    expect(FLASHLIGHT_FILL_Y).toBe(1.5525);
    expect(FLASHLIGHT_FILL_DISTANCE_GAIN).toBe(4.025);
    expect(FLASHLIGHT_FILL_DISTANCE_BASE).toBe(8.05);
    expect(FLASHLIGHT_FILL_COLOR).toBe(0xcaefff);
    expect(FLASHLIGHT_FILL_DECAY).toBe(1.74);
    const src = readFileSync(
      resolve(process.cwd(), "src/render/worldView.ts"),
      "utf8",
    );
    expect(src).toContain("export const FLASHLIGHT_SPOT_DISTANCE_EXTRA = 1.84");
    expect(src).toContain("export const FLASHLIGHT_SPOT_DISTANCE_GAIN = 2.3");
    expect(src).toContain(
      "torchSpot.distance = FLASHLIGHT_CONE_LENGTH + FLASHLIGHT_SPOT_DISTANCE_EXTRA + i * FLASHLIGHT_SPOT_DISTANCE_GAIN",
    );
    expect(src).toContain("FLASHLIGHT_CONE_LENGTH + 2.4");
    expect(src).not.toMatch(/export const FLASHLIGHT_SPOT_DISTANCE_EXTRA = 1\.6/);
    expect(src).not.toMatch(
      /torchSpot\.distance = FLASHLIGHT_CONE_LENGTH \+ FLASHLIGHT_SPOT_DISTANCE_EXTRA \+ i \* 2/,
    );
    expect(src).not.toMatch(
      /torchSpot\.distance = FLASHLIGHT_CONE_LENGTH \+ 1\.6 \+ i \* 2/,
    );
    expect(src).toContain("export const FLASHLIGHT_SPOT_Y = 1.7825");
    expect(src).toContain("torchSpot.position.set(0, FLASHLIGHT_SPOT_Y, 0)");
    expect(src).toContain("torchSpot.position.set(wx, FLASHLIGHT_SPOT_Y, wy)");
    expect(src).toContain("export const FLASHLIGHT_FILL_Y = 1.5525");
    expect(src).toContain("torchLight.position.set(0, FLASHLIGHT_FILL_Y, 0)");
    expect(src).toContain("torchLight.position.set(wx, FLASHLIGHT_FILL_Y, wy)");
    expect(src).toContain("export const WARM_LIGHT_Y = 1.7825");
    expect(src).toContain("warmLight.position.set(wx, WARM_LIGHT_Y, wy)");
    expect(src).not.toMatch(/warmLight\.position\.set\(wx, 1\.55, wy\)/);
    expect(WARM_LIGHT_Y).toBe(1.7825);
    expect(src).toContain("export const WARM_LIGHT_INTENSITY_MUL = 1.7825");
    expect(src).toContain("warmLight.intensity = i * WARM_LIGHT_INTENSITY_MUL");
    expect(src).not.toMatch(/warmLight\.intensity = i \* 1\.55/);
    expect(WARM_LIGHT_INTENSITY_MUL).toBe(1.7825);
    expect(src).toContain("export const FLASHLIGHT_FILL_DISTANCE_GAIN = 4.025");
    expect(src).toContain("export const FLASHLIGHT_FILL_DISTANCE_BASE = 8.05");
    expect(src).toContain(
      "torchLight.distance = FLASHLIGHT_FILL_DISTANCE_BASE + i * FLASHLIGHT_FILL_DISTANCE_GAIN",
    );
    expect(src).toContain("export const FLASHLIGHT_FILL_COLOR = 0xcaefff");
    expect(src).toContain("export const FLASHLIGHT_FILL_DECAY = 1.74");
    expect(src).toContain("new THREE.PointLight(FLASHLIGHT_FILL_COLOR, 0, 10, FLASHLIGHT_FILL_DECAY)");
    expect(src).not.toMatch(/new THREE\.PointLight\(FLASHLIGHT_FILL_COLOR, 0, 10, 2\)/);
    expect(src).toContain("torchLight.color.setHex(FLASHLIGHT_FILL_COLOR)");
    expect(src).toContain("export const TRACER_FLASH_COLOR = 0xffdd6e");
    expect(src).toContain("new THREE.PointLight(TRACER_FLASH_COLOR, TRACER_FLASH_INTENSITY, TRACER_FLASH_DISTANCE, TRACER_FLASH_DECAY)");
    expect(src).toContain("export const TRACER_COLOR = 0xffffb8");
    expect(src).toContain("color: TRACER_COLOR");
    expect(src).toContain("export const IMPACT_SPARK_LIGHT_COLOR = 0xffef93");
    expect(src).toContain("new THREE.PointLight(\n    IMPACT_SPARK_LIGHT_COLOR,");
    expect(src).toContain("export const IMPACT_SPARK_COLOR = 0xffef93");
    expect(src).toContain("color: IMPACT_SPARK_COLOR");
    expect(src).toContain("export const MUZZLE_LIGHT_COLOR = 0xffffb8");
    expect(src).toContain("new THREE.PointLight(\n    MUZZLE_LIGHT_COLOR,");
    expect(src).toContain("export const MUZZLE_FLASH_COLOR = 0xffffdd");
    expect(src).toContain("color: MUZZLE_FLASH_COLOR");
    expect(src).toContain("export const WARM_LIGHT_COLOR = 0xffca81");
    expect(src).toContain("new THREE.PointLight(WARM_LIGHT_COLOR, 0, 7.5, WARM_LIGHT_DECAY)");
    expect(src).toContain("export const POSSESSED_HEAD_EMISSIVE = 0x30124a");
    expect(src).toContain("emissive: POSSESSED_HEAD_EMISSIVE");
    expect(src).toContain("color: POSSESSED_HEAD_COLOR");
    expect(src).toContain("export const POSSESSED_HEAD_COLOR = 0x8c469f");
    expect(src).toContain("emissive: POSSESSED_EMISSIVE");
    expect(src).toContain("color: POSSESSED_COLOR");
    expect(src).toContain("const POSSESSED_EMISSIVE = 0x1e0925");
    expect(src).toContain("emissiveIntensity: 0.22");
    expect(src).toContain("color: HOSTILE_COLOR");
    expect(src).toContain("color: PLAYER_COLOR");
    expect(src).toContain("color: PLAYER_HEAD_COLOR");
    expect(src).toContain("emissive: PLAYER_HEAD_EMISSIVE");
    expect(TRACER_FLASH_COLOR).toBe(0xffdd6e);
    expect(TRACER_COLOR).toBe(0xffffb8);
    expect(IMPACT_SPARK_LIGHT_COLOR).toBe(0xffef93);
    expect(IMPACT_SPARK_COLOR).toBe(0xffef93);
    expect(MUZZLE_LIGHT_COLOR).toBe(0xffffb8);
    expect(MUZZLE_FLASH_COLOR).toBe(0xffffdd);
    expect(WARM_LIGHT_COLOR).toBe(0xffca81);
    expect(POSSESSED_HEAD_EMISSIVE).toBe(0x30124a);
    expect(POSSESSED_HEAD_COLOR).toBe(0x8c469f);
    expect(POSSESSED_COLOR).toBe(0x68347b);
    expect(HOSTILE_COLOR).toBe(0x7b1e1e);
    expect(PLAYER_COLOR).toBe(0x55a4f4);
    expect(PLAYER_HEAD_COLOR).toBe(0x91d1ff);
    expect(PLAYER_HEAD_EMISSIVE).toBe(0x122537);
    expect(MARKER_PALETTE.possessed.emissive).toBe(0x30124a);
    expect(MARKER_PALETTE.player.badge).toBe(0x91d1ff);
    expect(MARKER_PALETTE.player.emissive).toBe(0x1e4a6e);
    expect(MARKER_PALETTE.player.ring).toBe(0x4392f4);
    expect(MARKER_RING_OPACITY).toBe(0.6877);
    expect(muteBadgeY).toBe(2.3);
    expect(possessedBadgeY).toBe(2.3);
  });
});

describe("worldView flashlight spot distance gain", () => {
  test("flashlight spot distance gain 2 × 1.15 → 2.3; extra/spot-Y/fill-Y/gain/base/color/flash/mesh/impact/muzzle/warmLight/possessed/hostile/player/intensity/palette/badge-Y iguales", () => {
    expect(FLASHLIGHT_SPOT_DISTANCE_GAIN).toBe(2.3);
    expect(FLASHLIGHT_SPOT_DISTANCE_GAIN).toBeCloseTo(2 * 1.15, 10);
    expect(FLASHLIGHT_SPOT_DISTANCE_EXTRA).toBe(1.84);
    expect(FLASHLIGHT_SPOT_Y).toBe(1.7825);
    expect(FLASHLIGHT_FILL_Y).toBe(1.5525);
    expect(FLASHLIGHT_FILL_DISTANCE_GAIN).toBe(4.025);
    expect(FLASHLIGHT_FILL_DISTANCE_BASE).toBe(8.05);
    expect(FLASHLIGHT_FILL_COLOR).toBe(0xcaefff);
    expect(FLASHLIGHT_FILL_DECAY).toBe(1.74);
    const src = readFileSync(
      resolve(process.cwd(), "src/render/worldView.ts"),
      "utf8",
    );
    expect(src).toContain("export const FLASHLIGHT_SPOT_DISTANCE_GAIN = 2.3");
    expect(src).toContain("export const FLASHLIGHT_SPOT_DISTANCE_EXTRA = 1.84");
    expect(src).toContain(
      "torchSpot.distance = FLASHLIGHT_CONE_LENGTH + FLASHLIGHT_SPOT_DISTANCE_EXTRA + i * FLASHLIGHT_SPOT_DISTANCE_GAIN",
    );
    expect(src).toContain("FLASHLIGHT_CONE_LENGTH + 2.4");
    expect(src).not.toMatch(/export const FLASHLIGHT_SPOT_DISTANCE_GAIN = 2[^.]/);
    expect(src).not.toMatch(
      /torchSpot\.distance = FLASHLIGHT_CONE_LENGTH \+ FLASHLIGHT_SPOT_DISTANCE_EXTRA \+ i \* 2/,
    );
    expect(src).toContain("export const FLASHLIGHT_SPOT_Y = 1.7825");
    expect(src).toContain("torchSpot.position.set(0, FLASHLIGHT_SPOT_Y, 0)");
    expect(src).toContain("torchSpot.position.set(wx, FLASHLIGHT_SPOT_Y, wy)");
    expect(src).toContain("export const FLASHLIGHT_FILL_Y = 1.5525");
    expect(src).toContain("torchLight.position.set(0, FLASHLIGHT_FILL_Y, 0)");
    expect(src).toContain("torchLight.position.set(wx, FLASHLIGHT_FILL_Y, wy)");
    expect(src).toContain("export const WARM_LIGHT_Y = 1.7825");
    expect(src).toContain("warmLight.position.set(wx, WARM_LIGHT_Y, wy)");
    expect(src).not.toMatch(/warmLight\.position\.set\(wx, 1\.55, wy\)/);
    expect(WARM_LIGHT_Y).toBe(1.7825);
    expect(src).toContain("export const WARM_LIGHT_INTENSITY_MUL = 1.7825");
    expect(src).toContain("warmLight.intensity = i * WARM_LIGHT_INTENSITY_MUL");
    expect(src).not.toMatch(/warmLight\.intensity = i \* 1\.55/);
    expect(WARM_LIGHT_INTENSITY_MUL).toBe(1.7825);
    expect(src).toContain("export const FLASHLIGHT_FILL_DISTANCE_GAIN = 4.025");
    expect(src).toContain("export const FLASHLIGHT_FILL_DISTANCE_BASE = 8.05");
    expect(src).toContain(
      "torchLight.distance = FLASHLIGHT_FILL_DISTANCE_BASE + i * FLASHLIGHT_FILL_DISTANCE_GAIN",
    );
    expect(src).toContain("export const FLASHLIGHT_FILL_COLOR = 0xcaefff");
    expect(src).toContain("export const FLASHLIGHT_FILL_DECAY = 1.74");
    expect(src).toContain("new THREE.PointLight(FLASHLIGHT_FILL_COLOR, 0, 10, FLASHLIGHT_FILL_DECAY)");
    expect(src).not.toMatch(/new THREE\.PointLight\(FLASHLIGHT_FILL_COLOR, 0, 10, 2\)/);
    expect(src).toContain("torchLight.color.setHex(FLASHLIGHT_FILL_COLOR)");
    expect(src).toContain("export const TRACER_FLASH_COLOR = 0xffdd6e");
    expect(src).toContain("new THREE.PointLight(TRACER_FLASH_COLOR, TRACER_FLASH_INTENSITY, TRACER_FLASH_DISTANCE, TRACER_FLASH_DECAY)");
    expect(src).toContain("export const TRACER_COLOR = 0xffffb8");
    expect(src).toContain("color: TRACER_COLOR");
    expect(src).toContain("export const IMPACT_SPARK_LIGHT_COLOR = 0xffef93");
    expect(src).toContain("new THREE.PointLight(\n    IMPACT_SPARK_LIGHT_COLOR,");
    expect(src).toContain("export const IMPACT_SPARK_COLOR = 0xffef93");
    expect(src).toContain("color: IMPACT_SPARK_COLOR");
    expect(src).toContain("export const MUZZLE_LIGHT_COLOR = 0xffffb8");
    expect(src).toContain("new THREE.PointLight(\n    MUZZLE_LIGHT_COLOR,");
    expect(src).toContain("export const MUZZLE_FLASH_COLOR = 0xffffdd");
    expect(src).toContain("color: MUZZLE_FLASH_COLOR");
    expect(src).toContain("export const WARM_LIGHT_COLOR = 0xffca81");
    expect(src).toContain("new THREE.PointLight(WARM_LIGHT_COLOR, 0, 7.5, WARM_LIGHT_DECAY)");
    expect(src).toContain("export const POSSESSED_HEAD_EMISSIVE = 0x30124a");
    expect(src).toContain("emissive: POSSESSED_HEAD_EMISSIVE");
    expect(src).toContain("color: POSSESSED_HEAD_COLOR");
    expect(src).toContain("export const POSSESSED_HEAD_COLOR = 0x8c469f");
    expect(src).toContain("emissive: POSSESSED_EMISSIVE");
    expect(src).toContain("color: POSSESSED_COLOR");
    expect(src).toContain("const POSSESSED_EMISSIVE = 0x1e0925");
    expect(src).toContain("emissiveIntensity: 0.22");
    expect(src).toContain("color: HOSTILE_COLOR");
    expect(src).toContain("color: PLAYER_COLOR");
    expect(src).toContain("color: PLAYER_HEAD_COLOR");
    expect(src).toContain("emissive: PLAYER_HEAD_EMISSIVE");
    expect(TRACER_FLASH_COLOR).toBe(0xffdd6e);
    expect(TRACER_COLOR).toBe(0xffffb8);
    expect(IMPACT_SPARK_LIGHT_COLOR).toBe(0xffef93);
    expect(IMPACT_SPARK_COLOR).toBe(0xffef93);
    expect(MUZZLE_LIGHT_COLOR).toBe(0xffffb8);
    expect(MUZZLE_FLASH_COLOR).toBe(0xffffdd);
    expect(WARM_LIGHT_COLOR).toBe(0xffca81);
    expect(POSSESSED_HEAD_EMISSIVE).toBe(0x30124a);
    expect(POSSESSED_HEAD_COLOR).toBe(0x8c469f);
    expect(POSSESSED_COLOR).toBe(0x68347b);
    expect(HOSTILE_COLOR).toBe(0x7b1e1e);
    expect(PLAYER_COLOR).toBe(0x55a4f4);
    expect(PLAYER_HEAD_COLOR).toBe(0x91d1ff);
    expect(PLAYER_HEAD_EMISSIVE).toBe(0x122537);
    expect(MARKER_PALETTE.possessed.emissive).toBe(0x30124a);
    expect(MARKER_PALETTE.player.badge).toBe(0x91d1ff);
    expect(MARKER_PALETTE.player.emissive).toBe(0x1e4a6e);
    expect(MARKER_PALETTE.player.ring).toBe(0x4392f4);
    expect(MARKER_RING_OPACITY).toBe(0.6877);
    expect(muteBadgeY).toBe(2.3);
    expect(possessedBadgeY).toBe(2.3);
  });
});

describe("worldView warm light intensity mul", () => {
  test("warm light intensity mul 1.55 × 1.15 → 1.7825; color/spot-gain/extra/spot-Y/fill-Y/gain/base/flash/mesh/impact/muzzle/possessed/hostile/player/intensity/palette/badge-Y iguales", () => {
    expect(WARM_LIGHT_INTENSITY_MUL).toBe(1.7825);
    expect(WARM_LIGHT_INTENSITY_MUL).toBeCloseTo(1.55 * 1.15, 10);
    expect(FLASHLIGHT_SPOT_DISTANCE_GAIN).toBe(2.3);
    expect(FLASHLIGHT_SPOT_DISTANCE_EXTRA).toBe(1.84);
    expect(FLASHLIGHT_SPOT_Y).toBe(1.7825);
    expect(FLASHLIGHT_FILL_Y).toBe(1.5525);
    expect(FLASHLIGHT_FILL_DISTANCE_GAIN).toBe(4.025);
    expect(FLASHLIGHT_FILL_DISTANCE_BASE).toBe(8.05);
    expect(FLASHLIGHT_FILL_COLOR).toBe(0xcaefff);
    expect(FLASHLIGHT_FILL_DECAY).toBe(1.74);
    const src = readFileSync(
      resolve(process.cwd(), "src/render/worldView.ts"),
      "utf8",
    );
    expect(src).toContain("export const WARM_LIGHT_INTENSITY_MUL = 1.7825");
    expect(src).toContain("warmLight.intensity = i * WARM_LIGHT_INTENSITY_MUL");
    expect(src).not.toMatch(/export const WARM_LIGHT_INTENSITY_MUL = 1\.55/);
    expect(src).not.toMatch(/warmLight\.intensity = i \* 1\.55/);
    expect(src).toContain("export const WARM_LIGHT_Y = 1.7825");
    expect(src).toContain("warmLight.position.set(wx, WARM_LIGHT_Y, wy)");
    expect(src).not.toMatch(/export const WARM_LIGHT_Y = 1\.55/);
    expect(src).not.toMatch(/warmLight\.position\.set\(wx, 1\.55, wy\)/);
    expect(WARM_LIGHT_Y).toBe(1.7825);
    expect(src).toContain("warmLight.position.set(0, 1.6, 0)");
    expect(src).toContain("export const FLASHLIGHT_SPOT_DISTANCE_GAIN = 2.3");
    expect(src).toContain("export const FLASHLIGHT_SPOT_DISTANCE_EXTRA = 1.84");
    expect(src).toContain(
      "torchSpot.distance = FLASHLIGHT_CONE_LENGTH + FLASHLIGHT_SPOT_DISTANCE_EXTRA + i * FLASHLIGHT_SPOT_DISTANCE_GAIN",
    );
    expect(src).toContain("FLASHLIGHT_CONE_LENGTH + 2.4");
    expect(src).toContain("export const FLASHLIGHT_SPOT_Y = 1.7825");
    expect(src).toContain("torchSpot.position.set(0, FLASHLIGHT_SPOT_Y, 0)");
    expect(src).toContain("torchSpot.position.set(wx, FLASHLIGHT_SPOT_Y, wy)");
    expect(src).toContain("export const FLASHLIGHT_FILL_Y = 1.5525");
    expect(src).toContain("torchLight.position.set(0, FLASHLIGHT_FILL_Y, 0)");
    expect(src).toContain("torchLight.position.set(wx, FLASHLIGHT_FILL_Y, wy)");
    expect(src).toContain("export const FLASHLIGHT_FILL_DISTANCE_GAIN = 4.025");
    expect(src).toContain("export const FLASHLIGHT_FILL_DISTANCE_BASE = 8.05");
    expect(src).toContain(
      "torchLight.distance = FLASHLIGHT_FILL_DISTANCE_BASE + i * FLASHLIGHT_FILL_DISTANCE_GAIN",
    );
    expect(src).toContain("export const FLASHLIGHT_FILL_COLOR = 0xcaefff");
    expect(src).toContain("export const FLASHLIGHT_FILL_DECAY = 1.74");
    expect(src).toContain("new THREE.PointLight(FLASHLIGHT_FILL_COLOR, 0, 10, FLASHLIGHT_FILL_DECAY)");
    expect(src).not.toMatch(/new THREE\.PointLight\(FLASHLIGHT_FILL_COLOR, 0, 10, 2\)/);
    expect(src).toContain("torchLight.color.setHex(FLASHLIGHT_FILL_COLOR)");
    expect(src).toContain("export const TRACER_FLASH_COLOR = 0xffdd6e");
    expect(src).toContain("new THREE.PointLight(TRACER_FLASH_COLOR, TRACER_FLASH_INTENSITY, TRACER_FLASH_DISTANCE, TRACER_FLASH_DECAY)");
    expect(src).toContain("export const TRACER_COLOR = 0xffffb8");
    expect(src).toContain("color: TRACER_COLOR");
    expect(src).toContain("export const IMPACT_SPARK_LIGHT_COLOR = 0xffef93");
    expect(src).toContain("new THREE.PointLight(\n    IMPACT_SPARK_LIGHT_COLOR,");
    expect(src).toContain("export const IMPACT_SPARK_COLOR = 0xffef93");
    expect(src).toContain("color: IMPACT_SPARK_COLOR");
    expect(src).toContain("export const MUZZLE_LIGHT_COLOR = 0xffffb8");
    expect(src).toContain("new THREE.PointLight(\n    MUZZLE_LIGHT_COLOR,");
    expect(src).toContain("export const MUZZLE_FLASH_COLOR = 0xffffdd");
    expect(src).toContain("color: MUZZLE_FLASH_COLOR");
    expect(src).toContain("export const WARM_LIGHT_COLOR = 0xffca81");
    expect(src).toContain("new THREE.PointLight(WARM_LIGHT_COLOR, 0, 7.5, WARM_LIGHT_DECAY)");
    expect(src).toContain("export const POSSESSED_HEAD_EMISSIVE = 0x30124a");
    expect(src).toContain("emissive: POSSESSED_HEAD_EMISSIVE");
    expect(src).toContain("color: POSSESSED_HEAD_COLOR");
    expect(src).toContain("export const POSSESSED_HEAD_COLOR = 0x8c469f");
    expect(src).toContain("emissive: POSSESSED_EMISSIVE");
    expect(src).toContain("color: POSSESSED_COLOR");
    expect(src).toContain("const POSSESSED_EMISSIVE = 0x1e0925");
    expect(src).toContain("emissiveIntensity: 0.22");
    expect(src).toContain("color: HOSTILE_COLOR");
    expect(src).toContain("color: PLAYER_COLOR");
    expect(src).toContain("color: PLAYER_HEAD_COLOR");
    expect(src).toContain("emissive: PLAYER_HEAD_EMISSIVE");
    expect(TRACER_FLASH_COLOR).toBe(0xffdd6e);
    expect(TRACER_COLOR).toBe(0xffffb8);
    expect(IMPACT_SPARK_LIGHT_COLOR).toBe(0xffef93);
    expect(IMPACT_SPARK_COLOR).toBe(0xffef93);
    expect(MUZZLE_LIGHT_COLOR).toBe(0xffffb8);
    expect(MUZZLE_FLASH_COLOR).toBe(0xffffdd);
    expect(WARM_LIGHT_COLOR).toBe(0xffca81);
    expect(POSSESSED_HEAD_EMISSIVE).toBe(0x30124a);
    expect(POSSESSED_HEAD_COLOR).toBe(0x8c469f);
    expect(POSSESSED_COLOR).toBe(0x68347b);
    expect(HOSTILE_COLOR).toBe(0x7b1e1e);
    expect(PLAYER_COLOR).toBe(0x55a4f4);
    expect(PLAYER_HEAD_COLOR).toBe(0x91d1ff);
    expect(PLAYER_HEAD_EMISSIVE).toBe(0x122537);
    expect(MARKER_PALETTE.possessed.emissive).toBe(0x30124a);
    expect(MARKER_PALETTE.player.badge).toBe(0x91d1ff);
    expect(MARKER_PALETTE.player.emissive).toBe(0x1e4a6e);
    expect(MARKER_PALETTE.player.ring).toBe(0x4392f4);
    expect(MARKER_RING_OPACITY).toBe(0.6877);
    expect(muteBadgeY).toBe(2.3);
    expect(possessedBadgeY).toBe(2.3);
  });
});

describe("worldView warm light Y", () => {
  test("warm light Y 1.55 × 1.15 → 1.7825; intensity/color/spot-gain/extra/spot-Y/fill-Y/gain/base/flash/mesh/impact/muzzle/possessed/hostile/player/palette/badge-Y iguales", () => {
    expect(WARM_LIGHT_Y).toBe(1.7825);
    expect(WARM_LIGHT_Y).toBeCloseTo(1.55 * 1.15, 10);
    expect(WARM_LIGHT_INTENSITY_MUL).toBe(1.7825);
    expect(FLASHLIGHT_SPOT_DISTANCE_GAIN).toBe(2.3);
    expect(FLASHLIGHT_SPOT_DISTANCE_EXTRA).toBe(1.84);
    expect(FLASHLIGHT_SPOT_Y).toBe(1.7825);
    expect(FLASHLIGHT_FILL_Y).toBe(1.5525);
    expect(FLASHLIGHT_FILL_DISTANCE_GAIN).toBe(4.025);
    expect(FLASHLIGHT_FILL_DISTANCE_BASE).toBe(8.05);
    expect(FLASHLIGHT_FILL_COLOR).toBe(0xcaefff);
    expect(FLASHLIGHT_FILL_DECAY).toBe(1.74);
    const src = readFileSync(
      resolve(process.cwd(), "src/render/worldView.ts"),
      "utf8",
    );
    expect(src).toContain("export const WARM_LIGHT_Y = 1.7825");
    expect(src).toContain("warmLight.position.set(wx, WARM_LIGHT_Y, wy)");
    expect(src).not.toMatch(/export const WARM_LIGHT_Y = 1\.55/);
    expect(src).not.toMatch(/warmLight\.position\.set\(wx, 1\.55, wy\)/);
    expect(src).toContain("warmLight.position.set(0, 1.6, 0)");
    expect(src).toContain("export const WARM_LIGHT_INTENSITY_MUL = 1.7825");
    expect(src).toContain("warmLight.intensity = i * WARM_LIGHT_INTENSITY_MUL");
    expect(src).not.toMatch(/export const WARM_LIGHT_INTENSITY_MUL = 1\.55/);
    expect(src).not.toMatch(/warmLight\.intensity = i \* 1\.55/);
    expect(src).toContain("export const WARM_LIGHT_DISTANCE_BASE = 7.475");
    expect(src).toContain("export const WARM_LIGHT_DISTANCE_GAIN = 2.875");
    expect(src).toContain(
      "warmLight.distance = WARM_LIGHT_DISTANCE_BASE + i * WARM_LIGHT_DISTANCE_GAIN",
    );
    expect(src).not.toMatch(/warmLight\.distance = 6\.5 \+ i \* 2\.5/);
    expect(src).not.toMatch(
      /warmLight\.distance = WARM_LIGHT_DISTANCE_BASE \+ i \* 2\.5/,
    );
    expect(WARM_LIGHT_DISTANCE_BASE).toBe(7.475);
    expect(WARM_LIGHT_DISTANCE_GAIN).toBe(2.875);
    expect(src).toContain("new THREE.PointLight(WARM_LIGHT_COLOR, 0, 7.5, WARM_LIGHT_DECAY)");
    expect(src).toContain("export const FLASHLIGHT_SPOT_DISTANCE_GAIN = 2.3");
    expect(src).toContain("export const FLASHLIGHT_SPOT_DISTANCE_EXTRA = 1.84");
    expect(src).toContain(
      "torchSpot.distance = FLASHLIGHT_CONE_LENGTH + FLASHLIGHT_SPOT_DISTANCE_EXTRA + i * FLASHLIGHT_SPOT_DISTANCE_GAIN",
    );
    expect(src).toContain("FLASHLIGHT_CONE_LENGTH + 2.4");
    expect(src).toContain("export const FLASHLIGHT_SPOT_Y = 1.7825");
    expect(src).toContain("torchSpot.position.set(0, FLASHLIGHT_SPOT_Y, 0)");
    expect(src).toContain("torchSpot.position.set(wx, FLASHLIGHT_SPOT_Y, wy)");
    expect(src).toContain("export const FLASHLIGHT_FILL_Y = 1.5525");
    expect(src).toContain("torchLight.position.set(0, FLASHLIGHT_FILL_Y, 0)");
    expect(src).toContain("torchLight.position.set(wx, FLASHLIGHT_FILL_Y, wy)");
    expect(src).toContain("export const FLASHLIGHT_FILL_DISTANCE_GAIN = 4.025");
    expect(src).toContain("export const FLASHLIGHT_FILL_DISTANCE_BASE = 8.05");
    expect(src).toContain(
      "torchLight.distance = FLASHLIGHT_FILL_DISTANCE_BASE + i * FLASHLIGHT_FILL_DISTANCE_GAIN",
    );
    expect(src).toContain("export const FLASHLIGHT_FILL_COLOR = 0xcaefff");
    expect(src).toContain("export const FLASHLIGHT_FILL_DECAY = 1.74");
    expect(src).toContain("new THREE.PointLight(FLASHLIGHT_FILL_COLOR, 0, 10, FLASHLIGHT_FILL_DECAY)");
    expect(src).not.toMatch(/new THREE\.PointLight\(FLASHLIGHT_FILL_COLOR, 0, 10, 2\)/);
    expect(src).toContain("torchLight.color.setHex(FLASHLIGHT_FILL_COLOR)");
    expect(src).toContain("export const TRACER_FLASH_COLOR = 0xffdd6e");
    expect(src).toContain("new THREE.PointLight(TRACER_FLASH_COLOR, TRACER_FLASH_INTENSITY, TRACER_FLASH_DISTANCE, TRACER_FLASH_DECAY)");
    expect(src).toContain("export const TRACER_COLOR = 0xffffb8");
    expect(src).toContain("color: TRACER_COLOR");
    expect(src).toContain("export const IMPACT_SPARK_LIGHT_COLOR = 0xffef93");
    expect(src).toContain("new THREE.PointLight(\n    IMPACT_SPARK_LIGHT_COLOR,");
    expect(src).toContain("export const IMPACT_SPARK_COLOR = 0xffef93");
    expect(src).toContain("color: IMPACT_SPARK_COLOR");
    expect(src).toContain("export const MUZZLE_LIGHT_COLOR = 0xffffb8");
    expect(src).toContain("new THREE.PointLight(\n    MUZZLE_LIGHT_COLOR,");
    expect(src).toContain("export const MUZZLE_FLASH_COLOR = 0xffffdd");
    expect(src).toContain("color: MUZZLE_FLASH_COLOR");
    expect(src).toContain("export const WARM_LIGHT_COLOR = 0xffca81");
    expect(src).toContain("export const POSSESSED_HEAD_EMISSIVE = 0x30124a");
    expect(src).toContain("emissive: POSSESSED_HEAD_EMISSIVE");
    expect(src).toContain("color: POSSESSED_HEAD_COLOR");
    expect(src).toContain("export const POSSESSED_HEAD_COLOR = 0x8c469f");
    expect(src).toContain("emissive: POSSESSED_EMISSIVE");
    expect(src).toContain("color: POSSESSED_COLOR");
    expect(src).toContain("const POSSESSED_EMISSIVE = 0x1e0925");
    expect(src).toContain("emissiveIntensity: 0.22");
    expect(src).toContain("color: HOSTILE_COLOR");
    expect(src).toContain("color: PLAYER_COLOR");
    expect(src).toContain("color: PLAYER_HEAD_COLOR");
    expect(src).toContain("emissive: PLAYER_HEAD_EMISSIVE");
    expect(TRACER_FLASH_COLOR).toBe(0xffdd6e);
    expect(TRACER_COLOR).toBe(0xffffb8);
    expect(IMPACT_SPARK_LIGHT_COLOR).toBe(0xffef93);
    expect(IMPACT_SPARK_COLOR).toBe(0xffef93);
    expect(MUZZLE_LIGHT_COLOR).toBe(0xffffb8);
    expect(MUZZLE_FLASH_COLOR).toBe(0xffffdd);
    expect(WARM_LIGHT_COLOR).toBe(0xffca81);
    expect(WARM_LIGHT_INTENSITY_MUL).toBe(1.7825);
    expect(POSSESSED_HEAD_EMISSIVE).toBe(0x30124a);
    expect(POSSESSED_HEAD_COLOR).toBe(0x8c469f);
    expect(POSSESSED_COLOR).toBe(0x68347b);
    expect(HOSTILE_COLOR).toBe(0x7b1e1e);
    expect(PLAYER_COLOR).toBe(0x55a4f4);
    expect(PLAYER_HEAD_COLOR).toBe(0x91d1ff);
    expect(PLAYER_HEAD_EMISSIVE).toBe(0x122537);
    expect(MARKER_PALETTE.possessed.emissive).toBe(0x30124a);
    expect(MARKER_PALETTE.player.badge).toBe(0x91d1ff);
    expect(MARKER_PALETTE.player.emissive).toBe(0x1e4a6e);
    expect(MARKER_PALETTE.player.ring).toBe(0x4392f4);
    expect(MARKER_RING_OPACITY).toBe(0.6877);
    expect(muteBadgeY).toBe(2.3);
    expect(possessedBadgeY).toBe(2.3);
  });
});

describe("worldView warm light distance base", () => {
  test("warm light distance base 6.5 × 1.15 → 7.475; Y/intensity/color/spot-gain/extra/spot-Y/fill-Y/gain/base/flash/mesh/impact/muzzle/possessed/hostile/player/palette/badge-Y iguales", () => {
    expect(WARM_LIGHT_DISTANCE_BASE).toBe(7.475);
    expect(WARM_LIGHT_DISTANCE_BASE).toBeCloseTo(6.5 * 1.15, 10);
    expect(WARM_LIGHT_Y).toBe(1.7825);
    expect(WARM_LIGHT_INTENSITY_MUL).toBe(1.7825);
    expect(FLASHLIGHT_SPOT_DISTANCE_GAIN).toBe(2.3);
    expect(FLASHLIGHT_SPOT_DISTANCE_EXTRA).toBe(1.84);
    expect(FLASHLIGHT_SPOT_Y).toBe(1.7825);
    expect(FLASHLIGHT_FILL_Y).toBe(1.5525);
    expect(FLASHLIGHT_FILL_DISTANCE_GAIN).toBe(4.025);
    expect(FLASHLIGHT_FILL_DISTANCE_BASE).toBe(8.05);
    expect(FLASHLIGHT_FILL_COLOR).toBe(0xcaefff);
    expect(FLASHLIGHT_FILL_DECAY).toBe(1.74);
    const src = readFileSync(
      resolve(process.cwd(), "src/render/worldView.ts"),
      "utf8",
    );
    expect(src).toContain("export const WARM_LIGHT_DISTANCE_BASE = 7.475");
    expect(src).toContain("export const WARM_LIGHT_DISTANCE_GAIN = 2.875");
    expect(src).toContain(
      "warmLight.distance = WARM_LIGHT_DISTANCE_BASE + i * WARM_LIGHT_DISTANCE_GAIN",
    );
    expect(src).not.toMatch(/export const WARM_LIGHT_DISTANCE_BASE = 6\.5/);
    expect(src).not.toMatch(/warmLight\.distance = 6\.5 \+ i \* 2\.5/);
    expect(src).not.toMatch(
      /warmLight\.distance = WARM_LIGHT_DISTANCE_BASE \+ i \* 2\.5/,
    );
    expect(WARM_LIGHT_DISTANCE_GAIN).toBe(2.875);
    expect(src).toContain("new THREE.PointLight(WARM_LIGHT_COLOR, 0, 7.5, WARM_LIGHT_DECAY)");
    expect(src).toContain("export const WARM_LIGHT_Y = 1.7825");
    expect(src).toContain("warmLight.position.set(wx, WARM_LIGHT_Y, wy)");
    expect(src).not.toMatch(/export const WARM_LIGHT_Y = 1\.55/);
    expect(src).not.toMatch(/warmLight\.position\.set\(wx, 1\.55, wy\)/);
    expect(src).toContain("warmLight.position.set(0, 1.6, 0)");
    expect(src).toContain("export const WARM_LIGHT_INTENSITY_MUL = 1.7825");
    expect(src).toContain("warmLight.intensity = i * WARM_LIGHT_INTENSITY_MUL");
    expect(src).not.toMatch(/export const WARM_LIGHT_INTENSITY_MUL = 1\.55/);
    expect(src).not.toMatch(/warmLight\.intensity = i \* 1\.55/);
    expect(src).toContain("export const FLASHLIGHT_SPOT_DISTANCE_GAIN = 2.3");
    expect(src).toContain("export const FLASHLIGHT_SPOT_DISTANCE_EXTRA = 1.84");
    expect(src).toContain(
      "torchSpot.distance = FLASHLIGHT_CONE_LENGTH + FLASHLIGHT_SPOT_DISTANCE_EXTRA + i * FLASHLIGHT_SPOT_DISTANCE_GAIN",
    );
    expect(src).toContain("FLASHLIGHT_CONE_LENGTH + 2.4");
    expect(src).toContain("export const FLASHLIGHT_SPOT_Y = 1.7825");
    expect(src).toContain("torchSpot.position.set(0, FLASHLIGHT_SPOT_Y, 0)");
    expect(src).toContain("torchSpot.position.set(wx, FLASHLIGHT_SPOT_Y, wy)");
    expect(src).toContain("export const FLASHLIGHT_FILL_Y = 1.5525");
    expect(src).toContain("torchLight.position.set(0, FLASHLIGHT_FILL_Y, 0)");
    expect(src).toContain("torchLight.position.set(wx, FLASHLIGHT_FILL_Y, wy)");
    expect(src).toContain("export const FLASHLIGHT_FILL_DISTANCE_GAIN = 4.025");
    expect(src).toContain("export const FLASHLIGHT_FILL_DISTANCE_BASE = 8.05");
    expect(src).toContain(
      "torchLight.distance = FLASHLIGHT_FILL_DISTANCE_BASE + i * FLASHLIGHT_FILL_DISTANCE_GAIN",
    );
    expect(src).toContain("export const FLASHLIGHT_FILL_COLOR = 0xcaefff");
    expect(src).toContain("export const FLASHLIGHT_FILL_DECAY = 1.74");
    expect(src).toContain("new THREE.PointLight(FLASHLIGHT_FILL_COLOR, 0, 10, FLASHLIGHT_FILL_DECAY)");
    expect(src).not.toMatch(/new THREE\.PointLight\(FLASHLIGHT_FILL_COLOR, 0, 10, 2\)/);
    expect(src).toContain("torchLight.color.setHex(FLASHLIGHT_FILL_COLOR)");
    expect(src).toContain("export const TRACER_FLASH_COLOR = 0xffdd6e");
    expect(src).toContain("new THREE.PointLight(TRACER_FLASH_COLOR, TRACER_FLASH_INTENSITY, TRACER_FLASH_DISTANCE, TRACER_FLASH_DECAY)");
    expect(src).toContain("export const TRACER_COLOR = 0xffffb8");
    expect(src).toContain("color: TRACER_COLOR");
    expect(src).toContain("export const IMPACT_SPARK_LIGHT_COLOR = 0xffef93");
    expect(src).toContain("new THREE.PointLight(\n    IMPACT_SPARK_LIGHT_COLOR,");
    expect(src).toContain("export const IMPACT_SPARK_COLOR = 0xffef93");
    expect(src).toContain("color: IMPACT_SPARK_COLOR");
    expect(src).toContain("export const MUZZLE_LIGHT_COLOR = 0xffffb8");
    expect(src).toContain("new THREE.PointLight(\n    MUZZLE_LIGHT_COLOR,");
    expect(src).toContain("export const MUZZLE_FLASH_COLOR = 0xffffdd");
    expect(src).toContain("color: MUZZLE_FLASH_COLOR");
    expect(src).toContain("export const WARM_LIGHT_COLOR = 0xffca81");
    expect(src).toContain("export const POSSESSED_HEAD_EMISSIVE = 0x30124a");
    expect(src).toContain("emissive: POSSESSED_HEAD_EMISSIVE");
    expect(src).toContain("color: POSSESSED_HEAD_COLOR");
    expect(src).toContain("export const POSSESSED_HEAD_COLOR = 0x8c469f");
    expect(src).toContain("emissive: POSSESSED_EMISSIVE");
    expect(src).toContain("color: POSSESSED_COLOR");
    expect(src).toContain("const POSSESSED_EMISSIVE = 0x1e0925");
    expect(src).toContain("emissiveIntensity: 0.22");
    expect(src).toContain("color: HOSTILE_COLOR");
    expect(src).toContain("color: PLAYER_COLOR");
    expect(src).toContain("color: PLAYER_HEAD_COLOR");
    expect(src).toContain("emissive: PLAYER_HEAD_EMISSIVE");
    expect(TRACER_FLASH_COLOR).toBe(0xffdd6e);
    expect(TRACER_COLOR).toBe(0xffffb8);
    expect(IMPACT_SPARK_LIGHT_COLOR).toBe(0xffef93);
    expect(IMPACT_SPARK_COLOR).toBe(0xffef93);
    expect(MUZZLE_LIGHT_COLOR).toBe(0xffffb8);
    expect(MUZZLE_FLASH_COLOR).toBe(0xffffdd);
    expect(WARM_LIGHT_COLOR).toBe(0xffca81);
    expect(WARM_LIGHT_Y).toBe(1.7825);
    expect(WARM_LIGHT_INTENSITY_MUL).toBe(1.7825);
    expect(POSSESSED_HEAD_EMISSIVE).toBe(0x30124a);
    expect(POSSESSED_HEAD_COLOR).toBe(0x8c469f);
    expect(POSSESSED_COLOR).toBe(0x68347b);
    expect(HOSTILE_COLOR).toBe(0x7b1e1e);
    expect(PLAYER_COLOR).toBe(0x55a4f4);
    expect(PLAYER_HEAD_COLOR).toBe(0x91d1ff);
    expect(PLAYER_HEAD_EMISSIVE).toBe(0x122537);
    expect(MARKER_PALETTE.possessed.emissive).toBe(0x30124a);
    expect(MARKER_PALETTE.player.badge).toBe(0x91d1ff);
    expect(MARKER_PALETTE.player.emissive).toBe(0x1e4a6e);
    expect(MARKER_PALETTE.player.ring).toBe(0x4392f4);
    expect(MARKER_RING_OPACITY).toBe(0.6877);
    expect(muteBadgeY).toBe(2.3);
    expect(possessedBadgeY).toBe(2.3);
  });
});

describe("worldView warm light distance gain", () => {
  test("warm light distance gain 2.5 × 1.15 → 2.875; base/Y/intensity/color/spot-gain/extra/spot-Y/fill-Y/gain/base/flash/mesh/impact/muzzle/possessed/hostile/player/palette/badge-Y iguales", () => {
    expect(WARM_LIGHT_DISTANCE_GAIN).toBe(2.875);
    expect(WARM_LIGHT_DISTANCE_GAIN).toBeCloseTo(2.5 * 1.15, 10);
    expect(WARM_LIGHT_DISTANCE_BASE).toBe(7.475);
    expect(WARM_LIGHT_Y).toBe(1.7825);
    expect(WARM_LIGHT_INTENSITY_MUL).toBe(1.7825);
    expect(FLASHLIGHT_SPOT_DISTANCE_GAIN).toBe(2.3);
    expect(FLASHLIGHT_SPOT_DISTANCE_EXTRA).toBe(1.84);
    expect(FLASHLIGHT_SPOT_Y).toBe(1.7825);
    expect(FLASHLIGHT_FILL_Y).toBe(1.5525);
    expect(FLASHLIGHT_FILL_DISTANCE_GAIN).toBe(4.025);
    expect(FLASHLIGHT_FILL_DISTANCE_BASE).toBe(8.05);
    expect(FLASHLIGHT_FILL_COLOR).toBe(0xcaefff);
    expect(FLASHLIGHT_FILL_DECAY).toBe(1.74);
    const src = readFileSync(
      resolve(process.cwd(), "src/render/worldView.ts"),
      "utf8",
    );
    expect(src).toContain("export const WARM_LIGHT_DISTANCE_GAIN = 2.875");
    expect(src).toContain("export const WARM_LIGHT_DISTANCE_BASE = 7.475");
    expect(src).toContain(
      "warmLight.distance = WARM_LIGHT_DISTANCE_BASE + i * WARM_LIGHT_DISTANCE_GAIN",
    );
    expect(src).not.toMatch(/export const WARM_LIGHT_DISTANCE_GAIN = 2\.5/);
    expect(src).not.toMatch(
      /warmLight\.distance = WARM_LIGHT_DISTANCE_BASE \+ i \* 2\.5/,
    );
    expect(src).not.toMatch(/warmLight\.distance = 6\.5 \+ i \* 2\.5/);
    expect(src).toContain("new THREE.PointLight(WARM_LIGHT_COLOR, 0, 7.5, WARM_LIGHT_DECAY)");
    expect(src).toContain("export const WARM_LIGHT_Y = 1.7825");
    expect(src).toContain("warmLight.position.set(wx, WARM_LIGHT_Y, wy)");
    expect(src).not.toMatch(/export const WARM_LIGHT_Y = 1\.55/);
    expect(src).not.toMatch(/warmLight\.position\.set\(wx, 1\.55, wy\)/);
    expect(src).toContain("warmLight.position.set(0, 1.6, 0)");
    expect(src).toContain("export const WARM_LIGHT_INTENSITY_MUL = 1.7825");
    expect(src).toContain("warmLight.intensity = i * WARM_LIGHT_INTENSITY_MUL");
    expect(src).not.toMatch(/export const WARM_LIGHT_INTENSITY_MUL = 1\.55/);
    expect(src).not.toMatch(/warmLight\.intensity = i \* 1\.55/);
    expect(src).toContain("export const FLASHLIGHT_SPOT_DISTANCE_GAIN = 2.3");
    expect(src).toContain("export const FLASHLIGHT_SPOT_DISTANCE_EXTRA = 1.84");
    expect(src).toContain(
      "torchSpot.distance = FLASHLIGHT_CONE_LENGTH + FLASHLIGHT_SPOT_DISTANCE_EXTRA + i * FLASHLIGHT_SPOT_DISTANCE_GAIN",
    );
    expect(src).toContain("FLASHLIGHT_CONE_LENGTH + 2.4");
    expect(src).toContain("export const FLASHLIGHT_SPOT_Y = 1.7825");
    expect(src).toContain("torchSpot.position.set(0, FLASHLIGHT_SPOT_Y, 0)");
    expect(src).toContain("torchSpot.position.set(wx, FLASHLIGHT_SPOT_Y, wy)");
    expect(src).toContain("export const FLASHLIGHT_FILL_Y = 1.5525");
    expect(src).toContain("torchLight.position.set(0, FLASHLIGHT_FILL_Y, 0)");
    expect(src).toContain("torchLight.position.set(wx, FLASHLIGHT_FILL_Y, wy)");
    expect(src).toContain("export const FLASHLIGHT_FILL_DISTANCE_GAIN = 4.025");
    expect(src).toContain("export const FLASHLIGHT_FILL_DISTANCE_BASE = 8.05");
    expect(src).toContain(
      "torchLight.distance = FLASHLIGHT_FILL_DISTANCE_BASE + i * FLASHLIGHT_FILL_DISTANCE_GAIN",
    );
    expect(src).toContain("export const FLASHLIGHT_FILL_COLOR = 0xcaefff");
    expect(src).toContain("export const FLASHLIGHT_FILL_DECAY = 1.74");
    expect(src).toContain("new THREE.PointLight(FLASHLIGHT_FILL_COLOR, 0, 10, FLASHLIGHT_FILL_DECAY)");
    expect(src).not.toMatch(/new THREE\.PointLight\(FLASHLIGHT_FILL_COLOR, 0, 10, 2\)/);
    expect(src).toContain("torchLight.color.setHex(FLASHLIGHT_FILL_COLOR)");
    expect(src).toContain("export const TRACER_FLASH_COLOR = 0xffdd6e");
    expect(src).toContain("new THREE.PointLight(TRACER_FLASH_COLOR, TRACER_FLASH_INTENSITY, TRACER_FLASH_DISTANCE, TRACER_FLASH_DECAY)");
    expect(src).toContain("export const TRACER_COLOR = 0xffffb8");
    expect(src).toContain("color: TRACER_COLOR");
    expect(src).toContain("export const IMPACT_SPARK_LIGHT_COLOR = 0xffef93");
    expect(src).toContain("new THREE.PointLight(\n    IMPACT_SPARK_LIGHT_COLOR,");
    expect(src).toContain("export const IMPACT_SPARK_COLOR = 0xffef93");
    expect(src).toContain("color: IMPACT_SPARK_COLOR");
    expect(src).toContain("export const MUZZLE_LIGHT_COLOR = 0xffffb8");
    expect(src).toContain("new THREE.PointLight(\n    MUZZLE_LIGHT_COLOR,");
    expect(src).toContain("export const MUZZLE_FLASH_COLOR = 0xffffdd");
    expect(src).toContain("color: MUZZLE_FLASH_COLOR");
    expect(src).toContain("export const WARM_LIGHT_COLOR = 0xffca81");
    expect(src).toContain("export const POSSESSED_HEAD_EMISSIVE = 0x30124a");
    expect(src).toContain("emissive: POSSESSED_HEAD_EMISSIVE");
    expect(src).toContain("color: POSSESSED_HEAD_COLOR");
    expect(src).toContain("export const POSSESSED_HEAD_COLOR = 0x8c469f");
    expect(src).toContain("emissive: POSSESSED_EMISSIVE");
    expect(src).toContain("color: POSSESSED_COLOR");
    expect(src).toContain("const POSSESSED_EMISSIVE = 0x1e0925");
    expect(src).toContain("emissiveIntensity: 0.22");
    expect(src).toContain("color: HOSTILE_COLOR");
    expect(src).toContain("color: PLAYER_COLOR");
    expect(src).toContain("color: PLAYER_HEAD_COLOR");
    expect(src).toContain("emissive: PLAYER_HEAD_EMISSIVE");
    expect(TRACER_FLASH_COLOR).toBe(0xffdd6e);
    expect(TRACER_COLOR).toBe(0xffffb8);
    expect(IMPACT_SPARK_LIGHT_COLOR).toBe(0xffef93);
    expect(IMPACT_SPARK_COLOR).toBe(0xffef93);
    expect(MUZZLE_LIGHT_COLOR).toBe(0xffffb8);
    expect(MUZZLE_FLASH_COLOR).toBe(0xffffdd);
    expect(WARM_LIGHT_COLOR).toBe(0xffca81);
    expect(WARM_LIGHT_DISTANCE_BASE).toBe(7.475);
    expect(WARM_LIGHT_Y).toBe(1.7825);
    expect(WARM_LIGHT_INTENSITY_MUL).toBe(1.7825);
    expect(POSSESSED_HEAD_EMISSIVE).toBe(0x30124a);
    expect(POSSESSED_HEAD_COLOR).toBe(0x8c469f);
    expect(POSSESSED_COLOR).toBe(0x68347b);
    expect(HOSTILE_COLOR).toBe(0x7b1e1e);
    expect(PLAYER_COLOR).toBe(0x55a4f4);
    expect(PLAYER_HEAD_COLOR).toBe(0x91d1ff);
    expect(PLAYER_HEAD_EMISSIVE).toBe(0x122537);
    expect(MARKER_PALETTE.possessed.emissive).toBe(0x30124a);
    expect(MARKER_PALETTE.player.badge).toBe(0x91d1ff);
    expect(MARKER_PALETTE.player.emissive).toBe(0x1e4a6e);
    expect(MARKER_PALETTE.player.ring).toBe(0x4392f4);
    expect(MARKER_RING_OPACITY).toBe(0.6877);
    expect(muteBadgeY).toBe(2.3);
    expect(possessedBadgeY).toBe(2.3);
  });
});

describe("worldView warm light amber G gain", () => {
  test("warm light amber G gain 0.08 × 1.15 → 0.092; amber-G/gain/base/Y/intensity/color/spot-gain/extra/spot-Y/fill-Y/gain/base/flash/mesh/impact/muzzle/possessed/hostile/player/palette/badge-Y iguales", () => {
    expect(WARM_LIGHT_AMBER_G_GAIN).toBe(0.092);
    expect(WARM_LIGHT_AMBER_G_GAIN).toBeCloseTo(0.08 * 1.15, 10);
    expect(WARM_LIGHT_VISIBLE_EPS).toBe(0.0174);
    expect(WARM_LIGHT_DECAY).toBe(1.74);
    expect(WARM_LIGHT_AMBER_B_GAIN).toBe(0.115);
    expect(WARM_LIGHT_AMBER_B).toBe(0.437);
    expect(WARM_LIGHT_AMBER_G).toBe(0.759);
    expect(WARM_LIGHT_DISTANCE_GAIN).toBe(2.875);
    expect(WARM_LIGHT_DISTANCE_BASE).toBe(7.475);
    expect(WARM_LIGHT_Y).toBe(1.7825);
    expect(WARM_LIGHT_INTENSITY_MUL).toBe(1.7825);
    expect(FLASHLIGHT_SPOT_DISTANCE_GAIN).toBe(2.3);
    expect(FLASHLIGHT_SPOT_DISTANCE_EXTRA).toBe(1.84);
    expect(FLASHLIGHT_SPOT_Y).toBe(1.7825);
    expect(FLASHLIGHT_FILL_Y).toBe(1.5525);
    expect(FLASHLIGHT_FILL_DISTANCE_GAIN).toBe(4.025);
    expect(FLASHLIGHT_FILL_DISTANCE_BASE).toBe(8.05);
    expect(FLASHLIGHT_FILL_COLOR).toBe(0xcaefff);
    expect(FLASHLIGHT_FILL_DECAY).toBe(1.74);
    const src = readFileSync(
      resolve(process.cwd(), "src/render/worldView.ts"),
      "utf8",
    );
    expect(src).toContain("export const WARM_LIGHT_AMBER_G_GAIN = 0.092");
    expect(src).toContain("export const WARM_LIGHT_AMBER_B_GAIN = 0.115");
    expect(src).toContain("export const WARM_LIGHT_AMBER_B = 0.437");
    expect(src).toContain("export const WARM_LIGHT_AMBER_G = 0.759");
    expect(src).toContain(
      "warmLight.color.setRGB(1, WARM_LIGHT_AMBER_G + i * WARM_LIGHT_AMBER_G_GAIN, WARM_LIGHT_AMBER_B + i * WARM_LIGHT_AMBER_B_GAIN)",
    );
    expect(src).not.toMatch(/WARM_LIGHT_AMBER_B \+ i \* 0\.1/);
    expect(src).not.toMatch(/export const WARM_LIGHT_AMBER_G_GAIN = 0\.08/);
    expect(src).not.toMatch(
      /warmLight\.color\.setRGB\(1, WARM_LIGHT_AMBER_G \+ i \* 0\.08/,
    );
    expect(src).toContain("export const WARM_LIGHT_DISTANCE_GAIN = 2.875");
    expect(src).toContain("export const WARM_LIGHT_DISTANCE_BASE = 7.475");
    expect(src).toContain(
      "warmLight.distance = WARM_LIGHT_DISTANCE_BASE + i * WARM_LIGHT_DISTANCE_GAIN",
    );
    expect(src).not.toMatch(/export const WARM_LIGHT_DISTANCE_GAIN = 2\.5/);
    expect(src).not.toMatch(
      /warmLight\.distance = WARM_LIGHT_DISTANCE_BASE \+ i \* 2\.5/,
    );
    expect(src).not.toMatch(/warmLight\.distance = 6\.5 \+ i \* 2\.5/);
    expect(src).toContain("export const WARM_LIGHT_DECAY = 1.74");
    expect(src).toContain("new THREE.PointLight(WARM_LIGHT_COLOR, 0, 7.5, WARM_LIGHT_DECAY)");
    expect(src).not.toMatch(/new THREE\.PointLight\(WARM_LIGHT_COLOR, 0, 7\.5, 2\)/);
    expect(src).toContain("export const WARM_LIGHT_VISIBLE_EPS = 0.0174");
    expect(src).toContain("warmLight.visible = i > WARM_LIGHT_VISIBLE_EPS");
    expect(src).not.toMatch(/warmLight\.visible = i > 0\.02/);
    expect(src).toContain("export const WARM_LIGHT_Y = 1.7825");
    expect(src).toContain("warmLight.position.set(wx, WARM_LIGHT_Y, wy)");
    expect(src).not.toMatch(/export const WARM_LIGHT_Y = 1\.55/);
    expect(src).not.toMatch(/warmLight\.position\.set\(wx, 1\.55, wy\)/);
    expect(src).toContain("warmLight.position.set(0, 1.6, 0)");
    expect(src).toContain("export const WARM_LIGHT_INTENSITY_MUL = 1.7825");
    expect(src).toContain("warmLight.intensity = i * WARM_LIGHT_INTENSITY_MUL");
    expect(src).not.toMatch(/export const WARM_LIGHT_INTENSITY_MUL = 1\.55/);
    expect(src).not.toMatch(/warmLight\.intensity = i \* 1\.55/);
    expect(src).toContain("export const FLASHLIGHT_SPOT_DISTANCE_GAIN = 2.3");
    expect(src).toContain("export const FLASHLIGHT_SPOT_DISTANCE_EXTRA = 1.84");
    expect(src).toContain(
      "torchSpot.distance = FLASHLIGHT_CONE_LENGTH + FLASHLIGHT_SPOT_DISTANCE_EXTRA + i * FLASHLIGHT_SPOT_DISTANCE_GAIN",
    );
    expect(src).toContain("FLASHLIGHT_CONE_LENGTH + 2.4");
    expect(src).toContain("export const FLASHLIGHT_SPOT_Y = 1.7825");
    expect(src).toContain("torchSpot.position.set(0, FLASHLIGHT_SPOT_Y, 0)");
    expect(src).toContain("torchSpot.position.set(wx, FLASHLIGHT_SPOT_Y, wy)");
    expect(src).toContain("export const FLASHLIGHT_FILL_Y = 1.5525");
    expect(src).toContain("torchLight.position.set(0, FLASHLIGHT_FILL_Y, 0)");
    expect(src).toContain("torchLight.position.set(wx, FLASHLIGHT_FILL_Y, wy)");
    expect(src).toContain("export const FLASHLIGHT_FILL_DISTANCE_GAIN = 4.025");
    expect(src).toContain("export const FLASHLIGHT_FILL_DISTANCE_BASE = 8.05");
    expect(src).toContain(
      "torchLight.distance = FLASHLIGHT_FILL_DISTANCE_BASE + i * FLASHLIGHT_FILL_DISTANCE_GAIN",
    );
    expect(src).toContain("export const FLASHLIGHT_FILL_COLOR = 0xcaefff");
    expect(src).toContain("export const FLASHLIGHT_FILL_DECAY = 1.74");
    expect(src).toContain("new THREE.PointLight(FLASHLIGHT_FILL_COLOR, 0, 10, FLASHLIGHT_FILL_DECAY)");
    expect(src).not.toMatch(/new THREE\.PointLight\(FLASHLIGHT_FILL_COLOR, 0, 10, 2\)/);
    expect(src).toContain("torchLight.color.setHex(FLASHLIGHT_FILL_COLOR)");
    expect(src).toContain("export const TRACER_FLASH_COLOR = 0xffdd6e");
    expect(src).toContain("new THREE.PointLight(TRACER_FLASH_COLOR, TRACER_FLASH_INTENSITY, TRACER_FLASH_DISTANCE, TRACER_FLASH_DECAY)");
    expect(src).toContain("export const TRACER_COLOR = 0xffffb8");
    expect(src).toContain("color: TRACER_COLOR");
    expect(src).toContain("export const IMPACT_SPARK_LIGHT_COLOR = 0xffef93");
    expect(src).toContain("new THREE.PointLight(\n    IMPACT_SPARK_LIGHT_COLOR,");
    expect(src).toContain("export const IMPACT_SPARK_COLOR = 0xffef93");
    expect(src).toContain("color: IMPACT_SPARK_COLOR");
    expect(src).toContain("export const MUZZLE_LIGHT_COLOR = 0xffffb8");
    expect(src).toContain("new THREE.PointLight(\n    MUZZLE_LIGHT_COLOR,");
    expect(src).toContain("export const MUZZLE_FLASH_COLOR = 0xffffdd");
    expect(src).toContain("color: MUZZLE_FLASH_COLOR");
    expect(src).toContain("export const WARM_LIGHT_COLOR = 0xffca81");
    expect(src).toContain("export const POSSESSED_HEAD_EMISSIVE = 0x30124a");
    expect(src).toContain("emissive: POSSESSED_HEAD_EMISSIVE");
    expect(src).toContain("color: POSSESSED_HEAD_COLOR");
    expect(src).toContain("export const POSSESSED_HEAD_COLOR = 0x8c469f");
    expect(src).toContain("emissive: POSSESSED_EMISSIVE");
    expect(src).toContain("color: POSSESSED_COLOR");
    expect(src).toContain("const POSSESSED_EMISSIVE = 0x1e0925");
    expect(src).toContain("emissiveIntensity: 0.22");
    expect(src).toContain("color: HOSTILE_COLOR");
    expect(src).toContain("color: PLAYER_COLOR");
    expect(src).toContain("color: PLAYER_HEAD_COLOR");
    expect(src).toContain("emissive: PLAYER_HEAD_EMISSIVE");
    expect(TRACER_FLASH_COLOR).toBe(0xffdd6e);
    expect(TRACER_COLOR).toBe(0xffffb8);
    expect(IMPACT_SPARK_LIGHT_COLOR).toBe(0xffef93);
    expect(IMPACT_SPARK_COLOR).toBe(0xffef93);
    expect(MUZZLE_LIGHT_COLOR).toBe(0xffffb8);
    expect(MUZZLE_FLASH_COLOR).toBe(0xffffdd);
    expect(WARM_LIGHT_COLOR).toBe(0xffca81);
    expect(WARM_LIGHT_AMBER_G).toBe(0.759);
    expect(WARM_LIGHT_DISTANCE_GAIN).toBe(2.875);
    expect(WARM_LIGHT_DISTANCE_BASE).toBe(7.475);
    expect(WARM_LIGHT_Y).toBe(1.7825);
    expect(WARM_LIGHT_INTENSITY_MUL).toBe(1.7825);
    expect(POSSESSED_HEAD_EMISSIVE).toBe(0x30124a);
    expect(POSSESSED_HEAD_COLOR).toBe(0x8c469f);
    expect(POSSESSED_COLOR).toBe(0x68347b);
    expect(HOSTILE_COLOR).toBe(0x7b1e1e);
    expect(PLAYER_COLOR).toBe(0x55a4f4);
    expect(PLAYER_HEAD_COLOR).toBe(0x91d1ff);
    expect(PLAYER_HEAD_EMISSIVE).toBe(0x122537);
    expect(MARKER_PALETTE.possessed.emissive).toBe(0x30124a);
    expect(MARKER_PALETTE.player.badge).toBe(0x91d1ff);
    expect(MARKER_PALETTE.player.emissive).toBe(0x1e4a6e);
    expect(MARKER_PALETTE.player.ring).toBe(0x4392f4);
    expect(MARKER_RING_OPACITY).toBe(0.6877);
    expect(muteBadgeY).toBe(2.3);
    expect(possessedBadgeY).toBe(2.3);
  });
});

describe("worldView warm light amber B", () => {
  test("warm light amber B 0.38 × 1.15 → 0.437; amber-G-gain/amber-G/gain/base/Y/intensity/color/spot-gain/extra/spot-Y/fill-Y/gain/base/flash/mesh/impact/muzzle/possessed/hostile/player/palette/badge-Y iguales", () => {
    expect(WARM_LIGHT_AMBER_B).toBe(0.437);
    expect(WARM_LIGHT_AMBER_B).toBeCloseTo(0.38 * 1.15, 10);
    expect(WARM_LIGHT_VISIBLE_EPS).toBe(0.0174);
    expect(WARM_LIGHT_DECAY).toBe(1.74);
    expect(WARM_LIGHT_AMBER_B_GAIN).toBe(0.115);
    expect(WARM_LIGHT_AMBER_G_GAIN).toBe(0.092);
    expect(WARM_LIGHT_AMBER_G).toBe(0.759);
    expect(WARM_LIGHT_DISTANCE_GAIN).toBe(2.875);
    expect(WARM_LIGHT_DISTANCE_BASE).toBe(7.475);
    expect(WARM_LIGHT_Y).toBe(1.7825);
    expect(WARM_LIGHT_INTENSITY_MUL).toBe(1.7825);
    expect(FLASHLIGHT_SPOT_DISTANCE_GAIN).toBe(2.3);
    expect(FLASHLIGHT_SPOT_DISTANCE_EXTRA).toBe(1.84);
    expect(FLASHLIGHT_SPOT_Y).toBe(1.7825);
    expect(FLASHLIGHT_FILL_Y).toBe(1.5525);
    expect(FLASHLIGHT_FILL_DISTANCE_GAIN).toBe(4.025);
    expect(FLASHLIGHT_FILL_DISTANCE_BASE).toBe(8.05);
    expect(FLASHLIGHT_FILL_COLOR).toBe(0xcaefff);
    expect(FLASHLIGHT_FILL_DECAY).toBe(1.74);
    const src = readFileSync(
      resolve(process.cwd(), "src/render/worldView.ts"),
      "utf8",
    );
    expect(src).toContain("export const WARM_LIGHT_AMBER_B = 0.437");
    expect(src).toContain("export const WARM_LIGHT_AMBER_B_GAIN = 0.115");
    expect(src).toContain("export const WARM_LIGHT_AMBER_G_GAIN = 0.092");
    expect(src).toContain("export const WARM_LIGHT_AMBER_G = 0.759");
    expect(src).toContain(
      "warmLight.color.setRGB(1, WARM_LIGHT_AMBER_G + i * WARM_LIGHT_AMBER_G_GAIN, WARM_LIGHT_AMBER_B + i * WARM_LIGHT_AMBER_B_GAIN)",
    );
    expect(src).not.toMatch(/WARM_LIGHT_AMBER_B \+ i \* 0\.1/);
    expect(src).not.toMatch(/export const WARM_LIGHT_AMBER_B = 0\.38/);
    expect(src).not.toMatch(
      /warmLight\.color\.setRGB\(1, WARM_LIGHT_AMBER_G \+ i \* WARM_LIGHT_AMBER_G_GAIN, 0\.38/,
    );
    expect(src).toContain("export const WARM_LIGHT_DISTANCE_GAIN = 2.875");
    expect(src).toContain("export const WARM_LIGHT_DISTANCE_BASE = 7.475");
    expect(src).toContain(
      "warmLight.distance = WARM_LIGHT_DISTANCE_BASE + i * WARM_LIGHT_DISTANCE_GAIN",
    );
    expect(src).not.toMatch(/export const WARM_LIGHT_DISTANCE_GAIN = 2\.5/);
    expect(src).not.toMatch(
      /warmLight\.distance = WARM_LIGHT_DISTANCE_BASE \+ i \* 2\.5/,
    );
    expect(src).not.toMatch(/warmLight\.distance = 6\.5 \+ i \* 2\.5/);
    expect(src).toContain("export const WARM_LIGHT_DECAY = 1.74");
    expect(src).toContain("new THREE.PointLight(WARM_LIGHT_COLOR, 0, 7.5, WARM_LIGHT_DECAY)");
    expect(src).not.toMatch(/new THREE\.PointLight\(WARM_LIGHT_COLOR, 0, 7\.5, 2\)/);
    expect(src).toContain("export const WARM_LIGHT_VISIBLE_EPS = 0.0174");
    expect(src).toContain("warmLight.visible = i > WARM_LIGHT_VISIBLE_EPS");
    expect(src).not.toMatch(/warmLight\.visible = i > 0\.02/);
    expect(src).toContain("export const WARM_LIGHT_Y = 1.7825");
    expect(src).toContain("warmLight.position.set(wx, WARM_LIGHT_Y, wy)");
    expect(src).not.toMatch(/export const WARM_LIGHT_Y = 1\.55/);
    expect(src).not.toMatch(/warmLight\.position\.set\(wx, 1\.55, wy\)/);
    expect(src).toContain("warmLight.position.set(0, 1.6, 0)");
    expect(src).toContain("export const WARM_LIGHT_INTENSITY_MUL = 1.7825");
    expect(src).toContain("warmLight.intensity = i * WARM_LIGHT_INTENSITY_MUL");
    expect(src).not.toMatch(/export const WARM_LIGHT_INTENSITY_MUL = 1\.55/);
    expect(src).not.toMatch(/warmLight\.intensity = i \* 1\.55/);
    expect(src).toContain("export const FLASHLIGHT_SPOT_DISTANCE_GAIN = 2.3");
    expect(src).toContain("export const FLASHLIGHT_SPOT_DISTANCE_EXTRA = 1.84");
    expect(src).toContain(
      "torchSpot.distance = FLASHLIGHT_CONE_LENGTH + FLASHLIGHT_SPOT_DISTANCE_EXTRA + i * FLASHLIGHT_SPOT_DISTANCE_GAIN",
    );
    expect(src).toContain("FLASHLIGHT_CONE_LENGTH + 2.4");
    expect(src).toContain("export const FLASHLIGHT_SPOT_Y = 1.7825");
    expect(src).toContain("torchSpot.position.set(0, FLASHLIGHT_SPOT_Y, 0)");
    expect(src).toContain("torchSpot.position.set(wx, FLASHLIGHT_SPOT_Y, wy)");
    expect(src).toContain("export const FLASHLIGHT_FILL_Y = 1.5525");
    expect(src).toContain("torchLight.position.set(0, FLASHLIGHT_FILL_Y, 0)");
    expect(src).toContain("torchLight.position.set(wx, FLASHLIGHT_FILL_Y, wy)");
    expect(src).toContain("export const FLASHLIGHT_FILL_DISTANCE_GAIN = 4.025");
    expect(src).toContain("export const FLASHLIGHT_FILL_DISTANCE_BASE = 8.05");
    expect(src).toContain(
      "torchLight.distance = FLASHLIGHT_FILL_DISTANCE_BASE + i * FLASHLIGHT_FILL_DISTANCE_GAIN",
    );
    expect(src).toContain("export const FLASHLIGHT_FILL_COLOR = 0xcaefff");
    expect(src).toContain("export const FLASHLIGHT_FILL_DECAY = 1.74");
    expect(src).toContain("new THREE.PointLight(FLASHLIGHT_FILL_COLOR, 0, 10, FLASHLIGHT_FILL_DECAY)");
    expect(src).not.toMatch(/new THREE\.PointLight\(FLASHLIGHT_FILL_COLOR, 0, 10, 2\)/);
    expect(src).toContain("torchLight.color.setHex(FLASHLIGHT_FILL_COLOR)");
    expect(src).toContain("export const TRACER_FLASH_COLOR = 0xffdd6e");
    expect(src).toContain("new THREE.PointLight(TRACER_FLASH_COLOR, TRACER_FLASH_INTENSITY, TRACER_FLASH_DISTANCE, TRACER_FLASH_DECAY)");
    expect(src).toContain("export const TRACER_COLOR = 0xffffb8");
    expect(src).toContain("color: TRACER_COLOR");
    expect(src).toContain("export const IMPACT_SPARK_LIGHT_COLOR = 0xffef93");
    expect(src).toContain("new THREE.PointLight(\n    IMPACT_SPARK_LIGHT_COLOR,");
    expect(src).toContain("export const IMPACT_SPARK_COLOR = 0xffef93");
    expect(src).toContain("color: IMPACT_SPARK_COLOR");
    expect(src).toContain("export const MUZZLE_LIGHT_COLOR = 0xffffb8");
    expect(src).toContain("new THREE.PointLight(\n    MUZZLE_LIGHT_COLOR,");
    expect(src).toContain("export const MUZZLE_FLASH_COLOR = 0xffffdd");
    expect(src).toContain("color: MUZZLE_FLASH_COLOR");
    expect(src).toContain("export const WARM_LIGHT_COLOR = 0xffca81");
    expect(src).toContain("export const POSSESSED_HEAD_EMISSIVE = 0x30124a");
    expect(src).toContain("emissive: POSSESSED_HEAD_EMISSIVE");
    expect(src).toContain("color: POSSESSED_HEAD_COLOR");
    expect(src).toContain("export const POSSESSED_HEAD_COLOR = 0x8c469f");
    expect(src).toContain("emissive: POSSESSED_EMISSIVE");
    expect(src).toContain("color: POSSESSED_COLOR");
    expect(src).toContain("const POSSESSED_EMISSIVE = 0x1e0925");
    expect(src).toContain("emissiveIntensity: 0.22");
    expect(src).toContain("color: HOSTILE_COLOR");
    expect(src).toContain("color: PLAYER_COLOR");
    expect(src).toContain("color: PLAYER_HEAD_COLOR");
    expect(src).toContain("emissive: PLAYER_HEAD_EMISSIVE");
    expect(TRACER_FLASH_COLOR).toBe(0xffdd6e);
    expect(TRACER_COLOR).toBe(0xffffb8);
    expect(IMPACT_SPARK_LIGHT_COLOR).toBe(0xffef93);
    expect(IMPACT_SPARK_COLOR).toBe(0xffef93);
    expect(MUZZLE_LIGHT_COLOR).toBe(0xffffb8);
    expect(MUZZLE_FLASH_COLOR).toBe(0xffffdd);
    expect(WARM_LIGHT_COLOR).toBe(0xffca81);
    expect(WARM_LIGHT_AMBER_G_GAIN).toBe(0.092);
    expect(WARM_LIGHT_AMBER_G).toBe(0.759);
    expect(WARM_LIGHT_DISTANCE_GAIN).toBe(2.875);
    expect(WARM_LIGHT_DISTANCE_BASE).toBe(7.475);
    expect(WARM_LIGHT_Y).toBe(1.7825);
    expect(WARM_LIGHT_INTENSITY_MUL).toBe(1.7825);
    expect(POSSESSED_HEAD_EMISSIVE).toBe(0x30124a);
    expect(POSSESSED_HEAD_COLOR).toBe(0x8c469f);
    expect(POSSESSED_COLOR).toBe(0x68347b);
    expect(HOSTILE_COLOR).toBe(0x7b1e1e);
    expect(PLAYER_COLOR).toBe(0x55a4f4);
    expect(PLAYER_HEAD_COLOR).toBe(0x91d1ff);
    expect(PLAYER_HEAD_EMISSIVE).toBe(0x122537);
    expect(MARKER_PALETTE.possessed.emissive).toBe(0x30124a);
    expect(MARKER_PALETTE.player.badge).toBe(0x91d1ff);
    expect(MARKER_PALETTE.player.emissive).toBe(0x1e4a6e);
    expect(MARKER_PALETTE.player.ring).toBe(0x4392f4);
    expect(MARKER_RING_OPACITY).toBe(0.6877);
    expect(muteBadgeY).toBe(2.3);
    expect(possessedBadgeY).toBe(2.3);
  });
});

describe("worldView warm light amber B gain", () => {
  test("warm light amber B gain 0.1 × 1.15 → 0.115; amber-B/amber-G-gain/amber-G/gain/base/Y/intensity/color/spot-gain/extra/spot-Y/fill-Y/gain/base/flash/mesh/impact/muzzle/possessed/hostile/player/palette/badge-Y iguales", () => {
    expect(WARM_LIGHT_AMBER_B_GAIN).toBe(0.115);
    expect(WARM_LIGHT_AMBER_B_GAIN).toBeCloseTo(0.1 * 1.15, 10);
    expect(WARM_LIGHT_VISIBLE_EPS).toBe(0.0174);
    expect(WARM_LIGHT_DECAY).toBe(1.74);
    expect(WARM_LIGHT_AMBER_B).toBe(0.437);
    expect(WARM_LIGHT_AMBER_G_GAIN).toBe(0.092);
    expect(WARM_LIGHT_AMBER_G).toBe(0.759);
    expect(WARM_LIGHT_DISTANCE_GAIN).toBe(2.875);
    expect(WARM_LIGHT_DISTANCE_BASE).toBe(7.475);
    expect(WARM_LIGHT_Y).toBe(1.7825);
    expect(WARM_LIGHT_INTENSITY_MUL).toBe(1.7825);
    expect(FLASHLIGHT_SPOT_DISTANCE_GAIN).toBe(2.3);
    expect(FLASHLIGHT_SPOT_DISTANCE_EXTRA).toBe(1.84);
    expect(FLASHLIGHT_SPOT_Y).toBe(1.7825);
    expect(FLASHLIGHT_FILL_Y).toBe(1.5525);
    expect(FLASHLIGHT_FILL_DISTANCE_GAIN).toBe(4.025);
    expect(FLASHLIGHT_FILL_DISTANCE_BASE).toBe(8.05);
    expect(FLASHLIGHT_FILL_COLOR).toBe(0xcaefff);
    expect(FLASHLIGHT_FILL_DECAY).toBe(1.74);
    const src = readFileSync(
      resolve(process.cwd(), "src/render/worldView.ts"),
      "utf8",
    );
    expect(src).toContain("export const WARM_LIGHT_AMBER_B_GAIN = 0.115");
    expect(src).toContain("export const WARM_LIGHT_AMBER_B = 0.437");
    expect(src).toContain("export const WARM_LIGHT_AMBER_G_GAIN = 0.092");
    expect(src).toContain("export const WARM_LIGHT_AMBER_G = 0.759");
    expect(src).toContain(
      "warmLight.color.setRGB(1, WARM_LIGHT_AMBER_G + i * WARM_LIGHT_AMBER_G_GAIN, WARM_LIGHT_AMBER_B + i * WARM_LIGHT_AMBER_B_GAIN)",
    );
    expect(src).not.toMatch(/export const WARM_LIGHT_AMBER_B_GAIN = 0\.1[^\d]/);
    expect(src).not.toMatch(/WARM_LIGHT_AMBER_B \+ i \* 0\.1/);
    expect(src).toContain("export const WARM_LIGHT_DISTANCE_GAIN = 2.875");
    expect(src).toContain("export const WARM_LIGHT_DISTANCE_BASE = 7.475");
    expect(src).toContain(
      "warmLight.distance = WARM_LIGHT_DISTANCE_BASE + i * WARM_LIGHT_DISTANCE_GAIN",
    );
    expect(src).not.toMatch(/export const WARM_LIGHT_DISTANCE_GAIN = 2\.5/);
    expect(src).not.toMatch(
      /warmLight\.distance = WARM_LIGHT_DISTANCE_BASE \+ i \* 2\.5/,
    );
    expect(src).not.toMatch(/warmLight\.distance = 6\.5 \+ i \* 2\.5/);
    expect(src).toContain("export const WARM_LIGHT_DECAY = 1.74");
    expect(src).toContain("new THREE.PointLight(WARM_LIGHT_COLOR, 0, 7.5, WARM_LIGHT_DECAY)");
    expect(src).not.toMatch(/new THREE\.PointLight\(WARM_LIGHT_COLOR, 0, 7\.5, 2\)/);
    expect(src).toContain("export const WARM_LIGHT_VISIBLE_EPS = 0.0174");
    expect(src).toContain("warmLight.visible = i > WARM_LIGHT_VISIBLE_EPS");
    expect(src).not.toMatch(/warmLight\.visible = i > 0\.02/);
    expect(src).toContain("export const WARM_LIGHT_Y = 1.7825");
    expect(src).toContain("warmLight.position.set(wx, WARM_LIGHT_Y, wy)");
    expect(src).not.toMatch(/export const WARM_LIGHT_Y = 1\.55/);
    expect(src).not.toMatch(/warmLight\.position\.set\(wx, 1\.55, wy\)/);
    expect(src).toContain("warmLight.position.set(0, 1.6, 0)");
    expect(src).toContain("export const WARM_LIGHT_INTENSITY_MUL = 1.7825");
    expect(src).toContain("warmLight.intensity = i * WARM_LIGHT_INTENSITY_MUL");
    expect(src).not.toMatch(/export const WARM_LIGHT_INTENSITY_MUL = 1\.55/);
    expect(src).not.toMatch(/warmLight\.intensity = i \* 1\.55/);
    expect(src).toContain("export const FLASHLIGHT_SPOT_DISTANCE_GAIN = 2.3");
    expect(src).toContain("export const FLASHLIGHT_SPOT_DISTANCE_EXTRA = 1.84");
    expect(src).toContain(
      "torchSpot.distance = FLASHLIGHT_CONE_LENGTH + FLASHLIGHT_SPOT_DISTANCE_EXTRA + i * FLASHLIGHT_SPOT_DISTANCE_GAIN",
    );
    expect(src).toContain("FLASHLIGHT_CONE_LENGTH + 2.4");
    expect(src).toContain("export const FLASHLIGHT_SPOT_Y = 1.7825");
    expect(src).toContain("torchSpot.position.set(0, FLASHLIGHT_SPOT_Y, 0)");
    expect(src).toContain("torchSpot.position.set(wx, FLASHLIGHT_SPOT_Y, wy)");
    expect(src).toContain("export const FLASHLIGHT_FILL_Y = 1.5525");
    expect(src).toContain("torchLight.position.set(0, FLASHLIGHT_FILL_Y, 0)");
    expect(src).toContain("torchLight.position.set(wx, FLASHLIGHT_FILL_Y, wy)");
    expect(src).toContain("export const FLASHLIGHT_FILL_DISTANCE_GAIN = 4.025");
    expect(src).toContain("export const FLASHLIGHT_FILL_DISTANCE_BASE = 8.05");
    expect(src).toContain(
      "torchLight.distance = FLASHLIGHT_FILL_DISTANCE_BASE + i * FLASHLIGHT_FILL_DISTANCE_GAIN",
    );
    expect(src).toContain("export const FLASHLIGHT_FILL_COLOR = 0xcaefff");
    expect(src).toContain("export const FLASHLIGHT_FILL_DECAY = 1.74");
    expect(src).toContain("new THREE.PointLight(FLASHLIGHT_FILL_COLOR, 0, 10, FLASHLIGHT_FILL_DECAY)");
    expect(src).not.toMatch(/new THREE\.PointLight\(FLASHLIGHT_FILL_COLOR, 0, 10, 2\)/);
    expect(src).toContain("torchLight.color.setHex(FLASHLIGHT_FILL_COLOR)");
    expect(src).toContain("export const TRACER_FLASH_COLOR = 0xffdd6e");
    expect(src).toContain("new THREE.PointLight(TRACER_FLASH_COLOR, TRACER_FLASH_INTENSITY, TRACER_FLASH_DISTANCE, TRACER_FLASH_DECAY)");
    expect(src).toContain("export const TRACER_COLOR = 0xffffb8");
    expect(src).toContain("color: TRACER_COLOR");
    expect(src).toContain("export const IMPACT_SPARK_LIGHT_COLOR = 0xffef93");
    expect(src).toContain("new THREE.PointLight(\n    IMPACT_SPARK_LIGHT_COLOR,");
    expect(src).toContain("export const IMPACT_SPARK_COLOR = 0xffef93");
    expect(src).toContain("color: IMPACT_SPARK_COLOR");
    expect(src).toContain("export const MUZZLE_LIGHT_COLOR = 0xffffb8");
    expect(src).toContain("new THREE.PointLight(\n    MUZZLE_LIGHT_COLOR,");
    expect(src).toContain("export const MUZZLE_FLASH_COLOR = 0xffffdd");
    expect(src).toContain("color: MUZZLE_FLASH_COLOR");
    expect(src).toContain("export const WARM_LIGHT_COLOR = 0xffca81");
    expect(src).toContain("export const POSSESSED_HEAD_EMISSIVE = 0x30124a");
    expect(src).toContain("emissive: POSSESSED_HEAD_EMISSIVE");
    expect(src).toContain("color: POSSESSED_HEAD_COLOR");
    expect(src).toContain("export const POSSESSED_HEAD_COLOR = 0x8c469f");
    expect(src).toContain("emissive: POSSESSED_EMISSIVE");
    expect(src).toContain("color: POSSESSED_COLOR");
    expect(src).toContain("const POSSESSED_EMISSIVE = 0x1e0925");
    expect(src).toContain("emissiveIntensity: 0.22");
    expect(src).toContain("color: HOSTILE_COLOR");
    expect(src).toContain("color: PLAYER_COLOR");
    expect(src).toContain("color: PLAYER_HEAD_COLOR");
    expect(src).toContain("emissive: PLAYER_HEAD_EMISSIVE");
    expect(TRACER_FLASH_COLOR).toBe(0xffdd6e);
    expect(TRACER_COLOR).toBe(0xffffb8);
    expect(IMPACT_SPARK_LIGHT_COLOR).toBe(0xffef93);
    expect(IMPACT_SPARK_COLOR).toBe(0xffef93);
    expect(MUZZLE_LIGHT_COLOR).toBe(0xffffb8);
    expect(MUZZLE_FLASH_COLOR).toBe(0xffffdd);
    expect(WARM_LIGHT_COLOR).toBe(0xffca81);
    expect(WARM_LIGHT_AMBER_B).toBe(0.437);
    expect(WARM_LIGHT_AMBER_G_GAIN).toBe(0.092);
    expect(WARM_LIGHT_AMBER_G).toBe(0.759);
    expect(WARM_LIGHT_DISTANCE_GAIN).toBe(2.875);
    expect(WARM_LIGHT_DISTANCE_BASE).toBe(7.475);
    expect(WARM_LIGHT_Y).toBe(1.7825);
    expect(WARM_LIGHT_INTENSITY_MUL).toBe(1.7825);
    expect(POSSESSED_HEAD_EMISSIVE).toBe(0x30124a);
    expect(POSSESSED_HEAD_COLOR).toBe(0x8c469f);
    expect(POSSESSED_COLOR).toBe(0x68347b);
    expect(HOSTILE_COLOR).toBe(0x7b1e1e);
    expect(PLAYER_COLOR).toBe(0x55a4f4);
    expect(PLAYER_HEAD_COLOR).toBe(0x91d1ff);
    expect(PLAYER_HEAD_EMISSIVE).toBe(0x122537);
    expect(MARKER_PALETTE.possessed.emissive).toBe(0x30124a);
    expect(MARKER_PALETTE.player.badge).toBe(0x91d1ff);
    expect(MARKER_PALETTE.player.emissive).toBe(0x1e4a6e);
    expect(MARKER_PALETTE.player.ring).toBe(0x4392f4);
    expect(MARKER_RING_OPACITY).toBe(0.6877);
    expect(muteBadgeY).toBe(2.3);
    expect(possessedBadgeY).toBe(2.3);
  });
});

describe("worldView warm light amber G", () => {
  test("warm light amber G 0.66 × 1.15 → 0.759; gain/base/Y/intensity/color/spot-gain/extra/spot-Y/fill-Y/gain/base/flash/mesh/impact/muzzle/possessed/hostile/player/palette/badge-Y iguales", () => {
    expect(WARM_LIGHT_AMBER_G).toBe(0.759);
    expect(WARM_LIGHT_AMBER_G).toBeCloseTo(0.66 * 1.15, 10);
    expect(WARM_LIGHT_VISIBLE_EPS).toBe(0.0174);
    expect(WARM_LIGHT_DECAY).toBe(1.74);
    expect(WARM_LIGHT_AMBER_B_GAIN).toBe(0.115);
    expect(WARM_LIGHT_AMBER_B).toBe(0.437);
    expect(WARM_LIGHT_AMBER_G_GAIN).toBe(0.092);
    expect(WARM_LIGHT_DISTANCE_GAIN).toBe(2.875);
    expect(WARM_LIGHT_DISTANCE_BASE).toBe(7.475);
    expect(WARM_LIGHT_Y).toBe(1.7825);
    expect(WARM_LIGHT_INTENSITY_MUL).toBe(1.7825);
    expect(FLASHLIGHT_SPOT_DISTANCE_GAIN).toBe(2.3);
    expect(FLASHLIGHT_SPOT_DISTANCE_EXTRA).toBe(1.84);
    expect(FLASHLIGHT_SPOT_Y).toBe(1.7825);
    expect(FLASHLIGHT_FILL_Y).toBe(1.5525);
    expect(FLASHLIGHT_FILL_DISTANCE_GAIN).toBe(4.025);
    expect(FLASHLIGHT_FILL_DISTANCE_BASE).toBe(8.05);
    expect(FLASHLIGHT_FILL_COLOR).toBe(0xcaefff);
    expect(FLASHLIGHT_FILL_DECAY).toBe(1.74);
    const src = readFileSync(
      resolve(process.cwd(), "src/render/worldView.ts"),
      "utf8",
    );
    expect(src).toContain("export const WARM_LIGHT_AMBER_G = 0.759");
    expect(src).toContain("export const WARM_LIGHT_AMBER_B_GAIN = 0.115");
    expect(src).toContain("export const WARM_LIGHT_AMBER_B = 0.437");
    expect(src).toContain("export const WARM_LIGHT_AMBER_G_GAIN = 0.092");
    expect(src).toContain(
      "warmLight.color.setRGB(1, WARM_LIGHT_AMBER_G + i * WARM_LIGHT_AMBER_G_GAIN, WARM_LIGHT_AMBER_B + i * WARM_LIGHT_AMBER_B_GAIN)",
    );
    expect(src).not.toMatch(/WARM_LIGHT_AMBER_B \+ i \* 0\.1/);
    expect(src).not.toMatch(/export const WARM_LIGHT_AMBER_G = 0\.66/);
    expect(src).not.toMatch(
      /warmLight\.color\.setRGB\(1, 0\.66 \+ i \* 0\.08/,
    );
    expect(src).toContain("export const WARM_LIGHT_DISTANCE_GAIN = 2.875");
    expect(src).toContain("export const WARM_LIGHT_DISTANCE_BASE = 7.475");
    expect(src).toContain(
      "warmLight.distance = WARM_LIGHT_DISTANCE_BASE + i * WARM_LIGHT_DISTANCE_GAIN",
    );
    expect(src).not.toMatch(/export const WARM_LIGHT_DISTANCE_GAIN = 2\.5/);
    expect(src).not.toMatch(
      /warmLight\.distance = WARM_LIGHT_DISTANCE_BASE \+ i \* 2\.5/,
    );
    expect(src).not.toMatch(/warmLight\.distance = 6\.5 \+ i \* 2\.5/);
    expect(src).toContain("export const WARM_LIGHT_DECAY = 1.74");
    expect(src).toContain("new THREE.PointLight(WARM_LIGHT_COLOR, 0, 7.5, WARM_LIGHT_DECAY)");
    expect(src).not.toMatch(/new THREE\.PointLight\(WARM_LIGHT_COLOR, 0, 7\.5, 2\)/);
    expect(src).toContain("export const WARM_LIGHT_VISIBLE_EPS = 0.0174");
    expect(src).toContain("warmLight.visible = i > WARM_LIGHT_VISIBLE_EPS");
    expect(src).not.toMatch(/warmLight\.visible = i > 0\.02/);
    expect(src).toContain("export const WARM_LIGHT_Y = 1.7825");
    expect(src).toContain("warmLight.position.set(wx, WARM_LIGHT_Y, wy)");
    expect(src).not.toMatch(/export const WARM_LIGHT_Y = 1\.55/);
    expect(src).not.toMatch(/warmLight\.position\.set\(wx, 1\.55, wy\)/);
    expect(src).toContain("warmLight.position.set(0, 1.6, 0)");
    expect(src).toContain("export const WARM_LIGHT_INTENSITY_MUL = 1.7825");
    expect(src).toContain("warmLight.intensity = i * WARM_LIGHT_INTENSITY_MUL");
    expect(src).not.toMatch(/export const WARM_LIGHT_INTENSITY_MUL = 1\.55/);
    expect(src).not.toMatch(/warmLight\.intensity = i \* 1\.55/);
    expect(src).toContain("export const FLASHLIGHT_SPOT_DISTANCE_GAIN = 2.3");
    expect(src).toContain("export const FLASHLIGHT_SPOT_DISTANCE_EXTRA = 1.84");
    expect(src).toContain(
      "torchSpot.distance = FLASHLIGHT_CONE_LENGTH + FLASHLIGHT_SPOT_DISTANCE_EXTRA + i * FLASHLIGHT_SPOT_DISTANCE_GAIN",
    );
    expect(src).toContain("FLASHLIGHT_CONE_LENGTH + 2.4");
    expect(src).toContain("export const FLASHLIGHT_SPOT_Y = 1.7825");
    expect(src).toContain("torchSpot.position.set(0, FLASHLIGHT_SPOT_Y, 0)");
    expect(src).toContain("torchSpot.position.set(wx, FLASHLIGHT_SPOT_Y, wy)");
    expect(src).toContain("export const FLASHLIGHT_FILL_Y = 1.5525");
    expect(src).toContain("torchLight.position.set(0, FLASHLIGHT_FILL_Y, 0)");
    expect(src).toContain("torchLight.position.set(wx, FLASHLIGHT_FILL_Y, wy)");
    expect(src).toContain("export const FLASHLIGHT_FILL_DISTANCE_GAIN = 4.025");
    expect(src).toContain("export const FLASHLIGHT_FILL_DISTANCE_BASE = 8.05");
    expect(src).toContain(
      "torchLight.distance = FLASHLIGHT_FILL_DISTANCE_BASE + i * FLASHLIGHT_FILL_DISTANCE_GAIN",
    );
    expect(src).toContain("export const FLASHLIGHT_FILL_COLOR = 0xcaefff");
    expect(src).toContain("export const FLASHLIGHT_FILL_DECAY = 1.74");
    expect(src).toContain("new THREE.PointLight(FLASHLIGHT_FILL_COLOR, 0, 10, FLASHLIGHT_FILL_DECAY)");
    expect(src).not.toMatch(/new THREE\.PointLight\(FLASHLIGHT_FILL_COLOR, 0, 10, 2\)/);
    expect(src).toContain("torchLight.color.setHex(FLASHLIGHT_FILL_COLOR)");
    expect(src).toContain("export const TRACER_FLASH_COLOR = 0xffdd6e");
    expect(src).toContain("new THREE.PointLight(TRACER_FLASH_COLOR, TRACER_FLASH_INTENSITY, TRACER_FLASH_DISTANCE, TRACER_FLASH_DECAY)");
    expect(src).toContain("export const TRACER_COLOR = 0xffffb8");
    expect(src).toContain("color: TRACER_COLOR");
    expect(src).toContain("export const IMPACT_SPARK_LIGHT_COLOR = 0xffef93");
    expect(src).toContain("new THREE.PointLight(\n    IMPACT_SPARK_LIGHT_COLOR,");
    expect(src).toContain("export const IMPACT_SPARK_COLOR = 0xffef93");
    expect(src).toContain("color: IMPACT_SPARK_COLOR");
    expect(src).toContain("export const MUZZLE_LIGHT_COLOR = 0xffffb8");
    expect(src).toContain("new THREE.PointLight(\n    MUZZLE_LIGHT_COLOR,");
    expect(src).toContain("export const MUZZLE_FLASH_COLOR = 0xffffdd");
    expect(src).toContain("color: MUZZLE_FLASH_COLOR");
    expect(src).toContain("export const WARM_LIGHT_COLOR = 0xffca81");
    expect(src).toContain("export const POSSESSED_HEAD_EMISSIVE = 0x30124a");
    expect(src).toContain("emissive: POSSESSED_HEAD_EMISSIVE");
    expect(src).toContain("color: POSSESSED_HEAD_COLOR");
    expect(src).toContain("export const POSSESSED_HEAD_COLOR = 0x8c469f");
    expect(src).toContain("emissive: POSSESSED_EMISSIVE");
    expect(src).toContain("color: POSSESSED_COLOR");
    expect(src).toContain("const POSSESSED_EMISSIVE = 0x1e0925");
    expect(src).toContain("emissiveIntensity: 0.22");
    expect(src).toContain("color: HOSTILE_COLOR");
    expect(src).toContain("color: PLAYER_COLOR");
    expect(src).toContain("color: PLAYER_HEAD_COLOR");
    expect(src).toContain("emissive: PLAYER_HEAD_EMISSIVE");
    expect(TRACER_FLASH_COLOR).toBe(0xffdd6e);
    expect(TRACER_COLOR).toBe(0xffffb8);
    expect(IMPACT_SPARK_LIGHT_COLOR).toBe(0xffef93);
    expect(IMPACT_SPARK_COLOR).toBe(0xffef93);
    expect(MUZZLE_LIGHT_COLOR).toBe(0xffffb8);
    expect(MUZZLE_FLASH_COLOR).toBe(0xffffdd);
    expect(WARM_LIGHT_COLOR).toBe(0xffca81);
    expect(WARM_LIGHT_DISTANCE_GAIN).toBe(2.875);
    expect(WARM_LIGHT_DISTANCE_BASE).toBe(7.475);
    expect(WARM_LIGHT_Y).toBe(1.7825);
    expect(WARM_LIGHT_INTENSITY_MUL).toBe(1.7825);
    expect(POSSESSED_HEAD_EMISSIVE).toBe(0x30124a);
    expect(POSSESSED_HEAD_COLOR).toBe(0x8c469f);
    expect(POSSESSED_COLOR).toBe(0x68347b);
    expect(HOSTILE_COLOR).toBe(0x7b1e1e);
    expect(PLAYER_COLOR).toBe(0x55a4f4);
    expect(PLAYER_HEAD_COLOR).toBe(0x91d1ff);
    expect(PLAYER_HEAD_EMISSIVE).toBe(0x122537);
    expect(MARKER_PALETTE.possessed.emissive).toBe(0x30124a);
    expect(MARKER_PALETTE.player.badge).toBe(0x91d1ff);
    expect(MARKER_PALETTE.player.emissive).toBe(0x1e4a6e);
    expect(MARKER_PALETTE.player.ring).toBe(0x4392f4);
    expect(MARKER_RING_OPACITY).toBe(0.6877);
    expect(muteBadgeY).toBe(2.3);
    expect(possessedBadgeY).toBe(2.3);
  });
});

describe("worldView warm light visible eps", () => {
  test("warm light visible eps 0.02 × 0.87 → 0.0174; amber-B-gain/amber-B/amber-G-gain/amber-G/gain/base/Y/intensity/color/spot-gain/extra/spot-Y/fill-Y/gain/base/flash/mesh/impact/muzzle/possessed/hostile/player/palette/badge-Y iguales", () => {
    expect(WARM_LIGHT_VISIBLE_EPS).toBe(0.0174);
    expect(WARM_LIGHT_VISIBLE_EPS).toBeCloseTo(0.02 * 0.87, 10);
    expect(WARM_LIGHT_DECAY).toBe(1.74);
    expect(WARM_LIGHT_AMBER_B_GAIN).toBe(0.115);
    expect(WARM_LIGHT_AMBER_B).toBe(0.437);
    expect(WARM_LIGHT_AMBER_G_GAIN).toBe(0.092);
    expect(WARM_LIGHT_AMBER_G).toBe(0.759);
    expect(WARM_LIGHT_DISTANCE_GAIN).toBe(2.875);
    expect(WARM_LIGHT_DISTANCE_BASE).toBe(7.475);
    expect(WARM_LIGHT_Y).toBe(1.7825);
    expect(WARM_LIGHT_INTENSITY_MUL).toBe(1.7825);
    expect(FLASHLIGHT_SPOT_DISTANCE_GAIN).toBe(2.3);
    expect(FLASHLIGHT_SPOT_DISTANCE_EXTRA).toBe(1.84);
    expect(FLASHLIGHT_SPOT_Y).toBe(1.7825);
    expect(FLASHLIGHT_FILL_Y).toBe(1.5525);
    expect(FLASHLIGHT_FILL_DISTANCE_GAIN).toBe(4.025);
    expect(FLASHLIGHT_FILL_DISTANCE_BASE).toBe(8.05);
    expect(FLASHLIGHT_FILL_COLOR).toBe(0xcaefff);
    expect(FLASHLIGHT_FILL_DECAY).toBe(1.74);
    const src = readFileSync(
      resolve(process.cwd(), "src/render/worldView.ts"),
      "utf8",
    );
    expect(src).toContain("export const WARM_LIGHT_VISIBLE_EPS = 0.0174");
    expect(src).toContain("warmLight.visible = i > WARM_LIGHT_VISIBLE_EPS");
    expect(src).not.toMatch(/warmLight\.visible = i > 0\.02/);
    expect(src).not.toMatch(/export const WARM_LIGHT_VISIBLE_EPS = 0\.02/);
    expect(src).toContain("export const WARM_LIGHT_AMBER_B_GAIN = 0.115");
    expect(src).toContain("export const WARM_LIGHT_AMBER_B = 0.437");
    expect(src).toContain("export const WARM_LIGHT_AMBER_G_GAIN = 0.092");
    expect(src).toContain("export const WARM_LIGHT_AMBER_G = 0.759");
    expect(src).toContain(
      "warmLight.color.setRGB(1, WARM_LIGHT_AMBER_G + i * WARM_LIGHT_AMBER_G_GAIN, WARM_LIGHT_AMBER_B + i * WARM_LIGHT_AMBER_B_GAIN)",
    );
    expect(src).not.toMatch(/WARM_LIGHT_AMBER_B \+ i \* 0\.1/);
    expect(src).toContain("export const WARM_LIGHT_DISTANCE_GAIN = 2.875");
    expect(src).toContain("export const WARM_LIGHT_DISTANCE_BASE = 7.475");
    expect(src).toContain(
      "warmLight.distance = WARM_LIGHT_DISTANCE_BASE + i * WARM_LIGHT_DISTANCE_GAIN",
    );
    expect(src).not.toMatch(/export const WARM_LIGHT_DISTANCE_GAIN = 2\.5/);
    expect(src).not.toMatch(
      /warmLight\.distance = WARM_LIGHT_DISTANCE_BASE \+ i \* 2\.5/,
    );
    expect(src).not.toMatch(/warmLight\.distance = 6\.5 \+ i \* 2\.5/);
    expect(src).toContain("export const WARM_LIGHT_DECAY = 1.74");
    expect(src).toContain("new THREE.PointLight(WARM_LIGHT_COLOR, 0, 7.5, WARM_LIGHT_DECAY)");
    expect(src).not.toMatch(/new THREE\.PointLight\(WARM_LIGHT_COLOR, 0, 7\.5, 2\)/);
    expect(src).not.toMatch(/export const WARM_LIGHT_DECAY = 2/);
    expect(src).toContain("export const WARM_LIGHT_Y = 1.7825");
    expect(src).toContain("warmLight.position.set(wx, WARM_LIGHT_Y, wy)");
    expect(src).not.toMatch(/export const WARM_LIGHT_Y = 1\.55/);
    expect(src).not.toMatch(/warmLight\.position\.set\(wx, 1\.55, wy\)/);
    expect(src).toContain("warmLight.position.set(0, 1.6, 0)");
    expect(src).toContain("export const WARM_LIGHT_INTENSITY_MUL = 1.7825");
    expect(src).toContain("warmLight.intensity = i * WARM_LIGHT_INTENSITY_MUL");
    expect(src).not.toMatch(/export const WARM_LIGHT_INTENSITY_MUL = 1\.55/);
    expect(src).not.toMatch(/warmLight\.intensity = i \* 1\.55/);
    expect(src).toContain("export const FLASHLIGHT_SPOT_DISTANCE_GAIN = 2.3");
    expect(src).toContain("export const FLASHLIGHT_SPOT_DISTANCE_EXTRA = 1.84");
    expect(src).toContain(
      "torchSpot.distance = FLASHLIGHT_CONE_LENGTH + FLASHLIGHT_SPOT_DISTANCE_EXTRA + i * FLASHLIGHT_SPOT_DISTANCE_GAIN",
    );
    expect(src).toContain("FLASHLIGHT_CONE_LENGTH + 2.4");
    expect(src).toContain("export const FLASHLIGHT_SPOT_Y = 1.7825");
    expect(src).toContain("torchSpot.position.set(0, FLASHLIGHT_SPOT_Y, 0)");
    expect(src).toContain("torchSpot.position.set(wx, FLASHLIGHT_SPOT_Y, wy)");
    expect(src).toContain("export const FLASHLIGHT_FILL_Y = 1.5525");
    expect(src).toContain("torchLight.position.set(0, FLASHLIGHT_FILL_Y, 0)");
    expect(src).toContain("torchLight.position.set(wx, FLASHLIGHT_FILL_Y, wy)");
    expect(src).toContain("export const FLASHLIGHT_FILL_DISTANCE_GAIN = 4.025");
    expect(src).toContain("export const FLASHLIGHT_FILL_DISTANCE_BASE = 8.05");
    expect(src).toContain(
      "torchLight.distance = FLASHLIGHT_FILL_DISTANCE_BASE + i * FLASHLIGHT_FILL_DISTANCE_GAIN",
    );
    expect(src).toContain("export const FLASHLIGHT_FILL_COLOR = 0xcaefff");
    expect(src).toContain("export const FLASHLIGHT_FILL_DECAY = 1.74");
    expect(src).toContain("new THREE.PointLight(FLASHLIGHT_FILL_COLOR, 0, 10, FLASHLIGHT_FILL_DECAY)");
    expect(src).not.toMatch(/new THREE\.PointLight\(FLASHLIGHT_FILL_COLOR, 0, 10, 2\)/);
    expect(src).toContain("torchLight.color.setHex(FLASHLIGHT_FILL_COLOR)");
    expect(src).toContain("export const TRACER_FLASH_COLOR = 0xffdd6e");
    expect(src).toContain("new THREE.PointLight(TRACER_FLASH_COLOR, TRACER_FLASH_INTENSITY, TRACER_FLASH_DISTANCE, TRACER_FLASH_DECAY)");
    expect(src).toContain("export const TRACER_COLOR = 0xffffb8");
    expect(src).toContain("color: TRACER_COLOR");
    expect(src).toContain("export const IMPACT_SPARK_LIGHT_COLOR = 0xffef93");
    expect(src).toContain("new THREE.PointLight(\n    IMPACT_SPARK_LIGHT_COLOR,");
    expect(src).toContain("export const IMPACT_SPARK_COLOR = 0xffef93");
    expect(src).toContain("color: IMPACT_SPARK_COLOR");
    expect(src).toContain("export const MUZZLE_LIGHT_COLOR = 0xffffb8");
    expect(src).toContain("new THREE.PointLight(\n    MUZZLE_LIGHT_COLOR,");
    expect(src).toContain("export const MUZZLE_FLASH_COLOR = 0xffffdd");
    expect(src).toContain("color: MUZZLE_FLASH_COLOR");
    expect(src).toContain("export const WARM_LIGHT_COLOR = 0xffca81");
    expect(src).toContain("export const POSSESSED_HEAD_EMISSIVE = 0x30124a");
    expect(src).toContain("emissive: POSSESSED_HEAD_EMISSIVE");
    expect(src).toContain("color: POSSESSED_HEAD_COLOR");
    expect(src).toContain("export const POSSESSED_HEAD_COLOR = 0x8c469f");
    expect(src).toContain("emissive: POSSESSED_EMISSIVE");
    expect(src).toContain("color: POSSESSED_COLOR");
    expect(src).toContain("const POSSESSED_EMISSIVE = 0x1e0925");
    expect(src).toContain("emissiveIntensity: 0.22");
    expect(src).toContain("color: HOSTILE_COLOR");
    expect(src).toContain("color: PLAYER_COLOR");
    expect(src).toContain("color: PLAYER_HEAD_COLOR");
    expect(src).toContain("emissive: PLAYER_HEAD_EMISSIVE");
    expect(TRACER_FLASH_COLOR).toBe(0xffdd6e);
    expect(TRACER_COLOR).toBe(0xffffb8);
    expect(IMPACT_SPARK_LIGHT_COLOR).toBe(0xffef93);
    expect(IMPACT_SPARK_COLOR).toBe(0xffef93);
    expect(MUZZLE_LIGHT_COLOR).toBe(0xffffb8);
    expect(MUZZLE_FLASH_COLOR).toBe(0xffffdd);
    expect(WARM_LIGHT_COLOR).toBe(0xffca81);
    expect(WARM_LIGHT_DECAY).toBe(1.74);
    expect(WARM_LIGHT_AMBER_B_GAIN).toBe(0.115);
    expect(WARM_LIGHT_AMBER_B).toBe(0.437);
    expect(WARM_LIGHT_AMBER_G_GAIN).toBe(0.092);
    expect(WARM_LIGHT_AMBER_G).toBe(0.759);
    expect(WARM_LIGHT_DISTANCE_GAIN).toBe(2.875);
    expect(WARM_LIGHT_DISTANCE_BASE).toBe(7.475);
    expect(WARM_LIGHT_Y).toBe(1.7825);
    expect(WARM_LIGHT_INTENSITY_MUL).toBe(1.7825);
    expect(POSSESSED_HEAD_EMISSIVE).toBe(0x30124a);
    expect(POSSESSED_HEAD_COLOR).toBe(0x8c469f);
    expect(POSSESSED_COLOR).toBe(0x68347b);
    expect(HOSTILE_COLOR).toBe(0x7b1e1e);
    expect(PLAYER_COLOR).toBe(0x55a4f4);
    expect(PLAYER_HEAD_COLOR).toBe(0x91d1ff);
    expect(PLAYER_HEAD_EMISSIVE).toBe(0x122537);
    expect(MARKER_PALETTE.possessed.emissive).toBe(0x30124a);
    expect(MARKER_PALETTE.player.badge).toBe(0x91d1ff);
    expect(MARKER_PALETTE.player.emissive).toBe(0x1e4a6e);
    expect(MARKER_PALETTE.player.ring).toBe(0x4392f4);
    expect(MARKER_RING_OPACITY).toBe(0.6877);
    expect(muteBadgeY).toBe(2.3);
    expect(possessedBadgeY).toBe(2.3);
  });
});

describe("worldView warm light decay", () => {
  test("warm light decay 2 × 0.87 → 1.74; visible-eps/amber-B-gain/amber-B/amber-G-gain/amber-G/gain/base/Y/intensity/color/spot-gain/extra/spot-Y/fill-Y/gain/base/flash/mesh/impact/muzzle/possessed/hostile/player/palette/badge-Y iguales", () => {
    expect(WARM_LIGHT_DECAY).toBe(1.74);
    expect(WARM_LIGHT_DECAY).toBeCloseTo(2 * 0.87, 10);
    expect(WARM_LIGHT_VISIBLE_EPS).toBe(0.0174);
    expect(WARM_LIGHT_AMBER_B_GAIN).toBe(0.115);
    expect(WARM_LIGHT_AMBER_B).toBe(0.437);
    expect(WARM_LIGHT_AMBER_G_GAIN).toBe(0.092);
    expect(WARM_LIGHT_AMBER_G).toBe(0.759);
    expect(WARM_LIGHT_DISTANCE_GAIN).toBe(2.875);
    expect(WARM_LIGHT_DISTANCE_BASE).toBe(7.475);
    expect(WARM_LIGHT_Y).toBe(1.7825);
    expect(WARM_LIGHT_INTENSITY_MUL).toBe(1.7825);
    expect(FLASHLIGHT_SPOT_DISTANCE_GAIN).toBe(2.3);
    expect(FLASHLIGHT_SPOT_DISTANCE_EXTRA).toBe(1.84);
    expect(FLASHLIGHT_SPOT_Y).toBe(1.7825);
    expect(FLASHLIGHT_FILL_Y).toBe(1.5525);
    expect(FLASHLIGHT_FILL_DISTANCE_GAIN).toBe(4.025);
    expect(FLASHLIGHT_FILL_DISTANCE_BASE).toBe(8.05);
    expect(FLASHLIGHT_FILL_COLOR).toBe(0xcaefff);
    expect(FLASHLIGHT_FILL_DECAY).toBe(1.74);
    const src = readFileSync(
      resolve(process.cwd(), "src/render/worldView.ts"),
      "utf8",
    );
    expect(src).toContain("export const WARM_LIGHT_DECAY = 1.74");
    expect(src).toContain("new THREE.PointLight(WARM_LIGHT_COLOR, 0, 7.5, WARM_LIGHT_DECAY)");
    expect(src).not.toMatch(/new THREE\.PointLight\(WARM_LIGHT_COLOR, 0, 7\.5, 2\)/);
    expect(src).not.toMatch(/export const WARM_LIGHT_DECAY = 2/);
    expect(src).toContain("export const WARM_LIGHT_VISIBLE_EPS = 0.0174");
    expect(src).toContain("warmLight.visible = i > WARM_LIGHT_VISIBLE_EPS");
    expect(src).not.toMatch(/warmLight\.visible = i > 0\.02/);
    expect(src).not.toMatch(/export const WARM_LIGHT_VISIBLE_EPS = 0\.02/);
    expect(src).toContain("export const WARM_LIGHT_AMBER_B_GAIN = 0.115");
    expect(src).toContain("export const WARM_LIGHT_AMBER_B = 0.437");
    expect(src).toContain("export const WARM_LIGHT_AMBER_G_GAIN = 0.092");
    expect(src).toContain("export const WARM_LIGHT_AMBER_G = 0.759");
    expect(src).toContain(
      "warmLight.color.setRGB(1, WARM_LIGHT_AMBER_G + i * WARM_LIGHT_AMBER_G_GAIN, WARM_LIGHT_AMBER_B + i * WARM_LIGHT_AMBER_B_GAIN)",
    );
    expect(src).not.toMatch(/WARM_LIGHT_AMBER_B \+ i \* 0\.1/);
    expect(src).toContain("export const WARM_LIGHT_DISTANCE_GAIN = 2.875");
    expect(src).toContain("export const WARM_LIGHT_DISTANCE_BASE = 7.475");
    expect(src).toContain(
      "warmLight.distance = WARM_LIGHT_DISTANCE_BASE + i * WARM_LIGHT_DISTANCE_GAIN",
    );
    expect(src).not.toMatch(/export const WARM_LIGHT_DISTANCE_GAIN = 2\.5/);
    expect(src).not.toMatch(
      /warmLight\.distance = WARM_LIGHT_DISTANCE_BASE \+ i \* 2\.5/,
    );
    expect(src).not.toMatch(/warmLight\.distance = 6\.5 \+ i \* 2\.5/);
    expect(src).toContain("export const WARM_LIGHT_Y = 1.7825");
    expect(src).toContain("warmLight.position.set(wx, WARM_LIGHT_Y, wy)");
    expect(src).not.toMatch(/export const WARM_LIGHT_Y = 1\.55/);
    expect(src).not.toMatch(/warmLight\.position\.set\(wx, 1\.55, wy\)/);
    expect(src).toContain("warmLight.position.set(0, 1.6, 0)");
    expect(src).toContain("export const WARM_LIGHT_INTENSITY_MUL = 1.7825");
    expect(src).toContain("warmLight.intensity = i * WARM_LIGHT_INTENSITY_MUL");
    expect(src).not.toMatch(/export const WARM_LIGHT_INTENSITY_MUL = 1\.55/);
    expect(src).not.toMatch(/warmLight\.intensity = i \* 1\.55/);
    expect(src).toContain("export const FLASHLIGHT_SPOT_DISTANCE_GAIN = 2.3");
    expect(src).toContain("export const FLASHLIGHT_SPOT_DISTANCE_EXTRA = 1.84");
    expect(src).toContain(
      "torchSpot.distance = FLASHLIGHT_CONE_LENGTH + FLASHLIGHT_SPOT_DISTANCE_EXTRA + i * FLASHLIGHT_SPOT_DISTANCE_GAIN",
    );
    expect(src).toContain("FLASHLIGHT_CONE_LENGTH + 2.4");
    expect(src).toContain("export const FLASHLIGHT_SPOT_Y = 1.7825");
    expect(src).toContain("torchSpot.position.set(0, FLASHLIGHT_SPOT_Y, 0)");
    expect(src).toContain("torchSpot.position.set(wx, FLASHLIGHT_SPOT_Y, wy)");
    expect(src).toContain("export const FLASHLIGHT_FILL_Y = 1.5525");
    expect(src).toContain("torchLight.position.set(0, FLASHLIGHT_FILL_Y, 0)");
    expect(src).toContain("torchLight.position.set(wx, FLASHLIGHT_FILL_Y, wy)");
    expect(src).toContain("export const FLASHLIGHT_FILL_DISTANCE_GAIN = 4.025");
    expect(src).toContain("export const FLASHLIGHT_FILL_DISTANCE_BASE = 8.05");
    expect(src).toContain(
      "torchLight.distance = FLASHLIGHT_FILL_DISTANCE_BASE + i * FLASHLIGHT_FILL_DISTANCE_GAIN",
    );
    expect(src).toContain("export const FLASHLIGHT_FILL_COLOR = 0xcaefff");
    expect(src).toContain("export const FLASHLIGHT_FILL_DECAY = 1.74");
    expect(src).toContain("new THREE.PointLight(FLASHLIGHT_FILL_COLOR, 0, 10, FLASHLIGHT_FILL_DECAY)");
    expect(src).not.toMatch(/new THREE\.PointLight\(FLASHLIGHT_FILL_COLOR, 0, 10, 2\)/);
    expect(src).toContain("torchLight.color.setHex(FLASHLIGHT_FILL_COLOR)");
    expect(src).toContain("export const TRACER_FLASH_COLOR = 0xffdd6e");
    expect(src).toContain("new THREE.PointLight(TRACER_FLASH_COLOR, TRACER_FLASH_INTENSITY, TRACER_FLASH_DISTANCE, TRACER_FLASH_DECAY)");
    expect(src).toContain("export const TRACER_COLOR = 0xffffb8");
    expect(src).toContain("color: TRACER_COLOR");
    expect(src).toContain("export const IMPACT_SPARK_LIGHT_COLOR = 0xffef93");
    expect(src).toContain("new THREE.PointLight(\n    IMPACT_SPARK_LIGHT_COLOR,");
    expect(src).toContain("export const IMPACT_SPARK_COLOR = 0xffef93");
    expect(src).toContain("color: IMPACT_SPARK_COLOR");
    expect(src).toContain("export const MUZZLE_LIGHT_COLOR = 0xffffb8");
    expect(src).toContain("new THREE.PointLight(\n    MUZZLE_LIGHT_COLOR,");
    expect(src).toContain("export const MUZZLE_FLASH_COLOR = 0xffffdd");
    expect(src).toContain("color: MUZZLE_FLASH_COLOR");
    expect(src).toContain("export const WARM_LIGHT_COLOR = 0xffca81");
    expect(src).toContain("export const POSSESSED_HEAD_EMISSIVE = 0x30124a");
    expect(src).toContain("emissive: POSSESSED_HEAD_EMISSIVE");
    expect(src).toContain("color: POSSESSED_HEAD_COLOR");
    expect(src).toContain("export const POSSESSED_HEAD_COLOR = 0x8c469f");
    expect(src).toContain("emissive: POSSESSED_EMISSIVE");
    expect(src).toContain("color: POSSESSED_COLOR");
    expect(src).toContain("const POSSESSED_EMISSIVE = 0x1e0925");
    expect(src).toContain("emissiveIntensity: 0.22");
    expect(src).toContain("color: HOSTILE_COLOR");
    expect(src).toContain("color: PLAYER_COLOR");
    expect(src).toContain("color: PLAYER_HEAD_COLOR");
    expect(src).toContain("emissive: PLAYER_HEAD_EMISSIVE");
    expect(TRACER_FLASH_COLOR).toBe(0xffdd6e);
    expect(TRACER_COLOR).toBe(0xffffb8);
    expect(IMPACT_SPARK_LIGHT_COLOR).toBe(0xffef93);
    expect(IMPACT_SPARK_COLOR).toBe(0xffef93);
    expect(MUZZLE_LIGHT_COLOR).toBe(0xffffb8);
    expect(MUZZLE_FLASH_COLOR).toBe(0xffffdd);
    expect(WARM_LIGHT_COLOR).toBe(0xffca81);
    expect(WARM_LIGHT_VISIBLE_EPS).toBe(0.0174);
    expect(WARM_LIGHT_AMBER_B_GAIN).toBe(0.115);
    expect(WARM_LIGHT_AMBER_B).toBe(0.437);
    expect(WARM_LIGHT_AMBER_G_GAIN).toBe(0.092);
    expect(WARM_LIGHT_AMBER_G).toBe(0.759);
    expect(WARM_LIGHT_DISTANCE_GAIN).toBe(2.875);
    expect(WARM_LIGHT_DISTANCE_BASE).toBe(7.475);
    expect(WARM_LIGHT_Y).toBe(1.7825);
    expect(WARM_LIGHT_INTENSITY_MUL).toBe(1.7825);
    expect(POSSESSED_HEAD_EMISSIVE).toBe(0x30124a);
    expect(POSSESSED_HEAD_COLOR).toBe(0x8c469f);
    expect(POSSESSED_COLOR).toBe(0x68347b);
    expect(HOSTILE_COLOR).toBe(0x7b1e1e);
    expect(PLAYER_COLOR).toBe(0x55a4f4);
    expect(PLAYER_HEAD_COLOR).toBe(0x91d1ff);
    expect(PLAYER_HEAD_EMISSIVE).toBe(0x122537);
    expect(MARKER_PALETTE.possessed.emissive).toBe(0x30124a);
    expect(MARKER_PALETTE.player.badge).toBe(0x91d1ff);
    expect(MARKER_PALETTE.player.emissive).toBe(0x1e4a6e);
    expect(MARKER_PALETTE.player.ring).toBe(0x4392f4);
    expect(MARKER_RING_OPACITY).toBe(0.6877);
    expect(muteBadgeY).toBe(2.3);
    expect(possessedBadgeY).toBe(2.3);
  });
});

describe("worldView flashlight fill decay", () => {
  test("flashlight fill decay 2 × 0.87 → 1.74; warm-decay/visible-eps/amber-B-gain/amber-B/amber-G-gain/amber-G/gain/base/Y/intensity/color/spot-gain/extra/spot-Y/fill-Y/gain/base/flash/mesh/impact/muzzle/possessed/hostile/player/palette/badge-Y iguales", () => {
    expect(FLASHLIGHT_FILL_DECAY).toBe(1.74);
    expect(FLASHLIGHT_FILL_DECAY).toBeCloseTo(2 * 0.87, 10);
    expect(WARM_LIGHT_DECAY).toBe(1.74);
    expect(WARM_LIGHT_VISIBLE_EPS).toBe(0.0174);
    expect(WARM_LIGHT_AMBER_B_GAIN).toBe(0.115);
    expect(WARM_LIGHT_AMBER_B).toBe(0.437);
    expect(WARM_LIGHT_AMBER_G_GAIN).toBe(0.092);
    expect(WARM_LIGHT_AMBER_G).toBe(0.759);
    expect(WARM_LIGHT_DISTANCE_GAIN).toBe(2.875);
    expect(WARM_LIGHT_DISTANCE_BASE).toBe(7.475);
    expect(WARM_LIGHT_Y).toBe(1.7825);
    expect(WARM_LIGHT_INTENSITY_MUL).toBe(1.7825);
    expect(FLASHLIGHT_SPOT_DISTANCE_GAIN).toBe(2.3);
    expect(FLASHLIGHT_SPOT_DISTANCE_EXTRA).toBe(1.84);
    expect(FLASHLIGHT_SPOT_Y).toBe(1.7825);
    expect(FLASHLIGHT_SPOT_DECAY).toBe(1.74);
    expect(FLASHLIGHT_FILL_Y).toBe(1.5525);
    expect(FLASHLIGHT_FILL_DISTANCE_GAIN).toBe(4.025);
    expect(FLASHLIGHT_FILL_DISTANCE_BASE).toBe(8.05);
    expect(FLASHLIGHT_FILL_COLOR).toBe(0xcaefff);
    const src = readFileSync(
      resolve(process.cwd(), "src/render/worldView.ts"),
      "utf8",
    );
    expect(src).toContain("export const FLASHLIGHT_FILL_DECAY = 1.74");
    expect(src).toContain("new THREE.PointLight(FLASHLIGHT_FILL_COLOR, 0, 10, FLASHLIGHT_FILL_DECAY)");
    expect(src).not.toMatch(/new THREE\.PointLight\(FLASHLIGHT_FILL_COLOR, 0, 10, 2\)/);
    expect(src).not.toMatch(/export const FLASHLIGHT_FILL_DECAY = 2/);
    expect(src).toContain("export const WARM_LIGHT_DECAY = 1.74");
    expect(src).toContain("new THREE.PointLight(WARM_LIGHT_COLOR, 0, 7.5, WARM_LIGHT_DECAY)");
    expect(src).not.toMatch(/new THREE\.PointLight\(WARM_LIGHT_COLOR, 0, 7\.5, 2\)/);
    expect(src).toContain("export const WARM_LIGHT_VISIBLE_EPS = 0.0174");
    expect(src).toContain("warmLight.visible = i > WARM_LIGHT_VISIBLE_EPS");
    expect(src).not.toMatch(/warmLight\.visible = i > 0\.02/);
    expect(src).not.toMatch(/export const WARM_LIGHT_VISIBLE_EPS = 0\.02/);
    expect(src).toContain("export const WARM_LIGHT_AMBER_B_GAIN = 0.115");
    expect(src).toContain("export const WARM_LIGHT_AMBER_B = 0.437");
    expect(src).toContain("export const WARM_LIGHT_AMBER_G_GAIN = 0.092");
    expect(src).toContain("export const WARM_LIGHT_AMBER_G = 0.759");
    expect(src).toContain(
      "warmLight.color.setRGB(1, WARM_LIGHT_AMBER_G + i * WARM_LIGHT_AMBER_G_GAIN, WARM_LIGHT_AMBER_B + i * WARM_LIGHT_AMBER_B_GAIN)",
    );
    expect(src).not.toMatch(/WARM_LIGHT_AMBER_B \+ i \* 0\.1/);
    expect(src).toContain("export const WARM_LIGHT_DISTANCE_GAIN = 2.875");
    expect(src).toContain("export const WARM_LIGHT_DISTANCE_BASE = 7.475");
    expect(src).toContain(
      "warmLight.distance = WARM_LIGHT_DISTANCE_BASE + i * WARM_LIGHT_DISTANCE_GAIN",
    );
    expect(src).not.toMatch(/export const WARM_LIGHT_DISTANCE_GAIN = 2\.5/);
    expect(src).not.toMatch(
      /warmLight\.distance = WARM_LIGHT_DISTANCE_BASE \+ i \* 2\.5/,
    );
    expect(src).not.toMatch(/warmLight\.distance = 6\.5 \+ i \* 2\.5/);
    expect(src).toContain("export const WARM_LIGHT_Y = 1.7825");
    expect(src).toContain("warmLight.position.set(wx, WARM_LIGHT_Y, wy)");
    expect(src).not.toMatch(/export const WARM_LIGHT_Y = 1\.55/);
    expect(src).not.toMatch(/warmLight\.position\.set\(wx, 1\.55, wy\)/);
    expect(src).toContain("warmLight.position.set(0, 1.6, 0)");
    expect(src).toContain("export const WARM_LIGHT_INTENSITY_MUL = 1.7825");
    expect(src).toContain("warmLight.intensity = i * WARM_LIGHT_INTENSITY_MUL");
    expect(src).not.toMatch(/export const WARM_LIGHT_INTENSITY_MUL = 1\.55/);
    expect(src).not.toMatch(/warmLight\.intensity = i \* 1\.55/);
    expect(src).toContain("export const FLASHLIGHT_SPOT_DISTANCE_GAIN = 2.3");
    expect(src).toContain("export const FLASHLIGHT_SPOT_DISTANCE_EXTRA = 1.84");
    expect(src).toContain(
      "torchSpot.distance = FLASHLIGHT_CONE_LENGTH + FLASHLIGHT_SPOT_DISTANCE_EXTRA + i * FLASHLIGHT_SPOT_DISTANCE_GAIN",
    );
    expect(src).toContain("FLASHLIGHT_CONE_LENGTH + 2.4");
    expect(src).toContain("export const FLASHLIGHT_SPOT_DECAY = 1.74");
    expect(src).toContain("FLASHLIGHT_SPOT_PENUMBRA,\n    FLASHLIGHT_SPOT_DECAY,");
    expect(src).not.toMatch(/FLASHLIGHT_SPOT_PENUMBRA,\s*\n\s*2,/);
    expect(src).not.toMatch(/export const FLASHLIGHT_SPOT_DECAY = 2/);
    expect(src).toContain("export const FLASHLIGHT_SPOT_Y = 1.7825");
    expect(src).toContain("torchSpot.position.set(0, FLASHLIGHT_SPOT_Y, 0)");
    expect(src).toContain("torchSpot.position.set(wx, FLASHLIGHT_SPOT_Y, wy)");
    expect(src).toContain("export const FLASHLIGHT_FILL_Y = 1.5525");
    expect(src).toContain("torchLight.position.set(0, FLASHLIGHT_FILL_Y, 0)");
    expect(src).toContain("torchLight.position.set(wx, FLASHLIGHT_FILL_Y, wy)");
    expect(src).toContain("export const FLASHLIGHT_FILL_DISTANCE_GAIN = 4.025");
    expect(src).toContain("export const FLASHLIGHT_FILL_DISTANCE_BASE = 8.05");
    expect(src).toContain(
      "torchLight.distance = FLASHLIGHT_FILL_DISTANCE_BASE + i * FLASHLIGHT_FILL_DISTANCE_GAIN",
    );
    expect(src).toContain("export const FLASHLIGHT_FILL_COLOR = 0xcaefff");
    expect(src).toContain("torchLight.color.setHex(FLASHLIGHT_FILL_COLOR)");
    expect(src).toContain("export const TRACER_FLASH_COLOR = 0xffdd6e");
    expect(src).toContain("new THREE.PointLight(TRACER_FLASH_COLOR, TRACER_FLASH_INTENSITY, TRACER_FLASH_DISTANCE, TRACER_FLASH_DECAY)");
    expect(src).toContain("export const TRACER_COLOR = 0xffffb8");
    expect(src).toContain("color: TRACER_COLOR");
    expect(src).toContain("export const IMPACT_SPARK_LIGHT_COLOR = 0xffef93");
    expect(src).toContain("new THREE.PointLight(\n    IMPACT_SPARK_LIGHT_COLOR,");
    expect(src).toContain("export const IMPACT_SPARK_COLOR = 0xffef93");
    expect(src).toContain("color: IMPACT_SPARK_COLOR");
    expect(src).toContain("export const MUZZLE_LIGHT_COLOR = 0xffffb8");
    expect(src).toContain("new THREE.PointLight(\n    MUZZLE_LIGHT_COLOR,");
    expect(src).toContain("export const MUZZLE_FLASH_COLOR = 0xffffdd");
    expect(src).toContain("color: MUZZLE_FLASH_COLOR");
    expect(src).toContain("export const WARM_LIGHT_COLOR = 0xffca81");
    expect(src).toContain("export const POSSESSED_HEAD_EMISSIVE = 0x30124a");
    expect(src).toContain("emissive: POSSESSED_HEAD_EMISSIVE");
    expect(src).toContain("color: POSSESSED_HEAD_COLOR");
    expect(src).toContain("export const POSSESSED_HEAD_COLOR = 0x8c469f");
    expect(src).toContain("emissive: POSSESSED_EMISSIVE");
    expect(src).toContain("color: POSSESSED_COLOR");
    expect(src).toContain("const POSSESSED_EMISSIVE = 0x1e0925");
    expect(src).toContain("emissiveIntensity: 0.22");
    expect(src).toContain("color: HOSTILE_COLOR");
    expect(src).toContain("color: PLAYER_COLOR");
    expect(src).toContain("color: PLAYER_HEAD_COLOR");
    expect(src).toContain("emissive: PLAYER_HEAD_EMISSIVE");
    expect(TRACER_FLASH_COLOR).toBe(0xffdd6e);
    expect(TRACER_COLOR).toBe(0xffffb8);
    expect(IMPACT_SPARK_LIGHT_COLOR).toBe(0xffef93);
    expect(IMPACT_SPARK_COLOR).toBe(0xffef93);
    expect(MUZZLE_LIGHT_COLOR).toBe(0xffffb8);
    expect(MUZZLE_FLASH_COLOR).toBe(0xffffdd);
    expect(WARM_LIGHT_COLOR).toBe(0xffca81);
    expect(WARM_LIGHT_DECAY).toBe(1.74);
    expect(WARM_LIGHT_VISIBLE_EPS).toBe(0.0174);
    expect(WARM_LIGHT_AMBER_B_GAIN).toBe(0.115);
    expect(WARM_LIGHT_AMBER_B).toBe(0.437);
    expect(WARM_LIGHT_AMBER_G_GAIN).toBe(0.092);
    expect(WARM_LIGHT_AMBER_G).toBe(0.759);
    expect(WARM_LIGHT_DISTANCE_GAIN).toBe(2.875);
    expect(WARM_LIGHT_DISTANCE_BASE).toBe(7.475);
    expect(WARM_LIGHT_Y).toBe(1.7825);
    expect(WARM_LIGHT_INTENSITY_MUL).toBe(1.7825);
    expect(POSSESSED_HEAD_EMISSIVE).toBe(0x30124a);
    expect(POSSESSED_HEAD_COLOR).toBe(0x8c469f);
    expect(POSSESSED_COLOR).toBe(0x68347b);
    expect(HOSTILE_COLOR).toBe(0x7b1e1e);
    expect(PLAYER_COLOR).toBe(0x55a4f4);
    expect(PLAYER_HEAD_COLOR).toBe(0x91d1ff);
    expect(PLAYER_HEAD_EMISSIVE).toBe(0x122537);
    expect(MARKER_PALETTE.possessed.emissive).toBe(0x30124a);
    expect(MARKER_PALETTE.player.badge).toBe(0x91d1ff);
    expect(MARKER_PALETTE.player.emissive).toBe(0x1e4a6e);
    expect(MARKER_PALETTE.player.ring).toBe(0x4392f4);
    expect(MARKER_RING_OPACITY).toBe(0.6877);
    expect(muteBadgeY).toBe(2.3);
    expect(possessedBadgeY).toBe(2.3);
  });
});

describe("worldView flashlight spot decay", () => {
  test("flashlight spot decay 2 × 0.87 → 1.74; fill-decay/warm-decay/visible-eps/amber-B-gain/amber-B/amber-G-gain/amber-G/gain/base/Y/intensity/color/spot-gain/extra/spot-Y/fill-Y/gain/base/flash/mesh/impact/muzzle/possessed/hostile/player/palette/badge-Y iguales", () => {
    expect(FLASHLIGHT_SPOT_DECAY).toBe(1.74);
    expect(FLASHLIGHT_SPOT_DECAY).toBeCloseTo(2 * 0.87, 10);
    expect(FLASHLIGHT_FILL_DECAY).toBe(1.74);
    expect(WARM_LIGHT_DECAY).toBe(1.74);
    expect(WARM_LIGHT_VISIBLE_EPS).toBe(0.0174);
    expect(WARM_LIGHT_AMBER_B_GAIN).toBe(0.115);
    expect(WARM_LIGHT_AMBER_B).toBe(0.437);
    expect(WARM_LIGHT_AMBER_G_GAIN).toBe(0.092);
    expect(WARM_LIGHT_AMBER_G).toBe(0.759);
    expect(WARM_LIGHT_DISTANCE_GAIN).toBe(2.875);
    expect(WARM_LIGHT_DISTANCE_BASE).toBe(7.475);
    expect(WARM_LIGHT_Y).toBe(1.7825);
    expect(WARM_LIGHT_INTENSITY_MUL).toBe(1.7825);
    expect(FLASHLIGHT_SPOT_DISTANCE_GAIN).toBe(2.3);
    expect(FLASHLIGHT_SPOT_DISTANCE_EXTRA).toBe(1.84);
    expect(FLASHLIGHT_SPOT_Y).toBe(1.7825);
    expect(FLASHLIGHT_FILL_Y).toBe(1.5525);
    expect(FLASHLIGHT_FILL_DISTANCE_GAIN).toBe(4.025);
    expect(FLASHLIGHT_FILL_DISTANCE_BASE).toBe(8.05);
    expect(FLASHLIGHT_FILL_COLOR).toBe(0xcaefff);
    expect(MUZZLE_LIGHT_DECAY).toBe(1.74);
    const src = readFileSync(
      resolve(process.cwd(), "src/render/worldView.ts"),
      "utf8",
    );
    expect(src).toContain("export const FLASHLIGHT_SPOT_DECAY = 1.74");
    expect(src).toContain("FLASHLIGHT_SPOT_PENUMBRA,\n    FLASHLIGHT_SPOT_DECAY,");
    expect(src).not.toMatch(/FLASHLIGHT_SPOT_PENUMBRA,\s*\n\s*2,/);
    expect(src).not.toMatch(/export const FLASHLIGHT_SPOT_DECAY = 2/);
    expect(src).toContain("FLASHLIGHT_CONE_LENGTH + 2.4");
    expect(src).toContain("export const FLASHLIGHT_FILL_DECAY = 1.74");
    expect(src).toContain("new THREE.PointLight(FLASHLIGHT_FILL_COLOR, 0, 10, FLASHLIGHT_FILL_DECAY)");
    expect(src).not.toMatch(/new THREE\.PointLight\(FLASHLIGHT_FILL_COLOR, 0, 10, 2\)/);
    expect(src).not.toMatch(/export const FLASHLIGHT_FILL_DECAY = 2/);
    expect(src).toContain("export const MUZZLE_LIGHT_DECAY = 1.74");
    expect(src).toContain("MUZZLE_LIGHT_DISTANCE,\n    MUZZLE_LIGHT_DECAY,");
    expect(src).not.toMatch(/MUZZLE_LIGHT_DISTANCE,\s*\n\s*2,/);
    expect(src).not.toMatch(/export const MUZZLE_LIGHT_DECAY = 2/);
    expect(src).toContain("const MUZZLE_LIGHT_DISTANCE = 2.6");
    expect(src).toContain("export const WARM_LIGHT_DECAY = 1.74");
    expect(src).toContain("new THREE.PointLight(WARM_LIGHT_COLOR, 0, 7.5, WARM_LIGHT_DECAY)");
    expect(src).not.toMatch(/new THREE\.PointLight\(WARM_LIGHT_COLOR, 0, 7\.5, 2\)/);
    expect(src).toContain("export const WARM_LIGHT_VISIBLE_EPS = 0.0174");
    expect(src).toContain("warmLight.visible = i > WARM_LIGHT_VISIBLE_EPS");
    expect(src).not.toMatch(/warmLight\.visible = i > 0\.02/);
    expect(src).not.toMatch(/export const WARM_LIGHT_VISIBLE_EPS = 0\.02/);
    expect(src).toContain("export const WARM_LIGHT_AMBER_B_GAIN = 0.115");
    expect(src).toContain("export const WARM_LIGHT_AMBER_B = 0.437");
    expect(src).toContain("export const WARM_LIGHT_AMBER_G_GAIN = 0.092");
    expect(src).toContain("export const WARM_LIGHT_AMBER_G = 0.759");
    expect(src).toContain(
      "warmLight.color.setRGB(1, WARM_LIGHT_AMBER_G + i * WARM_LIGHT_AMBER_G_GAIN, WARM_LIGHT_AMBER_B + i * WARM_LIGHT_AMBER_B_GAIN)",
    );
    expect(src).not.toMatch(/WARM_LIGHT_AMBER_B \+ i \* 0\.1/);
    expect(src).toContain("export const WARM_LIGHT_DISTANCE_GAIN = 2.875");
    expect(src).toContain("export const WARM_LIGHT_DISTANCE_BASE = 7.475");
    expect(src).toContain(
      "warmLight.distance = WARM_LIGHT_DISTANCE_BASE + i * WARM_LIGHT_DISTANCE_GAIN",
    );
    expect(src).not.toMatch(/export const WARM_LIGHT_DISTANCE_GAIN = 2\.5/);
    expect(src).not.toMatch(
      /warmLight\.distance = WARM_LIGHT_DISTANCE_BASE \+ i \* 2\.5/,
    );
    expect(src).not.toMatch(/warmLight\.distance = 6\.5 \+ i \* 2\.5/);
    expect(src).toContain("export const WARM_LIGHT_Y = 1.7825");
    expect(src).toContain("warmLight.position.set(wx, WARM_LIGHT_Y, wy)");
    expect(src).not.toMatch(/export const WARM_LIGHT_Y = 1\.55/);
    expect(src).not.toMatch(/warmLight\.position\.set\(wx, 1\.55, wy\)/);
    expect(src).toContain("warmLight.position.set(0, 1.6, 0)");
    expect(src).toContain("export const WARM_LIGHT_INTENSITY_MUL = 1.7825");
    expect(src).toContain("warmLight.intensity = i * WARM_LIGHT_INTENSITY_MUL");
    expect(src).not.toMatch(/export const WARM_LIGHT_INTENSITY_MUL = 1\.55/);
    expect(src).not.toMatch(/warmLight\.intensity = i \* 1\.55/);
    expect(src).toContain("export const FLASHLIGHT_SPOT_DISTANCE_GAIN = 2.3");
    expect(src).toContain("export const FLASHLIGHT_SPOT_DISTANCE_EXTRA = 1.84");
    expect(src).toContain(
      "torchSpot.distance = FLASHLIGHT_CONE_LENGTH + FLASHLIGHT_SPOT_DISTANCE_EXTRA + i * FLASHLIGHT_SPOT_DISTANCE_GAIN",
    );
    expect(src).toContain("export const FLASHLIGHT_SPOT_Y = 1.7825");
    expect(src).toContain("torchSpot.position.set(0, FLASHLIGHT_SPOT_Y, 0)");
    expect(src).toContain("torchSpot.position.set(wx, FLASHLIGHT_SPOT_Y, wy)");
    expect(src).toContain("export const FLASHLIGHT_FILL_Y = 1.5525");
    expect(src).toContain("torchLight.position.set(0, FLASHLIGHT_FILL_Y, 0)");
    expect(src).toContain("torchLight.position.set(wx, FLASHLIGHT_FILL_Y, wy)");
    expect(src).toContain("export const FLASHLIGHT_FILL_DISTANCE_GAIN = 4.025");
    expect(src).toContain("export const FLASHLIGHT_FILL_DISTANCE_BASE = 8.05");
    expect(src).toContain(
      "torchLight.distance = FLASHLIGHT_FILL_DISTANCE_BASE + i * FLASHLIGHT_FILL_DISTANCE_GAIN",
    );
    expect(src).toContain("export const FLASHLIGHT_FILL_COLOR = 0xcaefff");
    expect(src).toContain("torchLight.color.setHex(FLASHLIGHT_FILL_COLOR)");
    expect(src).toContain("export const TRACER_FLASH_COLOR = 0xffdd6e");
    expect(src).toContain("new THREE.PointLight(TRACER_FLASH_COLOR, TRACER_FLASH_INTENSITY, TRACER_FLASH_DISTANCE, TRACER_FLASH_DECAY)");
    expect(src).toContain("export const TRACER_COLOR = 0xffffb8");
    expect(src).toContain("color: TRACER_COLOR");
    expect(src).toContain("export const IMPACT_SPARK_LIGHT_COLOR = 0xffef93");
    expect(src).toContain("new THREE.PointLight(\n    IMPACT_SPARK_LIGHT_COLOR,");
    expect(src).toContain("export const IMPACT_SPARK_COLOR = 0xffef93");
    expect(src).toContain("color: IMPACT_SPARK_COLOR");
    expect(src).toContain("export const MUZZLE_LIGHT_COLOR = 0xffffb8");
    expect(src).toContain("new THREE.PointLight(\n    MUZZLE_LIGHT_COLOR,");
    expect(src).toContain("export const MUZZLE_FLASH_COLOR = 0xffffdd");
    expect(src).toContain("color: MUZZLE_FLASH_COLOR");
    expect(src).toContain("export const WARM_LIGHT_COLOR = 0xffca81");
    expect(src).toContain("export const POSSESSED_HEAD_EMISSIVE = 0x30124a");
    expect(src).toContain("emissive: POSSESSED_HEAD_EMISSIVE");
    expect(src).toContain("color: POSSESSED_HEAD_COLOR");
    expect(src).toContain("export const POSSESSED_HEAD_COLOR = 0x8c469f");
    expect(src).toContain("emissive: POSSESSED_EMISSIVE");
    expect(src).toContain("color: POSSESSED_COLOR");
    expect(src).toContain("const POSSESSED_EMISSIVE = 0x1e0925");
    expect(src).toContain("emissiveIntensity: 0.22");
    expect(src).toContain("color: HOSTILE_COLOR");
    expect(src).toContain("color: PLAYER_COLOR");
    expect(src).toContain("color: PLAYER_HEAD_COLOR");
    expect(src).toContain("emissive: PLAYER_HEAD_EMISSIVE");
    expect(TRACER_FLASH_COLOR).toBe(0xffdd6e);
    expect(TRACER_COLOR).toBe(0xffffb8);
    expect(IMPACT_SPARK_LIGHT_COLOR).toBe(0xffef93);
    expect(IMPACT_SPARK_COLOR).toBe(0xffef93);
    expect(MUZZLE_LIGHT_COLOR).toBe(0xffffb8);
    expect(MUZZLE_LIGHT_DECAY).toBe(1.74);
    expect(MUZZLE_FLASH_COLOR).toBe(0xffffdd);
    expect(WARM_LIGHT_COLOR).toBe(0xffca81);
    expect(WARM_LIGHT_DECAY).toBe(1.74);
    expect(WARM_LIGHT_VISIBLE_EPS).toBe(0.0174);
    expect(WARM_LIGHT_AMBER_B_GAIN).toBe(0.115);
    expect(WARM_LIGHT_AMBER_B).toBe(0.437);
    expect(WARM_LIGHT_AMBER_G_GAIN).toBe(0.092);
    expect(WARM_LIGHT_AMBER_G).toBe(0.759);
    expect(WARM_LIGHT_DISTANCE_GAIN).toBe(2.875);
    expect(WARM_LIGHT_DISTANCE_BASE).toBe(7.475);
    expect(WARM_LIGHT_Y).toBe(1.7825);
    expect(WARM_LIGHT_INTENSITY_MUL).toBe(1.7825);
    expect(POSSESSED_HEAD_EMISSIVE).toBe(0x30124a);
    expect(POSSESSED_HEAD_COLOR).toBe(0x8c469f);
    expect(POSSESSED_COLOR).toBe(0x68347b);
    expect(HOSTILE_COLOR).toBe(0x7b1e1e);
    expect(PLAYER_COLOR).toBe(0x55a4f4);
    expect(PLAYER_HEAD_COLOR).toBe(0x91d1ff);
    expect(PLAYER_HEAD_EMISSIVE).toBe(0x122537);
    expect(MARKER_PALETTE.possessed.emissive).toBe(0x30124a);
    expect(MARKER_PALETTE.player.badge).toBe(0x91d1ff);
    expect(MARKER_PALETTE.player.emissive).toBe(0x1e4a6e);
    expect(MARKER_PALETTE.player.ring).toBe(0x4392f4);
    expect(MARKER_RING_OPACITY).toBe(0.6877);
    expect(muteBadgeY).toBe(2.3);
    expect(possessedBadgeY).toBe(2.3);
  });
});

describe("worldView muzzle light decay", () => {
  test("muzzle light decay 2 × 0.87 → 1.74; spot-decay/fill-decay/warm-decay/visible-eps/amber-B-gain/amber-B/amber-G-gain/amber-G/gain/base/Y/intensity/color/spot-gain/extra/spot-Y/fill-Y/gain/base/flash/mesh/impact/distance/possessed/hostile/player/palette/badge-Y iguales", () => {
    expect(MUZZLE_LIGHT_DECAY).toBe(1.74);
    expect(MUZZLE_LIGHT_DECAY).toBeCloseTo(2 * 0.87, 10);
    expect(FLASHLIGHT_SPOT_DECAY).toBe(1.74);
    expect(FLASHLIGHT_FILL_DECAY).toBe(1.74);
    expect(WARM_LIGHT_DECAY).toBe(1.74);
    expect(WARM_LIGHT_VISIBLE_EPS).toBe(0.0174);
    expect(WARM_LIGHT_AMBER_B_GAIN).toBe(0.115);
    expect(WARM_LIGHT_AMBER_B).toBe(0.437);
    expect(WARM_LIGHT_AMBER_G_GAIN).toBe(0.092);
    expect(WARM_LIGHT_AMBER_G).toBe(0.759);
    expect(WARM_LIGHT_DISTANCE_GAIN).toBe(2.875);
    expect(WARM_LIGHT_DISTANCE_BASE).toBe(7.475);
    expect(WARM_LIGHT_Y).toBe(1.7825);
    expect(WARM_LIGHT_INTENSITY_MUL).toBe(1.7825);
    expect(FLASHLIGHT_SPOT_DISTANCE_GAIN).toBe(2.3);
    expect(FLASHLIGHT_SPOT_DISTANCE_EXTRA).toBe(1.84);
    expect(FLASHLIGHT_SPOT_Y).toBe(1.7825);
    expect(FLASHLIGHT_FILL_Y).toBe(1.5525);
    expect(FLASHLIGHT_FILL_DISTANCE_GAIN).toBe(4.025);
    expect(FLASHLIGHT_FILL_DISTANCE_BASE).toBe(8.05);
    expect(FLASHLIGHT_FILL_COLOR).toBe(0xcaefff);
    expect(MUZZLE_LIGHT_COLOR).toBe(0xffffb8);
    expect(IMPACT_SPARK_LIGHT_DECAY).toBe(1.74);
    const src = readFileSync(
      resolve(process.cwd(), "src/render/worldView.ts"),
      "utf8",
    );
    expect(src).toContain("export const MUZZLE_LIGHT_DECAY = 1.74");
    expect(src).toContain("MUZZLE_LIGHT_DISTANCE,\n    MUZZLE_LIGHT_DECAY,");
    expect(src).not.toMatch(/MUZZLE_LIGHT_DISTANCE,\s*\n\s*2,/);
    expect(src).not.toMatch(/export const MUZZLE_LIGHT_DECAY = 2/);
    expect(src).toContain("const MUZZLE_LIGHT_DISTANCE = 2.6");
    expect(src).toContain("export const MUZZLE_LIGHT_COLOR = 0xffffb8");
    expect(src).toContain("new THREE.PointLight(\n    MUZZLE_LIGHT_COLOR,");
    expect(src).toContain("export const FLASHLIGHT_SPOT_DECAY = 1.74");
    expect(src).toContain("FLASHLIGHT_SPOT_PENUMBRA,\n    FLASHLIGHT_SPOT_DECAY,");
    expect(src).not.toMatch(/FLASHLIGHT_SPOT_PENUMBRA,\s*\n\s*2,/);
    expect(src).not.toMatch(/export const FLASHLIGHT_SPOT_DECAY = 2/);
    expect(src).toContain("FLASHLIGHT_CONE_LENGTH + 2.4");
    expect(src).toContain("export const FLASHLIGHT_FILL_DECAY = 1.74");
    expect(src).toContain("new THREE.PointLight(FLASHLIGHT_FILL_COLOR, 0, 10, FLASHLIGHT_FILL_DECAY)");
    expect(src).not.toMatch(/new THREE\.PointLight\(FLASHLIGHT_FILL_COLOR, 0, 10, 2\)/);
    expect(src).not.toMatch(/export const FLASHLIGHT_FILL_DECAY = 2/);
    expect(src).toContain("export const WARM_LIGHT_DECAY = 1.74");
    expect(src).toContain("new THREE.PointLight(WARM_LIGHT_COLOR, 0, 7.5, WARM_LIGHT_DECAY)");
    expect(src).not.toMatch(/new THREE\.PointLight\(WARM_LIGHT_COLOR, 0, 7\.5, 2\)/);
    expect(src).toContain("export const WARM_LIGHT_VISIBLE_EPS = 0.0174");
    expect(src).toContain("warmLight.visible = i > WARM_LIGHT_VISIBLE_EPS");
    expect(src).not.toMatch(/warmLight\.visible = i > 0\.02/);
    expect(src).not.toMatch(/export const WARM_LIGHT_VISIBLE_EPS = 0\.02/);
    expect(src).toContain("export const WARM_LIGHT_AMBER_B_GAIN = 0.115");
    expect(src).toContain("export const WARM_LIGHT_AMBER_B = 0.437");
    expect(src).toContain("export const WARM_LIGHT_AMBER_G_GAIN = 0.092");
    expect(src).toContain("export const WARM_LIGHT_AMBER_G = 0.759");
    expect(src).toContain(
      "warmLight.color.setRGB(1, WARM_LIGHT_AMBER_G + i * WARM_LIGHT_AMBER_G_GAIN, WARM_LIGHT_AMBER_B + i * WARM_LIGHT_AMBER_B_GAIN)",
    );
    expect(src).not.toMatch(/WARM_LIGHT_AMBER_B \+ i \* 0\.1/);
    expect(src).toContain("export const WARM_LIGHT_DISTANCE_GAIN = 2.875");
    expect(src).toContain("export const WARM_LIGHT_DISTANCE_BASE = 7.475");
    expect(src).toContain(
      "warmLight.distance = WARM_LIGHT_DISTANCE_BASE + i * WARM_LIGHT_DISTANCE_GAIN",
    );
    expect(src).not.toMatch(/export const WARM_LIGHT_DISTANCE_GAIN = 2\.5/);
    expect(src).not.toMatch(
      /warmLight\.distance = WARM_LIGHT_DISTANCE_BASE \+ i \* 2\.5/,
    );
    expect(src).not.toMatch(/warmLight\.distance = 6\.5 \+ i \* 2\.5/);
    expect(src).toContain("export const WARM_LIGHT_Y = 1.7825");
    expect(src).toContain("warmLight.position.set(wx, WARM_LIGHT_Y, wy)");
    expect(src).not.toMatch(/export const WARM_LIGHT_Y = 1\.55/);
    expect(src).not.toMatch(/warmLight\.position\.set\(wx, 1\.55, wy\)/);
    expect(src).toContain("warmLight.position.set(0, 1.6, 0)");
    expect(src).toContain("export const WARM_LIGHT_INTENSITY_MUL = 1.7825");
    expect(src).toContain("warmLight.intensity = i * WARM_LIGHT_INTENSITY_MUL");
    expect(src).not.toMatch(/export const WARM_LIGHT_INTENSITY_MUL = 1\.55/);
    expect(src).not.toMatch(/warmLight\.intensity = i \* 1\.55/);
    expect(src).toContain("export const FLASHLIGHT_SPOT_DISTANCE_GAIN = 2.3");
    expect(src).toContain("export const FLASHLIGHT_SPOT_DISTANCE_EXTRA = 1.84");
    expect(src).toContain(
      "torchSpot.distance = FLASHLIGHT_CONE_LENGTH + FLASHLIGHT_SPOT_DISTANCE_EXTRA + i * FLASHLIGHT_SPOT_DISTANCE_GAIN",
    );
    expect(src).toContain("export const FLASHLIGHT_SPOT_Y = 1.7825");
    expect(src).toContain("torchSpot.position.set(0, FLASHLIGHT_SPOT_Y, 0)");
    expect(src).toContain("torchSpot.position.set(wx, FLASHLIGHT_SPOT_Y, wy)");
    expect(src).toContain("export const FLASHLIGHT_FILL_Y = 1.5525");
    expect(src).toContain("torchLight.position.set(0, FLASHLIGHT_FILL_Y, 0)");
    expect(src).toContain("torchLight.position.set(wx, FLASHLIGHT_FILL_Y, wy)");
    expect(src).toContain("export const FLASHLIGHT_FILL_DISTANCE_GAIN = 4.025");
    expect(src).toContain("export const FLASHLIGHT_FILL_DISTANCE_BASE = 8.05");
    expect(src).toContain(
      "torchLight.distance = FLASHLIGHT_FILL_DISTANCE_BASE + i * FLASHLIGHT_FILL_DISTANCE_GAIN",
    );
    expect(src).toContain("export const FLASHLIGHT_FILL_COLOR = 0xcaefff");
    expect(src).toContain("torchLight.color.setHex(FLASHLIGHT_FILL_COLOR)");
    expect(src).toContain("export const TRACER_FLASH_COLOR = 0xffdd6e");
    expect(src).toContain("new THREE.PointLight(TRACER_FLASH_COLOR, TRACER_FLASH_INTENSITY, TRACER_FLASH_DISTANCE, TRACER_FLASH_DECAY)");
    expect(src).toContain("export const TRACER_COLOR = 0xffffb8");
    expect(src).toContain("color: TRACER_COLOR");
    expect(src).toContain("export const IMPACT_SPARK_LIGHT_COLOR = 0xffef93");
    expect(src).toContain("new THREE.PointLight(\n    IMPACT_SPARK_LIGHT_COLOR,");
    expect(src).toContain("export const IMPACT_SPARK_LIGHT_DECAY = 1.74");
    expect(src).toContain("IMPACT_SPARK_LIGHT_DISTANCE,\n    IMPACT_SPARK_LIGHT_DECAY,");
    expect(src).not.toMatch(/IMPACT_SPARK_LIGHT_DISTANCE,\s*\n\s*2,/);
    expect(src).not.toMatch(/export const IMPACT_SPARK_LIGHT_DECAY = 2/);
    expect(src).toContain("const IMPACT_SPARK_LIGHT_DISTANCE = 1.8");
    expect(src).toContain("export const IMPACT_SPARK_COLOR = 0xffef93");
    expect(src).toContain("color: IMPACT_SPARK_COLOR");
    expect(src).toContain("export const MUZZLE_FLASH_COLOR = 0xffffdd");
    expect(src).toContain("color: MUZZLE_FLASH_COLOR");
    expect(src).toContain("export const WARM_LIGHT_COLOR = 0xffca81");
    expect(src).toContain("export const POSSESSED_HEAD_EMISSIVE = 0x30124a");
    expect(src).toContain("emissive: POSSESSED_HEAD_EMISSIVE");
    expect(src).toContain("color: POSSESSED_HEAD_COLOR");
    expect(src).toContain("export const POSSESSED_HEAD_COLOR = 0x8c469f");
    expect(src).toContain("emissive: POSSESSED_EMISSIVE");
    expect(src).toContain("color: POSSESSED_COLOR");
    expect(src).toContain("const POSSESSED_EMISSIVE = 0x1e0925");
    expect(src).toContain("emissiveIntensity: 0.22");
    expect(src).toContain("color: HOSTILE_COLOR");
    expect(src).toContain("color: PLAYER_COLOR");
    expect(src).toContain("color: PLAYER_HEAD_COLOR");
    expect(src).toContain("emissive: PLAYER_HEAD_EMISSIVE");
    expect(TRACER_FLASH_COLOR).toBe(0xffdd6e);
    expect(TRACER_COLOR).toBe(0xffffb8);
    expect(IMPACT_SPARK_LIGHT_COLOR).toBe(0xffef93);
    expect(IMPACT_SPARK_LIGHT_DECAY).toBe(1.74);
    expect(IMPACT_SPARK_COLOR).toBe(0xffef93);
    expect(MUZZLE_LIGHT_COLOR).toBe(0xffffb8);
    expect(MUZZLE_FLASH_COLOR).toBe(0xffffdd);
    expect(WARM_LIGHT_COLOR).toBe(0xffca81);
    expect(WARM_LIGHT_DECAY).toBe(1.74);
    expect(WARM_LIGHT_VISIBLE_EPS).toBe(0.0174);
    expect(WARM_LIGHT_AMBER_B_GAIN).toBe(0.115);
    expect(WARM_LIGHT_AMBER_B).toBe(0.437);
    expect(WARM_LIGHT_AMBER_G_GAIN).toBe(0.092);
    expect(WARM_LIGHT_AMBER_G).toBe(0.759);
    expect(WARM_LIGHT_DISTANCE_GAIN).toBe(2.875);
    expect(WARM_LIGHT_DISTANCE_BASE).toBe(7.475);
    expect(WARM_LIGHT_Y).toBe(1.7825);
    expect(WARM_LIGHT_INTENSITY_MUL).toBe(1.7825);
    expect(POSSESSED_HEAD_EMISSIVE).toBe(0x30124a);
    expect(POSSESSED_HEAD_COLOR).toBe(0x8c469f);
    expect(POSSESSED_COLOR).toBe(0x68347b);
    expect(HOSTILE_COLOR).toBe(0x7b1e1e);
    expect(PLAYER_COLOR).toBe(0x55a4f4);
    expect(PLAYER_HEAD_COLOR).toBe(0x91d1ff);
    expect(PLAYER_HEAD_EMISSIVE).toBe(0x122537);
    expect(MARKER_PALETTE.possessed.emissive).toBe(0x30124a);
    expect(MARKER_PALETTE.player.badge).toBe(0x91d1ff);
    expect(MARKER_PALETTE.player.emissive).toBe(0x1e4a6e);
    expect(MARKER_PALETTE.player.ring).toBe(0x4392f4);
    expect(MARKER_RING_OPACITY).toBe(0.6877);
    expect(muteBadgeY).toBe(2.3);
    expect(possessedBadgeY).toBe(2.3);
  });
});

describe("worldView impact spark light decay", () => {
  test("impact spark light decay 2 × 0.87 → 1.74; muzzle-decay/spot-decay/fill-decay/warm-decay/visible-eps/amber-B-gain/amber-B/amber-G-gain/amber-G/gain/base/Y/intensity/color/spot-gain/extra/spot-Y/fill-Y/gain/base/flash/mesh/color/distance/possessed/hostile/player/palette/badge-Y iguales", () => {
    expect(IMPACT_SPARK_LIGHT_DECAY).toBe(1.74);
    expect(IMPACT_SPARK_LIGHT_DECAY).toBeCloseTo(2 * 0.87, 10);
    expect(MUZZLE_LIGHT_DECAY).toBe(1.74);
    expect(FLASHLIGHT_SPOT_DECAY).toBe(1.74);
    expect(FLASHLIGHT_FILL_DECAY).toBe(1.74);
    expect(WARM_LIGHT_DECAY).toBe(1.74);
    expect(WARM_LIGHT_VISIBLE_EPS).toBe(0.0174);
    expect(WARM_LIGHT_AMBER_B_GAIN).toBe(0.115);
    expect(WARM_LIGHT_AMBER_B).toBe(0.437);
    expect(WARM_LIGHT_AMBER_G_GAIN).toBe(0.092);
    expect(WARM_LIGHT_AMBER_G).toBe(0.759);
    expect(WARM_LIGHT_DISTANCE_GAIN).toBe(2.875);
    expect(WARM_LIGHT_DISTANCE_BASE).toBe(7.475);
    expect(WARM_LIGHT_Y).toBe(1.7825);
    expect(WARM_LIGHT_INTENSITY_MUL).toBe(1.7825);
    expect(FLASHLIGHT_SPOT_DISTANCE_GAIN).toBe(2.3);
    expect(FLASHLIGHT_SPOT_DISTANCE_EXTRA).toBe(1.84);
    expect(FLASHLIGHT_SPOT_Y).toBe(1.7825);
    expect(FLASHLIGHT_FILL_Y).toBe(1.5525);
    expect(FLASHLIGHT_FILL_DISTANCE_GAIN).toBe(4.025);
    expect(FLASHLIGHT_FILL_DISTANCE_BASE).toBe(8.05);
    expect(FLASHLIGHT_FILL_COLOR).toBe(0xcaefff);
    expect(MUZZLE_LIGHT_COLOR).toBe(0xffffb8);
    expect(IMPACT_SPARK_LIGHT_COLOR).toBe(0xffef93);
    expect(TRACER_FLASH_DECAY).toBe(1.74);
    expect(TRACER_FLASH_INTENSITY).toBe(2.76);
    expect(TRACER_FLASH_DISTANCE).toBe(3.68);
    const src = readFileSync(
      resolve(process.cwd(), "src/render/worldView.ts"),
      "utf8",
    );
    expect(src).toContain("export const IMPACT_SPARK_LIGHT_DECAY = 1.74");
    expect(src).toContain("IMPACT_SPARK_LIGHT_DISTANCE,\n    IMPACT_SPARK_LIGHT_DECAY,");
    expect(src).not.toMatch(/IMPACT_SPARK_LIGHT_DISTANCE,\s*\n\s*2,/);
    expect(src).not.toMatch(/export const IMPACT_SPARK_LIGHT_DECAY = 2/);
    expect(src).toContain("const IMPACT_SPARK_LIGHT_DISTANCE = 1.8");
    expect(src).toContain("export const IMPACT_SPARK_LIGHT_COLOR = 0xffef93");
    expect(src).toContain("new THREE.PointLight(\n    IMPACT_SPARK_LIGHT_COLOR,");
    expect(src).toContain("export const MUZZLE_LIGHT_DECAY = 1.74");
    expect(src).toContain("MUZZLE_LIGHT_DISTANCE,\n    MUZZLE_LIGHT_DECAY,");
    expect(src).not.toMatch(/MUZZLE_LIGHT_DISTANCE,\s*\n\s*2,/);
    expect(src).not.toMatch(/export const MUZZLE_LIGHT_DECAY = 2/);
    expect(src).toContain("const MUZZLE_LIGHT_DISTANCE = 2.6");
    expect(src).toContain("export const MUZZLE_LIGHT_COLOR = 0xffffb8");
    expect(src).toContain("new THREE.PointLight(\n    MUZZLE_LIGHT_COLOR,");
    expect(src).toContain("export const FLASHLIGHT_SPOT_DECAY = 1.74");
    expect(src).toContain("FLASHLIGHT_SPOT_PENUMBRA,\n    FLASHLIGHT_SPOT_DECAY,");
    expect(src).not.toMatch(/FLASHLIGHT_SPOT_PENUMBRA,\s*\n\s*2,/);
    expect(src).not.toMatch(/export const FLASHLIGHT_SPOT_DECAY = 2/);
    expect(src).toContain("FLASHLIGHT_CONE_LENGTH + 2.4");
    expect(src).toContain("export const FLASHLIGHT_FILL_DECAY = 1.74");
    expect(src).toContain("new THREE.PointLight(FLASHLIGHT_FILL_COLOR, 0, 10, FLASHLIGHT_FILL_DECAY)");
    expect(src).not.toMatch(/new THREE\.PointLight\(FLASHLIGHT_FILL_COLOR, 0, 10, 2\)/);
    expect(src).not.toMatch(/export const FLASHLIGHT_FILL_DECAY = 2/);
    expect(src).toContain("export const WARM_LIGHT_DECAY = 1.74");
    expect(src).toContain("new THREE.PointLight(WARM_LIGHT_COLOR, 0, 7.5, WARM_LIGHT_DECAY)");
    expect(src).not.toMatch(/new THREE\.PointLight\(WARM_LIGHT_COLOR, 0, 7\.5, 2\)/);
    expect(src).toContain("export const WARM_LIGHT_VISIBLE_EPS = 0.0174");
    expect(src).toContain("warmLight.visible = i > WARM_LIGHT_VISIBLE_EPS");
    expect(src).not.toMatch(/warmLight\.visible = i > 0\.02/);
    expect(src).not.toMatch(/export const WARM_LIGHT_VISIBLE_EPS = 0\.02/);
    expect(src).toContain("export const WARM_LIGHT_AMBER_B_GAIN = 0.115");
    expect(src).toContain("export const WARM_LIGHT_AMBER_B = 0.437");
    expect(src).toContain("export const WARM_LIGHT_AMBER_G_GAIN = 0.092");
    expect(src).toContain("export const WARM_LIGHT_AMBER_G = 0.759");
    expect(src).toContain(
      "warmLight.color.setRGB(1, WARM_LIGHT_AMBER_G + i * WARM_LIGHT_AMBER_G_GAIN, WARM_LIGHT_AMBER_B + i * WARM_LIGHT_AMBER_B_GAIN)",
    );
    expect(src).not.toMatch(/WARM_LIGHT_AMBER_B \+ i \* 0\.1/);
    expect(src).toContain("export const WARM_LIGHT_DISTANCE_GAIN = 2.875");
    expect(src).toContain("export const WARM_LIGHT_DISTANCE_BASE = 7.475");
    expect(src).toContain(
      "warmLight.distance = WARM_LIGHT_DISTANCE_BASE + i * WARM_LIGHT_DISTANCE_GAIN",
    );
    expect(src).not.toMatch(/export const WARM_LIGHT_DISTANCE_GAIN = 2\.5/);
    expect(src).not.toMatch(
      /warmLight\.distance = WARM_LIGHT_DISTANCE_BASE \+ i \* 2\.5/,
    );
    expect(src).not.toMatch(/warmLight\.distance = 6\.5 \+ i \* 2\.5/);
    expect(src).toContain("export const WARM_LIGHT_Y = 1.7825");
    expect(src).toContain("warmLight.position.set(wx, WARM_LIGHT_Y, wy)");
    expect(src).not.toMatch(/export const WARM_LIGHT_Y = 1\.55/);
    expect(src).not.toMatch(/warmLight\.position\.set\(wx, 1\.55, wy\)/);
    expect(src).toContain("warmLight.position.set(0, 1.6, 0)");
    expect(src).toContain("export const WARM_LIGHT_INTENSITY_MUL = 1.7825");
    expect(src).toContain("warmLight.intensity = i * WARM_LIGHT_INTENSITY_MUL");
    expect(src).not.toMatch(/export const WARM_LIGHT_INTENSITY_MUL = 1\.55/);
    expect(src).not.toMatch(/warmLight\.intensity = i \* 1\.55/);
    expect(src).toContain("export const FLASHLIGHT_SPOT_DISTANCE_GAIN = 2.3");
    expect(src).toContain("export const FLASHLIGHT_SPOT_DISTANCE_EXTRA = 1.84");
    expect(src).toContain(
      "torchSpot.distance = FLASHLIGHT_CONE_LENGTH + FLASHLIGHT_SPOT_DISTANCE_EXTRA + i * FLASHLIGHT_SPOT_DISTANCE_GAIN",
    );
    expect(src).toContain("export const FLASHLIGHT_SPOT_Y = 1.7825");
    expect(src).toContain("torchSpot.position.set(0, FLASHLIGHT_SPOT_Y, 0)");
    expect(src).toContain("torchSpot.position.set(wx, FLASHLIGHT_SPOT_Y, wy)");
    expect(src).toContain("export const FLASHLIGHT_FILL_Y = 1.5525");
    expect(src).toContain("torchLight.position.set(0, FLASHLIGHT_FILL_Y, 0)");
    expect(src).toContain("torchLight.position.set(wx, FLASHLIGHT_FILL_Y, wy)");
    expect(src).toContain("export const FLASHLIGHT_FILL_DISTANCE_GAIN = 4.025");
    expect(src).toContain("export const FLASHLIGHT_FILL_DISTANCE_BASE = 8.05");
    expect(src).toContain(
      "torchLight.distance = FLASHLIGHT_FILL_DISTANCE_BASE + i * FLASHLIGHT_FILL_DISTANCE_GAIN",
    );
    expect(src).toContain("export const FLASHLIGHT_FILL_COLOR = 0xcaefff");
    expect(src).toContain("torchLight.color.setHex(FLASHLIGHT_FILL_COLOR)");
    expect(src).toContain("export const TRACER_FLASH_COLOR = 0xffdd6e");
    expect(src).toContain("new THREE.PointLight(TRACER_FLASH_COLOR, TRACER_FLASH_INTENSITY, TRACER_FLASH_DISTANCE, TRACER_FLASH_DECAY)");
    expect(src).toContain("export const TRACER_FLASH_INTENSITY = 2.76");
    expect(src).toContain("export const TRACER_FLASH_DISTANCE = 3.68");
    expect(src).toContain("export const TRACER_FLASH_DECAY = 1.74");
    expect(src).not.toMatch(/new THREE\.PointLight\(TRACER_FLASH_COLOR, TRACER_FLASH_INTENSITY, 3\.2, TRACER_FLASH_DECAY\)/);
    expect(src).not.toMatch(/new THREE\.PointLight\(TRACER_FLASH_COLOR, 2\.4, 3\.2, TRACER_FLASH_DECAY\)/);
    expect(src).not.toMatch(/new THREE\.PointLight\(TRACER_FLASH_COLOR, 2\.4, 3\.2, 2\)/);
    expect(src).not.toMatch(/export const TRACER_FLASH_INTENSITY = 2\.4/);
    expect(src).not.toMatch(/export const TRACER_FLASH_DISTANCE = 3\.2/);
    expect(src).not.toMatch(/export const TRACER_FLASH_DECAY = 2/);
    expect(src).toContain("export const TRACER_COLOR = 0xffffb8");
    expect(src).toContain("color: TRACER_COLOR");
    expect(src).toContain("export const IMPACT_SPARK_COLOR = 0xffef93");
    expect(src).toContain("color: IMPACT_SPARK_COLOR");
    expect(src).toContain("export const MUZZLE_FLASH_COLOR = 0xffffdd");
    expect(src).toContain("color: MUZZLE_FLASH_COLOR");
    expect(src).toContain("export const WARM_LIGHT_COLOR = 0xffca81");
    expect(src).toContain("export const POSSESSED_HEAD_EMISSIVE = 0x30124a");
    expect(src).toContain("emissive: POSSESSED_HEAD_EMISSIVE");
    expect(src).toContain("color: POSSESSED_HEAD_COLOR");
    expect(src).toContain("export const POSSESSED_HEAD_COLOR = 0x8c469f");
    expect(src).toContain("emissive: POSSESSED_EMISSIVE");
    expect(src).toContain("color: POSSESSED_COLOR");
    expect(src).toContain("const POSSESSED_EMISSIVE = 0x1e0925");
    expect(src).toContain("emissiveIntensity: 0.22");
    expect(src).toContain("color: HOSTILE_COLOR");
    expect(src).toContain("color: PLAYER_COLOR");
    expect(src).toContain("color: PLAYER_HEAD_COLOR");
    expect(src).toContain("emissive: PLAYER_HEAD_EMISSIVE");
    expect(TRACER_FLASH_COLOR).toBe(0xffdd6e);
    expect(TRACER_FLASH_INTENSITY).toBe(2.76);
    expect(TRACER_FLASH_DISTANCE).toBe(3.68);
    expect(TRACER_FLASH_DECAY).toBe(1.74);
    expect(TRACER_COLOR).toBe(0xffffb8);
    expect(IMPACT_SPARK_LIGHT_COLOR).toBe(0xffef93);
    expect(IMPACT_SPARK_COLOR).toBe(0xffef93);
    expect(MUZZLE_LIGHT_COLOR).toBe(0xffffb8);
    expect(MUZZLE_LIGHT_DECAY).toBe(1.74);
    expect(MUZZLE_FLASH_COLOR).toBe(0xffffdd);
    expect(WARM_LIGHT_COLOR).toBe(0xffca81);
    expect(WARM_LIGHT_DECAY).toBe(1.74);
    expect(WARM_LIGHT_VISIBLE_EPS).toBe(0.0174);
    expect(WARM_LIGHT_AMBER_B_GAIN).toBe(0.115);
    expect(WARM_LIGHT_AMBER_B).toBe(0.437);
    expect(WARM_LIGHT_AMBER_G_GAIN).toBe(0.092);
    expect(WARM_LIGHT_AMBER_G).toBe(0.759);
    expect(WARM_LIGHT_DISTANCE_GAIN).toBe(2.875);
    expect(WARM_LIGHT_DISTANCE_BASE).toBe(7.475);
    expect(WARM_LIGHT_Y).toBe(1.7825);
    expect(WARM_LIGHT_INTENSITY_MUL).toBe(1.7825);
    expect(POSSESSED_HEAD_EMISSIVE).toBe(0x30124a);
    expect(POSSESSED_HEAD_COLOR).toBe(0x8c469f);
    expect(POSSESSED_COLOR).toBe(0x68347b);
    expect(HOSTILE_COLOR).toBe(0x7b1e1e);
    expect(PLAYER_COLOR).toBe(0x55a4f4);
    expect(PLAYER_HEAD_COLOR).toBe(0x91d1ff);
    expect(PLAYER_HEAD_EMISSIVE).toBe(0x122537);
    expect(MARKER_PALETTE.possessed.emissive).toBe(0x30124a);
    expect(MARKER_PALETTE.player.badge).toBe(0x91d1ff);
    expect(MARKER_PALETTE.player.emissive).toBe(0x1e4a6e);
    expect(MARKER_PALETTE.player.ring).toBe(0x4392f4);
    expect(MARKER_RING_OPACITY).toBe(0.6877);
    expect(muteBadgeY).toBe(2.3);
    expect(possessedBadgeY).toBe(2.3);
  });
});

describe("worldView tracer flash decay", () => {
  test("tracer flash decay 2 × 0.87 → 1.74; impact-spark-decay/muzzle-decay/spot-decay/fill-decay/warm-decay/visible-eps/amber-B-gain/amber-B/amber-G-gain/amber-G/gain/base/Y/intensity/color/spot-gain/extra/spot-Y/fill-Y/gain/base/flash/mesh/color/distance/possessed/hostile/player/palette/badge-Y iguales", () => {
    expect(TRACER_FLASH_DECAY).toBe(1.74);
    expect(TRACER_FLASH_DECAY).toBeCloseTo(2 * 0.87, 10);
    expect(IMPACT_SPARK_LIGHT_DECAY).toBe(1.74);
    expect(MUZZLE_LIGHT_DECAY).toBe(1.74);
    expect(FLASHLIGHT_SPOT_DECAY).toBe(1.74);
    expect(FLASHLIGHT_FILL_DECAY).toBe(1.74);
    expect(WARM_LIGHT_DECAY).toBe(1.74);
    expect(WARM_LIGHT_VISIBLE_EPS).toBe(0.0174);
    expect(WARM_LIGHT_AMBER_B_GAIN).toBe(0.115);
    expect(WARM_LIGHT_AMBER_B).toBe(0.437);
    expect(WARM_LIGHT_AMBER_G_GAIN).toBe(0.092);
    expect(WARM_LIGHT_AMBER_G).toBe(0.759);
    expect(WARM_LIGHT_DISTANCE_GAIN).toBe(2.875);
    expect(WARM_LIGHT_DISTANCE_BASE).toBe(7.475);
    expect(WARM_LIGHT_Y).toBe(1.7825);
    expect(WARM_LIGHT_INTENSITY_MUL).toBe(1.7825);
    expect(FLASHLIGHT_SPOT_DISTANCE_GAIN).toBe(2.3);
    expect(FLASHLIGHT_SPOT_DISTANCE_EXTRA).toBe(1.84);
    expect(FLASHLIGHT_SPOT_Y).toBe(1.7825);
    expect(FLASHLIGHT_FILL_Y).toBe(1.5525);
    expect(FLASHLIGHT_FILL_DISTANCE_GAIN).toBe(4.025);
    expect(FLASHLIGHT_FILL_DISTANCE_BASE).toBe(8.05);
    expect(FLASHLIGHT_FILL_COLOR).toBe(0xcaefff);
    expect(MUZZLE_LIGHT_COLOR).toBe(0xffffb8);
    expect(IMPACT_SPARK_LIGHT_COLOR).toBe(0xffef93);
    expect(TRACER_FLASH_COLOR).toBe(0xffdd6e);
    expect(TRACER_FLASH_INTENSITY).toBe(2.76);
    expect(TRACER_FLASH_DISTANCE).toBe(3.68);
    const src = readFileSync(
      resolve(process.cwd(), "src/render/worldView.ts"),
      "utf8",
    );
    expect(src).toContain("export const TRACER_FLASH_DECAY = 1.74");
    expect(src).toContain("export const TRACER_FLASH_INTENSITY = 2.76");
    expect(src).toContain("export const TRACER_FLASH_DISTANCE = 3.68");
    expect(src).toContain("new THREE.PointLight(TRACER_FLASH_COLOR, TRACER_FLASH_INTENSITY, TRACER_FLASH_DISTANCE, TRACER_FLASH_DECAY)");
    expect(src).not.toMatch(/new THREE\.PointLight\(TRACER_FLASH_COLOR, TRACER_FLASH_INTENSITY, 3\.2, TRACER_FLASH_DECAY\)/);
    expect(src).not.toMatch(/new THREE\.PointLight\(TRACER_FLASH_COLOR, 2\.4, 3\.2, TRACER_FLASH_DECAY\)/);
    expect(src).not.toMatch(/new THREE\.PointLight\(TRACER_FLASH_COLOR, 2\.4, 3\.2, 2\)/);
    expect(src).not.toMatch(/export const TRACER_FLASH_INTENSITY = 2\.4/);
    expect(src).not.toMatch(/export const TRACER_FLASH_DISTANCE = 3\.2/);
    expect(src).not.toMatch(/export const TRACER_FLASH_DECAY = 2/);
    expect(src).toContain("t.flash.intensity = TRACER_FLASH_INTENSITY * op");
    expect(src).not.toMatch(/t\.flash\.intensity = 2\.4 \* op/);
    expect(src).toContain("export const TRACER_FLASH_COLOR = 0xffdd6e");
    expect(src).toContain("export const IMPACT_SPARK_LIGHT_DECAY = 1.74");
    expect(src).toContain("IMPACT_SPARK_LIGHT_DISTANCE,\n    IMPACT_SPARK_LIGHT_DECAY,");
    expect(src).not.toMatch(/IMPACT_SPARK_LIGHT_DISTANCE,\s*\n\s*2,/);
    expect(src).not.toMatch(/export const IMPACT_SPARK_LIGHT_DECAY = 2/);
    expect(src).toContain("const IMPACT_SPARK_LIGHT_DISTANCE = 1.8");
    expect(src).toContain("export const IMPACT_SPARK_LIGHT_COLOR = 0xffef93");
    expect(src).toContain("new THREE.PointLight(\n    IMPACT_SPARK_LIGHT_COLOR,");
    expect(src).toContain("export const MUZZLE_LIGHT_DECAY = 1.74");
    expect(src).toContain("MUZZLE_LIGHT_DISTANCE,\n    MUZZLE_LIGHT_DECAY,");
    expect(src).not.toMatch(/MUZZLE_LIGHT_DISTANCE,\s*\n\s*2,/);
    expect(src).not.toMatch(/export const MUZZLE_LIGHT_DECAY = 2/);
    expect(src).toContain("const MUZZLE_LIGHT_DISTANCE = 2.6");
    expect(src).toContain("export const MUZZLE_LIGHT_COLOR = 0xffffb8");
    expect(src).toContain("new THREE.PointLight(\n    MUZZLE_LIGHT_COLOR,");
    expect(src).toContain("export const FLASHLIGHT_SPOT_DECAY = 1.74");
    expect(src).toContain("FLASHLIGHT_SPOT_PENUMBRA,\n    FLASHLIGHT_SPOT_DECAY,");
    expect(src).not.toMatch(/FLASHLIGHT_SPOT_PENUMBRA,\s*\n\s*2,/);
    expect(src).not.toMatch(/export const FLASHLIGHT_SPOT_DECAY = 2/);
    expect(src).toContain("FLASHLIGHT_CONE_LENGTH + 2.4");
    expect(src).toContain("export const FLASHLIGHT_FILL_DECAY = 1.74");
    expect(src).toContain("new THREE.PointLight(FLASHLIGHT_FILL_COLOR, 0, 10, FLASHLIGHT_FILL_DECAY)");
    expect(src).not.toMatch(/new THREE\.PointLight\(FLASHLIGHT_FILL_COLOR, 0, 10, 2\)/);
    expect(src).not.toMatch(/export const FLASHLIGHT_FILL_DECAY = 2/);
    expect(src).toContain("export const WARM_LIGHT_DECAY = 1.74");
    expect(src).toContain("new THREE.PointLight(WARM_LIGHT_COLOR, 0, 7.5, WARM_LIGHT_DECAY)");
    expect(src).not.toMatch(/new THREE\.PointLight\(WARM_LIGHT_COLOR, 0, 7\.5, 2\)/);
    expect(src).toContain("export const WARM_LIGHT_VISIBLE_EPS = 0.0174");
    expect(src).toContain("warmLight.visible = i > WARM_LIGHT_VISIBLE_EPS");
    expect(src).not.toMatch(/warmLight\.visible = i > 0\.02/);
    expect(src).not.toMatch(/export const WARM_LIGHT_VISIBLE_EPS = 0\.02/);
    expect(src).toContain("export const WARM_LIGHT_AMBER_B_GAIN = 0.115");
    expect(src).toContain("export const WARM_LIGHT_AMBER_B = 0.437");
    expect(src).toContain("export const WARM_LIGHT_AMBER_G_GAIN = 0.092");
    expect(src).toContain("export const WARM_LIGHT_AMBER_G = 0.759");
    expect(src).toContain(
      "warmLight.color.setRGB(1, WARM_LIGHT_AMBER_G + i * WARM_LIGHT_AMBER_G_GAIN, WARM_LIGHT_AMBER_B + i * WARM_LIGHT_AMBER_B_GAIN)",
    );
    expect(src).not.toMatch(/WARM_LIGHT_AMBER_B \+ i \* 0\.1/);
    expect(src).toContain("export const WARM_LIGHT_DISTANCE_GAIN = 2.875");
    expect(src).toContain("export const WARM_LIGHT_DISTANCE_BASE = 7.475");
    expect(src).toContain(
      "warmLight.distance = WARM_LIGHT_DISTANCE_BASE + i * WARM_LIGHT_DISTANCE_GAIN",
    );
    expect(src).not.toMatch(/export const WARM_LIGHT_DISTANCE_GAIN = 2\.5/);
    expect(src).not.toMatch(
      /warmLight\.distance = WARM_LIGHT_DISTANCE_BASE \+ i \* 2\.5/,
    );
    expect(src).not.toMatch(/warmLight\.distance = 6\.5 \+ i \* 2\.5/);
    expect(src).toContain("export const WARM_LIGHT_Y = 1.7825");
    expect(src).toContain("warmLight.position.set(wx, WARM_LIGHT_Y, wy)");
    expect(src).not.toMatch(/export const WARM_LIGHT_Y = 1\.55/);
    expect(src).not.toMatch(/warmLight\.position\.set\(wx, 1\.55, wy\)/);
    expect(src).toContain("warmLight.position.set(0, 1.6, 0)");
    expect(src).toContain("export const WARM_LIGHT_INTENSITY_MUL = 1.7825");
    expect(src).toContain("warmLight.intensity = i * WARM_LIGHT_INTENSITY_MUL");
    expect(src).not.toMatch(/export const WARM_LIGHT_INTENSITY_MUL = 1\.55/);
    expect(src).not.toMatch(/warmLight\.intensity = i \* 1\.55/);
    expect(src).toContain("export const FLASHLIGHT_SPOT_DISTANCE_GAIN = 2.3");
    expect(src).toContain("export const FLASHLIGHT_SPOT_DISTANCE_EXTRA = 1.84");
    expect(src).toContain(
      "torchSpot.distance = FLASHLIGHT_CONE_LENGTH + FLASHLIGHT_SPOT_DISTANCE_EXTRA + i * FLASHLIGHT_SPOT_DISTANCE_GAIN",
    );
    expect(src).toContain("export const FLASHLIGHT_SPOT_Y = 1.7825");
    expect(src).toContain("torchSpot.position.set(0, FLASHLIGHT_SPOT_Y, 0)");
    expect(src).toContain("torchSpot.position.set(wx, FLASHLIGHT_SPOT_Y, wy)");
    expect(src).toContain("export const FLASHLIGHT_FILL_Y = 1.5525");
    expect(src).toContain("torchLight.position.set(0, FLASHLIGHT_FILL_Y, 0)");
    expect(src).toContain("torchLight.position.set(wx, FLASHLIGHT_FILL_Y, wy)");
    expect(src).toContain("export const FLASHLIGHT_FILL_DISTANCE_GAIN = 4.025");
    expect(src).toContain("export const FLASHLIGHT_FILL_DISTANCE_BASE = 8.05");
    expect(src).toContain(
      "torchLight.distance = FLASHLIGHT_FILL_DISTANCE_BASE + i * FLASHLIGHT_FILL_DISTANCE_GAIN",
    );
    expect(src).toContain("export const FLASHLIGHT_FILL_COLOR = 0xcaefff");
    expect(src).toContain("torchLight.color.setHex(FLASHLIGHT_FILL_COLOR)");
    expect(src).toContain("export const TRACER_COLOR = 0xffffb8");
    expect(src).toContain("color: TRACER_COLOR");
    expect(src).toContain("export const IMPACT_SPARK_COLOR = 0xffef93");
    expect(src).toContain("color: IMPACT_SPARK_COLOR");
    expect(src).toContain("export const MUZZLE_FLASH_COLOR = 0xffffdd");
    expect(src).toContain("color: MUZZLE_FLASH_COLOR");
    expect(src).toContain("export const WARM_LIGHT_COLOR = 0xffca81");
    expect(src).toContain("export const POSSESSED_HEAD_EMISSIVE = 0x30124a");
    expect(src).toContain("emissive: POSSESSED_HEAD_EMISSIVE");
    expect(src).toContain("color: POSSESSED_HEAD_COLOR");
    expect(src).toContain("export const POSSESSED_HEAD_COLOR = 0x8c469f");
    expect(src).toContain("emissive: POSSESSED_EMISSIVE");
    expect(src).toContain("color: POSSESSED_COLOR");
    expect(src).toContain("const POSSESSED_EMISSIVE = 0x1e0925");
    expect(src).toContain("emissiveIntensity: 0.22");
    expect(src).toContain("color: HOSTILE_COLOR");
    expect(src).toContain("color: PLAYER_COLOR");
    expect(src).toContain("color: PLAYER_HEAD_COLOR");
    expect(src).toContain("emissive: PLAYER_HEAD_EMISSIVE");
    expect(TRACER_FLASH_COLOR).toBe(0xffdd6e);
    expect(TRACER_FLASH_INTENSITY).toBe(2.76);
    expect(TRACER_FLASH_DISTANCE).toBe(3.68);
    expect(TRACER_COLOR).toBe(0xffffb8);
    expect(IMPACT_SPARK_LIGHT_COLOR).toBe(0xffef93);
    expect(IMPACT_SPARK_COLOR).toBe(0xffef93);
    expect(IMPACT_SPARK_LIGHT_DECAY).toBe(1.74);
    expect(MUZZLE_LIGHT_COLOR).toBe(0xffffb8);
    expect(MUZZLE_LIGHT_DECAY).toBe(1.74);
    expect(MUZZLE_FLASH_COLOR).toBe(0xffffdd);
    expect(WARM_LIGHT_COLOR).toBe(0xffca81);
    expect(WARM_LIGHT_DECAY).toBe(1.74);
    expect(WARM_LIGHT_VISIBLE_EPS).toBe(0.0174);
    expect(WARM_LIGHT_AMBER_B_GAIN).toBe(0.115);
    expect(WARM_LIGHT_AMBER_B).toBe(0.437);
    expect(WARM_LIGHT_AMBER_G_GAIN).toBe(0.092);
    expect(WARM_LIGHT_AMBER_G).toBe(0.759);
    expect(WARM_LIGHT_DISTANCE_GAIN).toBe(2.875);
    expect(WARM_LIGHT_DISTANCE_BASE).toBe(7.475);
    expect(WARM_LIGHT_Y).toBe(1.7825);
    expect(WARM_LIGHT_INTENSITY_MUL).toBe(1.7825);
    expect(POSSESSED_HEAD_EMISSIVE).toBe(0x30124a);
    expect(POSSESSED_HEAD_COLOR).toBe(0x8c469f);
    expect(POSSESSED_COLOR).toBe(0x68347b);
    expect(HOSTILE_COLOR).toBe(0x7b1e1e);
    expect(PLAYER_COLOR).toBe(0x55a4f4);
    expect(PLAYER_HEAD_COLOR).toBe(0x91d1ff);
    expect(PLAYER_HEAD_EMISSIVE).toBe(0x122537);
    expect(MARKER_PALETTE.possessed.emissive).toBe(0x30124a);
    expect(MARKER_PALETTE.player.badge).toBe(0x91d1ff);
    expect(MARKER_PALETTE.player.emissive).toBe(0x1e4a6e);
    expect(MARKER_PALETTE.player.ring).toBe(0x4392f4);
    expect(MARKER_RING_OPACITY).toBe(0.6877);
    expect(muteBadgeY).toBe(2.3);
    expect(possessedBadgeY).toBe(2.3);
  });
});

describe("worldView tracer flash intensity", () => {
  test("tracer flash intensity 2.4 × 1.15 → 2.76; decay/impact-spark-decay/muzzle-decay/spot-decay/fill-decay/warm-decay/visible-eps/amber-B-gain/amber-B/amber-G-gain/amber-G/gain/base/Y/intensity/color/spot-gain/extra/spot-Y/fill-Y/gain/base/flash/mesh/color/distance/possessed/hostile/player/palette/badge-Y iguales", () => {
    expect(TRACER_FLASH_INTENSITY).toBe(2.76);
    expect(TRACER_FLASH_INTENSITY).toBeCloseTo(2.4 * 1.15, 10);
    expect(TRACER_FLASH_DISTANCE).toBe(3.68);
    expect(TRACER_FLASH_DECAY).toBe(1.74);
    expect(IMPACT_SPARK_LIGHT_DECAY).toBe(1.74);
    expect(MUZZLE_LIGHT_DECAY).toBe(1.74);
    expect(FLASHLIGHT_SPOT_DECAY).toBe(1.74);
    expect(FLASHLIGHT_FILL_DECAY).toBe(1.74);
    expect(WARM_LIGHT_DECAY).toBe(1.74);
    expect(WARM_LIGHT_VISIBLE_EPS).toBe(0.0174);
    expect(WARM_LIGHT_AMBER_B_GAIN).toBe(0.115);
    expect(WARM_LIGHT_AMBER_B).toBe(0.437);
    expect(WARM_LIGHT_AMBER_G_GAIN).toBe(0.092);
    expect(WARM_LIGHT_AMBER_G).toBe(0.759);
    expect(WARM_LIGHT_DISTANCE_GAIN).toBe(2.875);
    expect(WARM_LIGHT_DISTANCE_BASE).toBe(7.475);
    expect(WARM_LIGHT_Y).toBe(1.7825);
    expect(WARM_LIGHT_INTENSITY_MUL).toBe(1.7825);
    expect(FLASHLIGHT_SPOT_DISTANCE_GAIN).toBe(2.3);
    expect(FLASHLIGHT_SPOT_DISTANCE_EXTRA).toBe(1.84);
    expect(FLASHLIGHT_SPOT_Y).toBe(1.7825);
    expect(FLASHLIGHT_FILL_Y).toBe(1.5525);
    expect(FLASHLIGHT_FILL_DISTANCE_GAIN).toBe(4.025);
    expect(FLASHLIGHT_FILL_DISTANCE_BASE).toBe(8.05);
    expect(FLASHLIGHT_FILL_COLOR).toBe(0xcaefff);
    expect(MUZZLE_LIGHT_COLOR).toBe(0xffffb8);
    expect(IMPACT_SPARK_LIGHT_COLOR).toBe(0xffef93);
    expect(TRACER_FLASH_COLOR).toBe(0xffdd6e);
    expect(TRACER_FLASH_DISTANCE).toBe(3.68);
    const src = readFileSync(
      resolve(process.cwd(), "src/render/worldView.ts"),
      "utf8",
    );
    expect(src).toContain("export const TRACER_FLASH_INTENSITY = 2.76");
    expect(src).toContain("export const TRACER_FLASH_DISTANCE = 3.68");
    expect(src).toContain("new THREE.PointLight(TRACER_FLASH_COLOR, TRACER_FLASH_INTENSITY, TRACER_FLASH_DISTANCE, TRACER_FLASH_DECAY)");
    expect(src).not.toMatch(/new THREE\.PointLight\(TRACER_FLASH_COLOR, TRACER_FLASH_INTENSITY, 3\.2, TRACER_FLASH_DECAY\)/);
    expect(src).not.toMatch(/new THREE\.PointLight\(TRACER_FLASH_COLOR, 2\.4, 3\.2, TRACER_FLASH_DECAY\)/);
    expect(src).not.toMatch(/new THREE\.PointLight\(TRACER_FLASH_COLOR, 2\.4, 3\.2, 2\)/);
    expect(src).not.toMatch(/export const TRACER_FLASH_INTENSITY = 2\.4/);
    expect(src).not.toMatch(/export const TRACER_FLASH_DISTANCE = 3\.2/);
    expect(src).toContain("t.flash.intensity = TRACER_FLASH_INTENSITY * op");
    expect(src).not.toMatch(/t\.flash\.intensity = 2\.4 \* op/);
    expect(src).toContain("export const TRACER_FLASH_DECAY = 1.74");
    expect(src).toContain("export const TRACER_FLASH_COLOR = 0xffdd6e");
    expect(src).toContain("export const IMPACT_SPARK_LIGHT_DECAY = 1.74");
    expect(src).toContain("IMPACT_SPARK_LIGHT_DISTANCE,\n    IMPACT_SPARK_LIGHT_DECAY,");
    expect(src).not.toMatch(/IMPACT_SPARK_LIGHT_DISTANCE,\s*\n\s*2,/);
    expect(src).not.toMatch(/export const IMPACT_SPARK_LIGHT_DECAY = 2/);
    expect(src).toContain("const IMPACT_SPARK_LIGHT_DISTANCE = 1.8");
    expect(src).toContain("export const IMPACT_SPARK_LIGHT_COLOR = 0xffef93");
    expect(src).toContain("new THREE.PointLight(\n    IMPACT_SPARK_LIGHT_COLOR,");
    expect(src).toContain("export const MUZZLE_LIGHT_DECAY = 1.74");
    expect(src).toContain("MUZZLE_LIGHT_DISTANCE,\n    MUZZLE_LIGHT_DECAY,");
    expect(src).not.toMatch(/MUZZLE_LIGHT_DISTANCE,\s*\n\s*2,/);
    expect(src).not.toMatch(/export const MUZZLE_LIGHT_DECAY = 2/);
    expect(src).toContain("const MUZZLE_LIGHT_DISTANCE = 2.6");
    expect(src).toContain("export const MUZZLE_LIGHT_COLOR = 0xffffb8");
    expect(src).toContain("new THREE.PointLight(\n    MUZZLE_LIGHT_COLOR,");
    expect(src).toContain("export const FLASHLIGHT_SPOT_DECAY = 1.74");
    expect(src).toContain("FLASHLIGHT_SPOT_PENUMBRA,\n    FLASHLIGHT_SPOT_DECAY,");
    expect(src).not.toMatch(/FLASHLIGHT_SPOT_PENUMBRA,\s*\n\s*2,/);
    expect(src).not.toMatch(/export const FLASHLIGHT_SPOT_DECAY = 2/);
    expect(src).toContain("FLASHLIGHT_CONE_LENGTH + 2.4");
    expect(src).toContain("export const FLASHLIGHT_FILL_DECAY = 1.74");
    expect(src).toContain("new THREE.PointLight(FLASHLIGHT_FILL_COLOR, 0, 10, FLASHLIGHT_FILL_DECAY)");
    expect(src).not.toMatch(/new THREE\.PointLight\(FLASHLIGHT_FILL_COLOR, 0, 10, 2\)/);
    expect(src).not.toMatch(/export const FLASHLIGHT_FILL_DECAY = 2/);
    expect(src).toContain("export const WARM_LIGHT_DECAY = 1.74");
    expect(src).toContain("new THREE.PointLight(WARM_LIGHT_COLOR, 0, 7.5, WARM_LIGHT_DECAY)");
    expect(src).not.toMatch(/new THREE\.PointLight\(WARM_LIGHT_COLOR, 0, 7\.5, 2\)/);
    expect(src).toContain("export const WARM_LIGHT_VISIBLE_EPS = 0.0174");
    expect(src).toContain("warmLight.visible = i > WARM_LIGHT_VISIBLE_EPS");
    expect(src).not.toMatch(/warmLight\.visible = i > 0\.02/);
    expect(src).not.toMatch(/export const WARM_LIGHT_VISIBLE_EPS = 0\.02/);
    expect(src).toContain("export const WARM_LIGHT_AMBER_B_GAIN = 0.115");
    expect(src).toContain("export const WARM_LIGHT_AMBER_B = 0.437");
    expect(src).toContain("export const WARM_LIGHT_AMBER_G_GAIN = 0.092");
    expect(src).toContain("export const WARM_LIGHT_AMBER_G = 0.759");
    expect(src).toContain(
      "warmLight.color.setRGB(1, WARM_LIGHT_AMBER_G + i * WARM_LIGHT_AMBER_G_GAIN, WARM_LIGHT_AMBER_B + i * WARM_LIGHT_AMBER_B_GAIN)",
    );
    expect(src).not.toMatch(/WARM_LIGHT_AMBER_B \+ i \* 0\.1/);
    expect(src).toContain("export const WARM_LIGHT_DISTANCE_GAIN = 2.875");
    expect(src).toContain("export const WARM_LIGHT_DISTANCE_BASE = 7.475");
    expect(src).toContain(
      "warmLight.distance = WARM_LIGHT_DISTANCE_BASE + i * WARM_LIGHT_DISTANCE_GAIN",
    );
    expect(src).not.toMatch(/export const WARM_LIGHT_DISTANCE_GAIN = 2\.5/);
    expect(src).not.toMatch(
      /warmLight\.distance = WARM_LIGHT_DISTANCE_BASE \+ i \* 2\.5/,
    );
    expect(src).not.toMatch(/warmLight\.distance = 6\.5 \+ i \* 2\.5/);
    expect(src).toContain("export const WARM_LIGHT_Y = 1.7825");
    expect(src).toContain("warmLight.position.set(wx, WARM_LIGHT_Y, wy)");
    expect(src).not.toMatch(/export const WARM_LIGHT_Y = 1\.55/);
    expect(src).not.toMatch(/warmLight\.position\.set\(wx, 1\.55, wy\)/);
    expect(src).toContain("warmLight.position.set(0, 1.6, 0)");
    expect(src).toContain("export const WARM_LIGHT_INTENSITY_MUL = 1.7825");
    expect(src).toContain("warmLight.intensity = i * WARM_LIGHT_INTENSITY_MUL");
    expect(src).not.toMatch(/export const WARM_LIGHT_INTENSITY_MUL = 1\.55/);
    expect(src).not.toMatch(/warmLight\.intensity = i \* 1\.55/);
    expect(src).toContain("export const FLASHLIGHT_SPOT_DISTANCE_GAIN = 2.3");
    expect(src).toContain("export const FLASHLIGHT_SPOT_DISTANCE_EXTRA = 1.84");
    expect(src).toContain(
      "torchSpot.distance = FLASHLIGHT_CONE_LENGTH + FLASHLIGHT_SPOT_DISTANCE_EXTRA + i * FLASHLIGHT_SPOT_DISTANCE_GAIN",
    );
    expect(src).toContain("export const FLASHLIGHT_SPOT_Y = 1.7825");
    expect(src).toContain("torchSpot.position.set(0, FLASHLIGHT_SPOT_Y, 0)");
    expect(src).toContain("torchSpot.position.set(wx, FLASHLIGHT_SPOT_Y, wy)");
    expect(src).toContain("export const FLASHLIGHT_FILL_Y = 1.5525");
    expect(src).toContain("torchLight.position.set(0, FLASHLIGHT_FILL_Y, 0)");
    expect(src).toContain("torchLight.position.set(wx, FLASHLIGHT_FILL_Y, wy)");
    expect(src).toContain("export const FLASHLIGHT_FILL_DISTANCE_GAIN = 4.025");
    expect(src).toContain("export const FLASHLIGHT_FILL_DISTANCE_BASE = 8.05");
    expect(src).toContain(
      "torchLight.distance = FLASHLIGHT_FILL_DISTANCE_BASE + i * FLASHLIGHT_FILL_DISTANCE_GAIN",
    );
    expect(src).toContain("export const FLASHLIGHT_FILL_COLOR = 0xcaefff");
    expect(src).toContain("torchLight.color.setHex(FLASHLIGHT_FILL_COLOR)");
    expect(src).toContain("export const TRACER_COLOR = 0xffffb8");
    expect(src).toContain("color: TRACER_COLOR");
    expect(src).toContain("export const IMPACT_SPARK_COLOR = 0xffef93");
    expect(src).toContain("color: IMPACT_SPARK_COLOR");
    expect(src).toContain("export const MUZZLE_FLASH_COLOR = 0xffffdd");
    expect(src).toContain("color: MUZZLE_FLASH_COLOR");
    expect(src).toContain("export const WARM_LIGHT_COLOR = 0xffca81");
    expect(src).toContain("export const POSSESSED_HEAD_EMISSIVE = 0x30124a");
    expect(src).toContain("emissive: POSSESSED_HEAD_EMISSIVE");
    expect(src).toContain("color: POSSESSED_HEAD_COLOR");
    expect(src).toContain("export const POSSESSED_HEAD_COLOR = 0x8c469f");
    expect(src).toContain("emissive: POSSESSED_EMISSIVE");
    expect(src).toContain("color: POSSESSED_COLOR");
    expect(src).toContain("const POSSESSED_EMISSIVE = 0x1e0925");
    expect(src).toContain("emissiveIntensity: 0.22");
    expect(src).toContain("color: HOSTILE_COLOR");
    expect(src).toContain("color: PLAYER_COLOR");
    expect(src).toContain("color: PLAYER_HEAD_COLOR");
    expect(src).toContain("emissive: PLAYER_HEAD_EMISSIVE");
    expect(TRACER_FLASH_COLOR).toBe(0xffdd6e);
    expect(TRACER_FLASH_DISTANCE).toBe(3.68);
    expect(TRACER_FLASH_DECAY).toBe(1.74);
    expect(TRACER_COLOR).toBe(0xffffb8);
    expect(IMPACT_SPARK_LIGHT_COLOR).toBe(0xffef93);
    expect(IMPACT_SPARK_COLOR).toBe(0xffef93);
    expect(IMPACT_SPARK_LIGHT_DECAY).toBe(1.74);
    expect(MUZZLE_LIGHT_COLOR).toBe(0xffffb8);
    expect(MUZZLE_LIGHT_DECAY).toBe(1.74);
    expect(MUZZLE_FLASH_COLOR).toBe(0xffffdd);
    expect(WARM_LIGHT_COLOR).toBe(0xffca81);
    expect(WARM_LIGHT_DECAY).toBe(1.74);
    expect(WARM_LIGHT_VISIBLE_EPS).toBe(0.0174);
    expect(WARM_LIGHT_AMBER_B_GAIN).toBe(0.115);
    expect(WARM_LIGHT_AMBER_B).toBe(0.437);
    expect(WARM_LIGHT_AMBER_G_GAIN).toBe(0.092);
    expect(WARM_LIGHT_AMBER_G).toBe(0.759);
    expect(WARM_LIGHT_DISTANCE_GAIN).toBe(2.875);
    expect(WARM_LIGHT_DISTANCE_BASE).toBe(7.475);
    expect(WARM_LIGHT_Y).toBe(1.7825);
    expect(WARM_LIGHT_INTENSITY_MUL).toBe(1.7825);
    expect(POSSESSED_HEAD_EMISSIVE).toBe(0x30124a);
    expect(POSSESSED_HEAD_COLOR).toBe(0x8c469f);
    expect(POSSESSED_COLOR).toBe(0x68347b);
    expect(HOSTILE_COLOR).toBe(0x7b1e1e);
    expect(PLAYER_COLOR).toBe(0x55a4f4);
    expect(PLAYER_HEAD_COLOR).toBe(0x91d1ff);
    expect(PLAYER_HEAD_EMISSIVE).toBe(0x122537);
    expect(MARKER_PALETTE.possessed.emissive).toBe(0x30124a);
    expect(MARKER_PALETTE.player.badge).toBe(0x91d1ff);
    expect(MARKER_PALETTE.player.emissive).toBe(0x1e4a6e);
    expect(MARKER_PALETTE.player.ring).toBe(0x4392f4);
    expect(MARKER_RING_OPACITY).toBe(0.6877);
    expect(muteBadgeY).toBe(2.3);
    expect(possessedBadgeY).toBe(2.3);
  });
});

describe("worldView tracer flash distance", () => {
  test("tracer flash distance 3.2 × 1.15 → 3.68; intensity/decay/impact-spark-decay/muzzle-decay/spot-decay/fill-decay/warm-decay/visible-eps/amber-B-gain/amber-B/amber-G-gain/amber-G/gain/base/Y/intensity/color/spot-gain/extra/spot-Y/fill-Y/gain/base/flash/mesh/color/possessed/hostile/player/palette/badge-Y iguales", () => {
    expect(TRACER_FLASH_DISTANCE).toBe(3.68);
    expect(TRACER_FLASH_DISTANCE).toBeCloseTo(3.2 * 1.15, 10);
    expect(TRACER_FLASH_Y_OFFSET).toBe(0.1725);
    expect(TRACER_FLASH_INTENSITY).toBe(2.76);
    expect(TRACER_FLASH_DECAY).toBe(1.74);
    expect(IMPACT_SPARK_LIGHT_DECAY).toBe(1.74);
    expect(MUZZLE_LIGHT_DECAY).toBe(1.74);
    expect(FLASHLIGHT_SPOT_DECAY).toBe(1.74);
    expect(FLASHLIGHT_FILL_DECAY).toBe(1.74);
    expect(WARM_LIGHT_DECAY).toBe(1.74);
    expect(WARM_LIGHT_VISIBLE_EPS).toBe(0.0174);
    expect(WARM_LIGHT_AMBER_B_GAIN).toBe(0.115);
    expect(WARM_LIGHT_AMBER_B).toBe(0.437);
    expect(WARM_LIGHT_AMBER_G_GAIN).toBe(0.092);
    expect(WARM_LIGHT_AMBER_G).toBe(0.759);
    expect(WARM_LIGHT_DISTANCE_GAIN).toBe(2.875);
    expect(WARM_LIGHT_DISTANCE_BASE).toBe(7.475);
    expect(WARM_LIGHT_Y).toBe(1.7825);
    expect(WARM_LIGHT_INTENSITY_MUL).toBe(1.7825);
    expect(FLASHLIGHT_SPOT_DISTANCE_GAIN).toBe(2.3);
    expect(FLASHLIGHT_SPOT_DISTANCE_EXTRA).toBe(1.84);
    expect(FLASHLIGHT_SPOT_Y).toBe(1.7825);
    expect(FLASHLIGHT_FILL_Y).toBe(1.5525);
    expect(FLASHLIGHT_FILL_DISTANCE_GAIN).toBe(4.025);
    expect(FLASHLIGHT_FILL_DISTANCE_BASE).toBe(8.05);
    expect(FLASHLIGHT_FILL_COLOR).toBe(0xcaefff);
    expect(MUZZLE_LIGHT_COLOR).toBe(0xffffb8);
    expect(IMPACT_SPARK_LIGHT_COLOR).toBe(0xffef93);
    expect(TRACER_FLASH_COLOR).toBe(0xffdd6e);
    const src = readFileSync(
      resolve(process.cwd(), "src/render/worldView.ts"),
      "utf8",
    );
    expect(src).toContain("export const TRACER_FLASH_DISTANCE = 3.68");
    expect(src).toContain("export const TRACER_FLASH_Y_OFFSET = 0.1725");
    expect(src).toContain("flash.position.set(from.x, TRACER_HEIGHT + TRACER_FLASH_Y_OFFSET, from.y)");
    expect(src).not.toMatch(/flash\.position\.set\(from\.x, TRACER_HEIGHT \+ 0\.15, from\.y\)/);
    expect(src).not.toMatch(/export const TRACER_FLASH_Y_OFFSET = 0\.15/);
    expect(src).toContain("new THREE.PointLight(TRACER_FLASH_COLOR, TRACER_FLASH_INTENSITY, TRACER_FLASH_DISTANCE, TRACER_FLASH_DECAY)");
    expect(src).not.toMatch(/new THREE\.PointLight\(TRACER_FLASH_COLOR, TRACER_FLASH_INTENSITY, 3\.2, TRACER_FLASH_DECAY\)/);
    expect(src).not.toMatch(/new THREE\.PointLight\(TRACER_FLASH_COLOR, 2\.4, 3\.2, TRACER_FLASH_DECAY\)/);
    expect(src).not.toMatch(/new THREE\.PointLight\(TRACER_FLASH_COLOR, 2\.4, 3\.2, 2\)/);
    expect(src).not.toMatch(/export const TRACER_FLASH_DISTANCE = 3\.2/);
    expect(src).toContain("t.flash.intensity = TRACER_FLASH_INTENSITY * op");
    expect(src).not.toMatch(/t\.flash\.intensity = 2\.4 \* op/);
    expect(src).toContain("export const TRACER_FLASH_INTENSITY = 2.76");
    expect(src).toContain("export const TRACER_FLASH_DECAY = 1.74");
    expect(src).toContain("export const TRACER_FLASH_COLOR = 0xffdd6e");
    expect(src).toContain("export const IMPACT_SPARK_LIGHT_DECAY = 1.74");
    expect(src).toContain("IMPACT_SPARK_LIGHT_DISTANCE,\n    IMPACT_SPARK_LIGHT_DECAY,");
    expect(src).not.toMatch(/IMPACT_SPARK_LIGHT_DISTANCE,\s*\n\s*2,/);
    expect(src).not.toMatch(/export const IMPACT_SPARK_LIGHT_DECAY = 2/);
    expect(src).toContain("const IMPACT_SPARK_LIGHT_DISTANCE = 1.8");
    expect(src).toContain("export const IMPACT_SPARK_LIGHT_COLOR = 0xffef93");
    expect(src).toContain("new THREE.PointLight(\n    IMPACT_SPARK_LIGHT_COLOR,");
    expect(src).toContain("export const MUZZLE_LIGHT_DECAY = 1.74");
    expect(src).toContain("MUZZLE_LIGHT_DISTANCE,\n    MUZZLE_LIGHT_DECAY,");
    expect(src).not.toMatch(/MUZZLE_LIGHT_DISTANCE,\s*\n\s*2,/);
    expect(src).not.toMatch(/export const MUZZLE_LIGHT_DECAY = 2/);
    expect(src).toContain("const MUZZLE_LIGHT_DISTANCE = 2.6");
    expect(src).toContain("export const MUZZLE_LIGHT_COLOR = 0xffffb8");
    expect(src).toContain("new THREE.PointLight(\n    MUZZLE_LIGHT_COLOR,");
    expect(src).toContain("export const FLASHLIGHT_SPOT_DECAY = 1.74");
    expect(src).toContain("FLASHLIGHT_SPOT_PENUMBRA,\n    FLASHLIGHT_SPOT_DECAY,");
    expect(src).not.toMatch(/FLASHLIGHT_SPOT_PENUMBRA,\s*\n\s*2,/);
    expect(src).not.toMatch(/export const FLASHLIGHT_SPOT_DECAY = 2/);
    expect(src).toContain("FLASHLIGHT_CONE_LENGTH + 2.4");
    expect(src).toContain("export const FLASHLIGHT_FILL_DECAY = 1.74");
    expect(src).toContain("new THREE.PointLight(FLASHLIGHT_FILL_COLOR, 0, 10, FLASHLIGHT_FILL_DECAY)");
    expect(src).not.toMatch(/new THREE\.PointLight\(FLASHLIGHT_FILL_COLOR, 0, 10, 2\)/);
    expect(src).not.toMatch(/export const FLASHLIGHT_FILL_DECAY = 2/);
    expect(src).toContain("export const WARM_LIGHT_DECAY = 1.74");
    expect(src).toContain("new THREE.PointLight(WARM_LIGHT_COLOR, 0, 7.5, WARM_LIGHT_DECAY)");
    expect(src).not.toMatch(/new THREE\.PointLight\(WARM_LIGHT_COLOR, 0, 7\.5, 2\)/);
    expect(src).toContain("export const WARM_LIGHT_VISIBLE_EPS = 0.0174");
    expect(src).toContain("warmLight.visible = i > WARM_LIGHT_VISIBLE_EPS");
    expect(src).not.toMatch(/warmLight\.visible = i > 0\.02/);
    expect(src).not.toMatch(/export const WARM_LIGHT_VISIBLE_EPS = 0\.02/);
    expect(src).toContain("export const WARM_LIGHT_AMBER_B_GAIN = 0.115");
    expect(src).toContain("export const WARM_LIGHT_AMBER_B = 0.437");
    expect(src).toContain("export const WARM_LIGHT_AMBER_G_GAIN = 0.092");
    expect(src).toContain("export const WARM_LIGHT_AMBER_G = 0.759");
    expect(src).toContain(
      "warmLight.color.setRGB(1, WARM_LIGHT_AMBER_G + i * WARM_LIGHT_AMBER_G_GAIN, WARM_LIGHT_AMBER_B + i * WARM_LIGHT_AMBER_B_GAIN)",
    );
    expect(src).not.toMatch(/WARM_LIGHT_AMBER_B \+ i \* 0\.1/);
    expect(src).toContain("export const WARM_LIGHT_DISTANCE_GAIN = 2.875");
    expect(src).toContain("export const WARM_LIGHT_DISTANCE_BASE = 7.475");
    expect(src).toContain(
      "warmLight.distance = WARM_LIGHT_DISTANCE_BASE + i * WARM_LIGHT_DISTANCE_GAIN",
    );
    expect(src).not.toMatch(/export const WARM_LIGHT_DISTANCE_GAIN = 2\.5/);
    expect(src).not.toMatch(
      /warmLight\.distance = WARM_LIGHT_DISTANCE_BASE \+ i \* 2\.5/,
    );
    expect(src).not.toMatch(/warmLight\.distance = 6\.5 \+ i \* 2\.5/);
    expect(src).toContain("export const WARM_LIGHT_Y = 1.7825");
    expect(src).toContain("warmLight.position.set(wx, WARM_LIGHT_Y, wy)");
    expect(src).not.toMatch(/export const WARM_LIGHT_Y = 1\.55/);
    expect(src).not.toMatch(/warmLight\.position\.set\(wx, 1\.55, wy\)/);
    expect(src).toContain("warmLight.position.set(0, 1.6, 0)");
    expect(src).toContain("export const WARM_LIGHT_INTENSITY_MUL = 1.7825");
    expect(src).toContain("warmLight.intensity = i * WARM_LIGHT_INTENSITY_MUL");
    expect(src).not.toMatch(/export const WARM_LIGHT_INTENSITY_MUL = 1\.55/);
    expect(src).not.toMatch(/warmLight\.intensity = i \* 1\.55/);
    expect(src).toContain("export const FLASHLIGHT_SPOT_DISTANCE_GAIN = 2.3");
    expect(src).toContain("export const FLASHLIGHT_SPOT_DISTANCE_EXTRA = 1.84");
    expect(src).toContain(
      "torchSpot.distance = FLASHLIGHT_CONE_LENGTH + FLASHLIGHT_SPOT_DISTANCE_EXTRA + i * FLASHLIGHT_SPOT_DISTANCE_GAIN",
    );
    expect(src).toContain("export const FLASHLIGHT_SPOT_Y = 1.7825");
    expect(src).toContain("torchSpot.position.set(0, FLASHLIGHT_SPOT_Y, 0)");
    expect(src).toContain("torchSpot.position.set(wx, FLASHLIGHT_SPOT_Y, wy)");
    expect(src).toContain("export const FLASHLIGHT_FILL_Y = 1.5525");
    expect(src).toContain("torchLight.position.set(0, FLASHLIGHT_FILL_Y, 0)");
    expect(src).toContain("torchLight.position.set(wx, FLASHLIGHT_FILL_Y, wy)");
    expect(src).toContain("export const FLASHLIGHT_FILL_DISTANCE_GAIN = 4.025");
    expect(src).toContain("export const FLASHLIGHT_FILL_DISTANCE_BASE = 8.05");
    expect(src).toContain(
      "torchLight.distance = FLASHLIGHT_FILL_DISTANCE_BASE + i * FLASHLIGHT_FILL_DISTANCE_GAIN",
    );
    expect(src).toContain("export const FLASHLIGHT_FILL_COLOR = 0xcaefff");
    expect(src).toContain("torchLight.color.setHex(FLASHLIGHT_FILL_COLOR)");
    expect(src).toContain("export const TRACER_COLOR = 0xffffb8");
    expect(src).toContain("color: TRACER_COLOR");
    expect(src).toContain("export const IMPACT_SPARK_COLOR = 0xffef93");
    expect(src).toContain("color: IMPACT_SPARK_COLOR");
    expect(src).toContain("export const MUZZLE_FLASH_COLOR = 0xffffdd");
    expect(src).toContain("color: MUZZLE_FLASH_COLOR");
    expect(src).toContain("export const WARM_LIGHT_COLOR = 0xffca81");
    expect(src).toContain("export const POSSESSED_HEAD_EMISSIVE = 0x30124a");
    expect(src).toContain("emissive: POSSESSED_HEAD_EMISSIVE");
    expect(src).toContain("color: POSSESSED_HEAD_COLOR");
    expect(src).toContain("export const POSSESSED_HEAD_COLOR = 0x8c469f");
    expect(src).toContain("emissive: POSSESSED_EMISSIVE");
    expect(src).toContain("color: POSSESSED_COLOR");
    expect(src).toContain("const POSSESSED_EMISSIVE = 0x1e0925");
    expect(src).toContain("emissiveIntensity: 0.22");
    expect(src).toContain("color: HOSTILE_COLOR");
    expect(src).toContain("color: PLAYER_COLOR");
    expect(src).toContain("color: PLAYER_HEAD_COLOR");
    expect(src).toContain("emissive: PLAYER_HEAD_EMISSIVE");
    expect(TRACER_FLASH_COLOR).toBe(0xffdd6e);
    expect(TRACER_FLASH_INTENSITY).toBe(2.76);
    expect(TRACER_FLASH_DECAY).toBe(1.74);
    expect(TRACER_FLASH_Y_OFFSET).toBe(0.1725);
    expect(TRACER_COLOR).toBe(0xffffb8);
    expect(IMPACT_SPARK_LIGHT_COLOR).toBe(0xffef93);
    expect(IMPACT_SPARK_COLOR).toBe(0xffef93);
    expect(IMPACT_SPARK_LIGHT_DECAY).toBe(1.74);
    expect(MUZZLE_LIGHT_COLOR).toBe(0xffffb8);
    expect(MUZZLE_LIGHT_DECAY).toBe(1.74);
    expect(MUZZLE_FLASH_COLOR).toBe(0xffffdd);
    expect(WARM_LIGHT_COLOR).toBe(0xffca81);
    expect(WARM_LIGHT_DECAY).toBe(1.74);
    expect(WARM_LIGHT_VISIBLE_EPS).toBe(0.0174);
    expect(WARM_LIGHT_AMBER_B_GAIN).toBe(0.115);
    expect(WARM_LIGHT_AMBER_B).toBe(0.437);
    expect(WARM_LIGHT_AMBER_G_GAIN).toBe(0.092);
    expect(WARM_LIGHT_AMBER_G).toBe(0.759);
    expect(WARM_LIGHT_DISTANCE_GAIN).toBe(2.875);
    expect(WARM_LIGHT_DISTANCE_BASE).toBe(7.475);
    expect(WARM_LIGHT_Y).toBe(1.7825);
    expect(WARM_LIGHT_INTENSITY_MUL).toBe(1.7825);
    expect(POSSESSED_HEAD_EMISSIVE).toBe(0x30124a);
    expect(POSSESSED_HEAD_COLOR).toBe(0x8c469f);
    expect(POSSESSED_COLOR).toBe(0x68347b);
    expect(HOSTILE_COLOR).toBe(0x7b1e1e);
    expect(PLAYER_COLOR).toBe(0x55a4f4);
    expect(PLAYER_HEAD_COLOR).toBe(0x91d1ff);
    expect(PLAYER_HEAD_EMISSIVE).toBe(0x122537);
    expect(MARKER_PALETTE.possessed.emissive).toBe(0x30124a);
    expect(MARKER_PALETTE.player.badge).toBe(0x91d1ff);
    expect(MARKER_PALETTE.player.emissive).toBe(0x1e4a6e);
    expect(MARKER_PALETTE.player.ring).toBe(0x4392f4);
    expect(MARKER_RING_OPACITY).toBe(0.6877);
    expect(muteBadgeY).toBe(2.3);
    expect(possessedBadgeY).toBe(2.3);
  });
});

describe("worldView tracer flash Y offset", () => {
  test("tracer flash Y offset 0.15 × 1.15 → 0.1725; distance/intensity/decay/impact-spark-decay/muzzle-decay/spot-decay/fill-decay/warm-decay/visible-eps/amber-B-gain/amber-B/amber-G-gain/amber-G/gain/base/Y/intensity/color/spot-gain/extra/spot-Y/fill-Y/gain/base/flash/mesh/color/possessed/hostile/player/palette/badge-Y iguales", () => {
    expect(TRACER_FLASH_Y_OFFSET).toBe(0.1725);
    expect(TRACER_FLASH_Y_OFFSET).toBeCloseTo(0.15 * 1.15, 10);
    expect(TRACER_FLASH_DISTANCE).toBe(3.68);
    expect(TRACER_FLASH_INTENSITY).toBe(2.76);
    expect(TRACER_FLASH_DECAY).toBe(1.74);
    expect(IMPACT_SPARK_LIGHT_DECAY).toBe(1.74);
    expect(MUZZLE_LIGHT_DECAY).toBe(1.74);
    expect(FLASHLIGHT_SPOT_DECAY).toBe(1.74);
    expect(FLASHLIGHT_FILL_DECAY).toBe(1.74);
    expect(WARM_LIGHT_DECAY).toBe(1.74);
    expect(WARM_LIGHT_VISIBLE_EPS).toBe(0.0174);
    expect(WARM_LIGHT_AMBER_B_GAIN).toBe(0.115);
    expect(WARM_LIGHT_AMBER_B).toBe(0.437);
    expect(WARM_LIGHT_AMBER_G_GAIN).toBe(0.092);
    expect(WARM_LIGHT_AMBER_G).toBe(0.759);
    expect(WARM_LIGHT_DISTANCE_GAIN).toBe(2.875);
    expect(WARM_LIGHT_DISTANCE_BASE).toBe(7.475);
    expect(WARM_LIGHT_Y).toBe(1.7825);
    expect(WARM_LIGHT_INTENSITY_MUL).toBe(1.7825);
    expect(FLASHLIGHT_SPOT_DISTANCE_GAIN).toBe(2.3);
    expect(FLASHLIGHT_SPOT_DISTANCE_EXTRA).toBe(1.84);
    expect(FLASHLIGHT_SPOT_Y).toBe(1.7825);
    expect(FLASHLIGHT_SPOT_TARGET_Y).toBe(0.138);
    expect(FLASHLIGHT_FILL_Y).toBe(1.5525);
    expect(FLASHLIGHT_FILL_DISTANCE_GAIN).toBe(4.025);
    expect(FLASHLIGHT_FILL_DISTANCE_BASE).toBe(8.05);
    expect(FLASHLIGHT_FILL_COLOR).toBe(0xcaefff);
    expect(MUZZLE_LIGHT_COLOR).toBe(0xffffb8);
    expect(IMPACT_SPARK_LIGHT_COLOR).toBe(0xffef93);
    expect(TRACER_FLASH_COLOR).toBe(0xffdd6e);
    const src = readFileSync(
      resolve(process.cwd(), "src/render/worldView.ts"),
      "utf8",
    );
    expect(src).toContain("export const TRACER_FLASH_Y_OFFSET = 0.1725");
    expect(src).toContain("flash.position.set(from.x, TRACER_HEIGHT + TRACER_FLASH_Y_OFFSET, from.y)");
    expect(src).not.toMatch(/flash\.position\.set\(from\.x, TRACER_HEIGHT \+ 0\.15, from\.y\)/);
    expect(src).not.toMatch(/export const TRACER_FLASH_Y_OFFSET = 0\.15/);
    expect(src).toContain("export const TRACER_FLASH_DISTANCE = 3.68");
    expect(src).toContain("new THREE.PointLight(TRACER_FLASH_COLOR, TRACER_FLASH_INTENSITY, TRACER_FLASH_DISTANCE, TRACER_FLASH_DECAY)");
    expect(src).not.toMatch(/new THREE\.PointLight\(TRACER_FLASH_COLOR, TRACER_FLASH_INTENSITY, 3\.2, TRACER_FLASH_DECAY\)/);
    expect(src).not.toMatch(/new THREE\.PointLight\(TRACER_FLASH_COLOR, 2\.4, 3\.2, TRACER_FLASH_DECAY\)/);
    expect(src).not.toMatch(/new THREE\.PointLight\(TRACER_FLASH_COLOR, 2\.4, 3\.2, 2\)/);
    expect(src).not.toMatch(/export const TRACER_FLASH_DISTANCE = 3\.2/);
    expect(src).toContain("t.flash.intensity = TRACER_FLASH_INTENSITY * op");
    expect(src).not.toMatch(/t\.flash\.intensity = 2\.4 \* op/);
    expect(src).toContain("export const TRACER_FLASH_INTENSITY = 2.76");
    expect(src).toContain("export const TRACER_FLASH_DECAY = 1.74");
    expect(src).toContain("export const TRACER_FLASH_COLOR = 0xffdd6e");
    expect(src).toContain("export const IMPACT_SPARK_LIGHT_DECAY = 1.74");
    expect(src).toContain("IMPACT_SPARK_LIGHT_DISTANCE,\n    IMPACT_SPARK_LIGHT_DECAY,");
    expect(src).not.toMatch(/IMPACT_SPARK_LIGHT_DISTANCE,\s*\n\s*2,/);
    expect(src).not.toMatch(/export const IMPACT_SPARK_LIGHT_DECAY = 2/);
    expect(src).toContain("const IMPACT_SPARK_LIGHT_DISTANCE = 1.8");
    expect(src).toContain("export const IMPACT_SPARK_LIGHT_COLOR = 0xffef93");
    expect(src).toContain("new THREE.PointLight(\n    IMPACT_SPARK_LIGHT_COLOR,");
    expect(src).toContain("export const MUZZLE_LIGHT_DECAY = 1.74");
    expect(src).toContain("MUZZLE_LIGHT_DISTANCE,\n    MUZZLE_LIGHT_DECAY,");
    expect(src).not.toMatch(/MUZZLE_LIGHT_DISTANCE,\s*\n\s*2,/);
    expect(src).not.toMatch(/export const MUZZLE_LIGHT_DECAY = 2/);
    expect(src).toContain("const MUZZLE_LIGHT_DISTANCE = 2.6");
    expect(src).toContain("export const MUZZLE_LIGHT_COLOR = 0xffffb8");
    expect(src).toContain("new THREE.PointLight(\n    MUZZLE_LIGHT_COLOR,");
    expect(src).toContain("export const FLASHLIGHT_SPOT_DECAY = 1.74");
    expect(src).toContain("FLASHLIGHT_SPOT_PENUMBRA,\n    FLASHLIGHT_SPOT_DECAY,");
    expect(src).not.toMatch(/FLASHLIGHT_SPOT_PENUMBRA,\s*\n\s*2,/);
    expect(src).not.toMatch(/export const FLASHLIGHT_SPOT_DECAY = 2/);
    expect(src).toContain("FLASHLIGHT_CONE_LENGTH + 2.4");
    expect(src).toContain("export const FLASHLIGHT_FILL_DECAY = 1.74");
    expect(src).toContain("new THREE.PointLight(FLASHLIGHT_FILL_COLOR, 0, 10, FLASHLIGHT_FILL_DECAY)");
    expect(src).not.toMatch(/new THREE\.PointLight\(FLASHLIGHT_FILL_COLOR, 0, 10, 2\)/);
    expect(src).not.toMatch(/export const FLASHLIGHT_FILL_DECAY = 2/);
    expect(src).toContain("export const WARM_LIGHT_DECAY = 1.74");
    expect(src).toContain("new THREE.PointLight(WARM_LIGHT_COLOR, 0, 7.5, WARM_LIGHT_DECAY)");
    expect(src).not.toMatch(/new THREE\.PointLight\(WARM_LIGHT_COLOR, 0, 7\.5, 2\)/);
    expect(src).toContain("export const WARM_LIGHT_VISIBLE_EPS = 0.0174");
    expect(src).toContain("warmLight.visible = i > WARM_LIGHT_VISIBLE_EPS");
    expect(src).not.toMatch(/warmLight\.visible = i > 0\.02/);
    expect(src).not.toMatch(/export const WARM_LIGHT_VISIBLE_EPS = 0\.02/);
    expect(src).toContain("export const WARM_LIGHT_AMBER_B_GAIN = 0.115");
    expect(src).toContain("export const WARM_LIGHT_AMBER_B = 0.437");
    expect(src).toContain("export const WARM_LIGHT_AMBER_G_GAIN = 0.092");
    expect(src).toContain("export const WARM_LIGHT_AMBER_G = 0.759");
    expect(src).toContain(
      "warmLight.color.setRGB(1, WARM_LIGHT_AMBER_G + i * WARM_LIGHT_AMBER_G_GAIN, WARM_LIGHT_AMBER_B + i * WARM_LIGHT_AMBER_B_GAIN)",
    );
    expect(src).not.toMatch(/WARM_LIGHT_AMBER_B \+ i \* 0\.1/);
    expect(src).toContain("export const WARM_LIGHT_DISTANCE_GAIN = 2.875");
    expect(src).toContain("export const WARM_LIGHT_DISTANCE_BASE = 7.475");
    expect(src).toContain(
      "warmLight.distance = WARM_LIGHT_DISTANCE_BASE + i * WARM_LIGHT_DISTANCE_GAIN",
    );
    expect(src).not.toMatch(/export const WARM_LIGHT_DISTANCE_GAIN = 2\.5/);
    expect(src).not.toMatch(
      /warmLight\.distance = WARM_LIGHT_DISTANCE_BASE \+ i \* 2\.5/,
    );
    expect(src).not.toMatch(/warmLight\.distance = 6\.5 \+ i \* 2\.5/);
    expect(src).toContain("export const WARM_LIGHT_Y = 1.7825");
    expect(src).toContain("warmLight.position.set(wx, WARM_LIGHT_Y, wy)");
    expect(src).not.toMatch(/export const WARM_LIGHT_Y = 1\.55/);
    expect(src).not.toMatch(/warmLight\.position\.set\(wx, 1\.55, wy\)/);
    expect(src).toContain("warmLight.position.set(0, 1.6, 0)");
    expect(src).toContain("export const WARM_LIGHT_INTENSITY_MUL = 1.7825");
    expect(src).toContain("warmLight.intensity = i * WARM_LIGHT_INTENSITY_MUL");
    expect(src).not.toMatch(/export const WARM_LIGHT_INTENSITY_MUL = 1\.55/);
    expect(src).not.toMatch(/warmLight\.intensity = i \* 1\.55/);
    expect(src).toContain("export const FLASHLIGHT_SPOT_DISTANCE_GAIN = 2.3");
    expect(src).toContain("export const FLASHLIGHT_SPOT_DISTANCE_EXTRA = 1.84");
    expect(src).toContain(
      "torchSpot.distance = FLASHLIGHT_CONE_LENGTH + FLASHLIGHT_SPOT_DISTANCE_EXTRA + i * FLASHLIGHT_SPOT_DISTANCE_GAIN",
    );
    expect(src).toContain("export const FLASHLIGHT_SPOT_Y = 1.7825");
    expect(src).toContain("torchSpot.position.set(0, FLASHLIGHT_SPOT_Y, 0)");
    expect(src).toContain("torchSpot.position.set(wx, FLASHLIGHT_SPOT_Y, wy)");
    expect(src).toContain("export const FLASHLIGHT_SPOT_TARGET_Y = 0.138");
    expect(src).toContain("torchSpot.target.position.set(wx + tip.x, FLASHLIGHT_SPOT_TARGET_Y, wy + tip.z)");
    expect(src).not.toMatch(/torchSpot\.target\.position\.set\(wx \+ tip\.x, 0\.12, wy \+ tip\.z\)/);
    expect(src).not.toMatch(/export const FLASHLIGHT_SPOT_TARGET_Y = 0\.12/);
    expect(src).toContain("export const FLASHLIGHT_FILL_Y = 1.5525");
    expect(src).toContain("torchLight.position.set(0, FLASHLIGHT_FILL_Y, 0)");
    expect(src).toContain("torchLight.position.set(wx, FLASHLIGHT_FILL_Y, wy)");
    expect(src).toContain("export const FLASHLIGHT_FILL_DISTANCE_GAIN = 4.025");
    expect(src).toContain("export const FLASHLIGHT_FILL_DISTANCE_BASE = 8.05");
    expect(src).toContain(
      "torchLight.distance = FLASHLIGHT_FILL_DISTANCE_BASE + i * FLASHLIGHT_FILL_DISTANCE_GAIN",
    );
    expect(src).toContain("export const FLASHLIGHT_FILL_COLOR = 0xcaefff");
    expect(src).toContain("torchLight.color.setHex(FLASHLIGHT_FILL_COLOR)");
    expect(src).toContain("export const TRACER_COLOR = 0xffffb8");
    expect(src).toContain("color: TRACER_COLOR");
    expect(src).toContain("export const IMPACT_SPARK_COLOR = 0xffef93");
    expect(src).toContain("color: IMPACT_SPARK_COLOR");
    expect(src).toContain("export const MUZZLE_FLASH_COLOR = 0xffffdd");
    expect(src).toContain("color: MUZZLE_FLASH_COLOR");
    expect(src).toContain("export const WARM_LIGHT_COLOR = 0xffca81");
    expect(src).toContain("export const POSSESSED_HEAD_EMISSIVE = 0x30124a");
    expect(src).toContain("emissive: POSSESSED_HEAD_EMISSIVE");
    expect(src).toContain("color: POSSESSED_HEAD_COLOR");
    expect(src).toContain("export const POSSESSED_HEAD_COLOR = 0x8c469f");
    expect(src).toContain("emissive: POSSESSED_EMISSIVE");
    expect(src).toContain("color: POSSESSED_COLOR");
    expect(src).toContain("const POSSESSED_EMISSIVE = 0x1e0925");
    expect(src).toContain("emissiveIntensity: 0.22");
    expect(src).toContain("color: HOSTILE_COLOR");
    expect(src).toContain("color: PLAYER_COLOR");
    expect(src).toContain("color: PLAYER_HEAD_COLOR");
    expect(src).toContain("emissive: PLAYER_HEAD_EMISSIVE");
    expect(TRACER_FLASH_COLOR).toBe(0xffdd6e);
    expect(TRACER_FLASH_INTENSITY).toBe(2.76);
    expect(TRACER_FLASH_DISTANCE).toBe(3.68);
    expect(TRACER_FLASH_DECAY).toBe(1.74);
    expect(TRACER_COLOR).toBe(0xffffb8);
    expect(IMPACT_SPARK_LIGHT_COLOR).toBe(0xffef93);
    expect(IMPACT_SPARK_COLOR).toBe(0xffef93);
    expect(IMPACT_SPARK_LIGHT_DECAY).toBe(1.74);
    expect(MUZZLE_LIGHT_COLOR).toBe(0xffffb8);
    expect(MUZZLE_LIGHT_DECAY).toBe(1.74);
    expect(MUZZLE_FLASH_COLOR).toBe(0xffffdd);
    expect(WARM_LIGHT_COLOR).toBe(0xffca81);
    expect(WARM_LIGHT_DECAY).toBe(1.74);
    expect(WARM_LIGHT_VISIBLE_EPS).toBe(0.0174);
    expect(WARM_LIGHT_AMBER_B_GAIN).toBe(0.115);
    expect(WARM_LIGHT_AMBER_B).toBe(0.437);
    expect(WARM_LIGHT_AMBER_G_GAIN).toBe(0.092);
    expect(WARM_LIGHT_AMBER_G).toBe(0.759);
    expect(WARM_LIGHT_DISTANCE_GAIN).toBe(2.875);
    expect(WARM_LIGHT_DISTANCE_BASE).toBe(7.475);
    expect(WARM_LIGHT_Y).toBe(1.7825);
    expect(WARM_LIGHT_INTENSITY_MUL).toBe(1.7825);
    expect(POSSESSED_HEAD_EMISSIVE).toBe(0x30124a);
    expect(POSSESSED_HEAD_COLOR).toBe(0x8c469f);
    expect(POSSESSED_COLOR).toBe(0x68347b);
    expect(HOSTILE_COLOR).toBe(0x7b1e1e);
    expect(PLAYER_COLOR).toBe(0x55a4f4);
    expect(PLAYER_HEAD_COLOR).toBe(0x91d1ff);
    expect(PLAYER_HEAD_EMISSIVE).toBe(0x122537);
    expect(MARKER_PALETTE.possessed.emissive).toBe(0x30124a);
    expect(MARKER_PALETTE.player.badge).toBe(0x91d1ff);
    expect(MARKER_PALETTE.player.emissive).toBe(0x1e4a6e);
    expect(MARKER_PALETTE.player.ring).toBe(0x4392f4);
    expect(MARKER_RING_OPACITY).toBe(0.6877);
    expect(muteBadgeY).toBe(2.3);
    expect(possessedBadgeY).toBe(2.3);
  });
});

describe("worldView flashlight spot target Y", () => {
  test("flashlight spot target Y 0.12 × 1.15 → 0.138; y-offset/distance/intensity/decay/impact-spark-decay/muzzle-decay/spot-decay/fill-decay/warm-decay/visible-eps/amber-B-gain/amber-B/amber-G-gain/amber-G/gain/base/Y/intensity/color/spot-gain/extra/spot-Y/fill-Y/gain/base/flash/mesh/color/possessed/hostile/player/palette/badge-Y iguales", () => {
    expect(FLASHLIGHT_SPOT_TARGET_Y).toBe(0.138);
    expect(FLASHLIGHT_SPOT_TARGET_Y).toBeCloseTo(0.12 * 1.15, 10);
    expect(MUZZLE_FORWARD).toBe(0.552);
    expect(TRACER_FLASH_Y_OFFSET).toBe(0.1725);
    expect(TRACER_FLASH_DISTANCE).toBe(3.68);
    expect(TRACER_FLASH_INTENSITY).toBe(2.76);
    expect(TRACER_FLASH_DECAY).toBe(1.74);
    expect(IMPACT_SPARK_LIGHT_DECAY).toBe(1.74);
    expect(MUZZLE_LIGHT_DECAY).toBe(1.74);
    expect(FLASHLIGHT_SPOT_DECAY).toBe(1.74);
    expect(FLASHLIGHT_FILL_DECAY).toBe(1.74);
    expect(WARM_LIGHT_DECAY).toBe(1.74);
    expect(WARM_LIGHT_VISIBLE_EPS).toBe(0.0174);
    expect(WARM_LIGHT_AMBER_B_GAIN).toBe(0.115);
    expect(WARM_LIGHT_AMBER_B).toBe(0.437);
    expect(WARM_LIGHT_AMBER_G_GAIN).toBe(0.092);
    expect(WARM_LIGHT_AMBER_G).toBe(0.759);
    expect(WARM_LIGHT_DISTANCE_GAIN).toBe(2.875);
    expect(WARM_LIGHT_DISTANCE_BASE).toBe(7.475);
    expect(WARM_LIGHT_Y).toBe(1.7825);
    expect(WARM_LIGHT_INTENSITY_MUL).toBe(1.7825);
    expect(FLASHLIGHT_SPOT_DISTANCE_GAIN).toBe(2.3);
    expect(FLASHLIGHT_SPOT_DISTANCE_EXTRA).toBe(1.84);
    expect(FLASHLIGHT_SPOT_Y).toBe(1.7825);
    expect(FLASHLIGHT_FILL_Y).toBe(1.5525);
    expect(FLASHLIGHT_FILL_DISTANCE_GAIN).toBe(4.025);
    expect(FLASHLIGHT_FILL_DISTANCE_BASE).toBe(8.05);
    expect(FLASHLIGHT_FILL_COLOR).toBe(0xcaefff);
    expect(MUZZLE_LIGHT_COLOR).toBe(0xffffb8);
    expect(IMPACT_SPARK_LIGHT_COLOR).toBe(0xffef93);
    expect(TRACER_FLASH_COLOR).toBe(0xffdd6e);
    const src = readFileSync(
      resolve(process.cwd(), "src/render/worldView.ts"),
      "utf8",
    );
    expect(src).toContain("export const FLASHLIGHT_SPOT_TARGET_Y = 0.138");
    expect(src).toContain("torchSpot.target.position.set(wx + tip.x, FLASHLIGHT_SPOT_TARGET_Y, wy + tip.z)");
    expect(src).not.toMatch(/torchSpot\.target\.position\.set\(wx \+ tip\.x, 0\.12, wy \+ tip\.z\)/);
    expect(src).not.toMatch(/export const FLASHLIGHT_SPOT_TARGET_Y = 0\.12/);
    expect(src).toContain("export const MUZZLE_FORWARD = 0.552");
    expect(src).toContain("Math.sin(playerGltfYaw) * MUZZLE_FORWARD");
    expect(src).toContain("Math.cos(playerGltfYaw) * MUZZLE_FORWARD");
    expect(src).not.toMatch(/const MUZZLE_FORWARD = 0\.48/);
    expect(src).not.toMatch(/export const MUZZLE_FORWARD = 0\.48/);
    expect(src).toContain("export const TRACER_FLASH_Y_OFFSET = 0.1725");

    expect(src).toContain("flash.position.set(from.x, TRACER_HEIGHT + TRACER_FLASH_Y_OFFSET, from.y)");
    expect(src).not.toMatch(/flash\.position\.set\(from\.x, TRACER_HEIGHT \+ 0\.15, from\.y\)/);
    expect(src).not.toMatch(/export const TRACER_FLASH_Y_OFFSET = 0\.15/);
    expect(src).toContain("export const TRACER_FLASH_DISTANCE = 3.68");
    expect(src).toContain("new THREE.PointLight(TRACER_FLASH_COLOR, TRACER_FLASH_INTENSITY, TRACER_FLASH_DISTANCE, TRACER_FLASH_DECAY)");
    expect(src).not.toMatch(/new THREE\.PointLight\(TRACER_FLASH_COLOR, TRACER_FLASH_INTENSITY, 3\.2, TRACER_FLASH_DECAY\)/);
    expect(src).not.toMatch(/new THREE\.PointLight\(TRACER_FLASH_COLOR, 2\.4, 3\.2, TRACER_FLASH_DECAY\)/);
    expect(src).not.toMatch(/new THREE\.PointLight\(TRACER_FLASH_COLOR, 2\.4, 3\.2, 2\)/);
    expect(src).not.toMatch(/export const TRACER_FLASH_DISTANCE = 3\.2/);
    expect(src).toContain("t.flash.intensity = TRACER_FLASH_INTENSITY * op");
    expect(src).not.toMatch(/t\.flash\.intensity = 2\.4 \* op/);
    expect(src).toContain("export const TRACER_FLASH_INTENSITY = 2.76");
    expect(src).toContain("export const TRACER_FLASH_DECAY = 1.74");
    expect(src).toContain("export const TRACER_FLASH_COLOR = 0xffdd6e");
    expect(src).toContain("export const IMPACT_SPARK_LIGHT_DECAY = 1.74");
    expect(src).toContain("IMPACT_SPARK_LIGHT_DISTANCE,\n    IMPACT_SPARK_LIGHT_DECAY,");
    expect(src).not.toMatch(/IMPACT_SPARK_LIGHT_DISTANCE,\s*\n\s*2,/);
    expect(src).not.toMatch(/export const IMPACT_SPARK_LIGHT_DECAY = 2/);
    expect(src).toContain("const IMPACT_SPARK_LIGHT_DISTANCE = 1.8");
    expect(src).toContain("export const IMPACT_SPARK_LIGHT_COLOR = 0xffef93");
    expect(src).toContain("new THREE.PointLight(\n    IMPACT_SPARK_LIGHT_COLOR,");
    expect(src).toContain("export const MUZZLE_LIGHT_DECAY = 1.74");
    expect(src).toContain("MUZZLE_LIGHT_DISTANCE,\n    MUZZLE_LIGHT_DECAY,");
    expect(src).not.toMatch(/MUZZLE_LIGHT_DISTANCE,\s*\n\s*2,/);
    expect(src).not.toMatch(/export const MUZZLE_LIGHT_DECAY = 2/);
    expect(src).toContain("const MUZZLE_LIGHT_DISTANCE = 2.6");
    expect(src).toContain("export const MUZZLE_LIGHT_COLOR = 0xffffb8");
    expect(src).toContain("new THREE.PointLight(\n    MUZZLE_LIGHT_COLOR,");
    expect(src).toContain("export const FLASHLIGHT_SPOT_DECAY = 1.74");
    expect(src).toContain("FLASHLIGHT_SPOT_PENUMBRA,\n    FLASHLIGHT_SPOT_DECAY,");
    expect(src).not.toMatch(/FLASHLIGHT_SPOT_PENUMBRA,\s*\n\s*2,/);
    expect(src).not.toMatch(/export const FLASHLIGHT_SPOT_DECAY = 2/);
    expect(src).toContain("FLASHLIGHT_CONE_LENGTH + 2.4");
    expect(src).toContain("export const FLASHLIGHT_FILL_DECAY = 1.74");
    expect(src).toContain("new THREE.PointLight(FLASHLIGHT_FILL_COLOR, 0, 10, FLASHLIGHT_FILL_DECAY)");
    expect(src).not.toMatch(/new THREE\.PointLight\(FLASHLIGHT_FILL_COLOR, 0, 10, 2\)/);
    expect(src).not.toMatch(/export const FLASHLIGHT_FILL_DECAY = 2/);
    expect(src).toContain("export const WARM_LIGHT_DECAY = 1.74");
    expect(src).toContain("new THREE.PointLight(WARM_LIGHT_COLOR, 0, 7.5, WARM_LIGHT_DECAY)");
    expect(src).not.toMatch(/new THREE\.PointLight\(WARM_LIGHT_COLOR, 0, 7\.5, 2\)/);
    expect(src).toContain("export const WARM_LIGHT_VISIBLE_EPS = 0.0174");
    expect(src).toContain("warmLight.visible = i > WARM_LIGHT_VISIBLE_EPS");
    expect(src).not.toMatch(/warmLight\.visible = i > 0\.02/);
    expect(src).not.toMatch(/export const WARM_LIGHT_VISIBLE_EPS = 0\.02/);
    expect(src).toContain("export const WARM_LIGHT_AMBER_B_GAIN = 0.115");
    expect(src).toContain("export const WARM_LIGHT_AMBER_B = 0.437");
    expect(src).toContain("export const WARM_LIGHT_AMBER_G_GAIN = 0.092");
    expect(src).toContain("export const WARM_LIGHT_AMBER_G = 0.759");
    expect(src).toContain(
      "warmLight.color.setRGB(1, WARM_LIGHT_AMBER_G + i * WARM_LIGHT_AMBER_G_GAIN, WARM_LIGHT_AMBER_B + i * WARM_LIGHT_AMBER_B_GAIN)",
    );
    expect(src).not.toMatch(/WARM_LIGHT_AMBER_B \+ i \* 0\.1/);
    expect(src).toContain("export const WARM_LIGHT_DISTANCE_GAIN = 2.875");
    expect(src).toContain("export const WARM_LIGHT_DISTANCE_BASE = 7.475");
    expect(src).toContain(
      "warmLight.distance = WARM_LIGHT_DISTANCE_BASE + i * WARM_LIGHT_DISTANCE_GAIN",
    );
    expect(src).not.toMatch(/export const WARM_LIGHT_DISTANCE_GAIN = 2\.5/);
    expect(src).not.toMatch(
      /warmLight\.distance = WARM_LIGHT_DISTANCE_BASE \+ i \* 2\.5/,
    );
    expect(src).not.toMatch(/warmLight\.distance = 6\.5 \+ i \* 2\.5/);
    expect(src).toContain("export const WARM_LIGHT_Y = 1.7825");
    expect(src).toContain("warmLight.position.set(wx, WARM_LIGHT_Y, wy)");
    expect(src).not.toMatch(/export const WARM_LIGHT_Y = 1\.55/);
    expect(src).not.toMatch(/warmLight\.position\.set\(wx, 1\.55, wy\)/);
    expect(src).toContain("warmLight.position.set(0, 1.6, 0)");
    expect(src).toContain("export const WARM_LIGHT_INTENSITY_MUL = 1.7825");
    expect(src).toContain("warmLight.intensity = i * WARM_LIGHT_INTENSITY_MUL");
    expect(src).not.toMatch(/export const WARM_LIGHT_INTENSITY_MUL = 1\.55/);
    expect(src).not.toMatch(/warmLight\.intensity = i \* 1\.55/);
    expect(src).toContain("export const FLASHLIGHT_SPOT_DISTANCE_GAIN = 2.3");
    expect(src).toContain("export const FLASHLIGHT_SPOT_DISTANCE_EXTRA = 1.84");
    expect(src).toContain(
      "torchSpot.distance = FLASHLIGHT_CONE_LENGTH + FLASHLIGHT_SPOT_DISTANCE_EXTRA + i * FLASHLIGHT_SPOT_DISTANCE_GAIN",
    );
    expect(src).toContain("export const FLASHLIGHT_SPOT_Y = 1.7825");
    expect(src).toContain("torchSpot.position.set(0, FLASHLIGHT_SPOT_Y, 0)");
    expect(src).toContain("torchSpot.position.set(wx, FLASHLIGHT_SPOT_Y, wy)");
    expect(src).toContain("export const FLASHLIGHT_FILL_Y = 1.5525");
    expect(src).toContain("torchLight.position.set(0, FLASHLIGHT_FILL_Y, 0)");
    expect(src).toContain("torchLight.position.set(wx, FLASHLIGHT_FILL_Y, wy)");
    expect(src).toContain("export const FLASHLIGHT_FILL_DISTANCE_GAIN = 4.025");
    expect(src).toContain("export const FLASHLIGHT_FILL_DISTANCE_BASE = 8.05");
    expect(src).toContain(
      "torchLight.distance = FLASHLIGHT_FILL_DISTANCE_BASE + i * FLASHLIGHT_FILL_DISTANCE_GAIN",
    );
    expect(src).toContain("export const FLASHLIGHT_FILL_COLOR = 0xcaefff");
    expect(src).toContain("torchLight.color.setHex(FLASHLIGHT_FILL_COLOR)");
    expect(src).toContain("export const TRACER_COLOR = 0xffffb8");
    expect(src).toContain("color: TRACER_COLOR");
    expect(src).toContain("export const IMPACT_SPARK_COLOR = 0xffef93");
    expect(src).toContain("color: IMPACT_SPARK_COLOR");
    expect(src).toContain("export const MUZZLE_FLASH_COLOR = 0xffffdd");
    expect(src).toContain("color: MUZZLE_FLASH_COLOR");
    expect(src).toContain("export const WARM_LIGHT_COLOR = 0xffca81");
    expect(src).toContain("export const POSSESSED_HEAD_EMISSIVE = 0x30124a");
    expect(src).toContain("emissive: POSSESSED_HEAD_EMISSIVE");
    expect(src).toContain("color: POSSESSED_HEAD_COLOR");
    expect(src).toContain("export const POSSESSED_HEAD_COLOR = 0x8c469f");
    expect(src).toContain("emissive: POSSESSED_EMISSIVE");
    expect(src).toContain("color: POSSESSED_COLOR");
    expect(src).toContain("const POSSESSED_EMISSIVE = 0x1e0925");
    expect(src).toContain("emissiveIntensity: 0.22");
    expect(src).toContain("color: HOSTILE_COLOR");
    expect(src).toContain("color: PLAYER_COLOR");
    expect(src).toContain("color: PLAYER_HEAD_COLOR");
    expect(src).toContain("emissive: PLAYER_HEAD_EMISSIVE");
    expect(TRACER_FLASH_COLOR).toBe(0xffdd6e);
    expect(TRACER_FLASH_INTENSITY).toBe(2.76);
    expect(TRACER_FLASH_DISTANCE).toBe(3.68);
    expect(TRACER_FLASH_DECAY).toBe(1.74);
    expect(TRACER_FLASH_Y_OFFSET).toBe(0.1725);
    expect(TRACER_COLOR).toBe(0xffffb8);
    expect(IMPACT_SPARK_LIGHT_COLOR).toBe(0xffef93);
    expect(IMPACT_SPARK_COLOR).toBe(0xffef93);
    expect(IMPACT_SPARK_LIGHT_DECAY).toBe(1.74);
    expect(MUZZLE_LIGHT_COLOR).toBe(0xffffb8);
    expect(MUZZLE_LIGHT_DECAY).toBe(1.74);
    expect(MUZZLE_FLASH_COLOR).toBe(0xffffdd);
    expect(WARM_LIGHT_COLOR).toBe(0xffca81);
    expect(WARM_LIGHT_DECAY).toBe(1.74);
    expect(WARM_LIGHT_VISIBLE_EPS).toBe(0.0174);
    expect(WARM_LIGHT_AMBER_B_GAIN).toBe(0.115);
    expect(WARM_LIGHT_AMBER_B).toBe(0.437);
    expect(WARM_LIGHT_AMBER_G_GAIN).toBe(0.092);
    expect(WARM_LIGHT_AMBER_G).toBe(0.759);
    expect(WARM_LIGHT_DISTANCE_GAIN).toBe(2.875);
    expect(WARM_LIGHT_DISTANCE_BASE).toBe(7.475);
    expect(WARM_LIGHT_Y).toBe(1.7825);
    expect(WARM_LIGHT_INTENSITY_MUL).toBe(1.7825);
    expect(POSSESSED_HEAD_EMISSIVE).toBe(0x30124a);
    expect(POSSESSED_HEAD_COLOR).toBe(0x8c469f);
    expect(POSSESSED_COLOR).toBe(0x68347b);
    expect(HOSTILE_COLOR).toBe(0x7b1e1e);
    expect(PLAYER_COLOR).toBe(0x55a4f4);
    expect(PLAYER_HEAD_COLOR).toBe(0x91d1ff);
    expect(PLAYER_HEAD_EMISSIVE).toBe(0x122537);
    expect(MARKER_PALETTE.possessed.emissive).toBe(0x30124a);
    expect(MARKER_PALETTE.player.badge).toBe(0x91d1ff);
    expect(MARKER_PALETTE.player.emissive).toBe(0x1e4a6e);
    expect(MARKER_PALETTE.player.ring).toBe(0x4392f4);
    expect(MARKER_RING_OPACITY).toBe(0.6877);
    expect(muteBadgeY).toBe(2.3);
    expect(possessedBadgeY).toBe(2.3);
  });
});

describe("worldView muzzle forward", () => {
  test("muzzle forward 0.48 × 1.15 → 0.552; spot-target-y/y-offset/distance/intensity/decay/impact-spark-decay/muzzle-decay/spot-decay/fill-decay/warm-decay/visible-eps/amber-B-gain/amber-B/amber-G-gain/amber-G/gain/base/Y/intensity/color/spot-gain/extra/spot-Y/fill-Y/gain/base/flash/mesh/color/possessed/hostile/player/palette/badge-Y iguales", () => {
    expect(MUZZLE_FORWARD).toBe(0.552);
    expect(MUZZLE_FORWARD).toBeCloseTo(0.48 * 1.15, 10);
    expect(CHEVRON_Y).toBe(0.138);
    expect(FLASHLIGHT_SPOT_TARGET_Y).toBe(0.138);
    expect(TRACER_FLASH_Y_OFFSET).toBe(0.1725);
    expect(TRACER_FLASH_DISTANCE).toBe(3.68);
    expect(TRACER_FLASH_INTENSITY).toBe(2.76);
    expect(TRACER_FLASH_DECAY).toBe(1.74);
    expect(IMPACT_SPARK_LIGHT_DECAY).toBe(1.74);
    expect(MUZZLE_LIGHT_DECAY).toBe(1.74);
    expect(FLASHLIGHT_SPOT_DECAY).toBe(1.74);
    expect(FLASHLIGHT_FILL_DECAY).toBe(1.74);
    expect(WARM_LIGHT_DECAY).toBe(1.74);
    expect(WARM_LIGHT_VISIBLE_EPS).toBe(0.0174);
    expect(WARM_LIGHT_AMBER_B_GAIN).toBe(0.115);
    expect(WARM_LIGHT_AMBER_B).toBe(0.437);
    expect(WARM_LIGHT_AMBER_G_GAIN).toBe(0.092);
    expect(WARM_LIGHT_AMBER_G).toBe(0.759);
    expect(WARM_LIGHT_DISTANCE_GAIN).toBe(2.875);
    expect(WARM_LIGHT_DISTANCE_BASE).toBe(7.475);
    expect(WARM_LIGHT_Y).toBe(1.7825);
    expect(WARM_LIGHT_INTENSITY_MUL).toBe(1.7825);
    expect(FLASHLIGHT_SPOT_DISTANCE_GAIN).toBe(2.3);
    expect(FLASHLIGHT_SPOT_DISTANCE_EXTRA).toBe(1.84);
    expect(FLASHLIGHT_SPOT_Y).toBe(1.7825);
    expect(FLASHLIGHT_FILL_Y).toBe(1.5525);
    expect(FLASHLIGHT_FILL_DISTANCE_GAIN).toBe(4.025);
    expect(FLASHLIGHT_FILL_DISTANCE_BASE).toBe(8.05);
    expect(FLASHLIGHT_FILL_COLOR).toBe(0xcaefff);
    expect(MUZZLE_LIGHT_COLOR).toBe(0xffffb8);
    expect(IMPACT_SPARK_LIGHT_COLOR).toBe(0xffef93);
    expect(TRACER_FLASH_COLOR).toBe(0xffdd6e);
    const src = readFileSync(
      resolve(process.cwd(), "src/render/worldView.ts"),
      "utf8",
    );
    expect(src).toContain("export const MUZZLE_FORWARD = 0.552");
    expect(src).toContain("Math.sin(playerGltfYaw) * MUZZLE_FORWARD");
    expect(src).toContain("Math.cos(playerGltfYaw) * MUZZLE_FORWARD");
    expect(src).toContain("muzzleMesh.position.set(ox, TRACER_HEIGHT, oz)");
    expect(src).toContain("muzzleLight.position.set(ox, TRACER_HEIGHT, oz)");
    expect(src).not.toMatch(/const MUZZLE_FORWARD = 0\.48/);
    expect(src).not.toMatch(/export const MUZZLE_FORWARD = 0\.48/);
    expect(src).toContain("export const CHEVRON_Y = 0.138");
    expect(src).toContain("chevronMesh.position.set(x, CHEVRON_Y, z)");
    expect(src).not.toMatch(/const CHEVRON_Y = 0\.12/);
    expect(src).not.toMatch(/export const CHEVRON_Y = 0\.12/);
    expect(src).not.toMatch(/chevronMesh\.position\.set\(x, 0\.12, z\)/);
    expect(src).toContain("export const FLASHLIGHT_SPOT_TARGET_Y = 0.138");
    expect(src).toContain("torchSpot.target.position.set(wx + tip.x, FLASHLIGHT_SPOT_TARGET_Y, wy + tip.z)");
    expect(src).not.toMatch(/torchSpot\.target\.position\.set\(wx \+ tip\.x, 0\.12, wy \+ tip\.z\)/);
    expect(src).not.toMatch(/export const FLASHLIGHT_SPOT_TARGET_Y = 0\.12/);
    expect(src).toContain("export const TRACER_FLASH_Y_OFFSET = 0.1725");
    expect(src).toContain("flash.position.set(from.x, TRACER_HEIGHT + TRACER_FLASH_Y_OFFSET, from.y)");
    expect(src).not.toMatch(/flash\.position\.set\(from\.x, TRACER_HEIGHT \+ 0\.15, from\.y\)/);
    expect(src).not.toMatch(/export const TRACER_FLASH_Y_OFFSET = 0\.15/);
    expect(src).toContain("export const TRACER_FLASH_DISTANCE = 3.68");
    expect(src).toContain("new THREE.PointLight(TRACER_FLASH_COLOR, TRACER_FLASH_INTENSITY, TRACER_FLASH_DISTANCE, TRACER_FLASH_DECAY)");
    expect(src).not.toMatch(/new THREE\.PointLight\(TRACER_FLASH_COLOR, TRACER_FLASH_INTENSITY, 3\.2, TRACER_FLASH_DECAY\)/);
    expect(src).not.toMatch(/new THREE\.PointLight\(TRACER_FLASH_COLOR, 2\.4, 3\.2, TRACER_FLASH_DECAY\)/);
    expect(src).not.toMatch(/new THREE\.PointLight\(TRACER_FLASH_COLOR, 2\.4, 3\.2, 2\)/);
    expect(src).not.toMatch(/export const TRACER_FLASH_DISTANCE = 3\.2/);
    expect(src).toContain("t.flash.intensity = TRACER_FLASH_INTENSITY * op");
    expect(src).not.toMatch(/t\.flash\.intensity = 2\.4 \* op/);
    expect(src).toContain("export const TRACER_FLASH_INTENSITY = 2.76");
    expect(src).toContain("export const TRACER_FLASH_DECAY = 1.74");
    expect(src).toContain("export const TRACER_FLASH_COLOR = 0xffdd6e");
    expect(src).toContain("export const IMPACT_SPARK_LIGHT_DECAY = 1.74");
    expect(src).toContain("IMPACT_SPARK_LIGHT_DISTANCE,\n    IMPACT_SPARK_LIGHT_DECAY,");
    expect(src).not.toMatch(/IMPACT_SPARK_LIGHT_DISTANCE,\s*\n\s*2,/);
    expect(src).not.toMatch(/export const IMPACT_SPARK_LIGHT_DECAY = 2/);
    expect(src).toContain("const IMPACT_SPARK_LIGHT_DISTANCE = 1.8");
    expect(src).toContain("export const IMPACT_SPARK_LIGHT_COLOR = 0xffef93");
    expect(src).toContain("new THREE.PointLight(\n    IMPACT_SPARK_LIGHT_COLOR,");
    expect(src).toContain("export const MUZZLE_LIGHT_DECAY = 1.74");
    expect(src).toContain("MUZZLE_LIGHT_DISTANCE,\n    MUZZLE_LIGHT_DECAY,");
    expect(src).not.toMatch(/MUZZLE_LIGHT_DISTANCE,\s*\n\s*2,/);
    expect(src).not.toMatch(/export const MUZZLE_LIGHT_DECAY = 2/);
    expect(src).toContain("const MUZZLE_LIGHT_DISTANCE = 2.6");
    expect(src).toContain("export const MUZZLE_LIGHT_COLOR = 0xffffb8");
    expect(src).toContain("new THREE.PointLight(\n    MUZZLE_LIGHT_COLOR,");
    expect(src).toContain("export const FLASHLIGHT_SPOT_DECAY = 1.74");
    expect(src).toContain("FLASHLIGHT_SPOT_PENUMBRA,\n    FLASHLIGHT_SPOT_DECAY,");
    expect(src).not.toMatch(/FLASHLIGHT_SPOT_PENUMBRA,\s*\n\s*2,/);
    expect(src).not.toMatch(/export const FLASHLIGHT_SPOT_DECAY = 2/);
    expect(src).toContain("FLASHLIGHT_CONE_LENGTH + 2.4");
    expect(src).toContain("export const FLASHLIGHT_FILL_DECAY = 1.74");
    expect(src).toContain("new THREE.PointLight(FLASHLIGHT_FILL_COLOR, 0, 10, FLASHLIGHT_FILL_DECAY)");
    expect(src).not.toMatch(/new THREE\.PointLight\(FLASHLIGHT_FILL_COLOR, 0, 10, 2\)/);
    expect(src).not.toMatch(/export const FLASHLIGHT_FILL_DECAY = 2/);
    expect(src).toContain("export const WARM_LIGHT_DECAY = 1.74");
    expect(src).toContain("new THREE.PointLight(WARM_LIGHT_COLOR, 0, 7.5, WARM_LIGHT_DECAY)");
    expect(src).not.toMatch(/new THREE\.PointLight\(WARM_LIGHT_COLOR, 0, 7\.5, 2\)/);
    expect(src).toContain("export const WARM_LIGHT_VISIBLE_EPS = 0.0174");
    expect(src).toContain("warmLight.visible = i > WARM_LIGHT_VISIBLE_EPS");
    expect(src).not.toMatch(/warmLight\.visible = i > 0\.02/);
    expect(src).not.toMatch(/export const WARM_LIGHT_VISIBLE_EPS = 0\.02/);
    expect(src).toContain("export const WARM_LIGHT_AMBER_B_GAIN = 0.115");
    expect(src).toContain("export const WARM_LIGHT_AMBER_B = 0.437");
    expect(src).toContain("export const WARM_LIGHT_AMBER_G_GAIN = 0.092");
    expect(src).toContain("export const WARM_LIGHT_AMBER_G = 0.759");
    expect(src).toContain(
      "warmLight.color.setRGB(1, WARM_LIGHT_AMBER_G + i * WARM_LIGHT_AMBER_G_GAIN, WARM_LIGHT_AMBER_B + i * WARM_LIGHT_AMBER_B_GAIN)",
    );
    expect(src).not.toMatch(/WARM_LIGHT_AMBER_B \+ i \* 0\.1/);
    expect(src).toContain("export const WARM_LIGHT_DISTANCE_GAIN = 2.875");
    expect(src).toContain("export const WARM_LIGHT_DISTANCE_BASE = 7.475");
    expect(src).toContain(
      "warmLight.distance = WARM_LIGHT_DISTANCE_BASE + i * WARM_LIGHT_DISTANCE_GAIN",
    );
    expect(src).not.toMatch(/export const WARM_LIGHT_DISTANCE_GAIN = 2\.5/);
    expect(src).not.toMatch(
      /warmLight\.distance = WARM_LIGHT_DISTANCE_BASE \+ i \* 2\.5/,
    );
    expect(src).not.toMatch(/warmLight\.distance = 6\.5 \+ i \* 2\.5/);
    expect(src).toContain("export const WARM_LIGHT_Y = 1.7825");
    expect(src).toContain("warmLight.position.set(wx, WARM_LIGHT_Y, wy)");
    expect(src).not.toMatch(/export const WARM_LIGHT_Y = 1\.55/);
    expect(src).not.toMatch(/warmLight\.position\.set\(wx, 1\.55, wy\)/);
    expect(src).toContain("warmLight.position.set(0, 1.6, 0)");
    expect(src).toContain("export const WARM_LIGHT_INTENSITY_MUL = 1.7825");
    expect(src).toContain("warmLight.intensity = i * WARM_LIGHT_INTENSITY_MUL");
    expect(src).not.toMatch(/export const WARM_LIGHT_INTENSITY_MUL = 1\.55/);
    expect(src).not.toMatch(/warmLight\.intensity = i \* 1\.55/);
    expect(src).toContain("export const FLASHLIGHT_SPOT_DISTANCE_GAIN = 2.3");
    expect(src).toContain("export const FLASHLIGHT_SPOT_DISTANCE_EXTRA = 1.84");
    expect(src).toContain(
      "torchSpot.distance = FLASHLIGHT_CONE_LENGTH + FLASHLIGHT_SPOT_DISTANCE_EXTRA + i * FLASHLIGHT_SPOT_DISTANCE_GAIN",
    );
    expect(src).toContain("export const FLASHLIGHT_SPOT_Y = 1.7825");
    expect(src).toContain("torchSpot.position.set(0, FLASHLIGHT_SPOT_Y, 0)");
    expect(src).toContain("torchSpot.position.set(wx, FLASHLIGHT_SPOT_Y, wy)");
    expect(src).toContain("export const FLASHLIGHT_FILL_Y = 1.5525");
    expect(src).toContain("torchLight.position.set(0, FLASHLIGHT_FILL_Y, 0)");
    expect(src).toContain("torchLight.position.set(wx, FLASHLIGHT_FILL_Y, wy)");
    expect(src).toContain("export const FLASHLIGHT_FILL_DISTANCE_GAIN = 4.025");
    expect(src).toContain("export const FLASHLIGHT_FILL_DISTANCE_BASE = 8.05");
    expect(src).toContain(
      "torchLight.distance = FLASHLIGHT_FILL_DISTANCE_BASE + i * FLASHLIGHT_FILL_DISTANCE_GAIN",
    );
    expect(src).toContain("export const FLASHLIGHT_FILL_COLOR = 0xcaefff");
    expect(src).toContain("torchLight.color.setHex(FLASHLIGHT_FILL_COLOR)");
    expect(src).toContain("export const TRACER_COLOR = 0xffffb8");
    expect(src).toContain("color: TRACER_COLOR");
    expect(src).toContain("export const IMPACT_SPARK_COLOR = 0xffef93");
    expect(src).toContain("color: IMPACT_SPARK_COLOR");
    expect(src).toContain("export const MUZZLE_FLASH_COLOR = 0xffffdd");
    expect(src).toContain("color: MUZZLE_FLASH_COLOR");
    expect(src).toContain("export const WARM_LIGHT_COLOR = 0xffca81");
    expect(src).toContain("export const POSSESSED_HEAD_EMISSIVE = 0x30124a");
    expect(src).toContain("emissive: POSSESSED_HEAD_EMISSIVE");
    expect(src).toContain("color: POSSESSED_HEAD_COLOR");
    expect(src).toContain("export const POSSESSED_HEAD_COLOR = 0x8c469f");
    expect(src).toContain("emissive: POSSESSED_EMISSIVE");
    expect(src).toContain("color: POSSESSED_COLOR");
    expect(src).toContain("const POSSESSED_EMISSIVE = 0x1e0925");
    expect(src).toContain("emissiveIntensity: 0.22");
    expect(src).toContain("color: HOSTILE_COLOR");
    expect(src).toContain("color: PLAYER_COLOR");
    expect(src).toContain("color: PLAYER_HEAD_COLOR");
    expect(src).toContain("emissive: PLAYER_HEAD_EMISSIVE");
    expect(TRACER_FLASH_COLOR).toBe(0xffdd6e);
    expect(TRACER_FLASH_INTENSITY).toBe(2.76);
    expect(TRACER_FLASH_DISTANCE).toBe(3.68);
    expect(TRACER_FLASH_DECAY).toBe(1.74);
    expect(TRACER_FLASH_Y_OFFSET).toBe(0.1725);
    expect(TRACER_COLOR).toBe(0xffffb8);
    expect(IMPACT_SPARK_LIGHT_COLOR).toBe(0xffef93);
    expect(IMPACT_SPARK_COLOR).toBe(0xffef93);
    expect(IMPACT_SPARK_LIGHT_DECAY).toBe(1.74);
    expect(MUZZLE_LIGHT_COLOR).toBe(0xffffb8);
    expect(MUZZLE_LIGHT_DECAY).toBe(1.74);
    expect(MUZZLE_FLASH_COLOR).toBe(0xffffdd);
    expect(WARM_LIGHT_COLOR).toBe(0xffca81);
    expect(WARM_LIGHT_DECAY).toBe(1.74);
    expect(WARM_LIGHT_VISIBLE_EPS).toBe(0.0174);
    expect(WARM_LIGHT_AMBER_B_GAIN).toBe(0.115);
    expect(WARM_LIGHT_AMBER_B).toBe(0.437);
    expect(WARM_LIGHT_AMBER_G_GAIN).toBe(0.092);
    expect(WARM_LIGHT_AMBER_G).toBe(0.759);
    expect(WARM_LIGHT_DISTANCE_GAIN).toBe(2.875);
    expect(WARM_LIGHT_DISTANCE_BASE).toBe(7.475);
    expect(WARM_LIGHT_Y).toBe(1.7825);
    expect(WARM_LIGHT_INTENSITY_MUL).toBe(1.7825);
    expect(POSSESSED_HEAD_EMISSIVE).toBe(0x30124a);
    expect(POSSESSED_HEAD_COLOR).toBe(0x8c469f);
    expect(POSSESSED_COLOR).toBe(0x68347b);
    expect(HOSTILE_COLOR).toBe(0x7b1e1e);
    expect(PLAYER_COLOR).toBe(0x55a4f4);
    expect(PLAYER_HEAD_COLOR).toBe(0x91d1ff);
    expect(PLAYER_HEAD_EMISSIVE).toBe(0x122537);
    expect(MARKER_PALETTE.possessed.emissive).toBe(0x30124a);
    expect(MARKER_PALETTE.player.badge).toBe(0x91d1ff);
    expect(MARKER_PALETTE.player.emissive).toBe(0x1e4a6e);
    expect(MARKER_PALETTE.player.ring).toBe(0x4392f4);
    expect(MARKER_RING_OPACITY).toBe(0.6877);
    expect(muteBadgeY).toBe(2.3);
    expect(possessedBadgeY).toBe(2.3);
  });
});

describe("worldView facing chevron Y", () => {
  test("facing chevron Y 0.12 × 1.15 → 0.138; muzzle-forward/spot-target-y/y-offset/distance/intensity/decay/impact-spark-decay/muzzle-decay/spot-decay/fill-decay/warm-decay/visible-eps/amber-B-gain/amber-B/amber-G-gain/amber-G/gain/base/Y/intensity/color/spot-gain/extra/spot-Y/fill-Y/gain/base/flash/mesh/color/possessed/hostile/player/palette/badge-Y iguales", () => {
    expect(CHEVRON_Y).toBe(0.138);
    expect(CHEVRON_Y).toBeCloseTo(0.12 * 1.15, 10);
    expect(CHEVRON_TILT).toBe(-0.4025);
    expect(CHEVRON_TILT).toBeCloseTo(-0.35 * 1.15, 10);
    expect(MUZZLE_FORWARD).toBe(0.552);
    expect(FLASHLIGHT_SPOT_TARGET_Y).toBe(0.138);
    expect(TRACER_FLASH_Y_OFFSET).toBe(0.1725);
    expect(TRACER_FLASH_DISTANCE).toBe(3.68);
    expect(TRACER_FLASH_INTENSITY).toBe(2.76);
    expect(TRACER_FLASH_DECAY).toBe(1.74);
    expect(IMPACT_SPARK_LIGHT_DECAY).toBe(1.74);
    expect(MUZZLE_LIGHT_DECAY).toBe(1.74);
    expect(FLASHLIGHT_SPOT_DECAY).toBe(1.74);
    expect(FLASHLIGHT_FILL_DECAY).toBe(1.74);
    expect(WARM_LIGHT_DECAY).toBe(1.74);
    expect(WARM_LIGHT_VISIBLE_EPS).toBe(0.0174);
    expect(WARM_LIGHT_AMBER_B_GAIN).toBe(0.115);
    expect(WARM_LIGHT_AMBER_B).toBe(0.437);
    expect(WARM_LIGHT_AMBER_G_GAIN).toBe(0.092);
    expect(WARM_LIGHT_AMBER_G).toBe(0.759);
    expect(WARM_LIGHT_DISTANCE_GAIN).toBe(2.875);
    expect(WARM_LIGHT_DISTANCE_BASE).toBe(7.475);
    expect(WARM_LIGHT_Y).toBe(1.7825);
    expect(WARM_LIGHT_INTENSITY_MUL).toBe(1.7825);
    expect(FLASHLIGHT_SPOT_DISTANCE_GAIN).toBe(2.3);
    expect(FLASHLIGHT_SPOT_DISTANCE_EXTRA).toBe(1.84);
    expect(FLASHLIGHT_SPOT_Y).toBe(1.7825);
    expect(FLASHLIGHT_FILL_Y).toBe(1.5525);
    expect(FLASHLIGHT_FILL_DISTANCE_GAIN).toBe(4.025);
    expect(FLASHLIGHT_FILL_DISTANCE_BASE).toBe(8.05);
    expect(FLASHLIGHT_FILL_COLOR).toBe(0xcaefff);
    expect(MUZZLE_LIGHT_COLOR).toBe(0xffffb8);
    expect(IMPACT_SPARK_LIGHT_COLOR).toBe(0xffef93);
    expect(TRACER_FLASH_COLOR).toBe(0xffdd6e);
    const src = readFileSync(
      resolve(process.cwd(), "src/render/worldView.ts"),
      "utf8",
    );
    expect(src).toContain("export const CHEVRON_Y = 0.138");
    expect(src).toContain("chevronMesh.position.set(x, CHEVRON_Y, z)");
    expect(src).toContain("export const CHEVRON_TILT = -0.4025");
    expect(src).toContain("chevronMesh.rotation.x = CHEVRON_TILT");
    expect(src).not.toMatch(/chevronMesh\.rotation\.x = -0\.35/);
    expect(src).not.toMatch(/const CHEVRON_Y = 0\.12/);
    expect(src).not.toMatch(/export const CHEVRON_Y = 0\.12/);
    expect(src).not.toMatch(/chevronMesh\.position\.set\(x, 0\.12, z\)/);
    expect(src).toContain("export const MUZZLE_FORWARD = 0.552");
    expect(src).toContain("Math.sin(playerGltfYaw) * MUZZLE_FORWARD");
    expect(src).toContain("Math.cos(playerGltfYaw) * MUZZLE_FORWARD");
    expect(src).toContain("muzzleMesh.position.set(ox, TRACER_HEIGHT, oz)");
    expect(src).toContain("muzzleLight.position.set(ox, TRACER_HEIGHT, oz)");
    expect(src).not.toMatch(/const MUZZLE_FORWARD = 0\.48/);
    expect(src).not.toMatch(/export const MUZZLE_FORWARD = 0\.48/);
    expect(src).toContain("export const FLASHLIGHT_SPOT_TARGET_Y = 0.138");
    expect(src).toContain("torchSpot.target.position.set(wx + tip.x, FLASHLIGHT_SPOT_TARGET_Y, wy + tip.z)");
    expect(src).not.toMatch(/torchSpot\.target\.position\.set\(wx \+ tip\.x, 0\.12, wy \+ tip\.z\)/);
    expect(src).not.toMatch(/export const FLASHLIGHT_SPOT_TARGET_Y = 0\.12/);
    expect(src).toContain("export const TRACER_FLASH_Y_OFFSET = 0.1725");
    expect(src).toContain("flash.position.set(from.x, TRACER_HEIGHT + TRACER_FLASH_Y_OFFSET, from.y)");
    expect(src).not.toMatch(/flash\.position\.set\(from\.x, TRACER_HEIGHT \+ 0\.15, from\.y\)/);
    expect(src).not.toMatch(/export const TRACER_FLASH_Y_OFFSET = 0\.15/);
    expect(src).toContain("export const TRACER_FLASH_DISTANCE = 3.68");
    expect(src).toContain("new THREE.PointLight(TRACER_FLASH_COLOR, TRACER_FLASH_INTENSITY, TRACER_FLASH_DISTANCE, TRACER_FLASH_DECAY)");
    expect(src).not.toMatch(/new THREE\.PointLight\(TRACER_FLASH_COLOR, TRACER_FLASH_INTENSITY, 3\.2, TRACER_FLASH_DECAY\)/);
    expect(src).not.toMatch(/new THREE\.PointLight\(TRACER_FLASH_COLOR, 2\.4, 3\.2, TRACER_FLASH_DECAY\)/);
    expect(src).not.toMatch(/new THREE\.PointLight\(TRACER_FLASH_COLOR, 2\.4, 3\.2, 2\)/);
    expect(src).not.toMatch(/export const TRACER_FLASH_DISTANCE = 3\.2/);
    expect(src).toContain("t.flash.intensity = TRACER_FLASH_INTENSITY * op");
    expect(src).not.toMatch(/t\.flash\.intensity = 2\.4 \* op/);
    expect(src).toContain("export const TRACER_FLASH_INTENSITY = 2.76");
    expect(src).toContain("export const TRACER_FLASH_DECAY = 1.74");
    expect(src).toContain("export const TRACER_FLASH_COLOR = 0xffdd6e");
    expect(src).toContain("export const IMPACT_SPARK_LIGHT_DECAY = 1.74");
    expect(src).toContain("IMPACT_SPARK_LIGHT_DISTANCE,\n    IMPACT_SPARK_LIGHT_DECAY,");
    expect(src).not.toMatch(/IMPACT_SPARK_LIGHT_DISTANCE,\s*\n\s*2,/);
    expect(src).not.toMatch(/export const IMPACT_SPARK_LIGHT_DECAY = 2/);
    expect(src).toContain("const IMPACT_SPARK_LIGHT_DISTANCE = 1.8");
    expect(src).toContain("export const IMPACT_SPARK_LIGHT_COLOR = 0xffef93");
    expect(src).toContain("new THREE.PointLight(\n    IMPACT_SPARK_LIGHT_COLOR,");
    expect(src).toContain("export const MUZZLE_LIGHT_DECAY = 1.74");
    expect(src).toContain("MUZZLE_LIGHT_DISTANCE,\n    MUZZLE_LIGHT_DECAY,");
    expect(src).not.toMatch(/MUZZLE_LIGHT_DISTANCE,\s*\n\s*2,/);
    expect(src).not.toMatch(/export const MUZZLE_LIGHT_DECAY = 2/);
    expect(src).toContain("const MUZZLE_LIGHT_DISTANCE = 2.6");
    expect(src).toContain("export const MUZZLE_LIGHT_COLOR = 0xffffb8");
    expect(src).toContain("new THREE.PointLight(\n    MUZZLE_LIGHT_COLOR,");
    expect(src).toContain("export const FLASHLIGHT_SPOT_DECAY = 1.74");
    expect(src).toContain("FLASHLIGHT_SPOT_PENUMBRA,\n    FLASHLIGHT_SPOT_DECAY,");
    expect(src).not.toMatch(/FLASHLIGHT_SPOT_PENUMBRA,\s*\n\s*2,/);
    expect(src).not.toMatch(/export const FLASHLIGHT_SPOT_DECAY = 2/);
    expect(src).toContain("FLASHLIGHT_CONE_LENGTH + 2.4");
    expect(src).toContain("export const FLASHLIGHT_FILL_DECAY = 1.74");
    expect(src).toContain("new THREE.PointLight(FLASHLIGHT_FILL_COLOR, 0, 10, FLASHLIGHT_FILL_DECAY)");
    expect(src).not.toMatch(/new THREE\.PointLight\(FLASHLIGHT_FILL_COLOR, 0, 10, 2\)/);
    expect(src).not.toMatch(/export const FLASHLIGHT_FILL_DECAY = 2/);
    expect(src).toContain("export const WARM_LIGHT_DECAY = 1.74");
    expect(src).toContain("new THREE.PointLight(WARM_LIGHT_COLOR, 0, 7.5, WARM_LIGHT_DECAY)");
    expect(src).not.toMatch(/new THREE\.PointLight\(WARM_LIGHT_COLOR, 0, 7\.5, 2\)/);
    expect(src).toContain("export const WARM_LIGHT_VISIBLE_EPS = 0.0174");
    expect(src).toContain("warmLight.visible = i > WARM_LIGHT_VISIBLE_EPS");
    expect(src).not.toMatch(/warmLight\.visible = i > 0\.02/);
    expect(src).not.toMatch(/export const WARM_LIGHT_VISIBLE_EPS = 0\.02/);
    expect(src).toContain("export const WARM_LIGHT_AMBER_B_GAIN = 0.115");
    expect(src).toContain("export const WARM_LIGHT_AMBER_B = 0.437");
    expect(src).toContain("export const WARM_LIGHT_AMBER_G_GAIN = 0.092");
    expect(src).toContain("export const WARM_LIGHT_AMBER_G = 0.759");
    expect(src).toContain(
      "warmLight.color.setRGB(1, WARM_LIGHT_AMBER_G + i * WARM_LIGHT_AMBER_G_GAIN, WARM_LIGHT_AMBER_B + i * WARM_LIGHT_AMBER_B_GAIN)",
    );
    expect(src).not.toMatch(/WARM_LIGHT_AMBER_B \+ i \* 0\.1/);
    expect(src).toContain("export const WARM_LIGHT_DISTANCE_GAIN = 2.875");
    expect(src).toContain("export const WARM_LIGHT_DISTANCE_BASE = 7.475");
    expect(src).toContain(
      "warmLight.distance = WARM_LIGHT_DISTANCE_BASE + i * WARM_LIGHT_DISTANCE_GAIN",
    );
    expect(src).not.toMatch(/export const WARM_LIGHT_DISTANCE_GAIN = 2\.5/);
    expect(src).not.toMatch(
      /warmLight\.distance = WARM_LIGHT_DISTANCE_BASE \+ i \* 2\.5/,
    );
    expect(src).not.toMatch(/warmLight\.distance = 6\.5 \+ i \* 2\.5/);
    expect(src).toContain("export const WARM_LIGHT_Y = 1.7825");
    expect(src).toContain("warmLight.position.set(wx, WARM_LIGHT_Y, wy)");
    expect(src).not.toMatch(/export const WARM_LIGHT_Y = 1\.55/);
    expect(src).not.toMatch(/warmLight\.position\.set\(wx, 1\.55, wy\)/);
    expect(src).toContain("warmLight.position.set(0, 1.6, 0)");
    expect(src).toContain("export const WARM_LIGHT_INTENSITY_MUL = 1.7825");
    expect(src).toContain("warmLight.intensity = i * WARM_LIGHT_INTENSITY_MUL");
    expect(src).not.toMatch(/export const WARM_LIGHT_INTENSITY_MUL = 1\.55/);
    expect(src).not.toMatch(/warmLight\.intensity = i \* 1\.55/);
    expect(src).toContain("export const FLASHLIGHT_SPOT_DISTANCE_GAIN = 2.3");
    expect(src).toContain("export const FLASHLIGHT_SPOT_DISTANCE_EXTRA = 1.84");
    expect(src).toContain(
      "torchSpot.distance = FLASHLIGHT_CONE_LENGTH + FLASHLIGHT_SPOT_DISTANCE_EXTRA + i * FLASHLIGHT_SPOT_DISTANCE_GAIN",
    );
    expect(src).toContain("export const FLASHLIGHT_SPOT_Y = 1.7825");
    expect(src).toContain("torchSpot.position.set(0, FLASHLIGHT_SPOT_Y, 0)");
    expect(src).toContain("torchSpot.position.set(wx, FLASHLIGHT_SPOT_Y, wy)");
    expect(src).toContain("export const FLASHLIGHT_FILL_Y = 1.5525");
    expect(src).toContain("torchLight.position.set(0, FLASHLIGHT_FILL_Y, 0)");
    expect(src).toContain("torchLight.position.set(wx, FLASHLIGHT_FILL_Y, wy)");
    expect(src).toContain("export const FLASHLIGHT_FILL_DISTANCE_GAIN = 4.025");
    expect(src).toContain("export const FLASHLIGHT_FILL_DISTANCE_BASE = 8.05");
    expect(src).toContain(
      "torchLight.distance = FLASHLIGHT_FILL_DISTANCE_BASE + i * FLASHLIGHT_FILL_DISTANCE_GAIN",
    );
    expect(src).toContain("export const FLASHLIGHT_FILL_COLOR = 0xcaefff");
    expect(src).toContain("torchLight.color.setHex(FLASHLIGHT_FILL_COLOR)");
    expect(src).toContain("export const TRACER_COLOR = 0xffffb8");
    expect(src).toContain("color: TRACER_COLOR");
    expect(src).toContain("export const IMPACT_SPARK_COLOR = 0xffef93");
    expect(src).toContain("color: IMPACT_SPARK_COLOR");
    expect(src).toContain("export const MUZZLE_FLASH_COLOR = 0xffffdd");
    expect(src).toContain("color: MUZZLE_FLASH_COLOR");
    expect(src).toContain("export const WARM_LIGHT_COLOR = 0xffca81");
    expect(src).toContain("export const POSSESSED_HEAD_EMISSIVE = 0x30124a");
    expect(src).toContain("emissive: POSSESSED_HEAD_EMISSIVE");
    expect(src).toContain("color: POSSESSED_HEAD_COLOR");
    expect(src).toContain("export const POSSESSED_HEAD_COLOR = 0x8c469f");
    expect(src).toContain("emissive: POSSESSED_EMISSIVE");
    expect(src).toContain("color: POSSESSED_COLOR");
    expect(src).toContain("const POSSESSED_EMISSIVE = 0x1e0925");
    expect(src).toContain("emissiveIntensity: 0.22");
    expect(src).toContain("color: HOSTILE_COLOR");
    expect(src).toContain("color: PLAYER_COLOR");
    expect(src).toContain("color: PLAYER_HEAD_COLOR");
    expect(src).toContain("emissive: PLAYER_HEAD_EMISSIVE");
    expect(TRACER_FLASH_COLOR).toBe(0xffdd6e);
    expect(TRACER_FLASH_INTENSITY).toBe(2.76);
    expect(TRACER_FLASH_DISTANCE).toBe(3.68);
    expect(TRACER_FLASH_DECAY).toBe(1.74);
    expect(TRACER_FLASH_Y_OFFSET).toBe(0.1725);
    expect(TRACER_COLOR).toBe(0xffffb8);
    expect(IMPACT_SPARK_LIGHT_COLOR).toBe(0xffef93);
    expect(IMPACT_SPARK_COLOR).toBe(0xffef93);
    expect(IMPACT_SPARK_LIGHT_DECAY).toBe(1.74);
    expect(MUZZLE_LIGHT_COLOR).toBe(0xffffb8);
    expect(MUZZLE_LIGHT_DECAY).toBe(1.74);
    expect(MUZZLE_FLASH_COLOR).toBe(0xffffdd);
    expect(WARM_LIGHT_COLOR).toBe(0xffca81);
    expect(WARM_LIGHT_DECAY).toBe(1.74);
    expect(WARM_LIGHT_VISIBLE_EPS).toBe(0.0174);
    expect(WARM_LIGHT_AMBER_B_GAIN).toBe(0.115);
    expect(WARM_LIGHT_AMBER_B).toBe(0.437);
    expect(WARM_LIGHT_AMBER_G_GAIN).toBe(0.092);
    expect(WARM_LIGHT_AMBER_G).toBe(0.759);
    expect(WARM_LIGHT_DISTANCE_GAIN).toBe(2.875);
    expect(WARM_LIGHT_DISTANCE_BASE).toBe(7.475);
    expect(WARM_LIGHT_Y).toBe(1.7825);
    expect(WARM_LIGHT_INTENSITY_MUL).toBe(1.7825);
    expect(POSSESSED_HEAD_EMISSIVE).toBe(0x30124a);
    expect(POSSESSED_HEAD_COLOR).toBe(0x8c469f);
    expect(POSSESSED_COLOR).toBe(0x68347b);
    expect(HOSTILE_COLOR).toBe(0x7b1e1e);
    expect(PLAYER_COLOR).toBe(0x55a4f4);
    expect(PLAYER_HEAD_COLOR).toBe(0x91d1ff);
    expect(PLAYER_HEAD_EMISSIVE).toBe(0x122537);
    expect(MARKER_PALETTE.possessed.emissive).toBe(0x30124a);
    expect(MARKER_PALETTE.player.badge).toBe(0x91d1ff);
    expect(MARKER_PALETTE.player.emissive).toBe(0x1e4a6e);
    expect(MARKER_PALETTE.player.ring).toBe(0x4392f4);
    expect(MARKER_RING_OPACITY).toBe(0.6877);
    expect(muteBadgeY).toBe(2.3);
    expect(possessedBadgeY).toBe(2.3);
  });
});

describe("worldView facing chevron tilt", () => {
  test("facing chevron tilt -0.35 × 1.15 → -0.4025; chevron-y/muzzle-forward/spot-target-y/y-offset/distance/intensity/decay/impact-spark-decay/muzzle-decay/spot-decay/fill-decay/warm-decay/visible-eps/amber-B-gain/amber-B/amber-G-gain/amber-G/gain/base/Y/intensity/color/spot-gain/extra/spot-Y/fill-Y/gain/base/flash/mesh/color/possessed/hostile/player/palette/badge-Y iguales", () => {
    expect(CHEVRON_TILT).toBe(-0.4025);
    expect(CHEVRON_TILT).toBeCloseTo(-0.35 * 1.15, 10);
    expect(NOISE_RING_Y).toBe(0.0575);
    expect(NOISE_RING_Y).toBeCloseTo(0.05 * 1.15, 10);
    expect(MARKER_RING_Y).toBe(0.046);
    expect(MARKER_RING_Y).toBeCloseTo(0.04 * 1.15, 10);
    expect(MARKER_ICON_Y).toBe(0.023);
    expect(MARKER_ICON_Y).toBeCloseTo(0.02 * 1.15, 10);
    expect(MARKER_ICON_SIZE).toBe(0.207);
    expect(MARKER_ICON_SIZE).toBeCloseTo(0.18 * 1.15, 10);
    expect(MARKER_BADGE_RADIUS).toBe(0.184);
    expect(MARKER_BADGE_RADIUS).toBeCloseTo(0.16 * 1.15, 10);
    expect(MARKER_BADGE_TILT).toBe(2.99);
    expect(MARKER_BADGE_TILT).toBeCloseTo(2.6 * 1.15, 10);
    expect(PLAYER_BODY_BASE_Y).toBe(0.644);
    expect(PLAYER_BODY_BASE_Y).toBeCloseTo(0.56 * 1.15, 10);
    expect(PLAYER_HEAD_BASE_Y).toBe(1.518);
    expect(PLAYER_HEAD_BASE_Y).toBeCloseTo(1.32 * 1.15, 10);
    expect(HOSTILE_BODY_BASE_Y).toBe(0.644);
    expect(HOSTILE_BODY_BASE_Y).toBeCloseTo(0.56 * 1.15, 10);
    expect(HOSTILE_HEAD_BASE_Y).toBe(1.495);
    expect(HOSTILE_HEAD_BASE_Y).toBeCloseTo(1.3 * 1.15, 10);
    expect(HOSTILE_MUTE_XZ_SCALE).toBe(1.2075);
    expect(HOSTILE_MUTE_XZ_SCALE).toBeCloseTo(1.05 * 1.15, 10);
    expect(CHEVRON_Y).toBe(0.138);
    expect(MUZZLE_FORWARD).toBe(0.552);
    expect(FLASHLIGHT_SPOT_TARGET_Y).toBe(0.138);
    expect(TRACER_FLASH_Y_OFFSET).toBe(0.1725);
    expect(TRACER_FLASH_DISTANCE).toBe(3.68);
    expect(TRACER_FLASH_INTENSITY).toBe(2.76);
    expect(TRACER_FLASH_DECAY).toBe(1.74);
    expect(IMPACT_SPARK_LIGHT_DECAY).toBe(1.74);
    expect(MUZZLE_LIGHT_DECAY).toBe(1.74);
    expect(FLASHLIGHT_SPOT_DECAY).toBe(1.74);
    expect(FLASHLIGHT_FILL_DECAY).toBe(1.74);
    expect(WARM_LIGHT_DECAY).toBe(1.74);
    expect(WARM_LIGHT_VISIBLE_EPS).toBe(0.0174);
    expect(WARM_LIGHT_AMBER_B_GAIN).toBe(0.115);
    expect(WARM_LIGHT_AMBER_B).toBe(0.437);
    expect(WARM_LIGHT_AMBER_G_GAIN).toBe(0.092);
    expect(WARM_LIGHT_AMBER_G).toBe(0.759);
    expect(WARM_LIGHT_DISTANCE_GAIN).toBe(2.875);
    expect(WARM_LIGHT_DISTANCE_BASE).toBe(7.475);
    expect(WARM_LIGHT_Y).toBe(1.7825);
    expect(WARM_LIGHT_INTENSITY_MUL).toBe(1.7825);
    expect(FLASHLIGHT_SPOT_DISTANCE_GAIN).toBe(2.3);
    expect(FLASHLIGHT_SPOT_DISTANCE_EXTRA).toBe(1.84);
    expect(FLASHLIGHT_SPOT_Y).toBe(1.7825);
    expect(FLASHLIGHT_FILL_Y).toBe(1.5525);
    expect(FLASHLIGHT_FILL_DISTANCE_GAIN).toBe(4.025);
    expect(FLASHLIGHT_FILL_DISTANCE_BASE).toBe(8.05);
    expect(FLASHLIGHT_FILL_COLOR).toBe(0xcaefff);
    expect(MUZZLE_LIGHT_COLOR).toBe(0xffffb8);
    expect(IMPACT_SPARK_LIGHT_COLOR).toBe(0xffef93);
    expect(TRACER_FLASH_COLOR).toBe(0xffdd6e);
    const src = readFileSync(
      resolve(process.cwd(), "src/render/worldView.ts"),
      "utf8",
    );
    expect(src).toContain("export const CHEVRON_TILT = -0.4025");
    expect(src).toContain("chevronMesh.rotation.x = CHEVRON_TILT");
    expect(src).not.toMatch(/chevronMesh\.rotation\.x = -0\.35/);
    expect(src).not.toMatch(/const CHEVRON_TILT = -0\.35/);
    expect(src).not.toMatch(/export const CHEVRON_TILT = -0\.35/);
    expect(src).toContain("export const NOISE_RING_Y = 0.0575");
    expect(src).toContain("mesh.position.y = NOISE_RING_Y");
    expect(src).toContain("slot.mesh.position.set(state.x, NOISE_RING_Y, state.y)");
    expect(src).not.toMatch(/const NOISE_RING_Y = 0\.05;/);
    expect(src).not.toMatch(/export const NOISE_RING_Y = 0\.05;/);
    expect(src).toContain("export const MARKER_RING_Y = 0.046");
    expect(src).toContain("ring.position.y = MARKER_RING_Y");
    expect(src).not.toMatch(/ring\.position\.y = 0\.04/);
    expect(src).not.toMatch(/const MARKER_RING_Y = 0\.04;/);
    expect(src).not.toMatch(/export const MARKER_RING_Y = 0\.04;/);
    expect(src).toContain("export const MARKER_ICON_Y = 0.023");
    expect(src).toContain("icon.position.y = MARKER_ICON_Y");
    expect(src).not.toMatch(/icon\.position\.y = 0\.02/);
    expect(src).not.toMatch(/const MARKER_ICON_Y = 0\.02;/);
    expect(src).not.toMatch(/export const MARKER_ICON_Y = 0\.02;/);
    expect(src).toContain("export const MARKER_ICON_SIZE = 0.207");
    expect(src).toContain("new THREE.PlaneGeometry(MARKER_ICON_SIZE, MARKER_ICON_SIZE)");
    expect(src).not.toMatch(/new THREE\.PlaneGeometry\(0\.18, 0\.18\)/);
    expect(src).not.toMatch(/const MARKER_ICON_SIZE = 0\.18/);
    expect(src).not.toMatch(/export const MARKER_ICON_SIZE = 0\.18/);
    expect(src).toContain("export const MARKER_BADGE_RADIUS = 0.184");
    expect(src).toContain("new THREE.CircleGeometry(MARKER_BADGE_RADIUS, 20)");
    expect(src).not.toMatch(/new THREE\.CircleGeometry\(0\.16, 20\)/);
    expect(src).not.toMatch(/const MARKER_BADGE_RADIUS = 0\.16/);
    expect(src).not.toMatch(/export const MARKER_BADGE_RADIUS = 0\.16/);
    expect(src).toContain("export const MARKER_BADGE_TILT = 2.99");
    expect(src).toContain("disc.rotation.x = -Math.PI / MARKER_BADGE_TILT");
    expect(src).toContain("icon.rotation.x = -Math.PI / MARKER_BADGE_TILT");
    expect(src).not.toMatch(/disc\.rotation\.x = -Math\.PI \/ 2\.6/);
    expect(src).not.toMatch(/icon\.rotation\.x = -Math\.PI \/ 2\.6/);
    expect(src).not.toMatch(/const MARKER_BADGE_TILT = 2\.6/);
    expect(src).not.toMatch(/export const MARKER_BADGE_TILT = 2\.6/);
    expect(src).toContain("export const PLAYER_BODY_BASE_Y = 0.644");
    expect(src).toContain("playerBody.position.y = PLAYER_BODY_BASE_Y");
    expect(src).not.toMatch(/playerBody\.position\.y = 0\.56/);
    expect(src).not.toMatch(/const PLAYER_BODY_BASE_Y = 0\.56/);
    expect(src).not.toMatch(/export const PLAYER_BODY_BASE_Y = 0\.56/);
    expect(src).toContain("export const PLAYER_HEAD_BASE_Y = 1.518");
    expect(src).toContain("playerHead.position.y = PLAYER_HEAD_BASE_Y");
    expect(src).not.toMatch(/playerHead\.position\.y = 1\.32/);
    expect(src).not.toMatch(/const PLAYER_HEAD_BASE_Y = 1\.32/);
    expect(src).not.toMatch(/export const PLAYER_HEAD_BASE_Y = 1\.32/);
    expect(src).toContain("export const HOSTILE_BODY_BASE_Y = 0.644");
    expect(src).toContain("mesh.position.y = HOSTILE_BODY_BASE_Y");
    expect(src).toContain("body.position.y = HOSTILE_BODY_BASE_Y");
    expect(src).not.toMatch(/mesh\.position\.y = 0\.56/);
    expect(src).not.toMatch(/body\.position\.y = 0\.56/);
    expect(src).not.toMatch(/const HOSTILE_BODY_BASE_Y = 0\.56/);
    expect(src).not.toMatch(/export const HOSTILE_BODY_BASE_Y = 0\.56/);
    expect(src).toContain("export const HOSTILE_HEAD_BASE_Y = 1.495");
    expect(src).toContain("head.position.y = HOSTILE_HEAD_BASE_Y");
    expect(src).not.toMatch(/head\.position\.y = 1\.3/);
    expect(src).not.toMatch(/const HOSTILE_HEAD_BASE_Y = 1\.3/);
    expect(src).not.toMatch(/export const HOSTILE_HEAD_BASE_Y = 1\.3/);
    expect(src).toContain("export const HOSTILE_MUTE_XZ_SCALE = 1.2075");
    expect(src).toContain("mesh.scale.set(HOSTILE_MUTE_XZ_SCALE, 1, HOSTILE_MUTE_XZ_SCALE)");
    expect(src).not.toMatch(/mesh\.scale\.set\(1\.05, 1, 1\.05\)/);
    expect(src).not.toMatch(/const HOSTILE_MUTE_XZ_SCALE = 1\.05/);
    expect(src).not.toMatch(/export const HOSTILE_MUTE_XZ_SCALE = 1\.05/);
    expect(src).toContain("export const CHEVRON_Y = 0.138");
    expect(src).toContain("chevronMesh.position.set(x, CHEVRON_Y, z)");
    expect(src).not.toMatch(/const CHEVRON_Y = 0\.12/);
    expect(src).not.toMatch(/export const CHEVRON_Y = 0\.12/);
    expect(src).not.toMatch(/chevronMesh\.position\.set\(x, 0\.12, z\)/);
    expect(src).toContain("export const MUZZLE_FORWARD = 0.552");

    expect(src).toContain("Math.sin(playerGltfYaw) * MUZZLE_FORWARD");
    expect(src).toContain("Math.cos(playerGltfYaw) * MUZZLE_FORWARD");
    expect(src).toContain("muzzleMesh.position.set(ox, TRACER_HEIGHT, oz)");
    expect(src).toContain("muzzleLight.position.set(ox, TRACER_HEIGHT, oz)");
    expect(src).not.toMatch(/const MUZZLE_FORWARD = 0\.48/);
    expect(src).not.toMatch(/export const MUZZLE_FORWARD = 0\.48/);
    expect(src).toContain("export const FLASHLIGHT_SPOT_TARGET_Y = 0.138");
    expect(src).toContain("torchSpot.target.position.set(wx + tip.x, FLASHLIGHT_SPOT_TARGET_Y, wy + tip.z)");
    expect(src).not.toMatch(/torchSpot\.target\.position\.set\(wx \+ tip\.x, 0\.12, wy \+ tip\.z\)/);
    expect(src).not.toMatch(/export const FLASHLIGHT_SPOT_TARGET_Y = 0\.12/);
    expect(src).toContain("export const TRACER_FLASH_Y_OFFSET = 0.1725");
    expect(src).toContain("flash.position.set(from.x, TRACER_HEIGHT + TRACER_FLASH_Y_OFFSET, from.y)");
    expect(src).not.toMatch(/flash\.position\.set\(from\.x, TRACER_HEIGHT \+ 0\.15, from\.y\)/);
    expect(src).not.toMatch(/export const TRACER_FLASH_Y_OFFSET = 0\.15/);
    expect(src).toContain("export const TRACER_FLASH_DISTANCE = 3.68");
    expect(src).toContain("new THREE.PointLight(TRACER_FLASH_COLOR, TRACER_FLASH_INTENSITY, TRACER_FLASH_DISTANCE, TRACER_FLASH_DECAY)");
    expect(src).not.toMatch(/new THREE\.PointLight\(TRACER_FLASH_COLOR, TRACER_FLASH_INTENSITY, 3\.2, TRACER_FLASH_DECAY\)/);
    expect(src).not.toMatch(/new THREE\.PointLight\(TRACER_FLASH_COLOR, 2\.4, 3\.2, TRACER_FLASH_DECAY\)/);
    expect(src).not.toMatch(/new THREE\.PointLight\(TRACER_FLASH_COLOR, 2\.4, 3\.2, 2\)/);
    expect(src).not.toMatch(/export const TRACER_FLASH_DISTANCE = 3\.2/);
    expect(src).toContain("t.flash.intensity = TRACER_FLASH_INTENSITY * op");
    expect(src).not.toMatch(/t\.flash\.intensity = 2\.4 \* op/);
    expect(src).toContain("export const TRACER_FLASH_INTENSITY = 2.76");
    expect(src).toContain("export const TRACER_FLASH_DECAY = 1.74");
    expect(src).toContain("export const TRACER_FLASH_COLOR = 0xffdd6e");
    expect(src).toContain("export const IMPACT_SPARK_LIGHT_DECAY = 1.74");
    expect(src).toContain("IMPACT_SPARK_LIGHT_DISTANCE,\n    IMPACT_SPARK_LIGHT_DECAY,");
    expect(src).not.toMatch(/IMPACT_SPARK_LIGHT_DISTANCE,\s*\n\s*2,/);
    expect(src).not.toMatch(/export const IMPACT_SPARK_LIGHT_DECAY = 2/);
    expect(src).toContain("const IMPACT_SPARK_LIGHT_DISTANCE = 1.8");
    expect(src).toContain("export const IMPACT_SPARK_LIGHT_COLOR = 0xffef93");
    expect(src).toContain("new THREE.PointLight(\n    IMPACT_SPARK_LIGHT_COLOR,");
    expect(src).toContain("export const MUZZLE_LIGHT_DECAY = 1.74");
    expect(src).toContain("MUZZLE_LIGHT_DISTANCE,\n    MUZZLE_LIGHT_DECAY,");
    expect(src).not.toMatch(/MUZZLE_LIGHT_DISTANCE,\s*\n\s*2,/);
    expect(src).not.toMatch(/export const MUZZLE_LIGHT_DECAY = 2/);
    expect(src).toContain("const MUZZLE_LIGHT_DISTANCE = 2.6");
    expect(src).toContain("export const MUZZLE_LIGHT_COLOR = 0xffffb8");
    expect(src).toContain("new THREE.PointLight(\n    MUZZLE_LIGHT_COLOR,");
    expect(src).toContain("export const FLASHLIGHT_SPOT_DECAY = 1.74");
    expect(src).toContain("FLASHLIGHT_SPOT_PENUMBRA,\n    FLASHLIGHT_SPOT_DECAY,");
    expect(src).not.toMatch(/FLASHLIGHT_SPOT_PENUMBRA,\s*\n\s*2,/);
    expect(src).not.toMatch(/export const FLASHLIGHT_SPOT_DECAY = 2/);
    expect(src).toContain("FLASHLIGHT_CONE_LENGTH + 2.4");
    expect(src).toContain("export const FLASHLIGHT_FILL_DECAY = 1.74");
    expect(src).toContain("new THREE.PointLight(FLASHLIGHT_FILL_COLOR, 0, 10, FLASHLIGHT_FILL_DECAY)");
    expect(src).not.toMatch(/new THREE\.PointLight\(FLASHLIGHT_FILL_COLOR, 0, 10, 2\)/);
    expect(src).not.toMatch(/export const FLASHLIGHT_FILL_DECAY = 2/);
    expect(src).toContain("export const WARM_LIGHT_DECAY = 1.74");
    expect(src).toContain("new THREE.PointLight(WARM_LIGHT_COLOR, 0, 7.5, WARM_LIGHT_DECAY)");
    expect(src).not.toMatch(/new THREE\.PointLight\(WARM_LIGHT_COLOR, 0, 7\.5, 2\)/);
    expect(src).toContain("export const WARM_LIGHT_VISIBLE_EPS = 0.0174");
    expect(src).toContain("warmLight.visible = i > WARM_LIGHT_VISIBLE_EPS");
    expect(src).not.toMatch(/warmLight\.visible = i > 0\.02/);
    expect(src).not.toMatch(/export const WARM_LIGHT_VISIBLE_EPS = 0\.02/);
    expect(src).toContain("export const WARM_LIGHT_AMBER_B_GAIN = 0.115");
    expect(src).toContain("export const WARM_LIGHT_AMBER_B = 0.437");
    expect(src).toContain("export const WARM_LIGHT_AMBER_G_GAIN = 0.092");
    expect(src).toContain("export const WARM_LIGHT_AMBER_G = 0.759");
    expect(src).toContain(
      "warmLight.color.setRGB(1, WARM_LIGHT_AMBER_G + i * WARM_LIGHT_AMBER_G_GAIN, WARM_LIGHT_AMBER_B + i * WARM_LIGHT_AMBER_B_GAIN)",
    );
    expect(src).not.toMatch(/WARM_LIGHT_AMBER_B \+ i \* 0\.1/);
    expect(src).toContain("export const WARM_LIGHT_DISTANCE_GAIN = 2.875");
    expect(src).toContain("export const WARM_LIGHT_DISTANCE_BASE = 7.475");
    expect(src).toContain(
      "warmLight.distance = WARM_LIGHT_DISTANCE_BASE + i * WARM_LIGHT_DISTANCE_GAIN",
    );
    expect(src).not.toMatch(/export const WARM_LIGHT_DISTANCE_GAIN = 2\.5/);
    expect(src).not.toMatch(
      /warmLight\.distance = WARM_LIGHT_DISTANCE_BASE \+ i \* 2\.5/,
    );
    expect(src).not.toMatch(/warmLight\.distance = 6\.5 \+ i \* 2\.5/);
    expect(src).toContain("export const WARM_LIGHT_Y = 1.7825");
    expect(src).toContain("warmLight.position.set(wx, WARM_LIGHT_Y, wy)");
    expect(src).not.toMatch(/export const WARM_LIGHT_Y = 1\.55/);
    expect(src).not.toMatch(/warmLight\.position\.set\(wx, 1\.55, wy\)/);
    expect(src).toContain("warmLight.position.set(0, 1.6, 0)");
    expect(src).toContain("export const WARM_LIGHT_INTENSITY_MUL = 1.7825");
    expect(src).toContain("warmLight.intensity = i * WARM_LIGHT_INTENSITY_MUL");
    expect(src).not.toMatch(/export const WARM_LIGHT_INTENSITY_MUL = 1\.55/);
    expect(src).not.toMatch(/warmLight\.intensity = i \* 1\.55/);
    expect(src).toContain("export const FLASHLIGHT_SPOT_DISTANCE_GAIN = 2.3");
    expect(src).toContain("export const FLASHLIGHT_SPOT_DISTANCE_EXTRA = 1.84");
    expect(src).toContain(
      "torchSpot.distance = FLASHLIGHT_CONE_LENGTH + FLASHLIGHT_SPOT_DISTANCE_EXTRA + i * FLASHLIGHT_SPOT_DISTANCE_GAIN",
    );
    expect(src).toContain("export const FLASHLIGHT_SPOT_Y = 1.7825");
    expect(src).toContain("torchSpot.position.set(0, FLASHLIGHT_SPOT_Y, 0)");
    expect(src).toContain("torchSpot.position.set(wx, FLASHLIGHT_SPOT_Y, wy)");
    expect(src).toContain("export const FLASHLIGHT_FILL_Y = 1.5525");
    expect(src).toContain("torchLight.position.set(0, FLASHLIGHT_FILL_Y, 0)");
    expect(src).toContain("torchLight.position.set(wx, FLASHLIGHT_FILL_Y, wy)");
    expect(src).toContain("export const FLASHLIGHT_FILL_DISTANCE_GAIN = 4.025");
    expect(src).toContain("export const FLASHLIGHT_FILL_DISTANCE_BASE = 8.05");
    expect(src).toContain(
      "torchLight.distance = FLASHLIGHT_FILL_DISTANCE_BASE + i * FLASHLIGHT_FILL_DISTANCE_GAIN",
    );
    expect(src).toContain("export const FLASHLIGHT_FILL_COLOR = 0xcaefff");
    expect(src).toContain("torchLight.color.setHex(FLASHLIGHT_FILL_COLOR)");
    expect(src).toContain("export const TRACER_COLOR = 0xffffb8");
    expect(src).toContain("color: TRACER_COLOR");
    expect(src).toContain("export const IMPACT_SPARK_COLOR = 0xffef93");
    expect(src).toContain("color: IMPACT_SPARK_COLOR");
    expect(src).toContain("export const MUZZLE_FLASH_COLOR = 0xffffdd");
    expect(src).toContain("color: MUZZLE_FLASH_COLOR");
    expect(src).toContain("export const WARM_LIGHT_COLOR = 0xffca81");
    expect(src).toContain("export const POSSESSED_HEAD_EMISSIVE = 0x30124a");
    expect(src).toContain("emissive: POSSESSED_HEAD_EMISSIVE");
    expect(src).toContain("color: POSSESSED_HEAD_COLOR");
    expect(src).toContain("export const POSSESSED_HEAD_COLOR = 0x8c469f");
    expect(src).toContain("emissive: POSSESSED_EMISSIVE");
    expect(src).toContain("color: POSSESSED_COLOR");
    expect(src).toContain("const POSSESSED_EMISSIVE = 0x1e0925");
    expect(src).toContain("emissiveIntensity: 0.22");
    expect(src).toContain("color: HOSTILE_COLOR");
    expect(src).toContain("color: PLAYER_COLOR");
    expect(src).toContain("color: PLAYER_HEAD_COLOR");
    expect(src).toContain("emissive: PLAYER_HEAD_EMISSIVE");
    expect(TRACER_FLASH_COLOR).toBe(0xffdd6e);
    expect(TRACER_FLASH_INTENSITY).toBe(2.76);
    expect(TRACER_FLASH_DISTANCE).toBe(3.68);
    expect(TRACER_FLASH_DECAY).toBe(1.74);
    expect(TRACER_FLASH_Y_OFFSET).toBe(0.1725);
    expect(TRACER_COLOR).toBe(0xffffb8);
    expect(IMPACT_SPARK_LIGHT_COLOR).toBe(0xffef93);
    expect(IMPACT_SPARK_COLOR).toBe(0xffef93);
    expect(IMPACT_SPARK_LIGHT_DECAY).toBe(1.74);
    expect(MUZZLE_LIGHT_COLOR).toBe(0xffffb8);
    expect(MUZZLE_LIGHT_DECAY).toBe(1.74);
    expect(MUZZLE_FLASH_COLOR).toBe(0xffffdd);
    expect(WARM_LIGHT_COLOR).toBe(0xffca81);
    expect(WARM_LIGHT_DECAY).toBe(1.74);
    expect(WARM_LIGHT_VISIBLE_EPS).toBe(0.0174);
    expect(WARM_LIGHT_AMBER_B_GAIN).toBe(0.115);
    expect(WARM_LIGHT_AMBER_B).toBe(0.437);
    expect(WARM_LIGHT_AMBER_G_GAIN).toBe(0.092);
    expect(WARM_LIGHT_AMBER_G).toBe(0.759);
    expect(WARM_LIGHT_DISTANCE_GAIN).toBe(2.875);
    expect(WARM_LIGHT_DISTANCE_BASE).toBe(7.475);
    expect(WARM_LIGHT_Y).toBe(1.7825);
    expect(WARM_LIGHT_INTENSITY_MUL).toBe(1.7825);
    expect(POSSESSED_HEAD_EMISSIVE).toBe(0x30124a);
    expect(POSSESSED_HEAD_COLOR).toBe(0x8c469f);
    expect(POSSESSED_COLOR).toBe(0x68347b);
    expect(HOSTILE_COLOR).toBe(0x7b1e1e);
    expect(PLAYER_COLOR).toBe(0x55a4f4);
    expect(PLAYER_HEAD_COLOR).toBe(0x91d1ff);
    expect(PLAYER_HEAD_EMISSIVE).toBe(0x122537);
    expect(MARKER_PALETTE.possessed.emissive).toBe(0x30124a);
    expect(MARKER_PALETTE.player.badge).toBe(0x91d1ff);
    expect(MARKER_PALETTE.player.emissive).toBe(0x1e4a6e);
    expect(MARKER_PALETTE.player.ring).toBe(0x4392f4);
    expect(MARKER_RING_OPACITY).toBe(0.6877);
    expect(muteBadgeY).toBe(2.3);
    expect(possessedBadgeY).toBe(2.3);
  });
});

describe("worldView noise ring Y", () => {
  test("noise ring Y 0.05 × 1.15 → 0.0575; chevron-tilt/chevron-y/muzzle-forward/spot-target-y/y-offset/distance/intensity/decay/impact-spark-decay/muzzle-decay/spot-decay/fill-decay/warm-decay/visible-eps/amber-B-gain/amber-B/amber-G-gain/amber-G/gain/base/Y/intensity/color/spot-gain/extra/spot-Y/fill-Y/gain/base/flash/mesh/color/possessed/hostile/player/palette/badge-Y iguales", () => {
    expect(NOISE_RING_Y).toBe(0.0575);
    expect(NOISE_RING_Y).toBeCloseTo(0.05 * 1.15, 10);
    expect(MARKER_RING_Y).toBe(0.046);
    expect(MARKER_RING_Y).toBeCloseTo(0.04 * 1.15, 10);
    expect(MARKER_ICON_Y).toBe(0.023);
    expect(MARKER_ICON_Y).toBeCloseTo(0.02 * 1.15, 10);
    expect(MARKER_ICON_SIZE).toBe(0.207);
    expect(MARKER_ICON_SIZE).toBeCloseTo(0.18 * 1.15, 10);
    expect(MARKER_BADGE_RADIUS).toBe(0.184);
    expect(MARKER_BADGE_RADIUS).toBeCloseTo(0.16 * 1.15, 10);
    expect(MARKER_BADGE_TILT).toBe(2.99);
    expect(MARKER_BADGE_TILT).toBeCloseTo(2.6 * 1.15, 10);
    expect(PLAYER_BODY_BASE_Y).toBe(0.644);
    expect(PLAYER_BODY_BASE_Y).toBeCloseTo(0.56 * 1.15, 10);
    expect(PLAYER_HEAD_BASE_Y).toBe(1.518);
    expect(PLAYER_HEAD_BASE_Y).toBeCloseTo(1.32 * 1.15, 10);
    expect(HOSTILE_BODY_BASE_Y).toBe(0.644);
    expect(HOSTILE_BODY_BASE_Y).toBeCloseTo(0.56 * 1.15, 10);
    expect(HOSTILE_HEAD_BASE_Y).toBe(1.495);
    expect(HOSTILE_HEAD_BASE_Y).toBeCloseTo(1.3 * 1.15, 10);
    expect(HOSTILE_MUTE_XZ_SCALE).toBe(1.2075);
    expect(HOSTILE_MUTE_XZ_SCALE).toBeCloseTo(1.05 * 1.15, 10);
    expect(CHEVRON_TILT).toBe(-0.4025);
    expect(CHEVRON_Y).toBe(0.138);
    expect(MUZZLE_FORWARD).toBe(0.552);
    expect(FLASHLIGHT_SPOT_TARGET_Y).toBe(0.138);
    expect(TRACER_FLASH_Y_OFFSET).toBe(0.1725);
    expect(TRACER_FLASH_DISTANCE).toBe(3.68);
    expect(TRACER_FLASH_INTENSITY).toBe(2.76);
    expect(TRACER_FLASH_DECAY).toBe(1.74);
    expect(IMPACT_SPARK_LIGHT_DECAY).toBe(1.74);
    expect(MUZZLE_LIGHT_DECAY).toBe(1.74);
    expect(FLASHLIGHT_SPOT_DECAY).toBe(1.74);
    expect(FLASHLIGHT_FILL_DECAY).toBe(1.74);
    expect(WARM_LIGHT_DECAY).toBe(1.74);
    expect(WARM_LIGHT_VISIBLE_EPS).toBe(0.0174);
    expect(WARM_LIGHT_AMBER_B_GAIN).toBe(0.115);
    expect(WARM_LIGHT_AMBER_B).toBe(0.437);
    expect(WARM_LIGHT_AMBER_G_GAIN).toBe(0.092);
    expect(WARM_LIGHT_AMBER_G).toBe(0.759);
    expect(WARM_LIGHT_DISTANCE_GAIN).toBe(2.875);
    expect(WARM_LIGHT_DISTANCE_BASE).toBe(7.475);
    expect(WARM_LIGHT_Y).toBe(1.7825);
    expect(WARM_LIGHT_INTENSITY_MUL).toBe(1.7825);
    expect(FLASHLIGHT_SPOT_DISTANCE_GAIN).toBe(2.3);
    expect(FLASHLIGHT_SPOT_DISTANCE_EXTRA).toBe(1.84);
    expect(FLASHLIGHT_SPOT_Y).toBe(1.7825);
    expect(FLASHLIGHT_FILL_Y).toBe(1.5525);
    expect(FLASHLIGHT_FILL_DISTANCE_GAIN).toBe(4.025);
    expect(FLASHLIGHT_FILL_DISTANCE_BASE).toBe(8.05);
    expect(FLASHLIGHT_FILL_COLOR).toBe(0xcaefff);
    expect(MUZZLE_LIGHT_COLOR).toBe(0xffffb8);
    expect(IMPACT_SPARK_LIGHT_COLOR).toBe(0xffef93);
    expect(TRACER_FLASH_COLOR).toBe(0xffdd6e);
    const src = readFileSync(
      resolve(process.cwd(), "src/render/worldView.ts"),
      "utf8",
    );
    expect(src).toContain("export const NOISE_RING_Y = 0.0575");
    expect(src).toContain("mesh.position.y = NOISE_RING_Y");
    expect(src).toContain("slot.mesh.position.set(state.x, NOISE_RING_Y, state.y)");
    expect(src).not.toMatch(/const NOISE_RING_Y = 0\.05;/);
    expect(src).not.toMatch(/export const NOISE_RING_Y = 0\.05;/);
    expect(src).toContain("export const MARKER_RING_Y = 0.046");
    expect(src).toContain("ring.position.y = MARKER_RING_Y");
    expect(src).not.toMatch(/ring\.position\.y = 0\.04/);
    expect(src).not.toMatch(/const MARKER_RING_Y = 0\.04;/);
    expect(src).not.toMatch(/export const MARKER_RING_Y = 0\.04;/);
    expect(src).toContain("export const MARKER_ICON_Y = 0.023");
    expect(src).toContain("icon.position.y = MARKER_ICON_Y");
    expect(src).not.toMatch(/icon\.position\.y = 0\.02/);
    expect(src).not.toMatch(/const MARKER_ICON_Y = 0\.02;/);
    expect(src).not.toMatch(/export const MARKER_ICON_Y = 0\.02;/);
    expect(src).toContain("export const MARKER_ICON_SIZE = 0.207");
    expect(src).toContain("new THREE.PlaneGeometry(MARKER_ICON_SIZE, MARKER_ICON_SIZE)");
    expect(src).not.toMatch(/new THREE\.PlaneGeometry\(0\.18, 0\.18\)/);
    expect(src).not.toMatch(/const MARKER_ICON_SIZE = 0\.18/);
    expect(src).not.toMatch(/export const MARKER_ICON_SIZE = 0\.18/);
    expect(src).toContain("export const MARKER_BADGE_RADIUS = 0.184");
    expect(src).toContain("new THREE.CircleGeometry(MARKER_BADGE_RADIUS, 20)");
    expect(src).not.toMatch(/new THREE\.CircleGeometry\(0\.16, 20\)/);
    expect(src).not.toMatch(/const MARKER_BADGE_RADIUS = 0\.16/);
    expect(src).not.toMatch(/export const MARKER_BADGE_RADIUS = 0\.16/);
    expect(src).toContain("export const MARKER_BADGE_TILT = 2.99");
    expect(src).toContain("disc.rotation.x = -Math.PI / MARKER_BADGE_TILT");
    expect(src).toContain("icon.rotation.x = -Math.PI / MARKER_BADGE_TILT");
    expect(src).not.toMatch(/disc\.rotation\.x = -Math\.PI \/ 2\.6/);
    expect(src).not.toMatch(/icon\.rotation\.x = -Math\.PI \/ 2\.6/);
    expect(src).not.toMatch(/const MARKER_BADGE_TILT = 2\.6/);
    expect(src).not.toMatch(/export const MARKER_BADGE_TILT = 2\.6/);
    expect(src).toContain("export const PLAYER_BODY_BASE_Y = 0.644");
    expect(src).toContain("playerBody.position.y = PLAYER_BODY_BASE_Y");
    expect(src).not.toMatch(/playerBody\.position\.y = 0\.56/);
    expect(src).not.toMatch(/const PLAYER_BODY_BASE_Y = 0\.56/);
    expect(src).not.toMatch(/export const PLAYER_BODY_BASE_Y = 0\.56/);
    expect(src).toContain("export const PLAYER_HEAD_BASE_Y = 1.518");
    expect(src).toContain("playerHead.position.y = PLAYER_HEAD_BASE_Y");
    expect(src).not.toMatch(/playerHead\.position\.y = 1\.32/);
    expect(src).not.toMatch(/const PLAYER_HEAD_BASE_Y = 1\.32/);
    expect(src).not.toMatch(/export const PLAYER_HEAD_BASE_Y = 1\.32/);
    expect(src).toContain("export const HOSTILE_BODY_BASE_Y = 0.644");
    expect(src).toContain("mesh.position.y = HOSTILE_BODY_BASE_Y");
    expect(src).toContain("body.position.y = HOSTILE_BODY_BASE_Y");
    expect(src).not.toMatch(/mesh\.position\.y = 0\.56/);
    expect(src).not.toMatch(/body\.position\.y = 0\.56/);
    expect(src).not.toMatch(/const HOSTILE_BODY_BASE_Y = 0\.56/);
    expect(src).not.toMatch(/export const HOSTILE_BODY_BASE_Y = 0\.56/);
    expect(src).toContain("export const HOSTILE_HEAD_BASE_Y = 1.495");
    expect(src).toContain("head.position.y = HOSTILE_HEAD_BASE_Y");
    expect(src).not.toMatch(/head\.position\.y = 1\.3/);
    expect(src).not.toMatch(/const HOSTILE_HEAD_BASE_Y = 1\.3/);
    expect(src).not.toMatch(/export const HOSTILE_HEAD_BASE_Y = 1\.3/);
    expect(src).toContain("export const HOSTILE_MUTE_XZ_SCALE = 1.2075");
    expect(src).toContain("mesh.scale.set(HOSTILE_MUTE_XZ_SCALE, 1, HOSTILE_MUTE_XZ_SCALE)");
    expect(src).not.toMatch(/mesh\.scale\.set\(1\.05, 1, 1\.05\)/);
    expect(src).not.toMatch(/const HOSTILE_MUTE_XZ_SCALE = 1\.05/);
    expect(src).not.toMatch(/export const HOSTILE_MUTE_XZ_SCALE = 1\.05/);
    expect(src).toContain("export const CHEVRON_TILT = -0.4025");
    expect(src).toContain("chevronMesh.rotation.x = CHEVRON_TILT");
    expect(src).not.toMatch(/chevronMesh\.rotation\.x = -0\.35/);
    expect(src).not.toMatch(/const CHEVRON_TILT = -0\.35/);
    expect(src).not.toMatch(/export const CHEVRON_TILT = -0\.35/);
    expect(src).toContain("export const CHEVRON_Y = 0.138");
    expect(src).toContain("chevronMesh.position.set(x, CHEVRON_Y, z)");
    expect(src).not.toMatch(/const CHEVRON_Y = 0\.12/);
    expect(src).not.toMatch(/export const CHEVRON_Y = 0\.12/);
    expect(src).not.toMatch(/chevronMesh\.position\.set\(x, 0\.12, z\)/);
    expect(src).toContain("export const MUZZLE_FORWARD = 0.552");
    expect(src).toContain("Math.sin(playerGltfYaw) * MUZZLE_FORWARD");
    expect(src).toContain("Math.cos(playerGltfYaw) * MUZZLE_FORWARD");
    expect(src).toContain("muzzleMesh.position.set(ox, TRACER_HEIGHT, oz)");
    expect(src).toContain("muzzleLight.position.set(ox, TRACER_HEIGHT, oz)");
    expect(src).not.toMatch(/const MUZZLE_FORWARD = 0\.48/);
    expect(src).not.toMatch(/export const MUZZLE_FORWARD = 0\.48/);
    expect(src).toContain("export const FLASHLIGHT_SPOT_TARGET_Y = 0.138");
    expect(src).toContain("torchSpot.target.position.set(wx + tip.x, FLASHLIGHT_SPOT_TARGET_Y, wy + tip.z)");
    expect(src).not.toMatch(/torchSpot\.target\.position\.set\(wx \+ tip\.x, 0\.12, wy \+ tip\.z\)/);
    expect(src).not.toMatch(/export const FLASHLIGHT_SPOT_TARGET_Y = 0\.12/);
    expect(src).toContain("export const TRACER_FLASH_Y_OFFSET = 0.1725");
    expect(src).toContain("flash.position.set(from.x, TRACER_HEIGHT + TRACER_FLASH_Y_OFFSET, from.y)");
    expect(src).not.toMatch(/flash\.position\.set\(from\.x, TRACER_HEIGHT \+ 0\.15, from\.y\)/);
    expect(src).not.toMatch(/export const TRACER_FLASH_Y_OFFSET = 0\.15/);
    expect(src).toContain("export const TRACER_FLASH_DISTANCE = 3.68");
    expect(src).toContain("new THREE.PointLight(TRACER_FLASH_COLOR, TRACER_FLASH_INTENSITY, TRACER_FLASH_DISTANCE, TRACER_FLASH_DECAY)");
    expect(src).not.toMatch(/new THREE\.PointLight\(TRACER_FLASH_COLOR, TRACER_FLASH_INTENSITY, 3\.2, TRACER_FLASH_DECAY\)/);
    expect(src).not.toMatch(/new THREE\.PointLight\(TRACER_FLASH_COLOR, 2\.4, 3\.2, TRACER_FLASH_DECAY\)/);
    expect(src).not.toMatch(/new THREE\.PointLight\(TRACER_FLASH_COLOR, 2\.4, 3\.2, 2\)/);
    expect(src).not.toMatch(/export const TRACER_FLASH_DISTANCE = 3\.2/);
    expect(src).toContain("t.flash.intensity = TRACER_FLASH_INTENSITY * op");
    expect(src).not.toMatch(/t\.flash\.intensity = 2\.4 \* op/);
    expect(src).toContain("export const TRACER_FLASH_INTENSITY = 2.76");
    expect(src).toContain("export const TRACER_FLASH_DECAY = 1.74");
    expect(src).toContain("export const TRACER_FLASH_COLOR = 0xffdd6e");
    expect(src).toContain("export const IMPACT_SPARK_LIGHT_DECAY = 1.74");
    expect(src).toContain("IMPACT_SPARK_LIGHT_DISTANCE,\n    IMPACT_SPARK_LIGHT_DECAY,");
    expect(src).not.toMatch(/IMPACT_SPARK_LIGHT_DISTANCE,\s*\n\s*2,/);
    expect(src).not.toMatch(/export const IMPACT_SPARK_LIGHT_DECAY = 2/);
    expect(src).toContain("const IMPACT_SPARK_LIGHT_DISTANCE = 1.8");
    expect(src).toContain("export const IMPACT_SPARK_LIGHT_COLOR = 0xffef93");
    expect(src).toContain("new THREE.PointLight(\n    IMPACT_SPARK_LIGHT_COLOR,");
    expect(src).toContain("export const MUZZLE_LIGHT_DECAY = 1.74");
    expect(src).toContain("MUZZLE_LIGHT_DISTANCE,\n    MUZZLE_LIGHT_DECAY,");
    expect(src).not.toMatch(/MUZZLE_LIGHT_DISTANCE,\s*\n\s*2,/);
    expect(src).not.toMatch(/export const MUZZLE_LIGHT_DECAY = 2/);
    expect(src).toContain("const MUZZLE_LIGHT_DISTANCE = 2.6");
    expect(src).toContain("export const MUZZLE_LIGHT_COLOR = 0xffffb8");
    expect(src).toContain("new THREE.PointLight(\n    MUZZLE_LIGHT_COLOR,");
    expect(src).toContain("export const FLASHLIGHT_SPOT_DECAY = 1.74");
    expect(src).toContain("FLASHLIGHT_SPOT_PENUMBRA,\n    FLASHLIGHT_SPOT_DECAY,");
    expect(src).not.toMatch(/FLASHLIGHT_SPOT_PENUMBRA,\s*\n\s*2,/);
    expect(src).not.toMatch(/export const FLASHLIGHT_SPOT_DECAY = 2/);
    expect(src).toContain("FLASHLIGHT_CONE_LENGTH + 2.4");
    expect(src).toContain("export const FLASHLIGHT_FILL_DECAY = 1.74");
    expect(src).toContain("new THREE.PointLight(FLASHLIGHT_FILL_COLOR, 0, 10, FLASHLIGHT_FILL_DECAY)");
    expect(src).not.toMatch(/new THREE\.PointLight\(FLASHLIGHT_FILL_COLOR, 0, 10, 2\)/);
    expect(src).not.toMatch(/export const FLASHLIGHT_FILL_DECAY = 2/);
    expect(src).toContain("export const WARM_LIGHT_DECAY = 1.74");
    expect(src).toContain("new THREE.PointLight(WARM_LIGHT_COLOR, 0, 7.5, WARM_LIGHT_DECAY)");
    expect(src).not.toMatch(/new THREE\.PointLight\(WARM_LIGHT_COLOR, 0, 7\.5, 2\)/);
    expect(src).toContain("export const WARM_LIGHT_VISIBLE_EPS = 0.0174");
    expect(src).toContain("warmLight.visible = i > WARM_LIGHT_VISIBLE_EPS");
    expect(src).not.toMatch(/warmLight\.visible = i > 0\.02/);
    expect(src).not.toMatch(/export const WARM_LIGHT_VISIBLE_EPS = 0\.02/);
    expect(src).toContain("export const WARM_LIGHT_AMBER_B_GAIN = 0.115");
    expect(src).toContain("export const WARM_LIGHT_AMBER_B = 0.437");
    expect(src).toContain("export const WARM_LIGHT_AMBER_G_GAIN = 0.092");
    expect(src).toContain("export const WARM_LIGHT_AMBER_G = 0.759");
    expect(src).toContain(
      "warmLight.color.setRGB(1, WARM_LIGHT_AMBER_G + i * WARM_LIGHT_AMBER_G_GAIN, WARM_LIGHT_AMBER_B + i * WARM_LIGHT_AMBER_B_GAIN)",
    );
    expect(src).not.toMatch(/WARM_LIGHT_AMBER_B \+ i \* 0\.1/);
    expect(src).toContain("export const WARM_LIGHT_DISTANCE_GAIN = 2.875");
    expect(src).toContain("export const WARM_LIGHT_DISTANCE_BASE = 7.475");
    expect(src).toContain(
      "warmLight.distance = WARM_LIGHT_DISTANCE_BASE + i * WARM_LIGHT_DISTANCE_GAIN",
    );
    expect(src).not.toMatch(/export const WARM_LIGHT_DISTANCE_GAIN = 2\.5/);
    expect(src).not.toMatch(
      /warmLight\.distance = WARM_LIGHT_DISTANCE_BASE \+ i \* 2\.5/,
    );
    expect(src).not.toMatch(/warmLight\.distance = 6\.5 \+ i \* 2\.5/);
    expect(src).toContain("export const WARM_LIGHT_Y = 1.7825");
    expect(src).toContain("warmLight.position.set(wx, WARM_LIGHT_Y, wy)");
    expect(src).not.toMatch(/export const WARM_LIGHT_Y = 1\.55/);
    expect(src).not.toMatch(/warmLight\.position\.set\(wx, 1\.55, wy\)/);
    expect(src).toContain("warmLight.position.set(0, 1.6, 0)");
    expect(src).toContain("export const WARM_LIGHT_INTENSITY_MUL = 1.7825");
    expect(src).toContain("warmLight.intensity = i * WARM_LIGHT_INTENSITY_MUL");
    expect(src).not.toMatch(/export const WARM_LIGHT_INTENSITY_MUL = 1\.55/);
    expect(src).not.toMatch(/warmLight\.intensity = i \* 1\.55/);
    expect(src).toContain("export const FLASHLIGHT_SPOT_DISTANCE_GAIN = 2.3");
    expect(src).toContain("export const FLASHLIGHT_SPOT_DISTANCE_EXTRA = 1.84");
    expect(src).toContain(
      "torchSpot.distance = FLASHLIGHT_CONE_LENGTH + FLASHLIGHT_SPOT_DISTANCE_EXTRA + i * FLASHLIGHT_SPOT_DISTANCE_GAIN",
    );
    expect(src).toContain("export const FLASHLIGHT_SPOT_Y = 1.7825");
    expect(src).toContain("torchSpot.position.set(0, FLASHLIGHT_SPOT_Y, 0)");
    expect(src).toContain("torchSpot.position.set(wx, FLASHLIGHT_SPOT_Y, wy)");
    expect(src).toContain("export const FLASHLIGHT_FILL_Y = 1.5525");
    expect(src).toContain("torchLight.position.set(0, FLASHLIGHT_FILL_Y, 0)");
    expect(src).toContain("torchLight.position.set(wx, FLASHLIGHT_FILL_Y, wy)");
    expect(src).toContain("export const FLASHLIGHT_FILL_DISTANCE_GAIN = 4.025");
    expect(src).toContain("export const FLASHLIGHT_FILL_DISTANCE_BASE = 8.05");
    expect(src).toContain(
      "torchLight.distance = FLASHLIGHT_FILL_DISTANCE_BASE + i * FLASHLIGHT_FILL_DISTANCE_GAIN",
    );
    expect(src).toContain("export const FLASHLIGHT_FILL_COLOR = 0xcaefff");
    expect(src).toContain("torchLight.color.setHex(FLASHLIGHT_FILL_COLOR)");
    expect(src).toContain("export const TRACER_COLOR = 0xffffb8");
    expect(src).toContain("color: TRACER_COLOR");
    expect(src).toContain("export const IMPACT_SPARK_COLOR = 0xffef93");
    expect(src).toContain("color: IMPACT_SPARK_COLOR");
    expect(src).toContain("export const MUZZLE_FLASH_COLOR = 0xffffdd");
    expect(src).toContain("color: MUZZLE_FLASH_COLOR");
    expect(src).toContain("export const WARM_LIGHT_COLOR = 0xffca81");
    expect(src).toContain("export const POSSESSED_HEAD_EMISSIVE = 0x30124a");
    expect(src).toContain("emissive: POSSESSED_HEAD_EMISSIVE");
    expect(src).toContain("color: POSSESSED_HEAD_COLOR");
    expect(src).toContain("export const POSSESSED_HEAD_COLOR = 0x8c469f");
    expect(src).toContain("emissive: POSSESSED_EMISSIVE");
    expect(src).toContain("color: POSSESSED_COLOR");
    expect(src).toContain("const POSSESSED_EMISSIVE = 0x1e0925");
    expect(src).toContain("emissiveIntensity: 0.22");
    expect(src).toContain("color: HOSTILE_COLOR");
    expect(src).toContain("color: PLAYER_COLOR");
    expect(src).toContain("color: PLAYER_HEAD_COLOR");
    expect(src).toContain("emissive: PLAYER_HEAD_EMISSIVE");
    expect(TRACER_FLASH_COLOR).toBe(0xffdd6e);
    expect(TRACER_FLASH_INTENSITY).toBe(2.76);
    expect(TRACER_FLASH_DISTANCE).toBe(3.68);
    expect(TRACER_FLASH_DECAY).toBe(1.74);
    expect(TRACER_FLASH_Y_OFFSET).toBe(0.1725);
    expect(TRACER_COLOR).toBe(0xffffb8);
    expect(IMPACT_SPARK_LIGHT_COLOR).toBe(0xffef93);
    expect(IMPACT_SPARK_COLOR).toBe(0xffef93);
    expect(IMPACT_SPARK_LIGHT_DECAY).toBe(1.74);
    expect(MUZZLE_LIGHT_COLOR).toBe(0xffffb8);
    expect(MUZZLE_LIGHT_DECAY).toBe(1.74);
    expect(MUZZLE_FLASH_COLOR).toBe(0xffffdd);
    expect(WARM_LIGHT_COLOR).toBe(0xffca81);
    expect(WARM_LIGHT_DECAY).toBe(1.74);
    expect(WARM_LIGHT_VISIBLE_EPS).toBe(0.0174);
    expect(WARM_LIGHT_AMBER_B_GAIN).toBe(0.115);
    expect(WARM_LIGHT_AMBER_B).toBe(0.437);
    expect(WARM_LIGHT_AMBER_G_GAIN).toBe(0.092);
    expect(WARM_LIGHT_AMBER_G).toBe(0.759);
    expect(WARM_LIGHT_DISTANCE_GAIN).toBe(2.875);
    expect(WARM_LIGHT_DISTANCE_BASE).toBe(7.475);
    expect(WARM_LIGHT_Y).toBe(1.7825);
    expect(WARM_LIGHT_INTENSITY_MUL).toBe(1.7825);
    expect(POSSESSED_HEAD_EMISSIVE).toBe(0x30124a);
    expect(POSSESSED_HEAD_COLOR).toBe(0x8c469f);
    expect(POSSESSED_COLOR).toBe(0x68347b);
    expect(HOSTILE_COLOR).toBe(0x7b1e1e);
    expect(PLAYER_COLOR).toBe(0x55a4f4);
    expect(PLAYER_HEAD_COLOR).toBe(0x91d1ff);
    expect(PLAYER_HEAD_EMISSIVE).toBe(0x122537);
    expect(MARKER_PALETTE.possessed.emissive).toBe(0x30124a);
    expect(MARKER_PALETTE.player.badge).toBe(0x91d1ff);
    expect(MARKER_PALETTE.player.emissive).toBe(0x1e4a6e);
    expect(MARKER_PALETTE.player.ring).toBe(0x4392f4);
    expect(MARKER_RING_OPACITY).toBe(0.6877);
    expect(muteBadgeY).toBe(2.3);
    expect(possessedBadgeY).toBe(2.3);
  });
});

describe("worldView marker ring Y", () => {
  test("marker ring Y 0.04 × 1.15 → 0.046; noise-ring-y/chevron-tilt/chevron-y/muzzle-forward/spot-target-y/y-offset/distance/intensity/decay/impact-spark-decay/muzzle-decay/spot-decay/fill-decay/warm-decay/visible-eps/amber-B-gain/amber-B/amber-G-gain/amber-G/gain/base/Y/intensity/color/spot-gain/extra/spot-Y/fill-Y/gain/base/flash/mesh/color/possessed/hostile/player/palette/badge-Y iguales", () => {
    expect(MARKER_RING_Y).toBe(0.046);
    expect(MARKER_RING_Y).toBeCloseTo(0.04 * 1.15, 10);
    expect(MARKER_ICON_Y).toBe(0.023);
    expect(MARKER_ICON_Y).toBeCloseTo(0.02 * 1.15, 10);
    expect(MARKER_ICON_SIZE).toBe(0.207);
    expect(MARKER_ICON_SIZE).toBeCloseTo(0.18 * 1.15, 10);
    expect(MARKER_BADGE_RADIUS).toBe(0.184);
    expect(MARKER_BADGE_RADIUS).toBeCloseTo(0.16 * 1.15, 10);
    expect(MARKER_BADGE_TILT).toBe(2.99);
    expect(MARKER_BADGE_TILT).toBeCloseTo(2.6 * 1.15, 10);
    expect(PLAYER_BODY_BASE_Y).toBe(0.644);
    expect(PLAYER_BODY_BASE_Y).toBeCloseTo(0.56 * 1.15, 10);
    expect(PLAYER_HEAD_BASE_Y).toBe(1.518);
    expect(PLAYER_HEAD_BASE_Y).toBeCloseTo(1.32 * 1.15, 10);
    expect(HOSTILE_BODY_BASE_Y).toBe(0.644);
    expect(HOSTILE_BODY_BASE_Y).toBeCloseTo(0.56 * 1.15, 10);
    expect(HOSTILE_HEAD_BASE_Y).toBe(1.495);
    expect(HOSTILE_HEAD_BASE_Y).toBeCloseTo(1.3 * 1.15, 10);
    expect(HOSTILE_MUTE_XZ_SCALE).toBe(1.2075);
    expect(HOSTILE_MUTE_XZ_SCALE).toBeCloseTo(1.05 * 1.15, 10);
    expect(NOISE_RING_Y).toBe(0.0575);
    expect(NOISE_RING_Y).toBeCloseTo(0.05 * 1.15, 10);
    expect(CHEVRON_TILT).toBe(-0.4025);
    expect(CHEVRON_Y).toBe(0.138);
    expect(MUZZLE_FORWARD).toBe(0.552);
    expect(FLASHLIGHT_SPOT_TARGET_Y).toBe(0.138);
    expect(TRACER_FLASH_Y_OFFSET).toBe(0.1725);
    expect(TRACER_FLASH_DISTANCE).toBe(3.68);
    expect(TRACER_FLASH_INTENSITY).toBe(2.76);
    expect(TRACER_FLASH_DECAY).toBe(1.74);
    expect(IMPACT_SPARK_LIGHT_DECAY).toBe(1.74);
    expect(MUZZLE_LIGHT_DECAY).toBe(1.74);
    expect(FLASHLIGHT_SPOT_DECAY).toBe(1.74);
    expect(FLASHLIGHT_FILL_DECAY).toBe(1.74);
    expect(WARM_LIGHT_DECAY).toBe(1.74);
    expect(WARM_LIGHT_VISIBLE_EPS).toBe(0.0174);
    expect(WARM_LIGHT_AMBER_B_GAIN).toBe(0.115);
    expect(WARM_LIGHT_AMBER_B).toBe(0.437);
    expect(WARM_LIGHT_AMBER_G_GAIN).toBe(0.092);
    expect(WARM_LIGHT_AMBER_G).toBe(0.759);
    expect(WARM_LIGHT_DISTANCE_GAIN).toBe(2.875);
    expect(WARM_LIGHT_DISTANCE_BASE).toBe(7.475);
    expect(WARM_LIGHT_Y).toBe(1.7825);
    expect(WARM_LIGHT_INTENSITY_MUL).toBe(1.7825);
    expect(FLASHLIGHT_SPOT_DISTANCE_GAIN).toBe(2.3);
    expect(FLASHLIGHT_SPOT_DISTANCE_EXTRA).toBe(1.84);
    expect(FLASHLIGHT_SPOT_Y).toBe(1.7825);
    expect(FLASHLIGHT_FILL_Y).toBe(1.5525);
    expect(FLASHLIGHT_FILL_DISTANCE_GAIN).toBe(4.025);
    expect(FLASHLIGHT_FILL_DISTANCE_BASE).toBe(8.05);
    expect(FLASHLIGHT_FILL_COLOR).toBe(0xcaefff);
    expect(MUZZLE_LIGHT_COLOR).toBe(0xffffb8);
    expect(IMPACT_SPARK_LIGHT_COLOR).toBe(0xffef93);
    expect(TRACER_FLASH_COLOR).toBe(0xffdd6e);
    const src = readFileSync(
      resolve(process.cwd(), "src/render/worldView.ts"),
      "utf8",
    );
    expect(src).toContain("export const MARKER_RING_Y = 0.046");
    expect(src).toContain("ring.position.y = MARKER_RING_Y");
    expect(src).not.toMatch(/ring\.position\.y = 0\.04/);
    expect(src).not.toMatch(/const MARKER_RING_Y = 0\.04;/);
    expect(src).not.toMatch(/export const MARKER_RING_Y = 0\.04;/);
    expect(src).toContain("export const MARKER_ICON_Y = 0.023");
    expect(src).toContain("icon.position.y = MARKER_ICON_Y");
    expect(src).not.toMatch(/icon\.position\.y = 0\.02/);
    expect(src).not.toMatch(/const MARKER_ICON_Y = 0\.02;/);
    expect(src).not.toMatch(/export const MARKER_ICON_Y = 0\.02;/);
    expect(src).toContain("export const MARKER_ICON_SIZE = 0.207");
    expect(src).toContain("new THREE.PlaneGeometry(MARKER_ICON_SIZE, MARKER_ICON_SIZE)");
    expect(src).not.toMatch(/new THREE\.PlaneGeometry\(0\.18, 0\.18\)/);
    expect(src).not.toMatch(/const MARKER_ICON_SIZE = 0\.18/);
    expect(src).not.toMatch(/export const MARKER_ICON_SIZE = 0\.18/);
    expect(src).toContain("export const MARKER_BADGE_RADIUS = 0.184");
    expect(src).toContain("new THREE.CircleGeometry(MARKER_BADGE_RADIUS, 20)");
    expect(src).not.toMatch(/new THREE\.CircleGeometry\(0\.16, 20\)/);
    expect(src).not.toMatch(/const MARKER_BADGE_RADIUS = 0\.16/);
    expect(src).not.toMatch(/export const MARKER_BADGE_RADIUS = 0\.16/);
    expect(src).toContain("export const MARKER_BADGE_TILT = 2.99");
    expect(src).toContain("disc.rotation.x = -Math.PI / MARKER_BADGE_TILT");
    expect(src).toContain("icon.rotation.x = -Math.PI / MARKER_BADGE_TILT");
    expect(src).not.toMatch(/disc\.rotation\.x = -Math\.PI \/ 2\.6/);
    expect(src).not.toMatch(/icon\.rotation\.x = -Math\.PI \/ 2\.6/);
    expect(src).not.toMatch(/const MARKER_BADGE_TILT = 2\.6/);
    expect(src).not.toMatch(/export const MARKER_BADGE_TILT = 2\.6/);
    expect(src).toContain("export const PLAYER_BODY_BASE_Y = 0.644");
    expect(src).toContain("playerBody.position.y = PLAYER_BODY_BASE_Y");
    expect(src).not.toMatch(/playerBody\.position\.y = 0\.56/);
    expect(src).not.toMatch(/const PLAYER_BODY_BASE_Y = 0\.56/);
    expect(src).not.toMatch(/export const PLAYER_BODY_BASE_Y = 0\.56/);
    expect(src).toContain("export const PLAYER_HEAD_BASE_Y = 1.518");
    expect(src).toContain("playerHead.position.y = PLAYER_HEAD_BASE_Y");
    expect(src).not.toMatch(/playerHead\.position\.y = 1\.32/);
    expect(src).not.toMatch(/const PLAYER_HEAD_BASE_Y = 1\.32/);
    expect(src).not.toMatch(/export const PLAYER_HEAD_BASE_Y = 1\.32/);
    expect(src).toContain("export const HOSTILE_BODY_BASE_Y = 0.644");
    expect(src).toContain("mesh.position.y = HOSTILE_BODY_BASE_Y");
    expect(src).toContain("body.position.y = HOSTILE_BODY_BASE_Y");
    expect(src).not.toMatch(/mesh\.position\.y = 0\.56/);
    expect(src).not.toMatch(/body\.position\.y = 0\.56/);
    expect(src).not.toMatch(/const HOSTILE_BODY_BASE_Y = 0\.56/);
    expect(src).not.toMatch(/export const HOSTILE_BODY_BASE_Y = 0\.56/);
    expect(src).toContain("export const HOSTILE_HEAD_BASE_Y = 1.495");
    expect(src).toContain("head.position.y = HOSTILE_HEAD_BASE_Y");
    expect(src).not.toMatch(/head\.position\.y = 1\.3/);
    expect(src).not.toMatch(/const HOSTILE_HEAD_BASE_Y = 1\.3/);
    expect(src).not.toMatch(/export const HOSTILE_HEAD_BASE_Y = 1\.3/);
    expect(src).toContain("export const HOSTILE_MUTE_XZ_SCALE = 1.2075");
    expect(src).toContain("mesh.scale.set(HOSTILE_MUTE_XZ_SCALE, 1, HOSTILE_MUTE_XZ_SCALE)");
    expect(src).not.toMatch(/mesh\.scale\.set\(1\.05, 1, 1\.05\)/);
    expect(src).not.toMatch(/const HOSTILE_MUTE_XZ_SCALE = 1\.05/);
    expect(src).not.toMatch(/export const HOSTILE_MUTE_XZ_SCALE = 1\.05/);
    expect(src).toContain("export const NOISE_RING_Y = 0.0575");
    expect(src).toContain("mesh.position.y = NOISE_RING_Y");
    expect(src).toContain("slot.mesh.position.set(state.x, NOISE_RING_Y, state.y)");
    expect(src).not.toMatch(/const NOISE_RING_Y = 0\.05;/);
    expect(src).not.toMatch(/export const NOISE_RING_Y = 0\.05;/);
    expect(src).toContain("export const CHEVRON_TILT = -0.4025");
    expect(src).toContain("chevronMesh.rotation.x = CHEVRON_TILT");
    expect(src).not.toMatch(/chevronMesh\.rotation\.x = -0\.35/);
    expect(src).not.toMatch(/const CHEVRON_TILT = -0\.35/);
    expect(src).not.toMatch(/export const CHEVRON_TILT = -0\.35/);
    expect(src).toContain("export const CHEVRON_Y = 0.138");
    expect(src).toContain("chevronMesh.position.set(x, CHEVRON_Y, z)");
    expect(src).not.toMatch(/const CHEVRON_Y = 0\.12/);
    expect(src).not.toMatch(/export const CHEVRON_Y = 0\.12/);
    expect(src).not.toMatch(/chevronMesh\.position\.set\(x, 0\.12, z\)/);
    expect(src).toContain("export const MUZZLE_FORWARD = 0.552");
    expect(src).toContain("Math.sin(playerGltfYaw) * MUZZLE_FORWARD");
    expect(src).toContain("Math.cos(playerGltfYaw) * MUZZLE_FORWARD");
    expect(src).toContain("muzzleMesh.position.set(ox, TRACER_HEIGHT, oz)");
    expect(src).toContain("muzzleLight.position.set(ox, TRACER_HEIGHT, oz)");
    expect(src).not.toMatch(/const MUZZLE_FORWARD = 0\.48/);
    expect(src).not.toMatch(/export const MUZZLE_FORWARD = 0\.48/);
    expect(src).toContain("export const FLASHLIGHT_SPOT_TARGET_Y = 0.138");
    expect(src).toContain("torchSpot.target.position.set(wx + tip.x, FLASHLIGHT_SPOT_TARGET_Y, wy + tip.z)");
    expect(src).not.toMatch(/torchSpot\.target\.position\.set\(wx \+ tip\.x, 0\.12, wy \+ tip\.z\)/);
    expect(src).not.toMatch(/export const FLASHLIGHT_SPOT_TARGET_Y = 0\.12/);
    expect(src).toContain("export const TRACER_FLASH_Y_OFFSET = 0.1725");
    expect(src).toContain("flash.position.set(from.x, TRACER_HEIGHT + TRACER_FLASH_Y_OFFSET, from.y)");
    expect(src).not.toMatch(/flash\.position\.set\(from\.x, TRACER_HEIGHT \+ 0\.15, from\.y\)/);
    expect(src).not.toMatch(/export const TRACER_FLASH_Y_OFFSET = 0\.15/);
    expect(src).toContain("export const TRACER_FLASH_DISTANCE = 3.68");
    expect(src).toContain("new THREE.PointLight(TRACER_FLASH_COLOR, TRACER_FLASH_INTENSITY, TRACER_FLASH_DISTANCE, TRACER_FLASH_DECAY)");
    expect(src).not.toMatch(/new THREE\.PointLight\(TRACER_FLASH_COLOR, TRACER_FLASH_INTENSITY, 3\.2, TRACER_FLASH_DECAY\)/);
    expect(src).not.toMatch(/new THREE\.PointLight\(TRACER_FLASH_COLOR, 2\.4, 3\.2, TRACER_FLASH_DECAY\)/);
    expect(src).not.toMatch(/new THREE\.PointLight\(TRACER_FLASH_COLOR, 2\.4, 3\.2, 2\)/);
    expect(src).not.toMatch(/export const TRACER_FLASH_DISTANCE = 3\.2/);
    expect(src).toContain("t.flash.intensity = TRACER_FLASH_INTENSITY * op");
    expect(src).not.toMatch(/t\.flash\.intensity = 2\.4 \* op/);
    expect(src).toContain("export const TRACER_FLASH_INTENSITY = 2.76");
    expect(src).toContain("export const TRACER_FLASH_DECAY = 1.74");
    expect(src).toContain("export const TRACER_FLASH_COLOR = 0xffdd6e");
    expect(src).toContain("export const IMPACT_SPARK_LIGHT_DECAY = 1.74");
    expect(src).toContain("IMPACT_SPARK_LIGHT_DISTANCE,\n    IMPACT_SPARK_LIGHT_DECAY,");
    expect(src).not.toMatch(/IMPACT_SPARK_LIGHT_DISTANCE,\s*\n\s*2,/);
    expect(src).not.toMatch(/export const IMPACT_SPARK_LIGHT_DECAY = 2/);
    expect(src).toContain("const IMPACT_SPARK_LIGHT_DISTANCE = 1.8");
    expect(src).toContain("export const IMPACT_SPARK_LIGHT_COLOR = 0xffef93");
    expect(src).toContain("new THREE.PointLight(\n    IMPACT_SPARK_LIGHT_COLOR,");
    expect(src).toContain("export const MUZZLE_LIGHT_DECAY = 1.74");
    expect(src).toContain("MUZZLE_LIGHT_DISTANCE,\n    MUZZLE_LIGHT_DECAY,");
    expect(src).not.toMatch(/MUZZLE_LIGHT_DISTANCE,\s*\n\s*2,/);
    expect(src).not.toMatch(/export const MUZZLE_LIGHT_DECAY = 2/);
    expect(src).toContain("const MUZZLE_LIGHT_DISTANCE = 2.6");
    expect(src).toContain("export const MUZZLE_LIGHT_COLOR = 0xffffb8");
    expect(src).toContain("new THREE.PointLight(\n    MUZZLE_LIGHT_COLOR,");
    expect(src).toContain("export const FLASHLIGHT_SPOT_DECAY = 1.74");
    expect(src).toContain("FLASHLIGHT_SPOT_PENUMBRA,\n    FLASHLIGHT_SPOT_DECAY,");
    expect(src).not.toMatch(/FLASHLIGHT_SPOT_PENUMBRA,\s*\n\s*2,/);
    expect(src).not.toMatch(/export const FLASHLIGHT_SPOT_DECAY = 2/);
    expect(src).toContain("FLASHLIGHT_CONE_LENGTH + 2.4");
    expect(src).toContain("export const FLASHLIGHT_FILL_DECAY = 1.74");
    expect(src).toContain("new THREE.PointLight(FLASHLIGHT_FILL_COLOR, 0, 10, FLASHLIGHT_FILL_DECAY)");
    expect(src).not.toMatch(/new THREE\.PointLight\(FLASHLIGHT_FILL_COLOR, 0, 10, 2\)/);
    expect(src).not.toMatch(/export const FLASHLIGHT_FILL_DECAY = 2/);
    expect(src).toContain("export const WARM_LIGHT_DECAY = 1.74");
    expect(src).toContain("new THREE.PointLight(WARM_LIGHT_COLOR, 0, 7.5, WARM_LIGHT_DECAY)");
    expect(src).not.toMatch(/new THREE\.PointLight\(WARM_LIGHT_COLOR, 0, 7\.5, 2\)/);
    expect(src).toContain("export const WARM_LIGHT_VISIBLE_EPS = 0.0174");
    expect(src).toContain("warmLight.visible = i > WARM_LIGHT_VISIBLE_EPS");
    expect(src).not.toMatch(/warmLight\.visible = i > 0\.02/);
    expect(src).not.toMatch(/export const WARM_LIGHT_VISIBLE_EPS = 0\.02/);
    expect(src).toContain("export const WARM_LIGHT_AMBER_B_GAIN = 0.115");
    expect(src).toContain("export const WARM_LIGHT_AMBER_B = 0.437");
    expect(src).toContain("export const WARM_LIGHT_AMBER_G_GAIN = 0.092");
    expect(src).toContain("export const WARM_LIGHT_AMBER_G = 0.759");
    expect(src).toContain(
      "warmLight.color.setRGB(1, WARM_LIGHT_AMBER_G + i * WARM_LIGHT_AMBER_G_GAIN, WARM_LIGHT_AMBER_B + i * WARM_LIGHT_AMBER_B_GAIN)",
    );
    expect(src).not.toMatch(/WARM_LIGHT_AMBER_B \+ i \* 0\.1/);
    expect(src).toContain("export const WARM_LIGHT_DISTANCE_GAIN = 2.875");
    expect(src).toContain("export const WARM_LIGHT_DISTANCE_BASE = 7.475");
    expect(src).toContain(
      "warmLight.distance = WARM_LIGHT_DISTANCE_BASE + i * WARM_LIGHT_DISTANCE_GAIN",
    );
    expect(src).not.toMatch(/export const WARM_LIGHT_DISTANCE_GAIN = 2\.5/);
    expect(src).not.toMatch(
      /warmLight\.distance = WARM_LIGHT_DISTANCE_BASE \+ i \* 2\.5/,
    );
    expect(src).not.toMatch(/warmLight\.distance = 6\.5 \+ i \* 2\.5/);
    expect(src).toContain("export const WARM_LIGHT_Y = 1.7825");
    expect(src).toContain("warmLight.position.set(wx, WARM_LIGHT_Y, wy)");
    expect(src).not.toMatch(/export const WARM_LIGHT_Y = 1\.55/);
    expect(src).not.toMatch(/warmLight\.position\.set\(wx, 1\.55, wy\)/);
    expect(src).toContain("warmLight.position.set(0, 1.6, 0)");
    expect(src).toContain("export const WARM_LIGHT_INTENSITY_MUL = 1.7825");
    expect(src).toContain("warmLight.intensity = i * WARM_LIGHT_INTENSITY_MUL");
    expect(src).not.toMatch(/export const WARM_LIGHT_INTENSITY_MUL = 1\.55/);
    expect(src).not.toMatch(/warmLight\.intensity = i \* 1\.55/);
    expect(src).toContain("export const FLASHLIGHT_SPOT_DISTANCE_GAIN = 2.3");
    expect(src).toContain("export const FLASHLIGHT_SPOT_DISTANCE_EXTRA = 1.84");
    expect(src).toContain(
      "torchSpot.distance = FLASHLIGHT_CONE_LENGTH + FLASHLIGHT_SPOT_DISTANCE_EXTRA + i * FLASHLIGHT_SPOT_DISTANCE_GAIN",
    );
    expect(src).toContain("export const FLASHLIGHT_SPOT_Y = 1.7825");
    expect(src).toContain("torchSpot.position.set(0, FLASHLIGHT_SPOT_Y, 0)");
    expect(src).toContain("torchSpot.position.set(wx, FLASHLIGHT_SPOT_Y, wy)");
    expect(src).toContain("export const FLASHLIGHT_FILL_Y = 1.5525");
    expect(src).toContain("torchLight.position.set(0, FLASHLIGHT_FILL_Y, 0)");
    expect(src).toContain("torchLight.position.set(wx, FLASHLIGHT_FILL_Y, wy)");
    expect(src).toContain("export const FLASHLIGHT_FILL_DISTANCE_GAIN = 4.025");
    expect(src).toContain("export const FLASHLIGHT_FILL_DISTANCE_BASE = 8.05");
    expect(src).toContain(
      "torchLight.distance = FLASHLIGHT_FILL_DISTANCE_BASE + i * FLASHLIGHT_FILL_DISTANCE_GAIN",
    );
    expect(src).toContain("export const FLASHLIGHT_FILL_COLOR = 0xcaefff");
    expect(src).toContain("torchLight.color.setHex(FLASHLIGHT_FILL_COLOR)");
    expect(src).toContain("export const TRACER_COLOR = 0xffffb8");
    expect(src).toContain("color: TRACER_COLOR");
    expect(src).toContain("export const IMPACT_SPARK_COLOR = 0xffef93");
    expect(src).toContain("color: IMPACT_SPARK_COLOR");
    expect(src).toContain("export const MUZZLE_FLASH_COLOR = 0xffffdd");
    expect(src).toContain("color: MUZZLE_FLASH_COLOR");
    expect(src).toContain("export const WARM_LIGHT_COLOR = 0xffca81");
    expect(src).toContain("export const POSSESSED_HEAD_EMISSIVE = 0x30124a");
    expect(src).toContain("emissive: POSSESSED_HEAD_EMISSIVE");
    expect(src).toContain("color: POSSESSED_HEAD_COLOR");
    expect(src).toContain("export const POSSESSED_HEAD_COLOR = 0x8c469f");
    expect(src).toContain("emissive: POSSESSED_EMISSIVE");
    expect(src).toContain("color: POSSESSED_COLOR");
    expect(src).toContain("const POSSESSED_EMISSIVE = 0x1e0925");
    expect(src).toContain("emissiveIntensity: 0.22");
    expect(src).toContain("color: HOSTILE_COLOR");
    expect(src).toContain("color: PLAYER_COLOR");
    expect(src).toContain("color: PLAYER_HEAD_COLOR");
    expect(src).toContain("emissive: PLAYER_HEAD_EMISSIVE");
    expect(TRACER_FLASH_COLOR).toBe(0xffdd6e);
    expect(TRACER_FLASH_INTENSITY).toBe(2.76);
    expect(TRACER_FLASH_DISTANCE).toBe(3.68);
    expect(TRACER_FLASH_DECAY).toBe(1.74);
    expect(TRACER_FLASH_Y_OFFSET).toBe(0.1725);
    expect(TRACER_COLOR).toBe(0xffffb8);
    expect(IMPACT_SPARK_LIGHT_COLOR).toBe(0xffef93);
    expect(IMPACT_SPARK_COLOR).toBe(0xffef93);
    expect(IMPACT_SPARK_LIGHT_DECAY).toBe(1.74);
    expect(MUZZLE_LIGHT_COLOR).toBe(0xffffb8);
    expect(MUZZLE_LIGHT_DECAY).toBe(1.74);
    expect(MUZZLE_FLASH_COLOR).toBe(0xffffdd);
    expect(WARM_LIGHT_COLOR).toBe(0xffca81);
    expect(WARM_LIGHT_DECAY).toBe(1.74);
    expect(WARM_LIGHT_VISIBLE_EPS).toBe(0.0174);
    expect(WARM_LIGHT_AMBER_B_GAIN).toBe(0.115);
    expect(WARM_LIGHT_AMBER_B).toBe(0.437);
    expect(WARM_LIGHT_AMBER_G_GAIN).toBe(0.092);
    expect(WARM_LIGHT_AMBER_G).toBe(0.759);
    expect(WARM_LIGHT_DISTANCE_GAIN).toBe(2.875);
    expect(WARM_LIGHT_DISTANCE_BASE).toBe(7.475);
    expect(WARM_LIGHT_Y).toBe(1.7825);
    expect(WARM_LIGHT_INTENSITY_MUL).toBe(1.7825);
    expect(POSSESSED_HEAD_EMISSIVE).toBe(0x30124a);
    expect(POSSESSED_HEAD_COLOR).toBe(0x8c469f);
    expect(POSSESSED_COLOR).toBe(0x68347b);
    expect(HOSTILE_COLOR).toBe(0x7b1e1e);
    expect(PLAYER_COLOR).toBe(0x55a4f4);
    expect(PLAYER_HEAD_COLOR).toBe(0x91d1ff);
    expect(PLAYER_HEAD_EMISSIVE).toBe(0x122537);
    expect(MARKER_PALETTE.possessed.emissive).toBe(0x30124a);
    expect(MARKER_PALETTE.player.badge).toBe(0x91d1ff);
    expect(MARKER_PALETTE.player.emissive).toBe(0x1e4a6e);
    expect(MARKER_PALETTE.player.ring).toBe(0x4392f4);
    expect(MARKER_RING_OPACITY).toBe(0.6877);
    expect(muteBadgeY).toBe(2.3);
    expect(possessedBadgeY).toBe(2.3);
  });
});

describe("worldView marker icon Y", () => {
  test("marker icon Y 0.02 × 1.15 → 0.023; marker-ring-y/noise-ring-y/chevron-tilt/chevron-y/muzzle-forward/spot-target-y/y-offset/distance/intensity/decay/impact-spark-decay/muzzle-decay/spot-decay/fill-decay/warm-decay/visible-eps/amber-B-gain/amber-B/amber-G-gain/amber-G/gain/base/Y/intensity/color/spot-gain/extra/spot-Y/fill-Y/gain/base/flash/mesh/color/possessed/hostile/player/palette/badge-Y iguales", () => {
    expect(MARKER_ICON_Y).toBe(0.023);
    expect(MARKER_ICON_Y).toBeCloseTo(0.02 * 1.15, 10);
    expect(MARKER_ICON_SIZE).toBe(0.207);
    expect(MARKER_ICON_SIZE).toBeCloseTo(0.18 * 1.15, 10);
    expect(MARKER_BADGE_RADIUS).toBe(0.184);
    expect(MARKER_BADGE_RADIUS).toBeCloseTo(0.16 * 1.15, 10);
    expect(MARKER_BADGE_TILT).toBe(2.99);
    expect(MARKER_BADGE_TILT).toBeCloseTo(2.6 * 1.15, 10);
    expect(PLAYER_BODY_BASE_Y).toBe(0.644);
    expect(PLAYER_BODY_BASE_Y).toBeCloseTo(0.56 * 1.15, 10);
    expect(PLAYER_HEAD_BASE_Y).toBe(1.518);
    expect(PLAYER_HEAD_BASE_Y).toBeCloseTo(1.32 * 1.15, 10);
    expect(HOSTILE_BODY_BASE_Y).toBe(0.644);
    expect(HOSTILE_BODY_BASE_Y).toBeCloseTo(0.56 * 1.15, 10);
    expect(HOSTILE_HEAD_BASE_Y).toBe(1.495);
    expect(HOSTILE_HEAD_BASE_Y).toBeCloseTo(1.3 * 1.15, 10);
    expect(HOSTILE_MUTE_XZ_SCALE).toBe(1.2075);
    expect(HOSTILE_MUTE_XZ_SCALE).toBeCloseTo(1.05 * 1.15, 10);
    expect(MARKER_RING_Y).toBe(0.046);
    expect(MARKER_RING_Y).toBeCloseTo(0.04 * 1.15, 10);
    expect(NOISE_RING_Y).toBe(0.0575);
    expect(NOISE_RING_Y).toBeCloseTo(0.05 * 1.15, 10);
    expect(CHEVRON_TILT).toBe(-0.4025);
    expect(CHEVRON_Y).toBe(0.138);
    expect(MUZZLE_FORWARD).toBe(0.552);
    expect(FLASHLIGHT_SPOT_TARGET_Y).toBe(0.138);
    expect(TRACER_FLASH_Y_OFFSET).toBe(0.1725);
    expect(TRACER_FLASH_DISTANCE).toBe(3.68);
    expect(TRACER_FLASH_INTENSITY).toBe(2.76);
    expect(TRACER_FLASH_DECAY).toBe(1.74);
    expect(IMPACT_SPARK_LIGHT_DECAY).toBe(1.74);
    expect(MUZZLE_LIGHT_DECAY).toBe(1.74);
    expect(FLASHLIGHT_SPOT_DECAY).toBe(1.74);
    expect(FLASHLIGHT_FILL_DECAY).toBe(1.74);
    expect(WARM_LIGHT_DECAY).toBe(1.74);
    expect(WARM_LIGHT_VISIBLE_EPS).toBe(0.0174);
    expect(WARM_LIGHT_AMBER_B_GAIN).toBe(0.115);
    expect(WARM_LIGHT_AMBER_B).toBe(0.437);
    expect(WARM_LIGHT_AMBER_G_GAIN).toBe(0.092);
    expect(WARM_LIGHT_AMBER_G).toBe(0.759);
    expect(WARM_LIGHT_DISTANCE_GAIN).toBe(2.875);
    expect(WARM_LIGHT_DISTANCE_BASE).toBe(7.475);
    expect(WARM_LIGHT_Y).toBe(1.7825);
    expect(WARM_LIGHT_INTENSITY_MUL).toBe(1.7825);
    expect(FLASHLIGHT_SPOT_DISTANCE_GAIN).toBe(2.3);
    expect(FLASHLIGHT_SPOT_DISTANCE_EXTRA).toBe(1.84);
    expect(FLASHLIGHT_SPOT_Y).toBe(1.7825);
    expect(FLASHLIGHT_FILL_Y).toBe(1.5525);
    expect(FLASHLIGHT_FILL_DISTANCE_GAIN).toBe(4.025);
    expect(FLASHLIGHT_FILL_DISTANCE_BASE).toBe(8.05);
    expect(FLASHLIGHT_FILL_COLOR).toBe(0xcaefff);
    expect(MUZZLE_LIGHT_COLOR).toBe(0xffffb8);
    expect(IMPACT_SPARK_LIGHT_COLOR).toBe(0xffef93);
    expect(TRACER_FLASH_COLOR).toBe(0xffdd6e);
    const src = readFileSync(
      resolve(process.cwd(), "src/render/worldView.ts"),
      "utf8",
    );
    expect(src).toContain("export const MARKER_ICON_Y = 0.023");
    expect(src).toContain("icon.position.y = MARKER_ICON_Y");
    expect(src).not.toMatch(/icon\.position\.y = 0\.02/);
    expect(src).not.toMatch(/const MARKER_ICON_Y = 0\.02;/);
    expect(src).not.toMatch(/export const MARKER_ICON_Y = 0\.02;/);
    expect(src).toContain("export const MARKER_ICON_SIZE = 0.207");
    expect(src).toContain("new THREE.PlaneGeometry(MARKER_ICON_SIZE, MARKER_ICON_SIZE)");
    expect(src).not.toMatch(/new THREE\.PlaneGeometry\(0\.18, 0\.18\)/);
    expect(src).not.toMatch(/const MARKER_ICON_SIZE = 0\.18/);
    expect(src).not.toMatch(/export const MARKER_ICON_SIZE = 0\.18/);
    expect(src).toContain("export const MARKER_BADGE_RADIUS = 0.184");
    expect(src).toContain("new THREE.CircleGeometry(MARKER_BADGE_RADIUS, 20)");
    expect(src).not.toMatch(/new THREE\.CircleGeometry\(0\.16, 20\)/);
    expect(src).not.toMatch(/const MARKER_BADGE_RADIUS = 0\.16/);
    expect(src).not.toMatch(/export const MARKER_BADGE_RADIUS = 0\.16/);
    expect(src).toContain("export const MARKER_BADGE_TILT = 2.99");
    expect(src).toContain("disc.rotation.x = -Math.PI / MARKER_BADGE_TILT");
    expect(src).toContain("icon.rotation.x = -Math.PI / MARKER_BADGE_TILT");
    expect(src).not.toMatch(/disc\.rotation\.x = -Math\.PI \/ 2\.6/);
    expect(src).not.toMatch(/icon\.rotation\.x = -Math\.PI \/ 2\.6/);
    expect(src).not.toMatch(/const MARKER_BADGE_TILT = 2\.6/);
    expect(src).not.toMatch(/export const MARKER_BADGE_TILT = 2\.6/);
    expect(src).toContain("export const PLAYER_BODY_BASE_Y = 0.644");
    expect(src).toContain("playerBody.position.y = PLAYER_BODY_BASE_Y");
    expect(src).not.toMatch(/playerBody\.position\.y = 0\.56/);
    expect(src).not.toMatch(/const PLAYER_BODY_BASE_Y = 0\.56/);
    expect(src).not.toMatch(/export const PLAYER_BODY_BASE_Y = 0\.56/);
    expect(src).toContain("export const PLAYER_HEAD_BASE_Y = 1.518");
    expect(src).toContain("playerHead.position.y = PLAYER_HEAD_BASE_Y");
    expect(src).not.toMatch(/playerHead\.position\.y = 1\.32/);
    expect(src).not.toMatch(/const PLAYER_HEAD_BASE_Y = 1\.32/);
    expect(src).not.toMatch(/export const PLAYER_HEAD_BASE_Y = 1\.32/);
    expect(src).toContain("export const HOSTILE_BODY_BASE_Y = 0.644");
    expect(src).toContain("mesh.position.y = HOSTILE_BODY_BASE_Y");
    expect(src).toContain("body.position.y = HOSTILE_BODY_BASE_Y");
    expect(src).not.toMatch(/mesh\.position\.y = 0\.56/);
    expect(src).not.toMatch(/body\.position\.y = 0\.56/);
    expect(src).not.toMatch(/const HOSTILE_BODY_BASE_Y = 0\.56/);
    expect(src).not.toMatch(/export const HOSTILE_BODY_BASE_Y = 0\.56/);
    expect(src).toContain("export const HOSTILE_HEAD_BASE_Y = 1.495");
    expect(src).toContain("head.position.y = HOSTILE_HEAD_BASE_Y");
    expect(src).not.toMatch(/head\.position\.y = 1\.3/);
    expect(src).not.toMatch(/const HOSTILE_HEAD_BASE_Y = 1\.3/);
    expect(src).not.toMatch(/export const HOSTILE_HEAD_BASE_Y = 1\.3/);
    expect(src).toContain("export const HOSTILE_MUTE_XZ_SCALE = 1.2075");
    expect(src).toContain("mesh.scale.set(HOSTILE_MUTE_XZ_SCALE, 1, HOSTILE_MUTE_XZ_SCALE)");
    expect(src).not.toMatch(/mesh\.scale\.set\(1\.05, 1, 1\.05\)/);
    expect(src).not.toMatch(/const HOSTILE_MUTE_XZ_SCALE = 1\.05/);
    expect(src).not.toMatch(/export const HOSTILE_MUTE_XZ_SCALE = 1\.05/);
    expect(src).toContain("export const MARKER_RING_Y = 0.046");
    expect(src).toContain("ring.position.y = MARKER_RING_Y");
    expect(src).not.toMatch(/ring\.position\.y = 0\.04/);
    expect(src).not.toMatch(/const MARKER_RING_Y = 0\.04;/);
    expect(src).not.toMatch(/export const MARKER_RING_Y = 0\.04;/);
    expect(src).toContain("export const NOISE_RING_Y = 0.0575");
    expect(src).toContain("mesh.position.y = NOISE_RING_Y");
    expect(src).toContain("slot.mesh.position.set(state.x, NOISE_RING_Y, state.y)");
    expect(src).not.toMatch(/const NOISE_RING_Y = 0\.05;/);
    expect(src).not.toMatch(/export const NOISE_RING_Y = 0\.05;/);
    expect(src).toContain("export const CHEVRON_TILT = -0.4025");
    expect(src).toContain("chevronMesh.rotation.x = CHEVRON_TILT");
    expect(src).not.toMatch(/chevronMesh\.rotation\.x = -0\.35/);
    expect(src).not.toMatch(/const CHEVRON_TILT = -0\.35/);
    expect(src).not.toMatch(/export const CHEVRON_TILT = -0\.35/);
    expect(src).toContain("export const CHEVRON_Y = 0.138");
    expect(src).toContain("chevronMesh.position.set(x, CHEVRON_Y, z)");
    expect(src).not.toMatch(/const CHEVRON_Y = 0\.12/);
    expect(src).not.toMatch(/export const CHEVRON_Y = 0\.12/);
    expect(src).not.toMatch(/chevronMesh\.position\.set\(x, 0\.12, z\)/);
    expect(src).toContain("export const MUZZLE_FORWARD = 0.552");
    expect(src).toContain("Math.sin(playerGltfYaw) * MUZZLE_FORWARD");
    expect(src).toContain("Math.cos(playerGltfYaw) * MUZZLE_FORWARD");
    expect(src).toContain("muzzleMesh.position.set(ox, TRACER_HEIGHT, oz)");
    expect(src).toContain("muzzleLight.position.set(ox, TRACER_HEIGHT, oz)");
    expect(src).not.toMatch(/const MUZZLE_FORWARD = 0\.48/);
    expect(src).not.toMatch(/export const MUZZLE_FORWARD = 0\.48/);
    expect(src).toContain("export const FLASHLIGHT_SPOT_TARGET_Y = 0.138");
    expect(src).toContain("torchSpot.target.position.set(wx + tip.x, FLASHLIGHT_SPOT_TARGET_Y, wy + tip.z)");
    expect(src).not.toMatch(/torchSpot\.target\.position\.set\(wx \+ tip\.x, 0\.12, wy \+ tip\.z\)/);
    expect(src).not.toMatch(/export const FLASHLIGHT_SPOT_TARGET_Y = 0\.12/);
    expect(src).toContain("export const TRACER_FLASH_Y_OFFSET = 0.1725");
    expect(src).toContain("flash.position.set(from.x, TRACER_HEIGHT + TRACER_FLASH_Y_OFFSET, from.y)");
    expect(src).not.toMatch(/flash\.position\.set\(from\.x, TRACER_HEIGHT \+ 0\.15, from\.y\)/);
    expect(src).not.toMatch(/export const TRACER_FLASH_Y_OFFSET = 0\.15/);
    expect(src).toContain("export const TRACER_FLASH_DISTANCE = 3.68");
    expect(src).toContain("new THREE.PointLight(TRACER_FLASH_COLOR, TRACER_FLASH_INTENSITY, TRACER_FLASH_DISTANCE, TRACER_FLASH_DECAY)");
    expect(src).not.toMatch(/new THREE\.PointLight\(TRACER_FLASH_COLOR, TRACER_FLASH_INTENSITY, 3\.2, TRACER_FLASH_DECAY\)/);
    expect(src).not.toMatch(/new THREE\.PointLight\(TRACER_FLASH_COLOR, 2\.4, 3\.2, TRACER_FLASH_DECAY\)/);
    expect(src).not.toMatch(/new THREE\.PointLight\(TRACER_FLASH_COLOR, 2\.4, 3\.2, 2\)/);
    expect(src).not.toMatch(/export const TRACER_FLASH_DISTANCE = 3\.2/);
    expect(src).toContain("t.flash.intensity = TRACER_FLASH_INTENSITY * op");
    expect(src).not.toMatch(/t\.flash\.intensity = 2\.4 \* op/);
    expect(src).toContain("export const TRACER_FLASH_INTENSITY = 2.76");
    expect(src).toContain("export const TRACER_FLASH_DECAY = 1.74");
    expect(src).toContain("export const TRACER_FLASH_COLOR = 0xffdd6e");
    expect(src).toContain("export const IMPACT_SPARK_LIGHT_DECAY = 1.74");
    expect(src).toContain("IMPACT_SPARK_LIGHT_DISTANCE,\n    IMPACT_SPARK_LIGHT_DECAY,");
    expect(src).not.toMatch(/IMPACT_SPARK_LIGHT_DISTANCE,\s*\n\s*2,/);
    expect(src).not.toMatch(/export const IMPACT_SPARK_LIGHT_DECAY = 2/);
    expect(src).toContain("const IMPACT_SPARK_LIGHT_DISTANCE = 1.8");
    expect(src).toContain("export const IMPACT_SPARK_LIGHT_COLOR = 0xffef93");
    expect(src).toContain("new THREE.PointLight(\n    IMPACT_SPARK_LIGHT_COLOR,");
    expect(src).toContain("export const MUZZLE_LIGHT_DECAY = 1.74");
    expect(src).toContain("MUZZLE_LIGHT_DISTANCE,\n    MUZZLE_LIGHT_DECAY,");
    expect(src).not.toMatch(/MUZZLE_LIGHT_DISTANCE,\s*\n\s*2,/);
    expect(src).not.toMatch(/export const MUZZLE_LIGHT_DECAY = 2/);
    expect(src).toContain("const MUZZLE_LIGHT_DISTANCE = 2.6");
    expect(src).toContain("export const MUZZLE_LIGHT_COLOR = 0xffffb8");
    expect(src).toContain("new THREE.PointLight(\n    MUZZLE_LIGHT_COLOR,");
    expect(src).toContain("export const FLASHLIGHT_SPOT_DECAY = 1.74");
    expect(src).toContain("FLASHLIGHT_SPOT_PENUMBRA,\n    FLASHLIGHT_SPOT_DECAY,");
    expect(src).not.toMatch(/FLASHLIGHT_SPOT_PENUMBRA,\s*\n\s*2,/);
    expect(src).not.toMatch(/export const FLASHLIGHT_SPOT_DECAY = 2/);
    expect(src).toContain("FLASHLIGHT_CONE_LENGTH + 2.4");
    expect(src).toContain("export const FLASHLIGHT_FILL_DECAY = 1.74");
    expect(src).toContain("new THREE.PointLight(FLASHLIGHT_FILL_COLOR, 0, 10, FLASHLIGHT_FILL_DECAY)");
    expect(src).not.toMatch(/new THREE\.PointLight\(FLASHLIGHT_FILL_COLOR, 0, 10, 2\)/);
    expect(src).not.toMatch(/export const FLASHLIGHT_FILL_DECAY = 2/);
    expect(src).toContain("export const WARM_LIGHT_DECAY = 1.74");
    expect(src).toContain("new THREE.PointLight(WARM_LIGHT_COLOR, 0, 7.5, WARM_LIGHT_DECAY)");
    expect(src).not.toMatch(/new THREE\.PointLight\(WARM_LIGHT_COLOR, 0, 7\.5, 2\)/);
    expect(src).toContain("export const WARM_LIGHT_VISIBLE_EPS = 0.0174");
    expect(src).toContain("warmLight.visible = i > WARM_LIGHT_VISIBLE_EPS");
    expect(src).not.toMatch(/warmLight\.visible = i > 0\.02/);
    expect(src).not.toMatch(/export const WARM_LIGHT_VISIBLE_EPS = 0\.02/);
    expect(src).toContain("export const WARM_LIGHT_AMBER_B_GAIN = 0.115");
    expect(src).toContain("export const WARM_LIGHT_AMBER_B = 0.437");
    expect(src).toContain("export const WARM_LIGHT_AMBER_G_GAIN = 0.092");
    expect(src).toContain("export const WARM_LIGHT_AMBER_G = 0.759");
    expect(src).toContain(
      "warmLight.color.setRGB(1, WARM_LIGHT_AMBER_G + i * WARM_LIGHT_AMBER_G_GAIN, WARM_LIGHT_AMBER_B + i * WARM_LIGHT_AMBER_B_GAIN)",
    );
    expect(src).not.toMatch(/WARM_LIGHT_AMBER_B \+ i \* 0\.1/);
    expect(src).toContain("export const WARM_LIGHT_DISTANCE_GAIN = 2.875");
    expect(src).toContain("export const WARM_LIGHT_DISTANCE_BASE = 7.475");
    expect(src).toContain(
      "warmLight.distance = WARM_LIGHT_DISTANCE_BASE + i * WARM_LIGHT_DISTANCE_GAIN",
    );
    expect(src).not.toMatch(/export const WARM_LIGHT_DISTANCE_GAIN = 2\.5/);
    expect(src).not.toMatch(
      /warmLight\.distance = WARM_LIGHT_DISTANCE_BASE \+ i \* 2\.5/,
    );
    expect(src).not.toMatch(/warmLight\.distance = 6\.5 \+ i \* 2\.5/);
    expect(src).toContain("export const WARM_LIGHT_Y = 1.7825");
    expect(src).toContain("warmLight.position.set(wx, WARM_LIGHT_Y, wy)");
    expect(src).not.toMatch(/export const WARM_LIGHT_Y = 1\.55/);
    expect(src).not.toMatch(/warmLight\.position\.set\(wx, 1\.55, wy\)/);
    expect(src).toContain("warmLight.position.set(0, 1.6, 0)");
    expect(src).toContain("export const WARM_LIGHT_INTENSITY_MUL = 1.7825");
    expect(src).toContain("warmLight.intensity = i * WARM_LIGHT_INTENSITY_MUL");
    expect(src).not.toMatch(/export const WARM_LIGHT_INTENSITY_MUL = 1\.55/);
    expect(src).not.toMatch(/warmLight\.intensity = i \* 1\.55/);
    expect(src).toContain("export const FLASHLIGHT_SPOT_DISTANCE_GAIN = 2.3");
    expect(src).toContain("export const FLASHLIGHT_SPOT_DISTANCE_EXTRA = 1.84");
    expect(src).toContain(
      "torchSpot.distance = FLASHLIGHT_CONE_LENGTH + FLASHLIGHT_SPOT_DISTANCE_EXTRA + i * FLASHLIGHT_SPOT_DISTANCE_GAIN",
    );
    expect(src).toContain("export const FLASHLIGHT_SPOT_Y = 1.7825");
    expect(src).toContain("torchSpot.position.set(0, FLASHLIGHT_SPOT_Y, 0)");
    expect(src).toContain("torchSpot.position.set(wx, FLASHLIGHT_SPOT_Y, wy)");
    expect(src).toContain("export const FLASHLIGHT_FILL_Y = 1.5525");
    expect(src).toContain("torchLight.position.set(0, FLASHLIGHT_FILL_Y, 0)");
    expect(src).toContain("torchLight.position.set(wx, FLASHLIGHT_FILL_Y, wy)");
    expect(src).toContain("export const FLASHLIGHT_FILL_DISTANCE_GAIN = 4.025");
    expect(src).toContain("export const FLASHLIGHT_FILL_DISTANCE_BASE = 8.05");
    expect(src).toContain(
      "torchLight.distance = FLASHLIGHT_FILL_DISTANCE_BASE + i * FLASHLIGHT_FILL_DISTANCE_GAIN",
    );
    expect(src).toContain("export const FLASHLIGHT_FILL_COLOR = 0xcaefff");
    expect(src).toContain("torchLight.color.setHex(FLASHLIGHT_FILL_COLOR)");
    expect(src).toContain("export const TRACER_COLOR = 0xffffb8");
    expect(src).toContain("color: TRACER_COLOR");
    expect(src).toContain("export const IMPACT_SPARK_COLOR = 0xffef93");
    expect(src).toContain("color: IMPACT_SPARK_COLOR");
    expect(src).toContain("export const MUZZLE_FLASH_COLOR = 0xffffdd");
    expect(src).toContain("color: MUZZLE_FLASH_COLOR");
    expect(src).toContain("export const WARM_LIGHT_COLOR = 0xffca81");
    expect(src).toContain("export const POSSESSED_HEAD_EMISSIVE = 0x30124a");
    expect(src).toContain("emissive: POSSESSED_HEAD_EMISSIVE");
    expect(src).toContain("color: POSSESSED_HEAD_COLOR");
    expect(src).toContain("export const POSSESSED_HEAD_COLOR = 0x8c469f");
    expect(src).toContain("emissive: POSSESSED_EMISSIVE");
    expect(src).toContain("color: POSSESSED_COLOR");
    expect(src).toContain("const POSSESSED_EMISSIVE = 0x1e0925");
    expect(src).toContain("emissiveIntensity: 0.22");
    expect(src).toContain("color: HOSTILE_COLOR");
    expect(src).toContain("color: PLAYER_COLOR");
    expect(src).toContain("color: PLAYER_HEAD_COLOR");
    expect(src).toContain("emissive: PLAYER_HEAD_EMISSIVE");
    expect(TRACER_FLASH_COLOR).toBe(0xffdd6e);
    expect(TRACER_FLASH_INTENSITY).toBe(2.76);
    expect(TRACER_FLASH_DISTANCE).toBe(3.68);
    expect(TRACER_FLASH_DECAY).toBe(1.74);
    expect(TRACER_FLASH_Y_OFFSET).toBe(0.1725);
    expect(TRACER_COLOR).toBe(0xffffb8);
    expect(IMPACT_SPARK_LIGHT_COLOR).toBe(0xffef93);
    expect(IMPACT_SPARK_COLOR).toBe(0xffef93);
    expect(IMPACT_SPARK_LIGHT_DECAY).toBe(1.74);
    expect(MUZZLE_LIGHT_COLOR).toBe(0xffffb8);
    expect(MUZZLE_LIGHT_DECAY).toBe(1.74);
    expect(MUZZLE_FLASH_COLOR).toBe(0xffffdd);
    expect(WARM_LIGHT_COLOR).toBe(0xffca81);
    expect(WARM_LIGHT_DECAY).toBe(1.74);
    expect(WARM_LIGHT_VISIBLE_EPS).toBe(0.0174);
    expect(WARM_LIGHT_AMBER_B_GAIN).toBe(0.115);
    expect(WARM_LIGHT_AMBER_B).toBe(0.437);
    expect(WARM_LIGHT_AMBER_G_GAIN).toBe(0.092);
    expect(WARM_LIGHT_AMBER_G).toBe(0.759);
    expect(WARM_LIGHT_DISTANCE_GAIN).toBe(2.875);
    expect(WARM_LIGHT_DISTANCE_BASE).toBe(7.475);
    expect(WARM_LIGHT_Y).toBe(1.7825);
    expect(WARM_LIGHT_INTENSITY_MUL).toBe(1.7825);
    expect(POSSESSED_HEAD_EMISSIVE).toBe(0x30124a);
    expect(POSSESSED_HEAD_COLOR).toBe(0x8c469f);
    expect(POSSESSED_COLOR).toBe(0x68347b);
    expect(HOSTILE_COLOR).toBe(0x7b1e1e);
    expect(PLAYER_COLOR).toBe(0x55a4f4);
    expect(PLAYER_HEAD_COLOR).toBe(0x91d1ff);
    expect(PLAYER_HEAD_EMISSIVE).toBe(0x122537);
    expect(MARKER_PALETTE.possessed.emissive).toBe(0x30124a);
    expect(MARKER_PALETTE.player.badge).toBe(0x91d1ff);
    expect(MARKER_PALETTE.player.emissive).toBe(0x1e4a6e);
    expect(MARKER_PALETTE.player.ring).toBe(0x4392f4);
    expect(MARKER_RING_OPACITY).toBe(0.6877);
    expect(muteBadgeY).toBe(2.3);
    expect(possessedBadgeY).toBe(2.3);
  });
});

describe("worldView marker icon size", () => {
  test("marker icon size 0.18 × 1.15 → 0.207; marker-icon-y/marker-ring-y/noise-ring-y/chevron-tilt/chevron-y/muzzle-forward/spot-target-y/y-offset/distance/intensity/decay/impact-spark-decay/muzzle-decay/spot-decay/fill-decay/warm-decay/visible-eps/amber-B-gain/amber-B/amber-G-gain/amber-G/gain/base/Y/intensity/color/spot-gain/extra/spot-Y/fill-Y/gain/base/flash/mesh/color/possessed/hostile/player/palette/badge-Y iguales", () => {
    expect(MARKER_ICON_SIZE).toBe(0.207);
    expect(MARKER_ICON_SIZE).toBeCloseTo(0.18 * 1.15, 10);
    expect(MARKER_BADGE_RADIUS).toBe(0.184);
    expect(MARKER_BADGE_RADIUS).toBeCloseTo(0.16 * 1.15, 10);
    expect(MARKER_BADGE_TILT).toBe(2.99);
    expect(MARKER_BADGE_TILT).toBeCloseTo(2.6 * 1.15, 10);
    expect(PLAYER_BODY_BASE_Y).toBe(0.644);
    expect(PLAYER_BODY_BASE_Y).toBeCloseTo(0.56 * 1.15, 10);
    expect(PLAYER_HEAD_BASE_Y).toBe(1.518);
    expect(PLAYER_HEAD_BASE_Y).toBeCloseTo(1.32 * 1.15, 10);
    expect(HOSTILE_BODY_BASE_Y).toBe(0.644);
    expect(HOSTILE_BODY_BASE_Y).toBeCloseTo(0.56 * 1.15, 10);
    expect(HOSTILE_HEAD_BASE_Y).toBe(1.495);
    expect(HOSTILE_HEAD_BASE_Y).toBeCloseTo(1.3 * 1.15, 10);
    expect(HOSTILE_MUTE_XZ_SCALE).toBe(1.2075);
    expect(HOSTILE_MUTE_XZ_SCALE).toBeCloseTo(1.05 * 1.15, 10);
    expect(MARKER_ICON_Y).toBe(0.023);
    expect(MARKER_ICON_Y).toBeCloseTo(0.02 * 1.15, 10);
    expect(MARKER_RING_Y).toBe(0.046);
    expect(MARKER_RING_Y).toBeCloseTo(0.04 * 1.15, 10);
    expect(NOISE_RING_Y).toBe(0.0575);
    expect(NOISE_RING_Y).toBeCloseTo(0.05 * 1.15, 10);
    expect(CHEVRON_TILT).toBe(-0.4025);
    expect(CHEVRON_Y).toBe(0.138);
    expect(MUZZLE_FORWARD).toBe(0.552);
    expect(FLASHLIGHT_SPOT_TARGET_Y).toBe(0.138);
    expect(TRACER_FLASH_Y_OFFSET).toBe(0.1725);
    expect(TRACER_FLASH_DISTANCE).toBe(3.68);
    expect(TRACER_FLASH_INTENSITY).toBe(2.76);
    expect(TRACER_FLASH_DECAY).toBe(1.74);
    expect(IMPACT_SPARK_LIGHT_DECAY).toBe(1.74);
    expect(MUZZLE_LIGHT_DECAY).toBe(1.74);
    expect(FLASHLIGHT_SPOT_DECAY).toBe(1.74);
    expect(FLASHLIGHT_FILL_DECAY).toBe(1.74);
    expect(WARM_LIGHT_DECAY).toBe(1.74);
    expect(WARM_LIGHT_VISIBLE_EPS).toBe(0.0174);
    expect(WARM_LIGHT_AMBER_B_GAIN).toBe(0.115);
    expect(WARM_LIGHT_AMBER_B).toBe(0.437);
    expect(WARM_LIGHT_AMBER_G_GAIN).toBe(0.092);
    expect(WARM_LIGHT_AMBER_G).toBe(0.759);
    expect(WARM_LIGHT_DISTANCE_GAIN).toBe(2.875);
    expect(WARM_LIGHT_DISTANCE_BASE).toBe(7.475);
    expect(WARM_LIGHT_Y).toBe(1.7825);
    expect(WARM_LIGHT_INTENSITY_MUL).toBe(1.7825);
    expect(FLASHLIGHT_SPOT_DISTANCE_GAIN).toBe(2.3);
    expect(FLASHLIGHT_SPOT_DISTANCE_EXTRA).toBe(1.84);
    expect(FLASHLIGHT_SPOT_Y).toBe(1.7825);
    expect(FLASHLIGHT_FILL_Y).toBe(1.5525);
    expect(FLASHLIGHT_FILL_DISTANCE_GAIN).toBe(4.025);
    expect(FLASHLIGHT_FILL_DISTANCE_BASE).toBe(8.05);
    expect(FLASHLIGHT_FILL_COLOR).toBe(0xcaefff);
    expect(MUZZLE_LIGHT_COLOR).toBe(0xffffb8);
    expect(IMPACT_SPARK_LIGHT_COLOR).toBe(0xffef93);
    expect(TRACER_FLASH_COLOR).toBe(0xffdd6e);
    const src = readFileSync(
      resolve(process.cwd(), "src/render/worldView.ts"),
      "utf8",
    );
    expect(src).toContain("export const MARKER_ICON_SIZE = 0.207");
    expect(src).toContain("new THREE.PlaneGeometry(MARKER_ICON_SIZE, MARKER_ICON_SIZE)");
    expect(src).not.toMatch(/new THREE\.PlaneGeometry\(0\.18, 0\.18\)/);
    expect(src).not.toMatch(/const MARKER_ICON_SIZE = 0\.18/);
    expect(src).not.toMatch(/export const MARKER_ICON_SIZE = 0\.18/);
    expect(src).toContain("export const MARKER_BADGE_RADIUS = 0.184");
    expect(src).toContain("new THREE.CircleGeometry(MARKER_BADGE_RADIUS, 20)");
    expect(src).not.toMatch(/new THREE\.CircleGeometry\(0\.16, 20\)/);
    expect(src).not.toMatch(/const MARKER_BADGE_RADIUS = 0\.16/);
    expect(src).not.toMatch(/export const MARKER_BADGE_RADIUS = 0\.16/);
    expect(src).toContain("export const MARKER_BADGE_TILT = 2.99");
    expect(src).toContain("disc.rotation.x = -Math.PI / MARKER_BADGE_TILT");
    expect(src).toContain("icon.rotation.x = -Math.PI / MARKER_BADGE_TILT");
    expect(src).not.toMatch(/disc\.rotation\.x = -Math\.PI \/ 2\.6/);
    expect(src).not.toMatch(/icon\.rotation\.x = -Math\.PI \/ 2\.6/);
    expect(src).not.toMatch(/const MARKER_BADGE_TILT = 2\.6/);
    expect(src).not.toMatch(/export const MARKER_BADGE_TILT = 2\.6/);
    expect(src).toContain("export const PLAYER_BODY_BASE_Y = 0.644");
    expect(src).toContain("playerBody.position.y = PLAYER_BODY_BASE_Y");
    expect(src).not.toMatch(/playerBody\.position\.y = 0\.56/);
    expect(src).not.toMatch(/const PLAYER_BODY_BASE_Y = 0\.56/);
    expect(src).not.toMatch(/export const PLAYER_BODY_BASE_Y = 0\.56/);
    expect(src).toContain("export const PLAYER_HEAD_BASE_Y = 1.518");
    expect(src).toContain("playerHead.position.y = PLAYER_HEAD_BASE_Y");
    expect(src).not.toMatch(/playerHead\.position\.y = 1\.32/);
    expect(src).not.toMatch(/const PLAYER_HEAD_BASE_Y = 1\.32/);
    expect(src).not.toMatch(/export const PLAYER_HEAD_BASE_Y = 1\.32/);
    expect(src).toContain("export const HOSTILE_BODY_BASE_Y = 0.644");
    expect(src).toContain("mesh.position.y = HOSTILE_BODY_BASE_Y");
    expect(src).toContain("body.position.y = HOSTILE_BODY_BASE_Y");
    expect(src).not.toMatch(/mesh\.position\.y = 0\.56/);
    expect(src).not.toMatch(/body\.position\.y = 0\.56/);
    expect(src).not.toMatch(/const HOSTILE_BODY_BASE_Y = 0\.56/);
    expect(src).not.toMatch(/export const HOSTILE_BODY_BASE_Y = 0\.56/);
    expect(src).toContain("export const HOSTILE_HEAD_BASE_Y = 1.495");
    expect(src).toContain("head.position.y = HOSTILE_HEAD_BASE_Y");
    expect(src).not.toMatch(/head\.position\.y = 1\.3/);
    expect(src).not.toMatch(/const HOSTILE_HEAD_BASE_Y = 1\.3/);
    expect(src).not.toMatch(/export const HOSTILE_HEAD_BASE_Y = 1\.3/);
    expect(src).toContain("export const HOSTILE_MUTE_XZ_SCALE = 1.2075");
    expect(src).toContain("mesh.scale.set(HOSTILE_MUTE_XZ_SCALE, 1, HOSTILE_MUTE_XZ_SCALE)");
    expect(src).not.toMatch(/mesh\.scale\.set\(1\.05, 1, 1\.05\)/);
    expect(src).not.toMatch(/const HOSTILE_MUTE_XZ_SCALE = 1\.05/);
    expect(src).not.toMatch(/export const HOSTILE_MUTE_XZ_SCALE = 1\.05/);
    expect(src).toContain("export const MARKER_ICON_Y = 0.023");
    expect(src).toContain("icon.position.y = MARKER_ICON_Y");
    expect(src).not.toMatch(/icon\.position\.y = 0\.02/);
    expect(src).not.toMatch(/const MARKER_ICON_Y = 0\.02;/);
    expect(src).not.toMatch(/export const MARKER_ICON_Y = 0\.02;/);
    expect(src).toContain("export const MARKER_RING_Y = 0.046");
    expect(src).toContain("ring.position.y = MARKER_RING_Y");
    expect(src).not.toMatch(/ring\.position\.y = 0\.04/);
    expect(src).not.toMatch(/const MARKER_RING_Y = 0\.04;/);
    expect(src).not.toMatch(/export const MARKER_RING_Y = 0\.04;/);
    expect(src).toContain("export const NOISE_RING_Y = 0.0575");
    expect(src).toContain("mesh.position.y = NOISE_RING_Y");
    expect(src).toContain("slot.mesh.position.set(state.x, NOISE_RING_Y, state.y)");
    expect(src).not.toMatch(/const NOISE_RING_Y = 0\.05;/);
    expect(src).not.toMatch(/export const NOISE_RING_Y = 0\.05;/);
    expect(src).toContain("export const CHEVRON_TILT = -0.4025");
    expect(src).toContain("chevronMesh.rotation.x = CHEVRON_TILT");
    expect(src).not.toMatch(/chevronMesh\.rotation\.x = -0\.35/);
    expect(src).not.toMatch(/const CHEVRON_TILT = -0\.35/);
    expect(src).not.toMatch(/export const CHEVRON_TILT = -0\.35/);
    expect(src).toContain("export const CHEVRON_Y = 0.138");
    expect(src).toContain("chevronMesh.position.set(x, CHEVRON_Y, z)");
    expect(src).not.toMatch(/const CHEVRON_Y = 0\.12/);
    expect(src).not.toMatch(/export const CHEVRON_Y = 0\.12/);
    expect(src).not.toMatch(/chevronMesh\.position\.set\(x, 0\.12, z\)/);
    expect(src).toContain("export const MUZZLE_FORWARD = 0.552");
    expect(src).toContain("Math.sin(playerGltfYaw) * MUZZLE_FORWARD");
    expect(src).toContain("Math.cos(playerGltfYaw) * MUZZLE_FORWARD");
    expect(src).toContain("muzzleMesh.position.set(ox, TRACER_HEIGHT, oz)");
    expect(src).toContain("muzzleLight.position.set(ox, TRACER_HEIGHT, oz)");
    expect(src).not.toMatch(/const MUZZLE_FORWARD = 0\.48/);
    expect(src).not.toMatch(/export const MUZZLE_FORWARD = 0\.48/);
    expect(src).toContain("export const FLASHLIGHT_SPOT_TARGET_Y = 0.138");
    expect(src).toContain("torchSpot.target.position.set(wx + tip.x, FLASHLIGHT_SPOT_TARGET_Y, wy + tip.z)");
    expect(src).not.toMatch(/torchSpot\.target\.position\.set\(wx \+ tip\.x, 0\.12, wy \+ tip\.z\)/);
    expect(src).not.toMatch(/export const FLASHLIGHT_SPOT_TARGET_Y = 0\.12/);
    expect(src).toContain("export const TRACER_FLASH_Y_OFFSET = 0.1725");
    expect(src).toContain("flash.position.set(from.x, TRACER_HEIGHT + TRACER_FLASH_Y_OFFSET, from.y)");
    expect(src).not.toMatch(/flash\.position\.set\(from\.x, TRACER_HEIGHT \+ 0\.15, from\.y\)/);
    expect(src).not.toMatch(/export const TRACER_FLASH_Y_OFFSET = 0\.15/);
    expect(src).toContain("export const TRACER_FLASH_DISTANCE = 3.68");
    expect(src).toContain("new THREE.PointLight(TRACER_FLASH_COLOR, TRACER_FLASH_INTENSITY, TRACER_FLASH_DISTANCE, TRACER_FLASH_DECAY)");
    expect(src).not.toMatch(/new THREE\.PointLight\(TRACER_FLASH_COLOR, TRACER_FLASH_INTENSITY, 3\.2, TRACER_FLASH_DECAY\)/);
    expect(src).not.toMatch(/new THREE\.PointLight\(TRACER_FLASH_COLOR, 2\.4, 3\.2, TRACER_FLASH_DECAY\)/);
    expect(src).not.toMatch(/new THREE\.PointLight\(TRACER_FLASH_COLOR, 2\.4, 3\.2, 2\)/);
    expect(src).not.toMatch(/export const TRACER_FLASH_DISTANCE = 3\.2/);
    expect(src).toContain("t.flash.intensity = TRACER_FLASH_INTENSITY * op");
    expect(src).not.toMatch(/t\.flash\.intensity = 2\.4 \* op/);
    expect(src).toContain("export const TRACER_FLASH_INTENSITY = 2.76");
    expect(src).toContain("export const TRACER_FLASH_DECAY = 1.74");
    expect(src).toContain("export const TRACER_FLASH_COLOR = 0xffdd6e");
    expect(src).toContain("export const IMPACT_SPARK_LIGHT_DECAY = 1.74");
    expect(src).toContain("IMPACT_SPARK_LIGHT_DISTANCE,\n    IMPACT_SPARK_LIGHT_DECAY,");
    expect(src).not.toMatch(/IMPACT_SPARK_LIGHT_DISTANCE,\s*\n\s*2,/);
    expect(src).not.toMatch(/export const IMPACT_SPARK_LIGHT_DECAY = 2/);
    expect(src).toContain("const IMPACT_SPARK_LIGHT_DISTANCE = 1.8");
    expect(src).toContain("export const IMPACT_SPARK_LIGHT_COLOR = 0xffef93");
    expect(src).toContain("new THREE.PointLight(\n    IMPACT_SPARK_LIGHT_COLOR,");
    expect(src).toContain("export const MUZZLE_LIGHT_DECAY = 1.74");
    expect(src).toContain("MUZZLE_LIGHT_DISTANCE,\n    MUZZLE_LIGHT_DECAY,");
    expect(src).not.toMatch(/MUZZLE_LIGHT_DISTANCE,\s*\n\s*2,/);
    expect(src).not.toMatch(/export const MUZZLE_LIGHT_DECAY = 2/);
    expect(src).toContain("const MUZZLE_LIGHT_DISTANCE = 2.6");
    expect(src).toContain("export const MUZZLE_LIGHT_COLOR = 0xffffb8");
    expect(src).toContain("new THREE.PointLight(\n    MUZZLE_LIGHT_COLOR,");
    expect(src).toContain("export const FLASHLIGHT_SPOT_DECAY = 1.74");
    expect(src).toContain("FLASHLIGHT_SPOT_PENUMBRA,\n    FLASHLIGHT_SPOT_DECAY,");
    expect(src).not.toMatch(/FLASHLIGHT_SPOT_PENUMBRA,\s*\n\s*2,/);
    expect(src).not.toMatch(/export const FLASHLIGHT_SPOT_DECAY = 2/);
    expect(src).toContain("FLASHLIGHT_CONE_LENGTH + 2.4");
    expect(src).toContain("export const FLASHLIGHT_FILL_DECAY = 1.74");
    expect(src).toContain("new THREE.PointLight(FLASHLIGHT_FILL_COLOR, 0, 10, FLASHLIGHT_FILL_DECAY)");
    expect(src).not.toMatch(/new THREE\.PointLight\(FLASHLIGHT_FILL_COLOR, 0, 10, 2\)/);
    expect(src).not.toMatch(/export const FLASHLIGHT_FILL_DECAY = 2/);
    expect(src).toContain("export const WARM_LIGHT_DECAY = 1.74");
    expect(src).toContain("new THREE.PointLight(WARM_LIGHT_COLOR, 0, 7.5, WARM_LIGHT_DECAY)");
    expect(src).not.toMatch(/new THREE\.PointLight\(WARM_LIGHT_COLOR, 0, 7\.5, 2\)/);
    expect(src).toContain("export const WARM_LIGHT_VISIBLE_EPS = 0.0174");
    expect(src).toContain("warmLight.visible = i > WARM_LIGHT_VISIBLE_EPS");
    expect(src).not.toMatch(/warmLight\.visible = i > 0\.02/);
    expect(src).not.toMatch(/export const WARM_LIGHT_VISIBLE_EPS = 0\.02/);
    expect(src).toContain("export const WARM_LIGHT_AMBER_B_GAIN = 0.115");
    expect(src).toContain("export const WARM_LIGHT_AMBER_B = 0.437");
    expect(src).toContain("export const WARM_LIGHT_AMBER_G_GAIN = 0.092");
    expect(src).toContain("export const WARM_LIGHT_AMBER_G = 0.759");
    expect(src).toContain(
      "warmLight.color.setRGB(1, WARM_LIGHT_AMBER_G + i * WARM_LIGHT_AMBER_G_GAIN, WARM_LIGHT_AMBER_B + i * WARM_LIGHT_AMBER_B_GAIN)",
    );
    expect(src).not.toMatch(/WARM_LIGHT_AMBER_B \+ i \* 0\.1/);
    expect(src).toContain("export const WARM_LIGHT_DISTANCE_GAIN = 2.875");
    expect(src).toContain("export const WARM_LIGHT_DISTANCE_BASE = 7.475");
    expect(src).toContain(
      "warmLight.distance = WARM_LIGHT_DISTANCE_BASE + i * WARM_LIGHT_DISTANCE_GAIN",
    );
    expect(src).not.toMatch(/export const WARM_LIGHT_DISTANCE_GAIN = 2\.5/);
    expect(src).not.toMatch(
      /warmLight\.distance = WARM_LIGHT_DISTANCE_BASE \+ i \* 2\.5/,
    );
    expect(src).not.toMatch(/warmLight\.distance = 6\.5 \+ i \* 2\.5/);
    expect(src).toContain("export const WARM_LIGHT_Y = 1.7825");
    expect(src).toContain("warmLight.position.set(wx, WARM_LIGHT_Y, wy)");
    expect(src).not.toMatch(/export const WARM_LIGHT_Y = 1\.55/);
    expect(src).not.toMatch(/warmLight\.position\.set\(wx, 1\.55, wy\)/);
    expect(src).toContain("warmLight.position.set(0, 1.6, 0)");
    expect(src).toContain("export const WARM_LIGHT_INTENSITY_MUL = 1.7825");
    expect(src).toContain("warmLight.intensity = i * WARM_LIGHT_INTENSITY_MUL");
    expect(src).not.toMatch(/export const WARM_LIGHT_INTENSITY_MUL = 1\.55/);
    expect(src).not.toMatch(/warmLight\.intensity = i \* 1\.55/);
    expect(src).toContain("export const FLASHLIGHT_SPOT_DISTANCE_GAIN = 2.3");
    expect(src).toContain("export const FLASHLIGHT_SPOT_DISTANCE_EXTRA = 1.84");
    expect(src).toContain(
      "torchSpot.distance = FLASHLIGHT_CONE_LENGTH + FLASHLIGHT_SPOT_DISTANCE_EXTRA + i * FLASHLIGHT_SPOT_DISTANCE_GAIN",
    );
    expect(src).toContain("export const FLASHLIGHT_SPOT_Y = 1.7825");
    expect(src).toContain("torchSpot.position.set(0, FLASHLIGHT_SPOT_Y, 0)");
    expect(src).toContain("torchSpot.position.set(wx, FLASHLIGHT_SPOT_Y, wy)");
    expect(src).toContain("export const FLASHLIGHT_FILL_Y = 1.5525");
    expect(src).toContain("torchLight.position.set(0, FLASHLIGHT_FILL_Y, 0)");
    expect(src).toContain("torchLight.position.set(wx, FLASHLIGHT_FILL_Y, wy)");
    expect(src).toContain("export const FLASHLIGHT_FILL_DISTANCE_GAIN = 4.025");
    expect(src).toContain("export const FLASHLIGHT_FILL_DISTANCE_BASE = 8.05");
    expect(src).toContain(
      "torchLight.distance = FLASHLIGHT_FILL_DISTANCE_BASE + i * FLASHLIGHT_FILL_DISTANCE_GAIN",
    );
    expect(src).toContain("export const FLASHLIGHT_FILL_COLOR = 0xcaefff");
    expect(src).toContain("torchLight.color.setHex(FLASHLIGHT_FILL_COLOR)");
    expect(src).toContain("export const TRACER_COLOR = 0xffffb8");
    expect(src).toContain("color: TRACER_COLOR");
    expect(src).toContain("export const IMPACT_SPARK_COLOR = 0xffef93");
    expect(src).toContain("color: IMPACT_SPARK_COLOR");
    expect(src).toContain("export const MUZZLE_FLASH_COLOR = 0xffffdd");
    expect(src).toContain("color: MUZZLE_FLASH_COLOR");
    expect(src).toContain("export const WARM_LIGHT_COLOR = 0xffca81");
    expect(src).toContain("export const POSSESSED_HEAD_EMISSIVE = 0x30124a");
    expect(src).toContain("emissive: POSSESSED_HEAD_EMISSIVE");
    expect(src).toContain("color: POSSESSED_HEAD_COLOR");
    expect(src).toContain("export const POSSESSED_HEAD_COLOR = 0x8c469f");
    expect(src).toContain("emissive: POSSESSED_EMISSIVE");
    expect(src).toContain("color: POSSESSED_COLOR");
    expect(src).toContain("const POSSESSED_EMISSIVE = 0x1e0925");
    expect(src).toContain("emissiveIntensity: 0.22");
    expect(src).toContain("color: HOSTILE_COLOR");
    expect(src).toContain("color: PLAYER_COLOR");
    expect(src).toContain("color: PLAYER_HEAD_COLOR");
    expect(src).toContain("emissive: PLAYER_HEAD_EMISSIVE");
    expect(TRACER_FLASH_COLOR).toBe(0xffdd6e);
    expect(TRACER_FLASH_INTENSITY).toBe(2.76);
    expect(TRACER_FLASH_DISTANCE).toBe(3.68);
    expect(TRACER_FLASH_DECAY).toBe(1.74);
    expect(TRACER_FLASH_Y_OFFSET).toBe(0.1725);
    expect(TRACER_COLOR).toBe(0xffffb8);
    expect(IMPACT_SPARK_LIGHT_COLOR).toBe(0xffef93);
    expect(IMPACT_SPARK_COLOR).toBe(0xffef93);
    expect(IMPACT_SPARK_LIGHT_DECAY).toBe(1.74);
    expect(MUZZLE_LIGHT_COLOR).toBe(0xffffb8);
    expect(MUZZLE_LIGHT_DECAY).toBe(1.74);
    expect(MUZZLE_FLASH_COLOR).toBe(0xffffdd);
    expect(WARM_LIGHT_COLOR).toBe(0xffca81);
    expect(WARM_LIGHT_DECAY).toBe(1.74);
    expect(WARM_LIGHT_VISIBLE_EPS).toBe(0.0174);
    expect(WARM_LIGHT_AMBER_B_GAIN).toBe(0.115);
    expect(WARM_LIGHT_AMBER_B).toBe(0.437);
    expect(WARM_LIGHT_AMBER_G_GAIN).toBe(0.092);
    expect(WARM_LIGHT_AMBER_G).toBe(0.759);
    expect(WARM_LIGHT_DISTANCE_GAIN).toBe(2.875);
    expect(WARM_LIGHT_DISTANCE_BASE).toBe(7.475);
    expect(WARM_LIGHT_Y).toBe(1.7825);
    expect(WARM_LIGHT_INTENSITY_MUL).toBe(1.7825);
    expect(POSSESSED_HEAD_EMISSIVE).toBe(0x30124a);
    expect(POSSESSED_HEAD_COLOR).toBe(0x8c469f);
    expect(POSSESSED_COLOR).toBe(0x68347b);
    expect(HOSTILE_COLOR).toBe(0x7b1e1e);
    expect(PLAYER_COLOR).toBe(0x55a4f4);
    expect(PLAYER_HEAD_COLOR).toBe(0x91d1ff);
    expect(PLAYER_HEAD_EMISSIVE).toBe(0x122537);
    expect(MARKER_PALETTE.possessed.emissive).toBe(0x30124a);
    expect(MARKER_PALETTE.player.badge).toBe(0x91d1ff);
    expect(MARKER_PALETTE.player.emissive).toBe(0x1e4a6e);
    expect(MARKER_PALETTE.player.ring).toBe(0x4392f4);
    expect(MARKER_RING_OPACITY).toBe(0.6877);
    expect(muteBadgeY).toBe(2.3);
    expect(possessedBadgeY).toBe(2.3);
  });
});

describe("worldView marker badge radius", () => {
  test("marker badge radius 0.16 × 1.15 → 0.184; marker-icon-size/marker-icon-y/marker-ring-y/noise-ring-y/chevron-tilt/chevron-y/muzzle-forward/spot-target-y/y-offset/distance/intensity/decay/impact-spark-decay/muzzle-decay/spot-decay/fill-decay/warm-decay/visible-eps/amber-B-gain/amber-B/amber-G-gain/amber-G/gain/base/Y/intensity/color/spot-gain/extra/spot-Y/fill-Y/gain/base/flash/mesh/color/possessed/hostile/player/palette/badge-Y iguales", () => {
    expect(MARKER_BADGE_RADIUS).toBe(0.184);
    expect(MARKER_BADGE_RADIUS).toBeCloseTo(0.16 * 1.15, 10);
    expect(MARKER_BADGE_TILT).toBe(2.99);
    expect(MARKER_BADGE_TILT).toBeCloseTo(2.6 * 1.15, 10);
    expect(PLAYER_BODY_BASE_Y).toBe(0.644);
    expect(PLAYER_BODY_BASE_Y).toBeCloseTo(0.56 * 1.15, 10);
    expect(PLAYER_HEAD_BASE_Y).toBe(1.518);
    expect(PLAYER_HEAD_BASE_Y).toBeCloseTo(1.32 * 1.15, 10);
    expect(HOSTILE_BODY_BASE_Y).toBe(0.644);
    expect(HOSTILE_BODY_BASE_Y).toBeCloseTo(0.56 * 1.15, 10);
    expect(HOSTILE_HEAD_BASE_Y).toBe(1.495);
    expect(HOSTILE_HEAD_BASE_Y).toBeCloseTo(1.3 * 1.15, 10);
    expect(HOSTILE_MUTE_XZ_SCALE).toBe(1.2075);
    expect(HOSTILE_MUTE_XZ_SCALE).toBeCloseTo(1.05 * 1.15, 10);
    expect(MARKER_ICON_SIZE).toBe(0.207);
    expect(MARKER_ICON_SIZE).toBeCloseTo(0.18 * 1.15, 10);
    expect(MARKER_ICON_Y).toBe(0.023);
    expect(MARKER_ICON_Y).toBeCloseTo(0.02 * 1.15, 10);
    expect(MARKER_RING_Y).toBe(0.046);
    expect(MARKER_RING_Y).toBeCloseTo(0.04 * 1.15, 10);
    expect(NOISE_RING_Y).toBe(0.0575);
    expect(NOISE_RING_Y).toBeCloseTo(0.05 * 1.15, 10);
    expect(CHEVRON_TILT).toBe(-0.4025);
    expect(CHEVRON_Y).toBe(0.138);
    expect(MUZZLE_FORWARD).toBe(0.552);
    expect(FLASHLIGHT_SPOT_TARGET_Y).toBe(0.138);
    expect(TRACER_FLASH_Y_OFFSET).toBe(0.1725);
    expect(TRACER_FLASH_DISTANCE).toBe(3.68);
    expect(TRACER_FLASH_INTENSITY).toBe(2.76);
    expect(TRACER_FLASH_DECAY).toBe(1.74);
    expect(IMPACT_SPARK_LIGHT_DECAY).toBe(1.74);
    expect(MUZZLE_LIGHT_DECAY).toBe(1.74);
    expect(FLASHLIGHT_SPOT_DECAY).toBe(1.74);
    expect(FLASHLIGHT_FILL_DECAY).toBe(1.74);
    expect(WARM_LIGHT_DECAY).toBe(1.74);
    expect(WARM_LIGHT_VISIBLE_EPS).toBe(0.0174);
    expect(WARM_LIGHT_AMBER_B_GAIN).toBe(0.115);
    expect(WARM_LIGHT_AMBER_B).toBe(0.437);
    expect(WARM_LIGHT_AMBER_G_GAIN).toBe(0.092);
    expect(WARM_LIGHT_AMBER_G).toBe(0.759);
    expect(WARM_LIGHT_DISTANCE_GAIN).toBe(2.875);
    expect(WARM_LIGHT_DISTANCE_BASE).toBe(7.475);
    expect(WARM_LIGHT_Y).toBe(1.7825);
    expect(WARM_LIGHT_INTENSITY_MUL).toBe(1.7825);
    expect(FLASHLIGHT_SPOT_DISTANCE_GAIN).toBe(2.3);
    expect(FLASHLIGHT_SPOT_DISTANCE_EXTRA).toBe(1.84);
    expect(FLASHLIGHT_SPOT_Y).toBe(1.7825);
    expect(FLASHLIGHT_FILL_Y).toBe(1.5525);
    expect(FLASHLIGHT_FILL_DISTANCE_GAIN).toBe(4.025);
    expect(FLASHLIGHT_FILL_DISTANCE_BASE).toBe(8.05);
    expect(FLASHLIGHT_FILL_COLOR).toBe(0xcaefff);
    expect(MUZZLE_LIGHT_COLOR).toBe(0xffffb8);
    expect(IMPACT_SPARK_LIGHT_COLOR).toBe(0xffef93);
    expect(TRACER_FLASH_COLOR).toBe(0xffdd6e);
    const src = readFileSync(
      resolve(process.cwd(), "src/render/worldView.ts"),
      "utf8",
    );
    expect(src).toContain("export const MARKER_BADGE_RADIUS = 0.184");
    expect(src).toContain("new THREE.CircleGeometry(MARKER_BADGE_RADIUS, 20)");
    expect(src).not.toMatch(/new THREE\.CircleGeometry\(0\.16, 20\)/);
    expect(src).not.toMatch(/const MARKER_BADGE_RADIUS = 0\.16/);
    expect(src).not.toMatch(/export const MARKER_BADGE_RADIUS = 0\.16/);
    expect(src).toContain("export const MARKER_BADGE_TILT = 2.99");
    expect(src).toContain("disc.rotation.x = -Math.PI / MARKER_BADGE_TILT");
    expect(src).toContain("icon.rotation.x = -Math.PI / MARKER_BADGE_TILT");
    expect(src).not.toMatch(/disc\.rotation\.x = -Math\.PI \/ 2\.6/);
    expect(src).not.toMatch(/icon\.rotation\.x = -Math\.PI \/ 2\.6/);
    expect(src).not.toMatch(/const MARKER_BADGE_TILT = 2\.6/);
    expect(src).not.toMatch(/export const MARKER_BADGE_TILT = 2\.6/);
    expect(src).toContain("export const PLAYER_BODY_BASE_Y = 0.644");
    expect(src).toContain("playerBody.position.y = PLAYER_BODY_BASE_Y");
    expect(src).not.toMatch(/playerBody\.position\.y = 0\.56/);
    expect(src).not.toMatch(/const PLAYER_BODY_BASE_Y = 0\.56/);
    expect(src).not.toMatch(/export const PLAYER_BODY_BASE_Y = 0\.56/);
    expect(src).toContain("export const PLAYER_HEAD_BASE_Y = 1.518");
    expect(src).toContain("playerHead.position.y = PLAYER_HEAD_BASE_Y");
    expect(src).not.toMatch(/playerHead\.position\.y = 1\.32/);
    expect(src).not.toMatch(/const PLAYER_HEAD_BASE_Y = 1\.32/);
    expect(src).not.toMatch(/export const PLAYER_HEAD_BASE_Y = 1\.32/);
    expect(src).toContain("export const HOSTILE_BODY_BASE_Y = 0.644");
    expect(src).toContain("mesh.position.y = HOSTILE_BODY_BASE_Y");
    expect(src).toContain("body.position.y = HOSTILE_BODY_BASE_Y");
    expect(src).not.toMatch(/mesh\.position\.y = 0\.56/);
    expect(src).not.toMatch(/body\.position\.y = 0\.56/);
    expect(src).not.toMatch(/const HOSTILE_BODY_BASE_Y = 0\.56/);
    expect(src).not.toMatch(/export const HOSTILE_BODY_BASE_Y = 0\.56/);
    expect(src).toContain("export const HOSTILE_HEAD_BASE_Y = 1.495");
    expect(src).toContain("head.position.y = HOSTILE_HEAD_BASE_Y");
    expect(src).not.toMatch(/head\.position\.y = 1\.3/);
    expect(src).not.toMatch(/const HOSTILE_HEAD_BASE_Y = 1\.3/);
    expect(src).not.toMatch(/export const HOSTILE_HEAD_BASE_Y = 1\.3/);
    expect(src).toContain("export const HOSTILE_MUTE_XZ_SCALE = 1.2075");
    expect(src).toContain("mesh.scale.set(HOSTILE_MUTE_XZ_SCALE, 1, HOSTILE_MUTE_XZ_SCALE)");
    expect(src).not.toMatch(/mesh\.scale\.set\(1\.05, 1, 1\.05\)/);
    expect(src).not.toMatch(/const HOSTILE_MUTE_XZ_SCALE = 1\.05/);
    expect(src).not.toMatch(/export const HOSTILE_MUTE_XZ_SCALE = 1\.05/);
    expect(src).toContain("export const MARKER_ICON_SIZE = 0.207");
    expect(src).toContain("new THREE.PlaneGeometry(MARKER_ICON_SIZE, MARKER_ICON_SIZE)");
    expect(src).not.toMatch(/new THREE\.PlaneGeometry\(0\.18, 0\.18\)/);
    expect(src).not.toMatch(/const MARKER_ICON_SIZE = 0\.18/);
    expect(src).not.toMatch(/export const MARKER_ICON_SIZE = 0\.18/);
    expect(src).toContain("export const MARKER_ICON_Y = 0.023");
    expect(src).toContain("icon.position.y = MARKER_ICON_Y");
    expect(src).not.toMatch(/icon\.position\.y = 0\.02/);
    expect(src).not.toMatch(/const MARKER_ICON_Y = 0\.02;/);
    expect(src).not.toMatch(/export const MARKER_ICON_Y = 0\.02;/);
    expect(src).toContain("export const MARKER_RING_Y = 0.046");
    expect(src).toContain("ring.position.y = MARKER_RING_Y");
    expect(src).not.toMatch(/ring\.position\.y = 0\.04/);
    expect(src).not.toMatch(/const MARKER_RING_Y = 0\.04;/);
    expect(src).not.toMatch(/export const MARKER_RING_Y = 0\.04;/);
    expect(src).toContain("export const NOISE_RING_Y = 0.0575");
    expect(src).toContain("mesh.position.y = NOISE_RING_Y");
    expect(src).toContain("slot.mesh.position.set(state.x, NOISE_RING_Y, state.y)");
    expect(src).not.toMatch(/const NOISE_RING_Y = 0\.05;/);
    expect(src).not.toMatch(/export const NOISE_RING_Y = 0\.05;/);
    expect(src).toContain("export const CHEVRON_TILT = -0.4025");
    expect(src).toContain("chevronMesh.rotation.x = CHEVRON_TILT");
    expect(src).not.toMatch(/chevronMesh\.rotation\.x = -0\.35/);
    expect(src).not.toMatch(/const CHEVRON_TILT = -0\.35/);
    expect(src).not.toMatch(/export const CHEVRON_TILT = -0\.35/);
    expect(src).toContain("export const CHEVRON_Y = 0.138");
    expect(src).toContain("chevronMesh.position.set(x, CHEVRON_Y, z)");
    expect(src).not.toMatch(/const CHEVRON_Y = 0\.12/);
    expect(src).not.toMatch(/export const CHEVRON_Y = 0\.12/);
    expect(src).not.toMatch(/chevronMesh\.position\.set\(x, 0\.12, z\)/);
    expect(src).toContain("export const MUZZLE_FORWARD = 0.552");
    expect(src).toContain("Math.sin(playerGltfYaw) * MUZZLE_FORWARD");
    expect(src).toContain("Math.cos(playerGltfYaw) * MUZZLE_FORWARD");
    expect(src).toContain("muzzleMesh.position.set(ox, TRACER_HEIGHT, oz)");
    expect(src).toContain("muzzleLight.position.set(ox, TRACER_HEIGHT, oz)");
    expect(src).not.toMatch(/const MUZZLE_FORWARD = 0\.48/);
    expect(src).not.toMatch(/export const MUZZLE_FORWARD = 0\.48/);
    expect(src).toContain("export const FLASHLIGHT_SPOT_TARGET_Y = 0.138");
    expect(src).toContain("torchSpot.target.position.set(wx + tip.x, FLASHLIGHT_SPOT_TARGET_Y, wy + tip.z)");
    expect(src).not.toMatch(/torchSpot\.target\.position\.set\(wx \+ tip\.x, 0\.12, wy \+ tip\.z\)/);
    expect(src).not.toMatch(/export const FLASHLIGHT_SPOT_TARGET_Y = 0\.12/);
    expect(src).toContain("export const TRACER_FLASH_Y_OFFSET = 0.1725");
    expect(src).toContain("flash.position.set(from.x, TRACER_HEIGHT + TRACER_FLASH_Y_OFFSET, from.y)");
    expect(src).not.toMatch(/flash\.position\.set\(from\.x, TRACER_HEIGHT \+ 0\.15, from\.y\)/);
    expect(src).not.toMatch(/export const TRACER_FLASH_Y_OFFSET = 0\.15/);
    expect(src).toContain("export const TRACER_FLASH_DISTANCE = 3.68");
    expect(src).toContain("new THREE.PointLight(TRACER_FLASH_COLOR, TRACER_FLASH_INTENSITY, TRACER_FLASH_DISTANCE, TRACER_FLASH_DECAY)");
    expect(src).not.toMatch(/new THREE\.PointLight\(TRACER_FLASH_COLOR, TRACER_FLASH_INTENSITY, 3\.2, TRACER_FLASH_DECAY\)/);
    expect(src).not.toMatch(/new THREE\.PointLight\(TRACER_FLASH_COLOR, 2\.4, 3\.2, TRACER_FLASH_DECAY\)/);
    expect(src).not.toMatch(/new THREE\.PointLight\(TRACER_FLASH_COLOR, 2\.4, 3\.2, 2\)/);
    expect(src).not.toMatch(/export const TRACER_FLASH_DISTANCE = 3\.2/);
    expect(src).toContain("t.flash.intensity = TRACER_FLASH_INTENSITY * op");
    expect(src).not.toMatch(/t\.flash\.intensity = 2\.4 \* op/);
    expect(src).toContain("export const TRACER_FLASH_INTENSITY = 2.76");
    expect(src).toContain("export const TRACER_FLASH_DECAY = 1.74");
    expect(src).toContain("export const TRACER_FLASH_COLOR = 0xffdd6e");
    expect(src).toContain("export const IMPACT_SPARK_LIGHT_DECAY = 1.74");
    expect(src).toContain("IMPACT_SPARK_LIGHT_DISTANCE,\n    IMPACT_SPARK_LIGHT_DECAY,");
    expect(src).not.toMatch(/IMPACT_SPARK_LIGHT_DISTANCE,\s*\n\s*2,/);
    expect(src).not.toMatch(/export const IMPACT_SPARK_LIGHT_DECAY = 2/);
    expect(src).toContain("const IMPACT_SPARK_LIGHT_DISTANCE = 1.8");
    expect(src).toContain("export const IMPACT_SPARK_LIGHT_COLOR = 0xffef93");
    expect(src).toContain("new THREE.PointLight(\n    IMPACT_SPARK_LIGHT_COLOR,");
    expect(src).toContain("export const MUZZLE_LIGHT_DECAY = 1.74");
    expect(src).toContain("MUZZLE_LIGHT_DISTANCE,\n    MUZZLE_LIGHT_DECAY,");
    expect(src).not.toMatch(/MUZZLE_LIGHT_DISTANCE,\s*\n\s*2,/);
    expect(src).not.toMatch(/export const MUZZLE_LIGHT_DECAY = 2/);
    expect(src).toContain("const MUZZLE_LIGHT_DISTANCE = 2.6");
    expect(src).toContain("export const MUZZLE_LIGHT_COLOR = 0xffffb8");
    expect(src).toContain("new THREE.PointLight(\n    MUZZLE_LIGHT_COLOR,");
    expect(src).toContain("export const FLASHLIGHT_SPOT_DECAY = 1.74");
    expect(src).toContain("FLASHLIGHT_SPOT_PENUMBRA,\n    FLASHLIGHT_SPOT_DECAY,");
    expect(src).not.toMatch(/FLASHLIGHT_SPOT_PENUMBRA,\s*\n\s*2,/);
    expect(src).not.toMatch(/export const FLASHLIGHT_SPOT_DECAY = 2/);
    expect(src).toContain("FLASHLIGHT_CONE_LENGTH + 2.4");
    expect(src).toContain("export const FLASHLIGHT_FILL_DECAY = 1.74");
    expect(src).toContain("new THREE.PointLight(FLASHLIGHT_FILL_COLOR, 0, 10, FLASHLIGHT_FILL_DECAY)");
    expect(src).not.toMatch(/new THREE\.PointLight\(FLASHLIGHT_FILL_COLOR, 0, 10, 2\)/);
    expect(src).not.toMatch(/export const FLASHLIGHT_FILL_DECAY = 2/);
    expect(src).toContain("export const WARM_LIGHT_DECAY = 1.74");
    expect(src).toContain("new THREE.PointLight(WARM_LIGHT_COLOR, 0, 7.5, WARM_LIGHT_DECAY)");
    expect(src).not.toMatch(/new THREE\.PointLight\(WARM_LIGHT_COLOR, 0, 7\.5, 2\)/);
    expect(src).toContain("export const WARM_LIGHT_VISIBLE_EPS = 0.0174");
    expect(src).toContain("warmLight.visible = i > WARM_LIGHT_VISIBLE_EPS");
    expect(src).not.toMatch(/warmLight\.visible = i > 0\.02/);
    expect(src).not.toMatch(/export const WARM_LIGHT_VISIBLE_EPS = 0\.02/);
    expect(src).toContain("export const WARM_LIGHT_AMBER_B_GAIN = 0.115");
    expect(src).toContain("export const WARM_LIGHT_AMBER_B = 0.437");
    expect(src).toContain("export const WARM_LIGHT_AMBER_G_GAIN = 0.092");
    expect(src).toContain("export const WARM_LIGHT_AMBER_G = 0.759");
    expect(src).toContain(
      "warmLight.color.setRGB(1, WARM_LIGHT_AMBER_G + i * WARM_LIGHT_AMBER_G_GAIN, WARM_LIGHT_AMBER_B + i * WARM_LIGHT_AMBER_B_GAIN)",
    );
    expect(src).not.toMatch(/WARM_LIGHT_AMBER_B \+ i \* 0\.1/);
    expect(src).toContain("export const WARM_LIGHT_DISTANCE_GAIN = 2.875");
    expect(src).toContain("export const WARM_LIGHT_DISTANCE_BASE = 7.475");
    expect(src).toContain(
      "warmLight.distance = WARM_LIGHT_DISTANCE_BASE + i * WARM_LIGHT_DISTANCE_GAIN",
    );
    expect(src).not.toMatch(/export const WARM_LIGHT_DISTANCE_GAIN = 2\.5/);
    expect(src).not.toMatch(
      /warmLight\.distance = WARM_LIGHT_DISTANCE_BASE \+ i \* 2\.5/,
    );
    expect(src).not.toMatch(/warmLight\.distance = 6\.5 \+ i \* 2\.5/);
    expect(src).toContain("export const WARM_LIGHT_Y = 1.7825");
    expect(src).toContain("warmLight.position.set(wx, WARM_LIGHT_Y, wy)");
    expect(src).not.toMatch(/export const WARM_LIGHT_Y = 1\.55/);
    expect(src).not.toMatch(/warmLight\.position\.set\(wx, 1\.55, wy\)/);
    expect(src).toContain("warmLight.position.set(0, 1.6, 0)");
    expect(src).toContain("export const WARM_LIGHT_INTENSITY_MUL = 1.7825");
    expect(src).toContain("warmLight.intensity = i * WARM_LIGHT_INTENSITY_MUL");
    expect(src).not.toMatch(/export const WARM_LIGHT_INTENSITY_MUL = 1\.55/);
    expect(src).not.toMatch(/warmLight\.intensity = i \* 1\.55/);
    expect(src).toContain("export const FLASHLIGHT_SPOT_DISTANCE_GAIN = 2.3");
    expect(src).toContain("export const FLASHLIGHT_SPOT_DISTANCE_EXTRA = 1.84");
    expect(src).toContain(
      "torchSpot.distance = FLASHLIGHT_CONE_LENGTH + FLASHLIGHT_SPOT_DISTANCE_EXTRA + i * FLASHLIGHT_SPOT_DISTANCE_GAIN",
    );
    expect(src).toContain("export const FLASHLIGHT_SPOT_Y = 1.7825");
    expect(src).toContain("torchSpot.position.set(0, FLASHLIGHT_SPOT_Y, 0)");
    expect(src).toContain("torchSpot.position.set(wx, FLASHLIGHT_SPOT_Y, wy)");
    expect(src).toContain("export const FLASHLIGHT_FILL_Y = 1.5525");
    expect(src).toContain("torchLight.position.set(0, FLASHLIGHT_FILL_Y, 0)");
    expect(src).toContain("torchLight.position.set(wx, FLASHLIGHT_FILL_Y, wy)");
    expect(src).toContain("export const FLASHLIGHT_FILL_DISTANCE_GAIN = 4.025");
    expect(src).toContain("export const FLASHLIGHT_FILL_DISTANCE_BASE = 8.05");
    expect(src).toContain(
      "torchLight.distance = FLASHLIGHT_FILL_DISTANCE_BASE + i * FLASHLIGHT_FILL_DISTANCE_GAIN",
    );
    expect(src).toContain("export const FLASHLIGHT_FILL_COLOR = 0xcaefff");
    expect(src).toContain("torchLight.color.setHex(FLASHLIGHT_FILL_COLOR)");
    expect(src).toContain("export const TRACER_COLOR = 0xffffb8");
    expect(src).toContain("color: TRACER_COLOR");
    expect(src).toContain("export const IMPACT_SPARK_COLOR = 0xffef93");
    expect(src).toContain("color: IMPACT_SPARK_COLOR");
    expect(src).toContain("export const MUZZLE_FLASH_COLOR = 0xffffdd");
    expect(src).toContain("color: MUZZLE_FLASH_COLOR");
    expect(src).toContain("export const WARM_LIGHT_COLOR = 0xffca81");
    expect(src).toContain("export const POSSESSED_HEAD_EMISSIVE = 0x30124a");
    expect(src).toContain("emissive: POSSESSED_HEAD_EMISSIVE");
    expect(src).toContain("color: POSSESSED_HEAD_COLOR");
    expect(src).toContain("export const POSSESSED_HEAD_COLOR = 0x8c469f");
    expect(src).toContain("emissive: POSSESSED_EMISSIVE");
    expect(src).toContain("color: POSSESSED_COLOR");
    expect(src).toContain("const POSSESSED_EMISSIVE = 0x1e0925");
    expect(src).toContain("emissiveIntensity: 0.22");
    expect(src).toContain("color: HOSTILE_COLOR");
    expect(src).toContain("color: PLAYER_COLOR");
    expect(src).toContain("color: PLAYER_HEAD_COLOR");
    expect(src).toContain("emissive: PLAYER_HEAD_EMISSIVE");
    expect(TRACER_FLASH_COLOR).toBe(0xffdd6e);
    expect(TRACER_FLASH_INTENSITY).toBe(2.76);
    expect(TRACER_FLASH_DISTANCE).toBe(3.68);
    expect(TRACER_FLASH_DECAY).toBe(1.74);
    expect(TRACER_FLASH_Y_OFFSET).toBe(0.1725);
    expect(TRACER_COLOR).toBe(0xffffb8);
    expect(IMPACT_SPARK_LIGHT_COLOR).toBe(0xffef93);
    expect(IMPACT_SPARK_COLOR).toBe(0xffef93);
    expect(IMPACT_SPARK_LIGHT_DECAY).toBe(1.74);
    expect(MUZZLE_LIGHT_COLOR).toBe(0xffffb8);
    expect(MUZZLE_LIGHT_DECAY).toBe(1.74);
    expect(MUZZLE_FLASH_COLOR).toBe(0xffffdd);
    expect(WARM_LIGHT_COLOR).toBe(0xffca81);
    expect(WARM_LIGHT_DECAY).toBe(1.74);
    expect(WARM_LIGHT_VISIBLE_EPS).toBe(0.0174);
    expect(WARM_LIGHT_AMBER_B_GAIN).toBe(0.115);
    expect(WARM_LIGHT_AMBER_B).toBe(0.437);
    expect(WARM_LIGHT_AMBER_G_GAIN).toBe(0.092);
    expect(WARM_LIGHT_AMBER_G).toBe(0.759);
    expect(WARM_LIGHT_DISTANCE_GAIN).toBe(2.875);
    expect(WARM_LIGHT_DISTANCE_BASE).toBe(7.475);
    expect(WARM_LIGHT_Y).toBe(1.7825);
    expect(WARM_LIGHT_INTENSITY_MUL).toBe(1.7825);
    expect(POSSESSED_HEAD_EMISSIVE).toBe(0x30124a);
    expect(POSSESSED_HEAD_COLOR).toBe(0x8c469f);
    expect(POSSESSED_COLOR).toBe(0x68347b);
    expect(HOSTILE_COLOR).toBe(0x7b1e1e);
    expect(PLAYER_COLOR).toBe(0x55a4f4);
    expect(PLAYER_HEAD_COLOR).toBe(0x91d1ff);
    expect(PLAYER_HEAD_EMISSIVE).toBe(0x122537);
    expect(MARKER_PALETTE.possessed.emissive).toBe(0x30124a);
    expect(MARKER_PALETTE.player.badge).toBe(0x91d1ff);
    expect(MARKER_PALETTE.player.emissive).toBe(0x1e4a6e);
    expect(MARKER_PALETTE.player.ring).toBe(0x4392f4);
    expect(MARKER_RING_OPACITY).toBe(0.6877);
    expect(muteBadgeY).toBe(2.3);
    expect(possessedBadgeY).toBe(2.3);
  });
});

describe("worldView marker badge tilt", () => {
  test("marker badge tilt 2.6 × 1.15 → 2.99; marker-badge-radius/marker-icon-size/marker-icon-y/marker-ring-y/noise-ring-y/chevron-tilt/chevron-y/muzzle-forward/spot-target-y/y-offset/distance/intensity/decay/impact-spark-decay/muzzle-decay/spot-decay/fill-decay/warm-decay/visible-eps/amber-B-gain/amber-B/amber-G-gain/amber-G/gain/base/Y/intensity/color/spot-gain/extra/spot-Y/fill-Y/gain/base/flash/mesh/color/possessed/hostile/player/palette/badge-Y iguales", () => {
    expect(MARKER_BADGE_TILT).toBe(2.99);
    expect(MARKER_BADGE_TILT).toBeCloseTo(2.6 * 1.15, 10);
    expect(PLAYER_BODY_BASE_Y).toBe(0.644);
    expect(PLAYER_BODY_BASE_Y).toBeCloseTo(0.56 * 1.15, 10);
    expect(PLAYER_HEAD_BASE_Y).toBe(1.518);
    expect(PLAYER_HEAD_BASE_Y).toBeCloseTo(1.32 * 1.15, 10);
    expect(HOSTILE_BODY_BASE_Y).toBe(0.644);
    expect(HOSTILE_BODY_BASE_Y).toBeCloseTo(0.56 * 1.15, 10);
    expect(HOSTILE_HEAD_BASE_Y).toBe(1.495);
    expect(HOSTILE_HEAD_BASE_Y).toBeCloseTo(1.3 * 1.15, 10);
    expect(HOSTILE_MUTE_XZ_SCALE).toBe(1.2075);
    expect(HOSTILE_MUTE_XZ_SCALE).toBeCloseTo(1.05 * 1.15, 10);
    expect(MARKER_BADGE_RADIUS).toBe(0.184);
    expect(MARKER_BADGE_RADIUS).toBeCloseTo(0.16 * 1.15, 10);
    expect(MARKER_ICON_SIZE).toBe(0.207);
    expect(MARKER_ICON_SIZE).toBeCloseTo(0.18 * 1.15, 10);
    expect(MARKER_ICON_Y).toBe(0.023);
    expect(MARKER_ICON_Y).toBeCloseTo(0.02 * 1.15, 10);
    expect(MARKER_RING_Y).toBe(0.046);
    expect(MARKER_RING_Y).toBeCloseTo(0.04 * 1.15, 10);
    expect(NOISE_RING_Y).toBe(0.0575);
    expect(NOISE_RING_Y).toBeCloseTo(0.05 * 1.15, 10);
    expect(CHEVRON_TILT).toBe(-0.4025);
    expect(CHEVRON_Y).toBe(0.138);
    expect(MUZZLE_FORWARD).toBe(0.552);
    expect(FLASHLIGHT_SPOT_TARGET_Y).toBe(0.138);
    expect(TRACER_FLASH_Y_OFFSET).toBe(0.1725);
    expect(TRACER_FLASH_DISTANCE).toBe(3.68);
    expect(TRACER_FLASH_INTENSITY).toBe(2.76);
    expect(TRACER_FLASH_DECAY).toBe(1.74);
    expect(IMPACT_SPARK_LIGHT_DECAY).toBe(1.74);
    expect(MUZZLE_LIGHT_DECAY).toBe(1.74);
    expect(FLASHLIGHT_SPOT_DECAY).toBe(1.74);
    expect(FLASHLIGHT_FILL_DECAY).toBe(1.74);
    expect(WARM_LIGHT_DECAY).toBe(1.74);
    expect(WARM_LIGHT_VISIBLE_EPS).toBe(0.0174);
    expect(WARM_LIGHT_AMBER_B_GAIN).toBe(0.115);
    expect(WARM_LIGHT_AMBER_B).toBe(0.437);
    expect(WARM_LIGHT_AMBER_G_GAIN).toBe(0.092);
    expect(WARM_LIGHT_AMBER_G).toBe(0.759);
    expect(WARM_LIGHT_DISTANCE_GAIN).toBe(2.875);
    expect(WARM_LIGHT_DISTANCE_BASE).toBe(7.475);
    expect(WARM_LIGHT_Y).toBe(1.7825);
    expect(WARM_LIGHT_INTENSITY_MUL).toBe(1.7825);
    expect(FLASHLIGHT_SPOT_DISTANCE_GAIN).toBe(2.3);
    expect(FLASHLIGHT_SPOT_DISTANCE_EXTRA).toBe(1.84);
    expect(FLASHLIGHT_SPOT_Y).toBe(1.7825);
    expect(FLASHLIGHT_FILL_Y).toBe(1.5525);
    expect(FLASHLIGHT_FILL_DISTANCE_GAIN).toBe(4.025);
    expect(FLASHLIGHT_FILL_DISTANCE_BASE).toBe(8.05);
    expect(FLASHLIGHT_FILL_COLOR).toBe(0xcaefff);
    expect(MUZZLE_LIGHT_COLOR).toBe(0xffffb8);
    expect(IMPACT_SPARK_LIGHT_COLOR).toBe(0xffef93);
    expect(TRACER_FLASH_COLOR).toBe(0xffdd6e);
    const src = readFileSync(
      resolve(process.cwd(), "src/render/worldView.ts"),
      "utf8",
    );
    expect(src).toContain("export const MARKER_BADGE_TILT = 2.99");
    expect(src).toContain("disc.rotation.x = -Math.PI / MARKER_BADGE_TILT");
    expect(src).toContain("icon.rotation.x = -Math.PI / MARKER_BADGE_TILT");
    expect(src).not.toMatch(/disc\.rotation\.x = -Math\.PI \/ 2\.6/);
    expect(src).not.toMatch(/icon\.rotation\.x = -Math\.PI \/ 2\.6/);
    expect(src).not.toMatch(/const MARKER_BADGE_TILT = 2\.6/);
    expect(src).not.toMatch(/export const MARKER_BADGE_TILT = 2\.6/);
    expect(src).toContain("export const PLAYER_BODY_BASE_Y = 0.644");
    expect(src).toContain("playerBody.position.y = PLAYER_BODY_BASE_Y");
    expect(src).not.toMatch(/playerBody\.position\.y = 0\.56/);
    expect(src).not.toMatch(/const PLAYER_BODY_BASE_Y = 0\.56/);
    expect(src).not.toMatch(/export const PLAYER_BODY_BASE_Y = 0\.56/);
    expect(src).toContain("export const PLAYER_HEAD_BASE_Y = 1.518");
    expect(src).toContain("playerHead.position.y = PLAYER_HEAD_BASE_Y");
    expect(src).not.toMatch(/playerHead\.position\.y = 1\.32/);
    expect(src).not.toMatch(/const PLAYER_HEAD_BASE_Y = 1\.32/);
    expect(src).not.toMatch(/export const PLAYER_HEAD_BASE_Y = 1\.32/);
    expect(src).toContain("export const HOSTILE_BODY_BASE_Y = 0.644");
    expect(src).toContain("mesh.position.y = HOSTILE_BODY_BASE_Y");
    expect(src).toContain("body.position.y = HOSTILE_BODY_BASE_Y");
    expect(src).not.toMatch(/mesh\.position\.y = 0\.56/);
    expect(src).not.toMatch(/body\.position\.y = 0\.56/);
    expect(src).not.toMatch(/const HOSTILE_BODY_BASE_Y = 0\.56/);
    expect(src).not.toMatch(/export const HOSTILE_BODY_BASE_Y = 0\.56/);
    expect(src).toContain("export const HOSTILE_HEAD_BASE_Y = 1.495");
    expect(src).toContain("head.position.y = HOSTILE_HEAD_BASE_Y");
    expect(src).not.toMatch(/head\.position\.y = 1\.3/);
    expect(src).not.toMatch(/const HOSTILE_HEAD_BASE_Y = 1\.3/);
    expect(src).not.toMatch(/export const HOSTILE_HEAD_BASE_Y = 1\.3/);
    expect(src).toContain("export const HOSTILE_MUTE_XZ_SCALE = 1.2075");
    expect(src).toContain("mesh.scale.set(HOSTILE_MUTE_XZ_SCALE, 1, HOSTILE_MUTE_XZ_SCALE)");
    expect(src).not.toMatch(/mesh\.scale\.set\(1\.05, 1, 1\.05\)/);
    expect(src).not.toMatch(/const HOSTILE_MUTE_XZ_SCALE = 1\.05/);
    expect(src).not.toMatch(/export const HOSTILE_MUTE_XZ_SCALE = 1\.05/);
    expect(src).toContain("export const MARKER_BADGE_RADIUS = 0.184");
    expect(src).toContain("new THREE.CircleGeometry(MARKER_BADGE_RADIUS, 20)");
    expect(src).not.toMatch(/new THREE\.CircleGeometry\(0\.16, 20\)/);
    expect(src).not.toMatch(/const MARKER_BADGE_RADIUS = 0\.16/);
    expect(src).not.toMatch(/export const MARKER_BADGE_RADIUS = 0\.16/);
    expect(src).toContain("export const MARKER_ICON_SIZE = 0.207");
    expect(src).toContain("new THREE.PlaneGeometry(MARKER_ICON_SIZE, MARKER_ICON_SIZE)");
    expect(src).not.toMatch(/new THREE\.PlaneGeometry\(0\.18, 0\.18\)/);
    expect(src).not.toMatch(/const MARKER_ICON_SIZE = 0\.18/);
    expect(src).not.toMatch(/export const MARKER_ICON_SIZE = 0\.18/);
    expect(src).toContain("export const MARKER_ICON_Y = 0.023");
    expect(src).toContain("icon.position.y = MARKER_ICON_Y");
    expect(src).not.toMatch(/icon\.position\.y = 0\.02/);
    expect(src).not.toMatch(/const MARKER_ICON_Y = 0\.02;/);
    expect(src).not.toMatch(/export const MARKER_ICON_Y = 0\.02;/);
    expect(src).toContain("export const MARKER_RING_Y = 0.046");
    expect(src).toContain("ring.position.y = MARKER_RING_Y");
    expect(src).not.toMatch(/ring\.position\.y = 0\.04/);
    expect(src).not.toMatch(/const MARKER_RING_Y = 0\.04;/);
    expect(src).not.toMatch(/export const MARKER_RING_Y = 0\.04;/);
    expect(src).toContain("export const NOISE_RING_Y = 0.0575");
    expect(src).toContain("mesh.position.y = NOISE_RING_Y");
    expect(src).toContain("slot.mesh.position.set(state.x, NOISE_RING_Y, state.y)");
    expect(src).not.toMatch(/const NOISE_RING_Y = 0\.05;/);
    expect(src).not.toMatch(/export const NOISE_RING_Y = 0\.05;/);
    expect(src).toContain("export const CHEVRON_TILT = -0.4025");
    expect(src).toContain("chevronMesh.rotation.x = CHEVRON_TILT");
    expect(src).not.toMatch(/chevronMesh\.rotation\.x = -0\.35/);
    expect(src).not.toMatch(/const CHEVRON_TILT = -0\.35/);
    expect(src).not.toMatch(/export const CHEVRON_TILT = -0\.35/);
    expect(src).toContain("export const CHEVRON_Y = 0.138");
    expect(src).toContain("chevronMesh.position.set(x, CHEVRON_Y, z)");
    expect(src).not.toMatch(/const CHEVRON_Y = 0\.12/);
    expect(src).not.toMatch(/export const CHEVRON_Y = 0\.12/);
    expect(src).not.toMatch(/chevronMesh\.position\.set\(x, 0\.12, z\)/);
    expect(src).toContain("export const MUZZLE_FORWARD = 0.552");
    expect(src).toContain("Math.sin(playerGltfYaw) * MUZZLE_FORWARD");
    expect(src).toContain("Math.cos(playerGltfYaw) * MUZZLE_FORWARD");
    expect(src).toContain("muzzleMesh.position.set(ox, TRACER_HEIGHT, oz)");
    expect(src).toContain("muzzleLight.position.set(ox, TRACER_HEIGHT, oz)");
    expect(src).not.toMatch(/const MUZZLE_FORWARD = 0\.48/);
    expect(src).not.toMatch(/export const MUZZLE_FORWARD = 0\.48/);
    expect(src).toContain("export const FLASHLIGHT_SPOT_TARGET_Y = 0.138");
    expect(src).toContain("torchSpot.target.position.set(wx + tip.x, FLASHLIGHT_SPOT_TARGET_Y, wy + tip.z)");
    expect(src).not.toMatch(/torchSpot\.target\.position\.set\(wx \+ tip\.x, 0\.12, wy \+ tip\.z\)/);
    expect(src).not.toMatch(/export const FLASHLIGHT_SPOT_TARGET_Y = 0\.12/);
    expect(src).toContain("export const TRACER_FLASH_Y_OFFSET = 0.1725");
    expect(src).toContain("flash.position.set(from.x, TRACER_HEIGHT + TRACER_FLASH_Y_OFFSET, from.y)");
    expect(src).not.toMatch(/flash\.position\.set\(from\.x, TRACER_HEIGHT \+ 0\.15, from\.y\)/);
    expect(src).not.toMatch(/export const TRACER_FLASH_Y_OFFSET = 0\.15/);
    expect(src).toContain("export const TRACER_FLASH_DISTANCE = 3.68");
    expect(src).toContain("new THREE.PointLight(TRACER_FLASH_COLOR, TRACER_FLASH_INTENSITY, TRACER_FLASH_DISTANCE, TRACER_FLASH_DECAY)");
    expect(src).not.toMatch(/new THREE\.PointLight\(TRACER_FLASH_COLOR, TRACER_FLASH_INTENSITY, 3\.2, TRACER_FLASH_DECAY\)/);
    expect(src).not.toMatch(/new THREE\.PointLight\(TRACER_FLASH_COLOR, 2\.4, 3\.2, TRACER_FLASH_DECAY\)/);
    expect(src).not.toMatch(/new THREE\.PointLight\(TRACER_FLASH_COLOR, 2\.4, 3\.2, 2\)/);
    expect(src).not.toMatch(/export const TRACER_FLASH_DISTANCE = 3\.2/);
    expect(src).toContain("t.flash.intensity = TRACER_FLASH_INTENSITY * op");
    expect(src).not.toMatch(/t\.flash\.intensity = 2\.4 \* op/);
    expect(src).toContain("export const TRACER_FLASH_INTENSITY = 2.76");
    expect(src).toContain("export const TRACER_FLASH_DECAY = 1.74");
    expect(src).toContain("export const TRACER_FLASH_COLOR = 0xffdd6e");
    expect(src).toContain("export const IMPACT_SPARK_LIGHT_DECAY = 1.74");
    expect(src).toContain("IMPACT_SPARK_LIGHT_DISTANCE,\n    IMPACT_SPARK_LIGHT_DECAY,");
    expect(src).not.toMatch(/IMPACT_SPARK_LIGHT_DISTANCE,\s*\n\s*2,/);
    expect(src).not.toMatch(/export const IMPACT_SPARK_LIGHT_DECAY = 2/);
    expect(src).toContain("const IMPACT_SPARK_LIGHT_DISTANCE = 1.8");
    expect(src).toContain("export const IMPACT_SPARK_LIGHT_COLOR = 0xffef93");
    expect(src).toContain("new THREE.PointLight(\n    IMPACT_SPARK_LIGHT_COLOR,");
    expect(src).toContain("export const MUZZLE_LIGHT_DECAY = 1.74");
    expect(src).toContain("MUZZLE_LIGHT_DISTANCE,\n    MUZZLE_LIGHT_DECAY,");
    expect(src).not.toMatch(/MUZZLE_LIGHT_DISTANCE,\s*\n\s*2,/);
    expect(src).not.toMatch(/export const MUZZLE_LIGHT_DECAY = 2/);
    expect(src).toContain("const MUZZLE_LIGHT_DISTANCE = 2.6");
    expect(src).toContain("export const MUZZLE_LIGHT_COLOR = 0xffffb8");
    expect(src).toContain("new THREE.PointLight(\n    MUZZLE_LIGHT_COLOR,");
    expect(src).toContain("export const FLASHLIGHT_SPOT_DECAY = 1.74");
    expect(src).toContain("FLASHLIGHT_SPOT_PENUMBRA,\n    FLASHLIGHT_SPOT_DECAY,");
    expect(src).not.toMatch(/FLASHLIGHT_SPOT_PENUMBRA,\s*\n\s*2,/);
    expect(src).not.toMatch(/export const FLASHLIGHT_SPOT_DECAY = 2/);
    expect(src).toContain("FLASHLIGHT_CONE_LENGTH + 2.4");
    expect(src).toContain("export const FLASHLIGHT_FILL_DECAY = 1.74");
    expect(src).toContain("new THREE.PointLight(FLASHLIGHT_FILL_COLOR, 0, 10, FLASHLIGHT_FILL_DECAY)");
    expect(src).not.toMatch(/new THREE\.PointLight\(FLASHLIGHT_FILL_COLOR, 0, 10, 2\)/);
    expect(src).not.toMatch(/export const FLASHLIGHT_FILL_DECAY = 2/);
    expect(src).toContain("export const WARM_LIGHT_DECAY = 1.74");
    expect(src).toContain("new THREE.PointLight(WARM_LIGHT_COLOR, 0, 7.5, WARM_LIGHT_DECAY)");
    expect(src).not.toMatch(/new THREE\.PointLight\(WARM_LIGHT_COLOR, 0, 7\.5, 2\)/);
    expect(src).toContain("export const WARM_LIGHT_VISIBLE_EPS = 0.0174");
    expect(src).toContain("warmLight.visible = i > WARM_LIGHT_VISIBLE_EPS");
    expect(src).not.toMatch(/warmLight\.visible = i > 0\.02/);
    expect(src).not.toMatch(/export const WARM_LIGHT_VISIBLE_EPS = 0\.02/);
    expect(src).toContain("export const WARM_LIGHT_AMBER_B_GAIN = 0.115");
    expect(src).toContain("export const WARM_LIGHT_AMBER_B = 0.437");
    expect(src).toContain("export const WARM_LIGHT_AMBER_G_GAIN = 0.092");
    expect(src).toContain("export const WARM_LIGHT_AMBER_G = 0.759");
    expect(src).toContain(
      "warmLight.color.setRGB(1, WARM_LIGHT_AMBER_G + i * WARM_LIGHT_AMBER_G_GAIN, WARM_LIGHT_AMBER_B + i * WARM_LIGHT_AMBER_B_GAIN)",
    );
    expect(src).not.toMatch(/WARM_LIGHT_AMBER_B \+ i \* 0\.1/);
    expect(src).toContain("export const WARM_LIGHT_DISTANCE_GAIN = 2.875");
    expect(src).toContain("export const WARM_LIGHT_DISTANCE_BASE = 7.475");
    expect(src).toContain(
      "warmLight.distance = WARM_LIGHT_DISTANCE_BASE + i * WARM_LIGHT_DISTANCE_GAIN",
    );
    expect(src).not.toMatch(/export const WARM_LIGHT_DISTANCE_GAIN = 2\.5/);
    expect(src).not.toMatch(
      /warmLight\.distance = WARM_LIGHT_DISTANCE_BASE \+ i \* 2\.5/,
    );
    expect(src).not.toMatch(/warmLight\.distance = 6\.5 \+ i \* 2\.5/);
    expect(src).toContain("export const WARM_LIGHT_Y = 1.7825");
    expect(src).toContain("warmLight.position.set(wx, WARM_LIGHT_Y, wy)");
    expect(src).not.toMatch(/export const WARM_LIGHT_Y = 1\.55/);
    expect(src).not.toMatch(/warmLight\.position\.set\(wx, 1\.55, wy\)/);
    expect(src).toContain("warmLight.position.set(0, 1.6, 0)");
    expect(src).toContain("export const WARM_LIGHT_INTENSITY_MUL = 1.7825");
    expect(src).toContain("warmLight.intensity = i * WARM_LIGHT_INTENSITY_MUL");
    expect(src).not.toMatch(/export const WARM_LIGHT_INTENSITY_MUL = 1\.55/);
    expect(src).not.toMatch(/warmLight\.intensity = i \* 1\.55/);
    expect(src).toContain("export const FLASHLIGHT_SPOT_DISTANCE_GAIN = 2.3");
    expect(src).toContain("export const FLASHLIGHT_SPOT_DISTANCE_EXTRA = 1.84");
    expect(src).toContain(
      "torchSpot.distance = FLASHLIGHT_CONE_LENGTH + FLASHLIGHT_SPOT_DISTANCE_EXTRA + i * FLASHLIGHT_SPOT_DISTANCE_GAIN",
    );
    expect(src).toContain("export const FLASHLIGHT_SPOT_Y = 1.7825");
    expect(src).toContain("torchSpot.position.set(0, FLASHLIGHT_SPOT_Y, 0)");
    expect(src).toContain("torchSpot.position.set(wx, FLASHLIGHT_SPOT_Y, wy)");
    expect(src).toContain("export const FLASHLIGHT_FILL_Y = 1.5525");
    expect(src).toContain("torchLight.position.set(0, FLASHLIGHT_FILL_Y, 0)");
    expect(src).toContain("torchLight.position.set(wx, FLASHLIGHT_FILL_Y, wy)");
    expect(src).toContain("export const FLASHLIGHT_FILL_DISTANCE_GAIN = 4.025");
    expect(src).toContain("export const FLASHLIGHT_FILL_DISTANCE_BASE = 8.05");
    expect(src).toContain(
      "torchLight.distance = FLASHLIGHT_FILL_DISTANCE_BASE + i * FLASHLIGHT_FILL_DISTANCE_GAIN",
    );
    expect(src).toContain("export const FLASHLIGHT_FILL_COLOR = 0xcaefff");
    expect(src).toContain("torchLight.color.setHex(FLASHLIGHT_FILL_COLOR)");
    expect(src).toContain("export const TRACER_COLOR = 0xffffb8");
    expect(src).toContain("color: TRACER_COLOR");
    expect(src).toContain("export const IMPACT_SPARK_COLOR = 0xffef93");
    expect(src).toContain("color: IMPACT_SPARK_COLOR");
    expect(src).toContain("export const MUZZLE_FLASH_COLOR = 0xffffdd");
    expect(src).toContain("color: MUZZLE_FLASH_COLOR");
    expect(src).toContain("export const WARM_LIGHT_COLOR = 0xffca81");
    expect(src).toContain("export const POSSESSED_HEAD_EMISSIVE = 0x30124a");
    expect(src).toContain("emissive: POSSESSED_HEAD_EMISSIVE");
    expect(src).toContain("color: POSSESSED_HEAD_COLOR");
    expect(src).toContain("export const POSSESSED_HEAD_COLOR = 0x8c469f");
    expect(src).toContain("emissive: POSSESSED_EMISSIVE");
    expect(src).toContain("color: POSSESSED_COLOR");
    expect(src).toContain("const POSSESSED_EMISSIVE = 0x1e0925");
    expect(src).toContain("emissiveIntensity: 0.22");
    expect(src).toContain("color: HOSTILE_COLOR");
    expect(src).toContain("color: PLAYER_COLOR");
    expect(src).toContain("color: PLAYER_HEAD_COLOR");
    expect(src).toContain("emissive: PLAYER_HEAD_EMISSIVE");
    expect(TRACER_FLASH_COLOR).toBe(0xffdd6e);
    expect(TRACER_FLASH_INTENSITY).toBe(2.76);
    expect(TRACER_FLASH_DISTANCE).toBe(3.68);
    expect(TRACER_FLASH_DECAY).toBe(1.74);
    expect(TRACER_FLASH_Y_OFFSET).toBe(0.1725);
    expect(TRACER_COLOR).toBe(0xffffb8);
    expect(IMPACT_SPARK_LIGHT_COLOR).toBe(0xffef93);
    expect(IMPACT_SPARK_COLOR).toBe(0xffef93);
    expect(IMPACT_SPARK_LIGHT_DECAY).toBe(1.74);
    expect(MUZZLE_LIGHT_COLOR).toBe(0xffffb8);
    expect(MUZZLE_LIGHT_DECAY).toBe(1.74);
    expect(MUZZLE_FLASH_COLOR).toBe(0xffffdd);
    expect(WARM_LIGHT_COLOR).toBe(0xffca81);
    expect(WARM_LIGHT_DECAY).toBe(1.74);
    expect(WARM_LIGHT_VISIBLE_EPS).toBe(0.0174);
    expect(WARM_LIGHT_AMBER_B_GAIN).toBe(0.115);
    expect(WARM_LIGHT_AMBER_B).toBe(0.437);
    expect(WARM_LIGHT_AMBER_G_GAIN).toBe(0.092);
    expect(WARM_LIGHT_AMBER_G).toBe(0.759);
    expect(WARM_LIGHT_DISTANCE_GAIN).toBe(2.875);
    expect(WARM_LIGHT_DISTANCE_BASE).toBe(7.475);
    expect(WARM_LIGHT_Y).toBe(1.7825);
    expect(WARM_LIGHT_INTENSITY_MUL).toBe(1.7825);
    expect(POSSESSED_HEAD_EMISSIVE).toBe(0x30124a);
    expect(POSSESSED_HEAD_COLOR).toBe(0x8c469f);
    expect(POSSESSED_COLOR).toBe(0x68347b);
    expect(HOSTILE_COLOR).toBe(0x7b1e1e);
    expect(PLAYER_COLOR).toBe(0x55a4f4);
    expect(PLAYER_HEAD_COLOR).toBe(0x91d1ff);
    expect(PLAYER_HEAD_EMISSIVE).toBe(0x122537);
    expect(MARKER_PALETTE.possessed.emissive).toBe(0x30124a);
    expect(MARKER_PALETTE.player.badge).toBe(0x91d1ff);
    expect(MARKER_PALETTE.player.emissive).toBe(0x1e4a6e);
    expect(MARKER_PALETTE.player.ring).toBe(0x4392f4);
    expect(MARKER_RING_OPACITY).toBe(0.6877);
    expect(muteBadgeY).toBe(2.3);
    expect(possessedBadgeY).toBe(2.3);
  });
});

describe("worldView player body base Y", () => {
  test("player body base Y 0.56 × 1.15 → 0.644; marker-badge-tilt/marker-badge-radius/marker-icon-size/marker-icon-y/marker-ring-y/noise-ring-y/chevron-tilt/chevron-y/muzzle-forward/spot-target-y/y-offset/distance/intensity/decay/impact-spark-decay/muzzle-decay/spot-decay/fill-decay/warm-decay/visible-eps/amber-B-gain/amber-B/amber-G-gain/amber-G/gain/base/Y/intensity/color/spot-gain/extra/spot-Y/fill-Y/gain/base/flash/mesh/color/possessed/hostile/player/palette/badge-Y iguales", () => {
    expect(PLAYER_BODY_BASE_Y).toBe(0.644);
    expect(PLAYER_BODY_BASE_Y).toBeCloseTo(0.56 * 1.15, 10);
    expect(PLAYER_HEAD_BASE_Y).toBe(1.518);
    expect(PLAYER_HEAD_BASE_Y).toBeCloseTo(1.32 * 1.15, 10);
    expect(HOSTILE_BODY_BASE_Y).toBe(0.644);
    expect(HOSTILE_BODY_BASE_Y).toBeCloseTo(0.56 * 1.15, 10);
    expect(HOSTILE_HEAD_BASE_Y).toBe(1.495);
    expect(HOSTILE_HEAD_BASE_Y).toBeCloseTo(1.3 * 1.15, 10);
    expect(HOSTILE_MUTE_XZ_SCALE).toBe(1.2075);
    expect(HOSTILE_MUTE_XZ_SCALE).toBeCloseTo(1.05 * 1.15, 10);
    expect(MARKER_BADGE_TILT).toBe(2.99);
    expect(MARKER_BADGE_TILT).toBeCloseTo(2.6 * 1.15, 10);
    expect(MARKER_BADGE_RADIUS).toBe(0.184);
    expect(MARKER_BADGE_RADIUS).toBeCloseTo(0.16 * 1.15, 10);
    expect(MARKER_ICON_SIZE).toBe(0.207);
    expect(MARKER_ICON_SIZE).toBeCloseTo(0.18 * 1.15, 10);
    expect(MARKER_ICON_Y).toBe(0.023);
    expect(MARKER_ICON_Y).toBeCloseTo(0.02 * 1.15, 10);
    expect(MARKER_RING_Y).toBe(0.046);
    expect(MARKER_RING_Y).toBeCloseTo(0.04 * 1.15, 10);
    expect(NOISE_RING_Y).toBe(0.0575);
    expect(NOISE_RING_Y).toBeCloseTo(0.05 * 1.15, 10);
    expect(CHEVRON_TILT).toBe(-0.4025);
    expect(CHEVRON_Y).toBe(0.138);
    expect(MUZZLE_FORWARD).toBe(0.552);
    expect(FLASHLIGHT_SPOT_TARGET_Y).toBe(0.138);
    expect(TRACER_FLASH_Y_OFFSET).toBe(0.1725);
    expect(TRACER_FLASH_DISTANCE).toBe(3.68);
    expect(TRACER_FLASH_INTENSITY).toBe(2.76);
    expect(TRACER_FLASH_DECAY).toBe(1.74);
    expect(IMPACT_SPARK_LIGHT_DECAY).toBe(1.74);
    expect(MUZZLE_LIGHT_DECAY).toBe(1.74);
    expect(FLASHLIGHT_SPOT_DECAY).toBe(1.74);
    expect(FLASHLIGHT_FILL_DECAY).toBe(1.74);
    expect(WARM_LIGHT_DECAY).toBe(1.74);
    expect(WARM_LIGHT_VISIBLE_EPS).toBe(0.0174);
    expect(WARM_LIGHT_AMBER_B_GAIN).toBe(0.115);
    expect(WARM_LIGHT_AMBER_B).toBe(0.437);
    expect(WARM_LIGHT_AMBER_G_GAIN).toBe(0.092);
    expect(WARM_LIGHT_AMBER_G).toBe(0.759);
    expect(WARM_LIGHT_DISTANCE_GAIN).toBe(2.875);
    expect(WARM_LIGHT_DISTANCE_BASE).toBe(7.475);
    expect(WARM_LIGHT_Y).toBe(1.7825);
    expect(WARM_LIGHT_INTENSITY_MUL).toBe(1.7825);
    expect(FLASHLIGHT_SPOT_DISTANCE_GAIN).toBe(2.3);
    expect(FLASHLIGHT_SPOT_DISTANCE_EXTRA).toBe(1.84);
    expect(FLASHLIGHT_SPOT_Y).toBe(1.7825);
    expect(FLASHLIGHT_FILL_Y).toBe(1.5525);
    expect(FLASHLIGHT_FILL_DISTANCE_GAIN).toBe(4.025);
    expect(FLASHLIGHT_FILL_DISTANCE_BASE).toBe(8.05);
    expect(FLASHLIGHT_FILL_COLOR).toBe(0xcaefff);
    expect(MUZZLE_LIGHT_COLOR).toBe(0xffffb8);
    expect(IMPACT_SPARK_LIGHT_COLOR).toBe(0xffef93);
    expect(TRACER_FLASH_COLOR).toBe(0xffdd6e);
    const src = readFileSync(
      resolve(process.cwd(), "src/render/worldView.ts"),
      "utf8",
    );
    expect(src).toContain("export const PLAYER_BODY_BASE_Y = 0.644");
    expect(src).toContain("playerBody.position.y = PLAYER_BODY_BASE_Y");
    expect(src).not.toMatch(/playerBody\.position\.y = 0\.56/);
    expect(src).not.toMatch(/const PLAYER_BODY_BASE_Y = 0\.56/);
    expect(src).not.toMatch(/export const PLAYER_BODY_BASE_Y = 0\.56/);
    expect(src).toContain("export const PLAYER_HEAD_BASE_Y = 1.518");
    expect(src).toContain("playerHead.position.y = PLAYER_HEAD_BASE_Y");
    expect(src).not.toMatch(/playerHead\.position\.y = 1\.32/);
    expect(src).not.toMatch(/const PLAYER_HEAD_BASE_Y = 1\.32/);
    expect(src).not.toMatch(/export const PLAYER_HEAD_BASE_Y = 1\.32/);
    expect(src).toContain("export const HOSTILE_BODY_BASE_Y = 0.644");
    expect(src).toContain("mesh.position.y = HOSTILE_BODY_BASE_Y");
    expect(src).toContain("body.position.y = HOSTILE_BODY_BASE_Y");
    expect(src).not.toMatch(/mesh\.position\.y = 0\.56/);
    expect(src).not.toMatch(/body\.position\.y = 0\.56/);
    expect(src).not.toMatch(/const HOSTILE_BODY_BASE_Y = 0\.56/);
    expect(src).not.toMatch(/export const HOSTILE_BODY_BASE_Y = 0\.56/);
    expect(src).toContain("export const HOSTILE_HEAD_BASE_Y = 1.495");
    expect(src).toContain("head.position.y = HOSTILE_HEAD_BASE_Y");
    expect(src).not.toMatch(/head\.position\.y = 1\.3/);
    expect(src).not.toMatch(/const HOSTILE_HEAD_BASE_Y = 1\.3/);
    expect(src).not.toMatch(/export const HOSTILE_HEAD_BASE_Y = 1\.3/);
    expect(src).toContain("export const HOSTILE_MUTE_XZ_SCALE = 1.2075");
    expect(src).toContain("mesh.scale.set(HOSTILE_MUTE_XZ_SCALE, 1, HOSTILE_MUTE_XZ_SCALE)");
    expect(src).not.toMatch(/mesh\.scale\.set\(1\.05, 1, 1\.05\)/);
    expect(src).not.toMatch(/const HOSTILE_MUTE_XZ_SCALE = 1\.05/);
    expect(src).not.toMatch(/export const HOSTILE_MUTE_XZ_SCALE = 1\.05/);
    expect(src).toContain("export const MARKER_BADGE_TILT = 2.99");
    expect(src).toContain("disc.rotation.x = -Math.PI / MARKER_BADGE_TILT");
    expect(src).toContain("icon.rotation.x = -Math.PI / MARKER_BADGE_TILT");
    expect(src).not.toMatch(/disc\.rotation\.x = -Math\.PI \/ 2\.6/);
    expect(src).not.toMatch(/icon\.rotation\.x = -Math\.PI \/ 2\.6/);
    expect(src).not.toMatch(/const MARKER_BADGE_TILT = 2\.6/);
    expect(src).not.toMatch(/export const MARKER_BADGE_TILT = 2\.6/);
    expect(src).toContain("export const MARKER_BADGE_RADIUS = 0.184");
    expect(src).toContain("new THREE.CircleGeometry(MARKER_BADGE_RADIUS, 20)");
    expect(src).not.toMatch(/new THREE\.CircleGeometry\(0\.16, 20\)/);
    expect(src).not.toMatch(/const MARKER_BADGE_RADIUS = 0\.16/);
    expect(src).not.toMatch(/export const MARKER_BADGE_RADIUS = 0\.16/);
    expect(src).toContain("export const MARKER_ICON_SIZE = 0.207");
    expect(src).toContain("new THREE.PlaneGeometry(MARKER_ICON_SIZE, MARKER_ICON_SIZE)");
    expect(src).not.toMatch(/new THREE\.PlaneGeometry\(0\.18, 0\.18\)/);
    expect(src).not.toMatch(/const MARKER_ICON_SIZE = 0\.18/);
    expect(src).not.toMatch(/export const MARKER_ICON_SIZE = 0\.18/);
    expect(src).toContain("export const MARKER_ICON_Y = 0.023");
    expect(src).toContain("icon.position.y = MARKER_ICON_Y");
    expect(src).not.toMatch(/icon\.position\.y = 0\.02/);
    expect(src).not.toMatch(/const MARKER_ICON_Y = 0\.02;/);
    expect(src).not.toMatch(/export const MARKER_ICON_Y = 0\.02;/);
    expect(src).toContain("export const MARKER_RING_Y = 0.046");
    expect(src).toContain("ring.position.y = MARKER_RING_Y");
    expect(src).not.toMatch(/ring\.position\.y = 0\.04/);
    expect(src).not.toMatch(/const MARKER_RING_Y = 0\.04;/);
    expect(src).not.toMatch(/export const MARKER_RING_Y = 0\.04;/);
    expect(src).toContain("export const NOISE_RING_Y = 0.0575");
    expect(src).toContain("mesh.position.y = NOISE_RING_Y");
    expect(src).toContain("slot.mesh.position.set(state.x, NOISE_RING_Y, state.y)");
    expect(src).not.toMatch(/const NOISE_RING_Y = 0\.05;/);
    expect(src).not.toMatch(/export const NOISE_RING_Y = 0\.05;/);
    expect(src).toContain("export const CHEVRON_TILT = -0.4025");
    expect(src).toContain("chevronMesh.rotation.x = CHEVRON_TILT");
    expect(src).not.toMatch(/chevronMesh\.rotation\.x = -0\.35/);
    expect(src).not.toMatch(/const CHEVRON_TILT = -0\.35/);
    expect(src).not.toMatch(/export const CHEVRON_TILT = -0\.35/);
    expect(src).toContain("export const CHEVRON_Y = 0.138");
    expect(src).toContain("chevronMesh.position.set(x, CHEVRON_Y, z)");
    expect(src).not.toMatch(/const CHEVRON_Y = 0\.12/);
    expect(src).not.toMatch(/export const CHEVRON_Y = 0\.12/);
    expect(src).not.toMatch(/chevronMesh\.position\.set\(x, 0\.12, z\)/);
    expect(src).toContain("export const MUZZLE_FORWARD = 0.552");
    expect(src).toContain("Math.sin(playerGltfYaw) * MUZZLE_FORWARD");
    expect(src).toContain("Math.cos(playerGltfYaw) * MUZZLE_FORWARD");
    expect(src).toContain("muzzleMesh.position.set(ox, TRACER_HEIGHT, oz)");
    expect(src).toContain("muzzleLight.position.set(ox, TRACER_HEIGHT, oz)");
    expect(src).not.toMatch(/const MUZZLE_FORWARD = 0\.48/);
    expect(src).not.toMatch(/export const MUZZLE_FORWARD = 0\.48/);
    expect(src).toContain("export const FLASHLIGHT_SPOT_TARGET_Y = 0.138");
    expect(src).toContain("torchSpot.target.position.set(wx + tip.x, FLASHLIGHT_SPOT_TARGET_Y, wy + tip.z)");
    expect(src).not.toMatch(/torchSpot\.target\.position\.set\(wx \+ tip\.x, 0\.12, wy \+ tip\.z\)/);
    expect(src).not.toMatch(/export const FLASHLIGHT_SPOT_TARGET_Y = 0\.12/);
    expect(src).toContain("export const TRACER_FLASH_Y_OFFSET = 0.1725");
    expect(src).toContain("flash.position.set(from.x, TRACER_HEIGHT + TRACER_FLASH_Y_OFFSET, from.y)");
    expect(src).not.toMatch(/flash\.position\.set\(from\.x, TRACER_HEIGHT \+ 0\.15, from\.y\)/);
    expect(src).not.toMatch(/export const TRACER_FLASH_Y_OFFSET = 0\.15/);
    expect(src).toContain("export const TRACER_FLASH_DISTANCE = 3.68");
    expect(src).toContain("new THREE.PointLight(TRACER_FLASH_COLOR, TRACER_FLASH_INTENSITY, TRACER_FLASH_DISTANCE, TRACER_FLASH_DECAY)");
    expect(src).not.toMatch(/new THREE\.PointLight\(TRACER_FLASH_COLOR, TRACER_FLASH_INTENSITY, 3\.2, TRACER_FLASH_DECAY\)/);
    expect(src).not.toMatch(/new THREE\.PointLight\(TRACER_FLASH_COLOR, 2\.4, 3\.2, TRACER_FLASH_DECAY\)/);
    expect(src).not.toMatch(/new THREE\.PointLight\(TRACER_FLASH_COLOR, 2\.4, 3\.2, 2\)/);
    expect(src).not.toMatch(/export const TRACER_FLASH_DISTANCE = 3\.2/);
    expect(src).toContain("t.flash.intensity = TRACER_FLASH_INTENSITY * op");
    expect(src).not.toMatch(/t\.flash\.intensity = 2\.4 \* op/);
    expect(src).toContain("export const TRACER_FLASH_INTENSITY = 2.76");
    expect(src).toContain("export const TRACER_FLASH_DECAY = 1.74");
    expect(src).toContain("export const TRACER_FLASH_COLOR = 0xffdd6e");
    expect(src).toContain("export const IMPACT_SPARK_LIGHT_DECAY = 1.74");
    expect(src).toContain("IMPACT_SPARK_LIGHT_DISTANCE,\n    IMPACT_SPARK_LIGHT_DECAY,");
    expect(src).not.toMatch(/IMPACT_SPARK_LIGHT_DISTANCE,\s*\n\s*2,/);
    expect(src).not.toMatch(/export const IMPACT_SPARK_LIGHT_DECAY = 2/);
    expect(src).toContain("const IMPACT_SPARK_LIGHT_DISTANCE = 1.8");
    expect(src).toContain("export const IMPACT_SPARK_LIGHT_COLOR = 0xffef93");
    expect(src).toContain("new THREE.PointLight(\n    IMPACT_SPARK_LIGHT_COLOR,");
    expect(src).toContain("export const MUZZLE_LIGHT_DECAY = 1.74");
    expect(src).toContain("MUZZLE_LIGHT_DISTANCE,\n    MUZZLE_LIGHT_DECAY,");
    expect(src).not.toMatch(/MUZZLE_LIGHT_DISTANCE,\s*\n\s*2,/);
    expect(src).not.toMatch(/export const MUZZLE_LIGHT_DECAY = 2/);
    expect(src).toContain("const MUZZLE_LIGHT_DISTANCE = 2.6");
    expect(src).toContain("export const MUZZLE_LIGHT_COLOR = 0xffffb8");
    expect(src).toContain("new THREE.PointLight(\n    MUZZLE_LIGHT_COLOR,");
    expect(src).toContain("export const FLASHLIGHT_SPOT_DECAY = 1.74");
    expect(src).toContain("FLASHLIGHT_SPOT_PENUMBRA,\n    FLASHLIGHT_SPOT_DECAY,");
    expect(src).not.toMatch(/FLASHLIGHT_SPOT_PENUMBRA,\s*\n\s*2,/);
    expect(src).not.toMatch(/export const FLASHLIGHT_SPOT_DECAY = 2/);
    expect(src).toContain("FLASHLIGHT_CONE_LENGTH + 2.4");
    expect(src).toContain("export const FLASHLIGHT_FILL_DECAY = 1.74");
    expect(src).toContain("new THREE.PointLight(FLASHLIGHT_FILL_COLOR, 0, 10, FLASHLIGHT_FILL_DECAY)");
    expect(src).not.toMatch(/new THREE\.PointLight\(FLASHLIGHT_FILL_COLOR, 0, 10, 2\)/);
    expect(src).not.toMatch(/export const FLASHLIGHT_FILL_DECAY = 2/);
    expect(src).toContain("export const WARM_LIGHT_DECAY = 1.74");
    expect(src).toContain("new THREE.PointLight(WARM_LIGHT_COLOR, 0, 7.5, WARM_LIGHT_DECAY)");
    expect(src).not.toMatch(/new THREE\.PointLight\(WARM_LIGHT_COLOR, 0, 7\.5, 2\)/);
    expect(src).toContain("export const WARM_LIGHT_VISIBLE_EPS = 0.0174");
    expect(src).toContain("warmLight.visible = i > WARM_LIGHT_VISIBLE_EPS");
    expect(src).not.toMatch(/warmLight\.visible = i > 0\.02/);
    expect(src).not.toMatch(/export const WARM_LIGHT_VISIBLE_EPS = 0\.02/);
    expect(src).toContain("export const WARM_LIGHT_AMBER_B_GAIN = 0.115");
    expect(src).toContain("export const WARM_LIGHT_AMBER_B = 0.437");
    expect(src).toContain("export const WARM_LIGHT_AMBER_G_GAIN = 0.092");
    expect(src).toContain("export const WARM_LIGHT_AMBER_G = 0.759");
    expect(src).toContain(
      "warmLight.color.setRGB(1, WARM_LIGHT_AMBER_G + i * WARM_LIGHT_AMBER_G_GAIN, WARM_LIGHT_AMBER_B + i * WARM_LIGHT_AMBER_B_GAIN)",
    );
    expect(src).not.toMatch(/WARM_LIGHT_AMBER_B \+ i \* 0\.1/);
    expect(src).toContain("export const WARM_LIGHT_DISTANCE_GAIN = 2.875");
    expect(src).toContain("export const WARM_LIGHT_DISTANCE_BASE = 7.475");
    expect(src).toContain(
      "warmLight.distance = WARM_LIGHT_DISTANCE_BASE + i * WARM_LIGHT_DISTANCE_GAIN",
    );
    expect(src).not.toMatch(/export const WARM_LIGHT_DISTANCE_GAIN = 2\.5/);
    expect(src).not.toMatch(
      /warmLight\.distance = WARM_LIGHT_DISTANCE_BASE \+ i \* 2\.5/,
    );
    expect(src).not.toMatch(/warmLight\.distance = 6\.5 \+ i \* 2\.5/);
    expect(src).toContain("export const WARM_LIGHT_Y = 1.7825");
    expect(src).toContain("warmLight.position.set(wx, WARM_LIGHT_Y, wy)");
    expect(src).not.toMatch(/export const WARM_LIGHT_Y = 1\.55/);
    expect(src).not.toMatch(/warmLight\.position\.set\(wx, 1\.55, wy\)/);
    expect(src).toContain("warmLight.position.set(0, 1.6, 0)");
    expect(src).toContain("export const WARM_LIGHT_INTENSITY_MUL = 1.7825");
    expect(src).toContain("warmLight.intensity = i * WARM_LIGHT_INTENSITY_MUL");
    expect(src).not.toMatch(/export const WARM_LIGHT_INTENSITY_MUL = 1\.55/);
    expect(src).not.toMatch(/warmLight\.intensity = i \* 1\.55/);
    expect(src).toContain("export const FLASHLIGHT_SPOT_DISTANCE_GAIN = 2.3");
    expect(src).toContain("export const FLASHLIGHT_SPOT_DISTANCE_EXTRA = 1.84");
    expect(src).toContain(
      "torchSpot.distance = FLASHLIGHT_CONE_LENGTH + FLASHLIGHT_SPOT_DISTANCE_EXTRA + i * FLASHLIGHT_SPOT_DISTANCE_GAIN",
    );
    expect(src).toContain("export const FLASHLIGHT_SPOT_Y = 1.7825");
    expect(src).toContain("torchSpot.position.set(0, FLASHLIGHT_SPOT_Y, 0)");
    expect(src).toContain("torchSpot.position.set(wx, FLASHLIGHT_SPOT_Y, wy)");
    expect(src).toContain("export const FLASHLIGHT_FILL_Y = 1.5525");
    expect(src).toContain("torchLight.position.set(0, FLASHLIGHT_FILL_Y, 0)");
    expect(src).toContain("torchLight.position.set(wx, FLASHLIGHT_FILL_Y, wy)");
    expect(src).toContain("export const FLASHLIGHT_FILL_DISTANCE_GAIN = 4.025");
    expect(src).toContain("export const FLASHLIGHT_FILL_DISTANCE_BASE = 8.05");
    expect(src).toContain(
      "torchLight.distance = FLASHLIGHT_FILL_DISTANCE_BASE + i * FLASHLIGHT_FILL_DISTANCE_GAIN",
    );
    expect(src).toContain("export const FLASHLIGHT_FILL_COLOR = 0xcaefff");
    expect(src).toContain("torchLight.color.setHex(FLASHLIGHT_FILL_COLOR)");
    expect(src).toContain("export const TRACER_COLOR = 0xffffb8");
    expect(src).toContain("color: TRACER_COLOR");
    expect(src).toContain("export const IMPACT_SPARK_COLOR = 0xffef93");
    expect(src).toContain("color: IMPACT_SPARK_COLOR");
    expect(src).toContain("export const MUZZLE_FLASH_COLOR = 0xffffdd");
    expect(src).toContain("color: MUZZLE_FLASH_COLOR");
    expect(src).toContain("export const WARM_LIGHT_COLOR = 0xffca81");
    expect(src).toContain("export const POSSESSED_HEAD_EMISSIVE = 0x30124a");
    expect(src).toContain("emissive: POSSESSED_HEAD_EMISSIVE");
    expect(src).toContain("color: POSSESSED_HEAD_COLOR");
    expect(src).toContain("export const POSSESSED_HEAD_COLOR = 0x8c469f");
    expect(src).toContain("emissive: POSSESSED_EMISSIVE");
    expect(src).toContain("color: POSSESSED_COLOR");
    expect(src).toContain("const POSSESSED_EMISSIVE = 0x1e0925");
    expect(src).toContain("emissiveIntensity: 0.22");
    expect(src).toContain("color: HOSTILE_COLOR");
    expect(src).toContain("color: PLAYER_COLOR");
    expect(src).toContain("color: PLAYER_HEAD_COLOR");
    expect(src).toContain("emissive: PLAYER_HEAD_EMISSIVE");
    expect(TRACER_FLASH_COLOR).toBe(0xffdd6e);
    expect(TRACER_FLASH_INTENSITY).toBe(2.76);
    expect(TRACER_FLASH_DISTANCE).toBe(3.68);
    expect(TRACER_FLASH_DECAY).toBe(1.74);
    expect(TRACER_FLASH_Y_OFFSET).toBe(0.1725);
    expect(TRACER_COLOR).toBe(0xffffb8);
    expect(IMPACT_SPARK_LIGHT_COLOR).toBe(0xffef93);
    expect(IMPACT_SPARK_COLOR).toBe(0xffef93);
    expect(IMPACT_SPARK_LIGHT_DECAY).toBe(1.74);
    expect(MUZZLE_LIGHT_COLOR).toBe(0xffffb8);
    expect(MUZZLE_LIGHT_DECAY).toBe(1.74);
    expect(MUZZLE_FLASH_COLOR).toBe(0xffffdd);
    expect(WARM_LIGHT_COLOR).toBe(0xffca81);
    expect(WARM_LIGHT_DECAY).toBe(1.74);
    expect(WARM_LIGHT_VISIBLE_EPS).toBe(0.0174);
    expect(WARM_LIGHT_AMBER_B_GAIN).toBe(0.115);
    expect(WARM_LIGHT_AMBER_B).toBe(0.437);
    expect(WARM_LIGHT_AMBER_G_GAIN).toBe(0.092);
    expect(WARM_LIGHT_AMBER_G).toBe(0.759);
    expect(WARM_LIGHT_DISTANCE_GAIN).toBe(2.875);
    expect(WARM_LIGHT_DISTANCE_BASE).toBe(7.475);
    expect(WARM_LIGHT_Y).toBe(1.7825);
    expect(WARM_LIGHT_INTENSITY_MUL).toBe(1.7825);
    expect(POSSESSED_HEAD_EMISSIVE).toBe(0x30124a);
    expect(POSSESSED_HEAD_COLOR).toBe(0x8c469f);
    expect(POSSESSED_COLOR).toBe(0x68347b);
    expect(HOSTILE_COLOR).toBe(0x7b1e1e);
    expect(PLAYER_COLOR).toBe(0x55a4f4);
    expect(PLAYER_HEAD_COLOR).toBe(0x91d1ff);
    expect(PLAYER_HEAD_EMISSIVE).toBe(0x122537);
    expect(MARKER_PALETTE.possessed.emissive).toBe(0x30124a);
    expect(MARKER_PALETTE.player.badge).toBe(0x91d1ff);
    expect(MARKER_PALETTE.player.emissive).toBe(0x1e4a6e);
    expect(MARKER_PALETTE.player.ring).toBe(0x4392f4);
    expect(MARKER_RING_OPACITY).toBe(0.6877);
    expect(muteBadgeY).toBe(2.3);
    expect(possessedBadgeY).toBe(2.3);
  });
});

describe("worldView player head base Y", () => {
  test("player head base Y 1.32 × 1.15 → 1.518; player-body-base-y/marker-badge-tilt/marker-badge-radius/marker-icon-size/marker-icon-y/marker-ring-y/noise-ring-y/chevron-tilt/chevron-y/muzzle-forward/spot-target-y/y-offset/distance/intensity/decay/impact-spark-decay/muzzle-decay/spot-decay/fill-decay/warm-decay/visible-eps/amber-B-gain/amber-B/amber-G-gain/amber-G/gain/base/Y/intensity/color/spot-gain/extra/spot-Y/fill-Y/gain/base/flash/mesh/color/possessed/hostile/player/palette/badge-Y iguales", () => {
    expect(PLAYER_HEAD_BASE_Y).toBe(1.518);
    expect(PLAYER_HEAD_BASE_Y).toBeCloseTo(1.32 * 1.15, 10);
    expect(HOSTILE_BODY_BASE_Y).toBe(0.644);
    expect(HOSTILE_BODY_BASE_Y).toBeCloseTo(0.56 * 1.15, 10);
    expect(HOSTILE_HEAD_BASE_Y).toBe(1.495);
    expect(HOSTILE_HEAD_BASE_Y).toBeCloseTo(1.3 * 1.15, 10);
    expect(HOSTILE_MUTE_XZ_SCALE).toBe(1.2075);
    expect(HOSTILE_MUTE_XZ_SCALE).toBeCloseTo(1.05 * 1.15, 10);
    expect(PLAYER_BODY_BASE_Y).toBe(0.644);
    expect(PLAYER_BODY_BASE_Y).toBeCloseTo(0.56 * 1.15, 10);
    expect(MARKER_BADGE_TILT).toBe(2.99);
    expect(MARKER_BADGE_TILT).toBeCloseTo(2.6 * 1.15, 10);
    expect(MARKER_BADGE_RADIUS).toBe(0.184);
    expect(MARKER_BADGE_RADIUS).toBeCloseTo(0.16 * 1.15, 10);
    expect(MARKER_ICON_SIZE).toBe(0.207);
    expect(MARKER_ICON_SIZE).toBeCloseTo(0.18 * 1.15, 10);
    expect(MARKER_ICON_Y).toBe(0.023);
    expect(MARKER_ICON_Y).toBeCloseTo(0.02 * 1.15, 10);
    expect(MARKER_RING_Y).toBe(0.046);
    expect(MARKER_RING_Y).toBeCloseTo(0.04 * 1.15, 10);
    expect(NOISE_RING_Y).toBe(0.0575);
    expect(NOISE_RING_Y).toBeCloseTo(0.05 * 1.15, 10);
    expect(CHEVRON_TILT).toBe(-0.4025);
    expect(CHEVRON_Y).toBe(0.138);
    expect(MUZZLE_FORWARD).toBe(0.552);
    expect(FLASHLIGHT_SPOT_TARGET_Y).toBe(0.138);
    expect(TRACER_FLASH_Y_OFFSET).toBe(0.1725);
    expect(TRACER_FLASH_DISTANCE).toBe(3.68);
    expect(TRACER_FLASH_INTENSITY).toBe(2.76);
    expect(TRACER_FLASH_DECAY).toBe(1.74);
    expect(IMPACT_SPARK_LIGHT_DECAY).toBe(1.74);
    expect(MUZZLE_LIGHT_DECAY).toBe(1.74);
    expect(FLASHLIGHT_SPOT_DECAY).toBe(1.74);
    expect(FLASHLIGHT_FILL_DECAY).toBe(1.74);
    expect(WARM_LIGHT_DECAY).toBe(1.74);
    expect(WARM_LIGHT_VISIBLE_EPS).toBe(0.0174);
    expect(WARM_LIGHT_AMBER_B_GAIN).toBe(0.115);
    expect(WARM_LIGHT_AMBER_B).toBe(0.437);
    expect(WARM_LIGHT_AMBER_G_GAIN).toBe(0.092);
    expect(WARM_LIGHT_AMBER_G).toBe(0.759);
    expect(WARM_LIGHT_DISTANCE_GAIN).toBe(2.875);
    expect(WARM_LIGHT_DISTANCE_BASE).toBe(7.475);
    expect(WARM_LIGHT_Y).toBe(1.7825);
    expect(WARM_LIGHT_INTENSITY_MUL).toBe(1.7825);
    expect(FLASHLIGHT_SPOT_DISTANCE_GAIN).toBe(2.3);
    expect(FLASHLIGHT_SPOT_DISTANCE_EXTRA).toBe(1.84);
    expect(FLASHLIGHT_SPOT_Y).toBe(1.7825);
    expect(FLASHLIGHT_FILL_Y).toBe(1.5525);
    expect(FLASHLIGHT_FILL_DISTANCE_GAIN).toBe(4.025);
    expect(FLASHLIGHT_FILL_DISTANCE_BASE).toBe(8.05);
    expect(FLASHLIGHT_FILL_COLOR).toBe(0xcaefff);
    expect(MUZZLE_LIGHT_COLOR).toBe(0xffffb8);
    expect(IMPACT_SPARK_LIGHT_COLOR).toBe(0xffef93);
    expect(TRACER_FLASH_COLOR).toBe(0xffdd6e);
    const src = readFileSync(
      resolve(process.cwd(), "src/render/worldView.ts"),
      "utf8",
    );
    expect(src).toContain("export const PLAYER_HEAD_BASE_Y = 1.518");
    expect(src).toContain("playerHead.position.y = PLAYER_HEAD_BASE_Y");
    expect(src).not.toMatch(/playerHead\.position\.y = 1\.32/);
    expect(src).not.toMatch(/const PLAYER_HEAD_BASE_Y = 1\.32/);
    expect(src).not.toMatch(/export const PLAYER_HEAD_BASE_Y = 1\.32/);
    expect(src).toContain("export const HOSTILE_BODY_BASE_Y = 0.644");
    expect(src).toContain("mesh.position.y = HOSTILE_BODY_BASE_Y");
    expect(src).toContain("body.position.y = HOSTILE_BODY_BASE_Y");
    expect(src).not.toMatch(/mesh\.position\.y = 0\.56/);
    expect(src).not.toMatch(/body\.position\.y = 0\.56/);
    expect(src).not.toMatch(/const HOSTILE_BODY_BASE_Y = 0\.56/);
    expect(src).not.toMatch(/export const HOSTILE_BODY_BASE_Y = 0\.56/);
    expect(src).toContain("export const HOSTILE_HEAD_BASE_Y = 1.495");
    expect(src).toContain("head.position.y = HOSTILE_HEAD_BASE_Y");
    expect(src).not.toMatch(/head\.position\.y = 1\.3/);
    expect(src).not.toMatch(/const HOSTILE_HEAD_BASE_Y = 1\.3/);
    expect(src).not.toMatch(/export const HOSTILE_HEAD_BASE_Y = 1\.3/);
    expect(src).toContain("export const HOSTILE_MUTE_XZ_SCALE = 1.2075");
    expect(src).toContain("mesh.scale.set(HOSTILE_MUTE_XZ_SCALE, 1, HOSTILE_MUTE_XZ_SCALE)");
    expect(src).not.toMatch(/mesh\.scale\.set\(1\.05, 1, 1\.05\)/);
    expect(src).not.toMatch(/const HOSTILE_MUTE_XZ_SCALE = 1\.05/);
    expect(src).not.toMatch(/export const HOSTILE_MUTE_XZ_SCALE = 1\.05/);
    expect(src).toContain("export const PLAYER_BODY_BASE_Y = 0.644");
    expect(src).toContain("playerBody.position.y = PLAYER_BODY_BASE_Y");
    expect(src).not.toMatch(/playerBody\.position\.y = 0\.56/);
    expect(src).not.toMatch(/const PLAYER_BODY_BASE_Y = 0\.56/);
    expect(src).not.toMatch(/export const PLAYER_BODY_BASE_Y = 0\.56/);
    expect(src).toContain("export const MARKER_BADGE_TILT = 2.99");
    expect(src).toContain("disc.rotation.x = -Math.PI / MARKER_BADGE_TILT");
    expect(src).toContain("icon.rotation.x = -Math.PI / MARKER_BADGE_TILT");
    expect(src).not.toMatch(/disc\.rotation\.x = -Math\.PI \/ 2\.6/);
    expect(src).not.toMatch(/icon\.rotation\.x = -Math\.PI \/ 2\.6/);
    expect(src).not.toMatch(/const MARKER_BADGE_TILT = 2\.6/);
    expect(src).not.toMatch(/export const MARKER_BADGE_TILT = 2\.6/);
    expect(src).toContain("export const MARKER_BADGE_RADIUS = 0.184");
    expect(src).toContain("new THREE.CircleGeometry(MARKER_BADGE_RADIUS, 20)");
    expect(src).not.toMatch(/new THREE\.CircleGeometry\(0\.16, 20\)/);
    expect(src).not.toMatch(/const MARKER_BADGE_RADIUS = 0\.16/);
    expect(src).not.toMatch(/export const MARKER_BADGE_RADIUS = 0\.16/);
    expect(src).toContain("export const MARKER_ICON_SIZE = 0.207");
    expect(src).toContain("new THREE.PlaneGeometry(MARKER_ICON_SIZE, MARKER_ICON_SIZE)");
    expect(src).not.toMatch(/new THREE\.PlaneGeometry\(0\.18, 0\.18\)/);
    expect(src).not.toMatch(/const MARKER_ICON_SIZE = 0\.18/);
    expect(src).not.toMatch(/export const MARKER_ICON_SIZE = 0\.18/);
    expect(src).toContain("export const MARKER_ICON_Y = 0.023");
    expect(src).toContain("icon.position.y = MARKER_ICON_Y");
    expect(src).not.toMatch(/icon\.position\.y = 0\.02/);
    expect(src).not.toMatch(/const MARKER_ICON_Y = 0\.02;/);
    expect(src).not.toMatch(/export const MARKER_ICON_Y = 0\.02;/);
    expect(src).toContain("export const MARKER_RING_Y = 0.046");
    expect(src).toContain("ring.position.y = MARKER_RING_Y");
    expect(src).not.toMatch(/ring\.position\.y = 0\.04/);
    expect(src).not.toMatch(/const MARKER_RING_Y = 0\.04;/);
    expect(src).not.toMatch(/export const MARKER_RING_Y = 0\.04;/);
    expect(src).toContain("export const NOISE_RING_Y = 0.0575");
    expect(src).toContain("mesh.position.y = NOISE_RING_Y");
    expect(src).toContain("slot.mesh.position.set(state.x, NOISE_RING_Y, state.y)");
    expect(src).not.toMatch(/const NOISE_RING_Y = 0\.05;/);
    expect(src).not.toMatch(/export const NOISE_RING_Y = 0\.05;/);
    expect(src).toContain("export const CHEVRON_TILT = -0.4025");
    expect(src).toContain("chevronMesh.rotation.x = CHEVRON_TILT");
    expect(src).not.toMatch(/chevronMesh\.rotation\.x = -0\.35/);
    expect(src).not.toMatch(/const CHEVRON_TILT = -0\.35/);
    expect(src).not.toMatch(/export const CHEVRON_TILT = -0\.35/);
    expect(src).toContain("export const CHEVRON_Y = 0.138");
    expect(src).toContain("chevronMesh.position.set(x, CHEVRON_Y, z)");
    expect(src).not.toMatch(/const CHEVRON_Y = 0\.12/);
    expect(src).not.toMatch(/export const CHEVRON_Y = 0\.12/);
    expect(src).not.toMatch(/chevronMesh\.position\.set\(x, 0\.12, z\)/);
    expect(src).toContain("export const MUZZLE_FORWARD = 0.552");
    expect(src).toContain("Math.sin(playerGltfYaw) * MUZZLE_FORWARD");
    expect(src).toContain("Math.cos(playerGltfYaw) * MUZZLE_FORWARD");
    expect(src).toContain("muzzleMesh.position.set(ox, TRACER_HEIGHT, oz)");
    expect(src).toContain("muzzleLight.position.set(ox, TRACER_HEIGHT, oz)");
    expect(src).not.toMatch(/const MUZZLE_FORWARD = 0\.48/);
    expect(src).not.toMatch(/export const MUZZLE_FORWARD = 0\.48/);
    expect(src).toContain("export const FLASHLIGHT_SPOT_TARGET_Y = 0.138");
    expect(src).toContain("torchSpot.target.position.set(wx + tip.x, FLASHLIGHT_SPOT_TARGET_Y, wy + tip.z)");
    expect(src).not.toMatch(/torchSpot\.target\.position\.set\(wx \+ tip\.x, 0\.12, wy \+ tip\.z\)/);
    expect(src).not.toMatch(/export const FLASHLIGHT_SPOT_TARGET_Y = 0\.12/);
    expect(src).toContain("export const TRACER_FLASH_Y_OFFSET = 0.1725");
    expect(src).toContain("flash.position.set(from.x, TRACER_HEIGHT + TRACER_FLASH_Y_OFFSET, from.y)");
    expect(src).not.toMatch(/flash\.position\.set\(from\.x, TRACER_HEIGHT \+ 0\.15, from\.y\)/);
    expect(src).not.toMatch(/export const TRACER_FLASH_Y_OFFSET = 0\.15/);
    expect(src).toContain("export const TRACER_FLASH_DISTANCE = 3.68");
    expect(src).toContain("new THREE.PointLight(TRACER_FLASH_COLOR, TRACER_FLASH_INTENSITY, TRACER_FLASH_DISTANCE, TRACER_FLASH_DECAY)");
    expect(src).not.toMatch(/new THREE\.PointLight\(TRACER_FLASH_COLOR, TRACER_FLASH_INTENSITY, 3\.2, TRACER_FLASH_DECAY\)/);
    expect(src).not.toMatch(/new THREE\.PointLight\(TRACER_FLASH_COLOR, 2\.4, 3\.2, TRACER_FLASH_DECAY\)/);
    expect(src).not.toMatch(/new THREE\.PointLight\(TRACER_FLASH_COLOR, 2\.4, 3\.2, 2\)/);
    expect(src).not.toMatch(/export const TRACER_FLASH_DISTANCE = 3\.2/);
    expect(src).toContain("t.flash.intensity = TRACER_FLASH_INTENSITY * op");
    expect(src).not.toMatch(/t\.flash\.intensity = 2\.4 \* op/);
    expect(src).toContain("export const TRACER_FLASH_INTENSITY = 2.76");
    expect(src).toContain("export const TRACER_FLASH_DECAY = 1.74");
    expect(src).toContain("export const TRACER_FLASH_COLOR = 0xffdd6e");
    expect(src).toContain("export const IMPACT_SPARK_LIGHT_DECAY = 1.74");
    expect(src).toContain("IMPACT_SPARK_LIGHT_DISTANCE,\n    IMPACT_SPARK_LIGHT_DECAY,");
    expect(src).not.toMatch(/IMPACT_SPARK_LIGHT_DISTANCE,\s*\n\s*2,/);
    expect(src).not.toMatch(/export const IMPACT_SPARK_LIGHT_DECAY = 2/);
    expect(src).toContain("const IMPACT_SPARK_LIGHT_DISTANCE = 1.8");
    expect(src).toContain("export const IMPACT_SPARK_LIGHT_COLOR = 0xffef93");
    expect(src).toContain("new THREE.PointLight(\n    IMPACT_SPARK_LIGHT_COLOR,");
    expect(src).toContain("export const MUZZLE_LIGHT_DECAY = 1.74");
    expect(src).toContain("MUZZLE_LIGHT_DISTANCE,\n    MUZZLE_LIGHT_DECAY,");
    expect(src).not.toMatch(/MUZZLE_LIGHT_DISTANCE,\s*\n\s*2,/);
    expect(src).not.toMatch(/export const MUZZLE_LIGHT_DECAY = 2/);
    expect(src).toContain("const MUZZLE_LIGHT_DISTANCE = 2.6");
    expect(src).toContain("export const MUZZLE_LIGHT_COLOR = 0xffffb8");
    expect(src).toContain("new THREE.PointLight(\n    MUZZLE_LIGHT_COLOR,");
    expect(src).toContain("export const FLASHLIGHT_SPOT_DECAY = 1.74");
    expect(src).toContain("FLASHLIGHT_SPOT_PENUMBRA,\n    FLASHLIGHT_SPOT_DECAY,");
    expect(src).not.toMatch(/FLASHLIGHT_SPOT_PENUMBRA,\s*\n\s*2,/);
    expect(src).not.toMatch(/export const FLASHLIGHT_SPOT_DECAY = 2/);
    expect(src).toContain("FLASHLIGHT_CONE_LENGTH + 2.4");
    expect(src).toContain("export const FLASHLIGHT_FILL_DECAY = 1.74");
    expect(src).toContain("new THREE.PointLight(FLASHLIGHT_FILL_COLOR, 0, 10, FLASHLIGHT_FILL_DECAY)");
    expect(src).not.toMatch(/new THREE\.PointLight\(FLASHLIGHT_FILL_COLOR, 0, 10, 2\)/);
    expect(src).not.toMatch(/export const FLASHLIGHT_FILL_DECAY = 2/);
    expect(src).toContain("export const WARM_LIGHT_DECAY = 1.74");
    expect(src).toContain("new THREE.PointLight(WARM_LIGHT_COLOR, 0, 7.5, WARM_LIGHT_DECAY)");
    expect(src).not.toMatch(/new THREE\.PointLight\(WARM_LIGHT_COLOR, 0, 7\.5, 2\)/);
    expect(src).toContain("export const WARM_LIGHT_VISIBLE_EPS = 0.0174");
    expect(src).toContain("warmLight.visible = i > WARM_LIGHT_VISIBLE_EPS");
    expect(src).not.toMatch(/warmLight\.visible = i > 0\.02/);
    expect(src).not.toMatch(/export const WARM_LIGHT_VISIBLE_EPS = 0\.02/);
    expect(src).toContain("export const WARM_LIGHT_AMBER_B_GAIN = 0.115");
    expect(src).toContain("export const WARM_LIGHT_AMBER_B = 0.437");
    expect(src).toContain("export const WARM_LIGHT_AMBER_G_GAIN = 0.092");
    expect(src).toContain("export const WARM_LIGHT_AMBER_G = 0.759");
    expect(src).toContain(
      "warmLight.color.setRGB(1, WARM_LIGHT_AMBER_G + i * WARM_LIGHT_AMBER_G_GAIN, WARM_LIGHT_AMBER_B + i * WARM_LIGHT_AMBER_B_GAIN)",
    );
    expect(src).not.toMatch(/WARM_LIGHT_AMBER_B \+ i \* 0\.1/);
    expect(src).toContain("export const WARM_LIGHT_DISTANCE_GAIN = 2.875");
    expect(src).toContain("export const WARM_LIGHT_DISTANCE_BASE = 7.475");
    expect(src).toContain(
      "warmLight.distance = WARM_LIGHT_DISTANCE_BASE + i * WARM_LIGHT_DISTANCE_GAIN",
    );
    expect(src).not.toMatch(/export const WARM_LIGHT_DISTANCE_GAIN = 2\.5/);
    expect(src).not.toMatch(
      /warmLight\.distance = WARM_LIGHT_DISTANCE_BASE \+ i \* 2\.5/,
    );
    expect(src).not.toMatch(/warmLight\.distance = 6\.5 \+ i \* 2\.5/);
    expect(src).toContain("export const WARM_LIGHT_Y = 1.7825");
    expect(src).toContain("warmLight.position.set(wx, WARM_LIGHT_Y, wy)");
    expect(src).not.toMatch(/export const WARM_LIGHT_Y = 1\.55/);
    expect(src).not.toMatch(/warmLight\.position\.set\(wx, 1\.55, wy\)/);
    expect(src).toContain("warmLight.position.set(0, 1.6, 0)");
    expect(src).toContain("export const WARM_LIGHT_INTENSITY_MUL = 1.7825");
    expect(src).toContain("warmLight.intensity = i * WARM_LIGHT_INTENSITY_MUL");
    expect(src).not.toMatch(/export const WARM_LIGHT_INTENSITY_MUL = 1\.55/);
    expect(src).not.toMatch(/warmLight\.intensity = i \* 1\.55/);
    expect(src).toContain("export const FLASHLIGHT_SPOT_DISTANCE_GAIN = 2.3");
    expect(src).toContain("export const FLASHLIGHT_SPOT_DISTANCE_EXTRA = 1.84");
    expect(src).toContain(
      "torchSpot.distance = FLASHLIGHT_CONE_LENGTH + FLASHLIGHT_SPOT_DISTANCE_EXTRA + i * FLASHLIGHT_SPOT_DISTANCE_GAIN",
    );
    expect(src).toContain("export const FLASHLIGHT_SPOT_Y = 1.7825");
    expect(src).toContain("torchSpot.position.set(0, FLASHLIGHT_SPOT_Y, 0)");
    expect(src).toContain("torchSpot.position.set(wx, FLASHLIGHT_SPOT_Y, wy)");
    expect(src).toContain("export const FLASHLIGHT_FILL_Y = 1.5525");
    expect(src).toContain("torchLight.position.set(0, FLASHLIGHT_FILL_Y, 0)");
    expect(src).toContain("torchLight.position.set(wx, FLASHLIGHT_FILL_Y, wy)");
    expect(src).toContain("export const FLASHLIGHT_FILL_DISTANCE_GAIN = 4.025");
    expect(src).toContain("export const FLASHLIGHT_FILL_DISTANCE_BASE = 8.05");
    expect(src).toContain(
      "torchLight.distance = FLASHLIGHT_FILL_DISTANCE_BASE + i * FLASHLIGHT_FILL_DISTANCE_GAIN",
    );
    expect(src).toContain("export const FLASHLIGHT_FILL_COLOR = 0xcaefff");
    expect(src).toContain("torchLight.color.setHex(FLASHLIGHT_FILL_COLOR)");
    expect(src).toContain("export const TRACER_COLOR = 0xffffb8");
    expect(src).toContain("color: TRACER_COLOR");
    expect(src).toContain("export const IMPACT_SPARK_COLOR = 0xffef93");
    expect(src).toContain("color: IMPACT_SPARK_COLOR");
    expect(src).toContain("export const MUZZLE_FLASH_COLOR = 0xffffdd");
    expect(src).toContain("color: MUZZLE_FLASH_COLOR");
    expect(src).toContain("export const WARM_LIGHT_COLOR = 0xffca81");
    expect(src).toContain("export const POSSESSED_HEAD_EMISSIVE = 0x30124a");
    expect(src).toContain("emissive: POSSESSED_HEAD_EMISSIVE");
    expect(src).toContain("color: POSSESSED_HEAD_COLOR");
    expect(src).toContain("export const POSSESSED_HEAD_COLOR = 0x8c469f");
    expect(src).toContain("emissive: POSSESSED_EMISSIVE");
    expect(src).toContain("color: POSSESSED_COLOR");
    expect(src).toContain("const POSSESSED_EMISSIVE = 0x1e0925");
    expect(src).toContain("emissiveIntensity: 0.22");
    expect(src).toContain("color: HOSTILE_COLOR");
    expect(src).toContain("color: PLAYER_COLOR");
    expect(src).toContain("color: PLAYER_HEAD_COLOR");
    expect(src).toContain("emissive: PLAYER_HEAD_EMISSIVE");
    expect(TRACER_FLASH_COLOR).toBe(0xffdd6e);
    expect(TRACER_FLASH_INTENSITY).toBe(2.76);
    expect(TRACER_FLASH_DISTANCE).toBe(3.68);
    expect(TRACER_FLASH_DECAY).toBe(1.74);
    expect(TRACER_FLASH_Y_OFFSET).toBe(0.1725);
    expect(TRACER_COLOR).toBe(0xffffb8);
    expect(IMPACT_SPARK_LIGHT_COLOR).toBe(0xffef93);
    expect(IMPACT_SPARK_COLOR).toBe(0xffef93);
    expect(IMPACT_SPARK_LIGHT_DECAY).toBe(1.74);
    expect(MUZZLE_LIGHT_COLOR).toBe(0xffffb8);
    expect(MUZZLE_LIGHT_DECAY).toBe(1.74);
    expect(MUZZLE_FLASH_COLOR).toBe(0xffffdd);
    expect(WARM_LIGHT_COLOR).toBe(0xffca81);
    expect(WARM_LIGHT_DECAY).toBe(1.74);
    expect(WARM_LIGHT_VISIBLE_EPS).toBe(0.0174);
    expect(WARM_LIGHT_AMBER_B_GAIN).toBe(0.115);
    expect(WARM_LIGHT_AMBER_B).toBe(0.437);
    expect(WARM_LIGHT_AMBER_G_GAIN).toBe(0.092);
    expect(WARM_LIGHT_AMBER_G).toBe(0.759);
    expect(WARM_LIGHT_DISTANCE_GAIN).toBe(2.875);
    expect(WARM_LIGHT_DISTANCE_BASE).toBe(7.475);
    expect(WARM_LIGHT_Y).toBe(1.7825);
    expect(WARM_LIGHT_INTENSITY_MUL).toBe(1.7825);
    expect(POSSESSED_HEAD_EMISSIVE).toBe(0x30124a);
    expect(POSSESSED_HEAD_COLOR).toBe(0x8c469f);
    expect(POSSESSED_COLOR).toBe(0x68347b);
    expect(HOSTILE_COLOR).toBe(0x7b1e1e);
    expect(PLAYER_COLOR).toBe(0x55a4f4);
    expect(PLAYER_HEAD_COLOR).toBe(0x91d1ff);
    expect(PLAYER_HEAD_EMISSIVE).toBe(0x122537);
    expect(MARKER_PALETTE.possessed.emissive).toBe(0x30124a);
    expect(MARKER_PALETTE.player.badge).toBe(0x91d1ff);
    expect(MARKER_PALETTE.player.emissive).toBe(0x1e4a6e);
    expect(MARKER_PALETTE.player.ring).toBe(0x4392f4);
    expect(MARKER_RING_OPACITY).toBe(0.6877);
    expect(muteBadgeY).toBe(2.3);
    expect(possessedBadgeY).toBe(2.3);
  });
});

describe("worldView hostile body base Y", () => {
  test("hostile body base Y 0.56 × 1.15 → 0.644; player-head-base-y/player-body-base-y/marker-badge-tilt/marker-badge-radius/marker-icon-size/marker-icon-y/marker-ring-y/noise-ring-y/chevron-tilt/chevron-y/muzzle-forward/spot-target-y/y-offset/distance/intensity/decay/impact-spark-decay/muzzle-decay/spot-decay/fill-decay/warm-decay/visible-eps/amber-B-gain/amber-B/amber-G-gain/amber-G/gain/base/Y/intensity/color/spot-gain/extra/spot-Y/fill-Y/gain/base/flash/mesh/color/possessed/hostile/player/palette/badge-Y iguales", () => {
    expect(HOSTILE_BODY_BASE_Y).toBe(0.644);
    expect(HOSTILE_BODY_BASE_Y).toBeCloseTo(0.56 * 1.15, 10);
    expect(HOSTILE_HEAD_BASE_Y).toBe(1.495);
    expect(HOSTILE_HEAD_BASE_Y).toBeCloseTo(1.3 * 1.15, 10);
    expect(HOSTILE_MUTE_XZ_SCALE).toBe(1.2075);
    expect(HOSTILE_MUTE_XZ_SCALE).toBeCloseTo(1.05 * 1.15, 10);
    expect(PLAYER_HEAD_BASE_Y).toBe(1.518);
    expect(PLAYER_HEAD_BASE_Y).toBeCloseTo(1.32 * 1.15, 10);
    expect(PLAYER_BODY_BASE_Y).toBe(0.644);
    expect(PLAYER_BODY_BASE_Y).toBeCloseTo(0.56 * 1.15, 10);
    expect(MARKER_BADGE_TILT).toBe(2.99);
    expect(MARKER_BADGE_TILT).toBeCloseTo(2.6 * 1.15, 10);
    expect(MARKER_BADGE_RADIUS).toBe(0.184);
    expect(MARKER_BADGE_RADIUS).toBeCloseTo(0.16 * 1.15, 10);
    expect(MARKER_ICON_SIZE).toBe(0.207);
    expect(MARKER_ICON_SIZE).toBeCloseTo(0.18 * 1.15, 10);
    expect(MARKER_ICON_Y).toBe(0.023);
    expect(MARKER_ICON_Y).toBeCloseTo(0.02 * 1.15, 10);
    expect(MARKER_RING_Y).toBe(0.046);
    expect(MARKER_RING_Y).toBeCloseTo(0.04 * 1.15, 10);
    expect(NOISE_RING_Y).toBe(0.0575);
    expect(NOISE_RING_Y).toBeCloseTo(0.05 * 1.15, 10);
    expect(CHEVRON_TILT).toBe(-0.4025);
    expect(CHEVRON_Y).toBe(0.138);
    expect(MUZZLE_FORWARD).toBe(0.552);
    expect(FLASHLIGHT_SPOT_TARGET_Y).toBe(0.138);
    expect(TRACER_FLASH_Y_OFFSET).toBe(0.1725);
    expect(TRACER_FLASH_DISTANCE).toBe(3.68);
    expect(TRACER_FLASH_INTENSITY).toBe(2.76);
    expect(TRACER_FLASH_DECAY).toBe(1.74);
    expect(IMPACT_SPARK_LIGHT_DECAY).toBe(1.74);
    expect(MUZZLE_LIGHT_DECAY).toBe(1.74);
    expect(FLASHLIGHT_SPOT_DECAY).toBe(1.74);
    expect(FLASHLIGHT_FILL_DECAY).toBe(1.74);
    expect(WARM_LIGHT_DECAY).toBe(1.74);
    expect(WARM_LIGHT_VISIBLE_EPS).toBe(0.0174);
    expect(WARM_LIGHT_AMBER_B_GAIN).toBe(0.115);
    expect(WARM_LIGHT_AMBER_B).toBe(0.437);
    expect(WARM_LIGHT_AMBER_G_GAIN).toBe(0.092);
    expect(WARM_LIGHT_AMBER_G).toBe(0.759);
    expect(WARM_LIGHT_DISTANCE_GAIN).toBe(2.875);
    expect(WARM_LIGHT_DISTANCE_BASE).toBe(7.475);
    expect(WARM_LIGHT_Y).toBe(1.7825);
    expect(WARM_LIGHT_INTENSITY_MUL).toBe(1.7825);
    expect(FLASHLIGHT_SPOT_DISTANCE_GAIN).toBe(2.3);
    expect(FLASHLIGHT_SPOT_DISTANCE_EXTRA).toBe(1.84);
    expect(FLASHLIGHT_SPOT_Y).toBe(1.7825);
    expect(FLASHLIGHT_FILL_Y).toBe(1.5525);
    expect(FLASHLIGHT_FILL_DISTANCE_GAIN).toBe(4.025);
    expect(FLASHLIGHT_FILL_DISTANCE_BASE).toBe(8.05);
    expect(FLASHLIGHT_FILL_COLOR).toBe(0xcaefff);
    expect(MUZZLE_LIGHT_COLOR).toBe(0xffffb8);
    expect(IMPACT_SPARK_LIGHT_COLOR).toBe(0xffef93);
    expect(TRACER_FLASH_COLOR).toBe(0xffdd6e);
    const src = readFileSync(
      resolve(process.cwd(), "src/render/worldView.ts"),
      "utf8",
    );
    expect(src).toContain("export const HOSTILE_BODY_BASE_Y = 0.644");
    expect(src).toContain("mesh.position.y = HOSTILE_BODY_BASE_Y");
    expect(src).toContain("body.position.y = HOSTILE_BODY_BASE_Y");
    expect(src).not.toMatch(/mesh\.position\.y = 0\.56/);
    expect(src).not.toMatch(/body\.position\.y = 0\.56/);
    expect(src).not.toMatch(/const HOSTILE_BODY_BASE_Y = 0\.56/);
    expect(src).not.toMatch(/export const HOSTILE_BODY_BASE_Y = 0\.56/);
    expect(src).toContain("export const HOSTILE_HEAD_BASE_Y = 1.495");
    expect(src).toContain("head.position.y = HOSTILE_HEAD_BASE_Y");
    expect(src).not.toMatch(/head\.position\.y = 1\.3/);
    expect(src).not.toMatch(/const HOSTILE_HEAD_BASE_Y = 1\.3/);
    expect(src).not.toMatch(/export const HOSTILE_HEAD_BASE_Y = 1\.3/);
    expect(src).toContain("export const HOSTILE_MUTE_XZ_SCALE = 1.2075");
    expect(src).toContain("mesh.scale.set(HOSTILE_MUTE_XZ_SCALE, 1, HOSTILE_MUTE_XZ_SCALE)");
    expect(src).not.toMatch(/mesh\.scale\.set\(1\.05, 1, 1\.05\)/);
    expect(src).not.toMatch(/const HOSTILE_MUTE_XZ_SCALE = 1\.05/);
    expect(src).not.toMatch(/export const HOSTILE_MUTE_XZ_SCALE = 1\.05/);
    expect(src).toContain("export const PLAYER_HEAD_BASE_Y = 1.518");
    expect(src).toContain("playerHead.position.y = PLAYER_HEAD_BASE_Y");
    expect(src).not.toMatch(/playerHead\.position\.y = 1\.32/);
    expect(src).not.toMatch(/const PLAYER_HEAD_BASE_Y = 1\.32/);
    expect(src).not.toMatch(/export const PLAYER_HEAD_BASE_Y = 1\.32/);
    expect(src).toContain("export const PLAYER_BODY_BASE_Y = 0.644");
    expect(src).toContain("playerBody.position.y = PLAYER_BODY_BASE_Y");
    expect(src).not.toMatch(/playerBody\.position\.y = 0\.56/);
    expect(src).not.toMatch(/const PLAYER_BODY_BASE_Y = 0\.56/);
    expect(src).not.toMatch(/export const PLAYER_BODY_BASE_Y = 0\.56/);
    expect(src).toContain("export const MARKER_BADGE_TILT = 2.99");
    expect(src).toContain("disc.rotation.x = -Math.PI / MARKER_BADGE_TILT");
    expect(src).toContain("icon.rotation.x = -Math.PI / MARKER_BADGE_TILT");
    expect(src).not.toMatch(/disc\.rotation\.x = -Math\.PI \/ 2\.6/);
    expect(src).not.toMatch(/icon\.rotation\.x = -Math\.PI \/ 2\.6/);
    expect(src).not.toMatch(/const MARKER_BADGE_TILT = 2\.6/);
    expect(src).not.toMatch(/export const MARKER_BADGE_TILT = 2\.6/);
    expect(src).toContain("export const MARKER_BADGE_RADIUS = 0.184");
    expect(src).toContain("new THREE.CircleGeometry(MARKER_BADGE_RADIUS, 20)");
    expect(src).not.toMatch(/new THREE\.CircleGeometry\(0\.16, 20\)/);
    expect(src).not.toMatch(/const MARKER_BADGE_RADIUS = 0\.16/);
    expect(src).not.toMatch(/export const MARKER_BADGE_RADIUS = 0\.16/);
    expect(src).toContain("export const MARKER_ICON_SIZE = 0.207");
    expect(src).toContain("new THREE.PlaneGeometry(MARKER_ICON_SIZE, MARKER_ICON_SIZE)");
    expect(src).not.toMatch(/new THREE\.PlaneGeometry\(0\.18, 0\.18\)/);
    expect(src).not.toMatch(/const MARKER_ICON_SIZE = 0\.18/);
    expect(src).not.toMatch(/export const MARKER_ICON_SIZE = 0\.18/);
    expect(src).toContain("export const MARKER_ICON_Y = 0.023");
    expect(src).toContain("icon.position.y = MARKER_ICON_Y");
    expect(src).not.toMatch(/icon\.position\.y = 0\.02/);
    expect(src).not.toMatch(/const MARKER_ICON_Y = 0\.02;/);
    expect(src).not.toMatch(/export const MARKER_ICON_Y = 0\.02;/);
    expect(src).toContain("export const MARKER_RING_Y = 0.046");
    expect(src).toContain("ring.position.y = MARKER_RING_Y");
    expect(src).not.toMatch(/ring\.position\.y = 0\.04/);
    expect(src).not.toMatch(/const MARKER_RING_Y = 0\.04;/);
    expect(src).not.toMatch(/export const MARKER_RING_Y = 0\.04;/);
    expect(src).toContain("export const NOISE_RING_Y = 0.0575");
    expect(src).toContain("mesh.position.y = NOISE_RING_Y");
    expect(src).toContain("slot.mesh.position.set(state.x, NOISE_RING_Y, state.y)");
    expect(src).not.toMatch(/const NOISE_RING_Y = 0\.05;/);
    expect(src).not.toMatch(/export const NOISE_RING_Y = 0\.05;/);
    expect(src).toContain("export const CHEVRON_TILT = -0.4025");
    expect(src).toContain("chevronMesh.rotation.x = CHEVRON_TILT");
    expect(src).not.toMatch(/chevronMesh\.rotation\.x = -0\.35/);
    expect(src).not.toMatch(/const CHEVRON_TILT = -0\.35/);
    expect(src).not.toMatch(/export const CHEVRON_TILT = -0\.35/);
    expect(src).toContain("export const CHEVRON_Y = 0.138");
    expect(src).toContain("chevronMesh.position.set(x, CHEVRON_Y, z)");
    expect(src).not.toMatch(/const CHEVRON_Y = 0\.12/);
    expect(src).not.toMatch(/export const CHEVRON_Y = 0\.12/);
    expect(src).not.toMatch(/chevronMesh\.position\.set\(x, 0\.12, z\)/);
    expect(src).toContain("export const MUZZLE_FORWARD = 0.552");
    expect(src).toContain("Math.sin(playerGltfYaw) * MUZZLE_FORWARD");
    expect(src).toContain("Math.cos(playerGltfYaw) * MUZZLE_FORWARD");
    expect(src).toContain("muzzleMesh.position.set(ox, TRACER_HEIGHT, oz)");
    expect(src).toContain("muzzleLight.position.set(ox, TRACER_HEIGHT, oz)");
    expect(src).not.toMatch(/const MUZZLE_FORWARD = 0\.48/);
    expect(src).not.toMatch(/export const MUZZLE_FORWARD = 0\.48/);
    expect(src).toContain("export const FLASHLIGHT_SPOT_TARGET_Y = 0.138");
    expect(src).toContain("torchSpot.target.position.set(wx + tip.x, FLASHLIGHT_SPOT_TARGET_Y, wy + tip.z)");
    expect(src).not.toMatch(/torchSpot\.target\.position\.set\(wx \+ tip\.x, 0\.12, wy \+ tip\.z\)/);
    expect(src).not.toMatch(/export const FLASHLIGHT_SPOT_TARGET_Y = 0\.12/);
    expect(src).toContain("export const TRACER_FLASH_Y_OFFSET = 0.1725");
    expect(src).toContain("flash.position.set(from.x, TRACER_HEIGHT + TRACER_FLASH_Y_OFFSET, from.y)");
    expect(src).not.toMatch(/flash\.position\.set\(from\.x, TRACER_HEIGHT \+ 0\.15, from\.y\)/);
    expect(src).not.toMatch(/export const TRACER_FLASH_Y_OFFSET = 0\.15/);
    expect(src).toContain("export const TRACER_FLASH_DISTANCE = 3.68");
    expect(src).toContain("new THREE.PointLight(TRACER_FLASH_COLOR, TRACER_FLASH_INTENSITY, TRACER_FLASH_DISTANCE, TRACER_FLASH_DECAY)");
    expect(src).not.toMatch(/new THREE\.PointLight\(TRACER_FLASH_COLOR, TRACER_FLASH_INTENSITY, 3\.2, TRACER_FLASH_DECAY\)/);
    expect(src).not.toMatch(/new THREE\.PointLight\(TRACER_FLASH_COLOR, 2\.4, 3\.2, TRACER_FLASH_DECAY\)/);
    expect(src).not.toMatch(/new THREE\.PointLight\(TRACER_FLASH_COLOR, 2\.4, 3\.2, 2\)/);
    expect(src).not.toMatch(/export const TRACER_FLASH_DISTANCE = 3\.2/);
    expect(src).toContain("t.flash.intensity = TRACER_FLASH_INTENSITY * op");
    expect(src).not.toMatch(/t\.flash\.intensity = 2\.4 \* op/);
    expect(src).toContain("export const TRACER_FLASH_INTENSITY = 2.76");
    expect(src).toContain("export const TRACER_FLASH_DECAY = 1.74");
    expect(src).toContain("export const TRACER_FLASH_COLOR = 0xffdd6e");
    expect(src).toContain("export const IMPACT_SPARK_LIGHT_DECAY = 1.74");
    expect(src).toContain("IMPACT_SPARK_LIGHT_DISTANCE,\n    IMPACT_SPARK_LIGHT_DECAY,");
    expect(src).not.toMatch(/IMPACT_SPARK_LIGHT_DISTANCE,\s*\n\s*2,/);
    expect(src).not.toMatch(/export const IMPACT_SPARK_LIGHT_DECAY = 2/);
    expect(src).toContain("const IMPACT_SPARK_LIGHT_DISTANCE = 1.8");
    expect(src).toContain("export const IMPACT_SPARK_LIGHT_COLOR = 0xffef93");
    expect(src).toContain("new THREE.PointLight(\n    IMPACT_SPARK_LIGHT_COLOR,");
    expect(src).toContain("export const MUZZLE_LIGHT_DECAY = 1.74");
    expect(src).toContain("MUZZLE_LIGHT_DISTANCE,\n    MUZZLE_LIGHT_DECAY,");
    expect(src).not.toMatch(/MUZZLE_LIGHT_DISTANCE,\s*\n\s*2,/);
    expect(src).not.toMatch(/export const MUZZLE_LIGHT_DECAY = 2/);
    expect(src).toContain("const MUZZLE_LIGHT_DISTANCE = 2.6");
    expect(src).toContain("export const MUZZLE_LIGHT_COLOR = 0xffffb8");
    expect(src).toContain("new THREE.PointLight(\n    MUZZLE_LIGHT_COLOR,");
    expect(src).toContain("export const FLASHLIGHT_SPOT_DECAY = 1.74");
    expect(src).toContain("FLASHLIGHT_SPOT_PENUMBRA,\n    FLASHLIGHT_SPOT_DECAY,");
    expect(src).not.toMatch(/FLASHLIGHT_SPOT_PENUMBRA,\s*\n\s*2,/);
    expect(src).not.toMatch(/export const FLASHLIGHT_SPOT_DECAY = 2/);
    expect(src).toContain("FLASHLIGHT_CONE_LENGTH + 2.4");
    expect(src).toContain("export const FLASHLIGHT_FILL_DECAY = 1.74");
    expect(src).toContain("new THREE.PointLight(FLASHLIGHT_FILL_COLOR, 0, 10, FLASHLIGHT_FILL_DECAY)");
    expect(src).not.toMatch(/new THREE\.PointLight\(FLASHLIGHT_FILL_COLOR, 0, 10, 2\)/);
    expect(src).not.toMatch(/export const FLASHLIGHT_FILL_DECAY = 2/);
    expect(src).toContain("export const WARM_LIGHT_DECAY = 1.74");
    expect(src).toContain("new THREE.PointLight(WARM_LIGHT_COLOR, 0, 7.5, WARM_LIGHT_DECAY)");
    expect(src).not.toMatch(/new THREE\.PointLight\(WARM_LIGHT_COLOR, 0, 7\.5, 2\)/);
    expect(src).toContain("export const WARM_LIGHT_VISIBLE_EPS = 0.0174");
    expect(src).toContain("warmLight.visible = i > WARM_LIGHT_VISIBLE_EPS");
    expect(src).not.toMatch(/warmLight\.visible = i > 0\.02/);
    expect(src).not.toMatch(/export const WARM_LIGHT_VISIBLE_EPS = 0\.02/);
    expect(src).toContain("export const WARM_LIGHT_AMBER_B_GAIN = 0.115");
    expect(src).toContain("export const WARM_LIGHT_AMBER_B = 0.437");
    expect(src).toContain("export const WARM_LIGHT_AMBER_G_GAIN = 0.092");
    expect(src).toContain("export const WARM_LIGHT_AMBER_G = 0.759");
    expect(src).toContain(
      "warmLight.color.setRGB(1, WARM_LIGHT_AMBER_G + i * WARM_LIGHT_AMBER_G_GAIN, WARM_LIGHT_AMBER_B + i * WARM_LIGHT_AMBER_B_GAIN)",
    );
    expect(src).not.toMatch(/WARM_LIGHT_AMBER_B \+ i \* 0\.1/);
    expect(src).toContain("export const WARM_LIGHT_DISTANCE_GAIN = 2.875");
    expect(src).toContain("export const WARM_LIGHT_DISTANCE_BASE = 7.475");
    expect(src).toContain(
      "warmLight.distance = WARM_LIGHT_DISTANCE_BASE + i * WARM_LIGHT_DISTANCE_GAIN",
    );
    expect(src).not.toMatch(/export const WARM_LIGHT_DISTANCE_GAIN = 2\.5/);
    expect(src).not.toMatch(
      /warmLight\.distance = WARM_LIGHT_DISTANCE_BASE \+ i \* 2\.5/,
    );
    expect(src).not.toMatch(/warmLight\.distance = 6\.5 \+ i \* 2\.5/);
    expect(src).toContain("export const WARM_LIGHT_Y = 1.7825");
    expect(src).toContain("warmLight.position.set(wx, WARM_LIGHT_Y, wy)");
    expect(src).not.toMatch(/export const WARM_LIGHT_Y = 1\.55/);
    expect(src).not.toMatch(/warmLight\.position\.set\(wx, 1\.55, wy\)/);
    expect(src).toContain("warmLight.position.set(0, 1.6, 0)");
    expect(src).toContain("export const WARM_LIGHT_INTENSITY_MUL = 1.7825");
    expect(src).toContain("warmLight.intensity = i * WARM_LIGHT_INTENSITY_MUL");
    expect(src).not.toMatch(/export const WARM_LIGHT_INTENSITY_MUL = 1\.55/);
    expect(src).not.toMatch(/warmLight\.intensity = i \* 1\.55/);
    expect(src).toContain("export const FLASHLIGHT_SPOT_DISTANCE_GAIN = 2.3");
    expect(src).toContain("export const FLASHLIGHT_SPOT_DISTANCE_EXTRA = 1.84");
    expect(src).toContain(
      "torchSpot.distance = FLASHLIGHT_CONE_LENGTH + FLASHLIGHT_SPOT_DISTANCE_EXTRA + i * FLASHLIGHT_SPOT_DISTANCE_GAIN",
    );
    expect(src).toContain("export const FLASHLIGHT_SPOT_Y = 1.7825");
    expect(src).toContain("torchSpot.position.set(0, FLASHLIGHT_SPOT_Y, 0)");
    expect(src).toContain("torchSpot.position.set(wx, FLASHLIGHT_SPOT_Y, wy)");
    expect(src).toContain("export const FLASHLIGHT_FILL_Y = 1.5525");
    expect(src).toContain("torchLight.position.set(0, FLASHLIGHT_FILL_Y, 0)");
    expect(src).toContain("torchLight.position.set(wx, FLASHLIGHT_FILL_Y, wy)");
    expect(src).toContain("export const FLASHLIGHT_FILL_DISTANCE_GAIN = 4.025");
    expect(src).toContain("export const FLASHLIGHT_FILL_DISTANCE_BASE = 8.05");
    expect(src).toContain(
      "torchLight.distance = FLASHLIGHT_FILL_DISTANCE_BASE + i * FLASHLIGHT_FILL_DISTANCE_GAIN",
    );
    expect(src).toContain("export const FLASHLIGHT_FILL_COLOR = 0xcaefff");
    expect(src).toContain("torchLight.color.setHex(FLASHLIGHT_FILL_COLOR)");
    expect(src).toContain("export const TRACER_COLOR = 0xffffb8");
    expect(src).toContain("color: TRACER_COLOR");
    expect(src).toContain("export const IMPACT_SPARK_COLOR = 0xffef93");
    expect(src).toContain("color: IMPACT_SPARK_COLOR");
    expect(src).toContain("export const MUZZLE_FLASH_COLOR = 0xffffdd");
    expect(src).toContain("color: MUZZLE_FLASH_COLOR");
    expect(src).toContain("export const WARM_LIGHT_COLOR = 0xffca81");
    expect(src).toContain("export const POSSESSED_HEAD_EMISSIVE = 0x30124a");
    expect(src).toContain("emissive: POSSESSED_HEAD_EMISSIVE");
    expect(src).toContain("color: POSSESSED_HEAD_COLOR");
    expect(src).toContain("export const POSSESSED_HEAD_COLOR = 0x8c469f");
    expect(src).toContain("emissive: POSSESSED_EMISSIVE");
    expect(src).toContain("color: POSSESSED_COLOR");
    expect(src).toContain("const POSSESSED_EMISSIVE = 0x1e0925");
    expect(src).toContain("emissiveIntensity: 0.22");
    expect(src).toContain("color: HOSTILE_COLOR");
    expect(src).toContain("color: PLAYER_COLOR");
    expect(src).toContain("color: PLAYER_HEAD_COLOR");
    expect(src).toContain("emissive: PLAYER_HEAD_EMISSIVE");
    expect(TRACER_FLASH_COLOR).toBe(0xffdd6e);
    expect(TRACER_FLASH_INTENSITY).toBe(2.76);
    expect(TRACER_FLASH_DISTANCE).toBe(3.68);
    expect(TRACER_FLASH_DECAY).toBe(1.74);
    expect(TRACER_FLASH_Y_OFFSET).toBe(0.1725);
    expect(TRACER_COLOR).toBe(0xffffb8);
    expect(IMPACT_SPARK_LIGHT_COLOR).toBe(0xffef93);
    expect(IMPACT_SPARK_COLOR).toBe(0xffef93);
    expect(IMPACT_SPARK_LIGHT_DECAY).toBe(1.74);
    expect(MUZZLE_LIGHT_COLOR).toBe(0xffffb8);
    expect(MUZZLE_LIGHT_DECAY).toBe(1.74);
    expect(MUZZLE_FLASH_COLOR).toBe(0xffffdd);
    expect(WARM_LIGHT_COLOR).toBe(0xffca81);
    expect(WARM_LIGHT_DECAY).toBe(1.74);
    expect(WARM_LIGHT_VISIBLE_EPS).toBe(0.0174);
    expect(WARM_LIGHT_AMBER_B_GAIN).toBe(0.115);
    expect(WARM_LIGHT_AMBER_B).toBe(0.437);
    expect(WARM_LIGHT_AMBER_G_GAIN).toBe(0.092);
    expect(WARM_LIGHT_AMBER_G).toBe(0.759);
    expect(WARM_LIGHT_DISTANCE_GAIN).toBe(2.875);
    expect(WARM_LIGHT_DISTANCE_BASE).toBe(7.475);
    expect(WARM_LIGHT_Y).toBe(1.7825);
    expect(WARM_LIGHT_INTENSITY_MUL).toBe(1.7825);
    expect(POSSESSED_HEAD_EMISSIVE).toBe(0x30124a);
    expect(POSSESSED_HEAD_COLOR).toBe(0x8c469f);
    expect(POSSESSED_COLOR).toBe(0x68347b);
    expect(HOSTILE_COLOR).toBe(0x7b1e1e);
    expect(PLAYER_COLOR).toBe(0x55a4f4);
    expect(PLAYER_HEAD_COLOR).toBe(0x91d1ff);
    expect(PLAYER_HEAD_EMISSIVE).toBe(0x122537);
    expect(MARKER_PALETTE.possessed.emissive).toBe(0x30124a);
    expect(MARKER_PALETTE.player.badge).toBe(0x91d1ff);
    expect(MARKER_PALETTE.player.emissive).toBe(0x1e4a6e);
    expect(MARKER_PALETTE.player.ring).toBe(0x4392f4);
    expect(MARKER_RING_OPACITY).toBe(0.6877);
    expect(muteBadgeY).toBe(2.3);
    expect(possessedBadgeY).toBe(2.3);
  });
});

describe("worldView hostile head base Y", () => {
  test("hostile head base Y 1.3 × 1.15 → 1.495; hostile-body-base-y/player-head-base-y/player-body-base-y/marker-badge-tilt/marker-badge-radius/marker-icon-size/marker-icon-y/marker-ring-y/noise-ring-y/chevron-tilt/chevron-y/muzzle-forward/spot-target-y/y-offset/distance/intensity/decay/impact-spark-decay/muzzle-decay/spot-decay/fill-decay/warm-decay/visible-eps/amber-B-gain/amber-B/amber-G-gain/amber-G/gain/base/Y/intensity/color/spot-gain/extra/spot-Y/fill-Y/gain/base/flash/mesh/color/possessed/hostile/player/palette/badge-Y iguales", () => {
    expect(HOSTILE_HEAD_BASE_Y).toBe(1.495);
    expect(HOSTILE_HEAD_BASE_Y).toBeCloseTo(1.3 * 1.15, 10);
    expect(HOSTILE_MUTE_XZ_SCALE).toBe(1.2075);
    expect(HOSTILE_MUTE_XZ_SCALE).toBeCloseTo(1.05 * 1.15, 10);
    expect(HOSTILE_BODY_BASE_Y).toBe(0.644);
    expect(HOSTILE_BODY_BASE_Y).toBeCloseTo(0.56 * 1.15, 10);
    expect(PLAYER_HEAD_BASE_Y).toBe(1.518);
    expect(PLAYER_HEAD_BASE_Y).toBeCloseTo(1.32 * 1.15, 10);
    expect(PLAYER_BODY_BASE_Y).toBe(0.644);
    expect(PLAYER_BODY_BASE_Y).toBeCloseTo(0.56 * 1.15, 10);
    expect(MARKER_BADGE_TILT).toBe(2.99);
    expect(MARKER_BADGE_TILT).toBeCloseTo(2.6 * 1.15, 10);
    expect(MARKER_BADGE_RADIUS).toBe(0.184);
    expect(MARKER_BADGE_RADIUS).toBeCloseTo(0.16 * 1.15, 10);
    expect(MARKER_ICON_SIZE).toBe(0.207);
    expect(MARKER_ICON_SIZE).toBeCloseTo(0.18 * 1.15, 10);
    expect(MARKER_ICON_Y).toBe(0.023);
    expect(MARKER_ICON_Y).toBeCloseTo(0.02 * 1.15, 10);
    expect(MARKER_RING_Y).toBe(0.046);
    expect(MARKER_RING_Y).toBeCloseTo(0.04 * 1.15, 10);
    expect(NOISE_RING_Y).toBe(0.0575);
    expect(NOISE_RING_Y).toBeCloseTo(0.05 * 1.15, 10);
    expect(CHEVRON_TILT).toBe(-0.4025);
    expect(CHEVRON_Y).toBe(0.138);
    expect(MUZZLE_FORWARD).toBe(0.552);
    expect(FLASHLIGHT_SPOT_TARGET_Y).toBe(0.138);
    expect(TRACER_FLASH_Y_OFFSET).toBe(0.1725);
    expect(TRACER_FLASH_DISTANCE).toBe(3.68);
    expect(TRACER_FLASH_INTENSITY).toBe(2.76);
    expect(TRACER_FLASH_DECAY).toBe(1.74);
    expect(IMPACT_SPARK_LIGHT_DECAY).toBe(1.74);
    expect(MUZZLE_LIGHT_DECAY).toBe(1.74);
    expect(FLASHLIGHT_SPOT_DECAY).toBe(1.74);
    expect(FLASHLIGHT_FILL_DECAY).toBe(1.74);
    expect(WARM_LIGHT_DECAY).toBe(1.74);
    expect(WARM_LIGHT_VISIBLE_EPS).toBe(0.0174);
    expect(WARM_LIGHT_AMBER_B_GAIN).toBe(0.115);
    expect(WARM_LIGHT_AMBER_B).toBe(0.437);
    expect(WARM_LIGHT_AMBER_G_GAIN).toBe(0.092);
    expect(WARM_LIGHT_AMBER_G).toBe(0.759);
    expect(WARM_LIGHT_DISTANCE_GAIN).toBe(2.875);
    expect(WARM_LIGHT_DISTANCE_BASE).toBe(7.475);
    expect(WARM_LIGHT_Y).toBe(1.7825);
    expect(WARM_LIGHT_INTENSITY_MUL).toBe(1.7825);
    expect(FLASHLIGHT_SPOT_DISTANCE_GAIN).toBe(2.3);
    expect(FLASHLIGHT_SPOT_DISTANCE_EXTRA).toBe(1.84);
    expect(FLASHLIGHT_SPOT_Y).toBe(1.7825);
    expect(FLASHLIGHT_FILL_Y).toBe(1.5525);
    expect(FLASHLIGHT_FILL_DISTANCE_GAIN).toBe(4.025);
    expect(FLASHLIGHT_FILL_DISTANCE_BASE).toBe(8.05);
    expect(FLASHLIGHT_FILL_COLOR).toBe(0xcaefff);
    expect(MUZZLE_LIGHT_COLOR).toBe(0xffffb8);
    expect(IMPACT_SPARK_LIGHT_COLOR).toBe(0xffef93);
    expect(TRACER_FLASH_COLOR).toBe(0xffdd6e);
    const src = readFileSync(
      resolve(process.cwd(), "src/render/worldView.ts"),
      "utf8",
    );
    expect(src).toContain("export const HOSTILE_HEAD_BASE_Y = 1.495");
    expect(src).toContain("head.position.y = HOSTILE_HEAD_BASE_Y");
    expect(src).not.toMatch(/head\.position\.y = 1\.3/);
    expect(src).not.toMatch(/const HOSTILE_HEAD_BASE_Y = 1\.3/);
    expect(src).not.toMatch(/export const HOSTILE_HEAD_BASE_Y = 1\.3/);
    expect(src).toContain("export const HOSTILE_MUTE_XZ_SCALE = 1.2075");
    expect(src).toContain("mesh.scale.set(HOSTILE_MUTE_XZ_SCALE, 1, HOSTILE_MUTE_XZ_SCALE)");
    expect(src).not.toMatch(/mesh\.scale\.set\(1\.05, 1, 1\.05\)/);
    expect(src).not.toMatch(/const HOSTILE_MUTE_XZ_SCALE = 1\.05/);
    expect(src).not.toMatch(/export const HOSTILE_MUTE_XZ_SCALE = 1\.05/);
    expect(src).toContain("export const HOSTILE_BODY_BASE_Y = 0.644");
    expect(src).toContain("mesh.position.y = HOSTILE_BODY_BASE_Y");
    expect(src).toContain("body.position.y = HOSTILE_BODY_BASE_Y");
    expect(src).not.toMatch(/mesh\.position\.y = 0\.56/);
    expect(src).not.toMatch(/body\.position\.y = 0\.56/);
    expect(src).not.toMatch(/const HOSTILE_BODY_BASE_Y = 0\.56/);
    expect(src).not.toMatch(/export const HOSTILE_BODY_BASE_Y = 0\.56/);
    expect(src).toContain("export const PLAYER_HEAD_BASE_Y = 1.518");
    expect(src).toContain("playerHead.position.y = PLAYER_HEAD_BASE_Y");
    expect(src).not.toMatch(/playerHead\.position\.y = 1\.32/);
    expect(src).not.toMatch(/const PLAYER_HEAD_BASE_Y = 1\.32/);
    expect(src).not.toMatch(/export const PLAYER_HEAD_BASE_Y = 1\.32/);
    expect(src).toContain("export const PLAYER_BODY_BASE_Y = 0.644");
    expect(src).toContain("playerBody.position.y = PLAYER_BODY_BASE_Y");
    expect(src).not.toMatch(/playerBody\.position\.y = 0\.56/);
    expect(src).not.toMatch(/const PLAYER_BODY_BASE_Y = 0\.56/);
    expect(src).not.toMatch(/export const PLAYER_BODY_BASE_Y = 0\.56/);
    expect(src).toContain("export const MARKER_BADGE_TILT = 2.99");
    expect(src).toContain("disc.rotation.x = -Math.PI / MARKER_BADGE_TILT");
    expect(src).toContain("icon.rotation.x = -Math.PI / MARKER_BADGE_TILT");
    expect(src).not.toMatch(/disc\.rotation\.x = -Math\.PI \/ 2\.6/);
    expect(src).not.toMatch(/icon\.rotation\.x = -Math\.PI \/ 2\.6/);
    expect(src).not.toMatch(/const MARKER_BADGE_TILT = 2\.6/);
    expect(src).not.toMatch(/export const MARKER_BADGE_TILT = 2\.6/);
    expect(src).toContain("export const MARKER_BADGE_RADIUS = 0.184");
    expect(src).toContain("new THREE.CircleGeometry(MARKER_BADGE_RADIUS, 20)");
    expect(src).not.toMatch(/new THREE\.CircleGeometry\(0\.16, 20\)/);
    expect(src).not.toMatch(/const MARKER_BADGE_RADIUS = 0\.16/);
    expect(src).not.toMatch(/export const MARKER_BADGE_RADIUS = 0\.16/);
    expect(src).toContain("export const MARKER_ICON_SIZE = 0.207");
    expect(src).toContain("new THREE.PlaneGeometry(MARKER_ICON_SIZE, MARKER_ICON_SIZE)");
    expect(src).not.toMatch(/new THREE\.PlaneGeometry\(0\.18, 0\.18\)/);
    expect(src).not.toMatch(/const MARKER_ICON_SIZE = 0\.18/);
    expect(src).not.toMatch(/export const MARKER_ICON_SIZE = 0\.18/);
    expect(src).toContain("export const MARKER_ICON_Y = 0.023");
    expect(src).toContain("icon.position.y = MARKER_ICON_Y");
    expect(src).not.toMatch(/icon\.position\.y = 0\.02/);
    expect(src).not.toMatch(/const MARKER_ICON_Y = 0\.02;/);
    expect(src).not.toMatch(/export const MARKER_ICON_Y = 0\.02;/);
    expect(src).toContain("export const MARKER_RING_Y = 0.046");
    expect(src).toContain("ring.position.y = MARKER_RING_Y");
    expect(src).not.toMatch(/ring\.position\.y = 0\.04/);
    expect(src).not.toMatch(/const MARKER_RING_Y = 0\.04;/);
    expect(src).not.toMatch(/export const MARKER_RING_Y = 0\.04;/);
    expect(src).toContain("export const NOISE_RING_Y = 0.0575");
    expect(src).toContain("mesh.position.y = NOISE_RING_Y");
    expect(src).toContain("slot.mesh.position.set(state.x, NOISE_RING_Y, state.y)");
    expect(src).not.toMatch(/const NOISE_RING_Y = 0\.05;/);
    expect(src).not.toMatch(/export const NOISE_RING_Y = 0\.05;/);
    expect(src).toContain("export const CHEVRON_TILT = -0.4025");
    expect(src).toContain("chevronMesh.rotation.x = CHEVRON_TILT");
    expect(src).not.toMatch(/chevronMesh\.rotation\.x = -0\.35/);
    expect(src).not.toMatch(/const CHEVRON_TILT = -0\.35/);
    expect(src).not.toMatch(/export const CHEVRON_TILT = -0\.35/);
    expect(src).toContain("export const CHEVRON_Y = 0.138");
    expect(src).toContain("chevronMesh.position.set(x, CHEVRON_Y, z)");
    expect(src).not.toMatch(/const CHEVRON_Y = 0\.12/);
    expect(src).not.toMatch(/export const CHEVRON_Y = 0\.12/);
    expect(src).not.toMatch(/chevronMesh\.position\.set\(x, 0\.12, z\)/);
    expect(src).toContain("export const MUZZLE_FORWARD = 0.552");
    expect(src).toContain("Math.sin(playerGltfYaw) * MUZZLE_FORWARD");
    expect(src).toContain("Math.cos(playerGltfYaw) * MUZZLE_FORWARD");
    expect(src).toContain("muzzleMesh.position.set(ox, TRACER_HEIGHT, oz)");
    expect(src).toContain("muzzleLight.position.set(ox, TRACER_HEIGHT, oz)");
    expect(src).not.toMatch(/const MUZZLE_FORWARD = 0\.48/);
    expect(src).not.toMatch(/export const MUZZLE_FORWARD = 0\.48/);
    expect(src).toContain("export const FLASHLIGHT_SPOT_TARGET_Y = 0.138");
    expect(src).toContain("torchSpot.target.position.set(wx + tip.x, FLASHLIGHT_SPOT_TARGET_Y, wy + tip.z)");
    expect(src).not.toMatch(/torchSpot\.target\.position\.set\(wx \+ tip\.x, 0\.12, wy \+ tip\.z\)/);
    expect(src).not.toMatch(/export const FLASHLIGHT_SPOT_TARGET_Y = 0\.12/);
    expect(src).toContain("export const TRACER_FLASH_Y_OFFSET = 0.1725");
    expect(src).toContain("flash.position.set(from.x, TRACER_HEIGHT + TRACER_FLASH_Y_OFFSET, from.y)");
    expect(src).not.toMatch(/flash\.position\.set\(from\.x, TRACER_HEIGHT \+ 0\.15, from\.y\)/);
    expect(src).not.toMatch(/export const TRACER_FLASH_Y_OFFSET = 0\.15/);
    expect(src).toContain("export const TRACER_FLASH_DISTANCE = 3.68");
    expect(src).toContain("new THREE.PointLight(TRACER_FLASH_COLOR, TRACER_FLASH_INTENSITY, TRACER_FLASH_DISTANCE, TRACER_FLASH_DECAY)");
    expect(src).not.toMatch(/new THREE\.PointLight\(TRACER_FLASH_COLOR, TRACER_FLASH_INTENSITY, 3\.2, TRACER_FLASH_DECAY\)/);
    expect(src).not.toMatch(/new THREE\.PointLight\(TRACER_FLASH_COLOR, 2\.4, 3\.2, TRACER_FLASH_DECAY\)/);
    expect(src).not.toMatch(/new THREE\.PointLight\(TRACER_FLASH_COLOR, 2\.4, 3\.2, 2\)/);
    expect(src).not.toMatch(/export const TRACER_FLASH_DISTANCE = 3\.2/);
    expect(src).toContain("t.flash.intensity = TRACER_FLASH_INTENSITY * op");
    expect(src).not.toMatch(/t\.flash\.intensity = 2\.4 \* op/);
    expect(src).toContain("export const TRACER_FLASH_INTENSITY = 2.76");
    expect(src).toContain("export const TRACER_FLASH_DECAY = 1.74");
    expect(src).toContain("export const TRACER_FLASH_COLOR = 0xffdd6e");
    expect(src).toContain("export const IMPACT_SPARK_LIGHT_DECAY = 1.74");
    expect(src).toContain("IMPACT_SPARK_LIGHT_DISTANCE,\n    IMPACT_SPARK_LIGHT_DECAY,");
    expect(src).not.toMatch(/IMPACT_SPARK_LIGHT_DISTANCE,\s*\n\s*2,/);
    expect(src).not.toMatch(/export const IMPACT_SPARK_LIGHT_DECAY = 2/);
    expect(src).toContain("const IMPACT_SPARK_LIGHT_DISTANCE = 1.8");
    expect(src).toContain("export const IMPACT_SPARK_LIGHT_COLOR = 0xffef93");
    expect(src).toContain("new THREE.PointLight(\n    IMPACT_SPARK_LIGHT_COLOR,");
    expect(src).toContain("export const MUZZLE_LIGHT_DECAY = 1.74");
    expect(src).toContain("MUZZLE_LIGHT_DISTANCE,\n    MUZZLE_LIGHT_DECAY,");
    expect(src).not.toMatch(/MUZZLE_LIGHT_DISTANCE,\s*\n\s*2,/);
    expect(src).not.toMatch(/export const MUZZLE_LIGHT_DECAY = 2/);
    expect(src).toContain("const MUZZLE_LIGHT_DISTANCE = 2.6");
    expect(src).toContain("export const MUZZLE_LIGHT_COLOR = 0xffffb8");
    expect(src).toContain("new THREE.PointLight(\n    MUZZLE_LIGHT_COLOR,");
    expect(src).toContain("export const FLASHLIGHT_SPOT_DECAY = 1.74");
    expect(src).toContain("FLASHLIGHT_SPOT_PENUMBRA,\n    FLASHLIGHT_SPOT_DECAY,");
    expect(src).not.toMatch(/FLASHLIGHT_SPOT_PENUMBRA,\s*\n\s*2,/);
    expect(src).not.toMatch(/export const FLASHLIGHT_SPOT_DECAY = 2/);
    expect(src).toContain("FLASHLIGHT_CONE_LENGTH + 2.4");
    expect(src).toContain("export const FLASHLIGHT_FILL_DECAY = 1.74");
    expect(src).toContain("new THREE.PointLight(FLASHLIGHT_FILL_COLOR, 0, 10, FLASHLIGHT_FILL_DECAY)");
    expect(src).not.toMatch(/new THREE\.PointLight\(FLASHLIGHT_FILL_COLOR, 0, 10, 2\)/);
    expect(src).not.toMatch(/export const FLASHLIGHT_FILL_DECAY = 2/);
    expect(src).toContain("export const WARM_LIGHT_DECAY = 1.74");
    expect(src).toContain("new THREE.PointLight(WARM_LIGHT_COLOR, 0, 7.5, WARM_LIGHT_DECAY)");
    expect(src).not.toMatch(/new THREE\.PointLight\(WARM_LIGHT_COLOR, 0, 7\.5, 2\)/);
    expect(src).toContain("export const WARM_LIGHT_VISIBLE_EPS = 0.0174");
    expect(src).toContain("warmLight.visible = i > WARM_LIGHT_VISIBLE_EPS");
    expect(src).not.toMatch(/warmLight\.visible = i > 0\.02/);
    expect(src).not.toMatch(/export const WARM_LIGHT_VISIBLE_EPS = 0\.02/);
    expect(src).toContain("export const WARM_LIGHT_AMBER_B_GAIN = 0.115");
    expect(src).toContain("export const WARM_LIGHT_AMBER_B = 0.437");
    expect(src).toContain("export const WARM_LIGHT_AMBER_G_GAIN = 0.092");
    expect(src).toContain("export const WARM_LIGHT_AMBER_G = 0.759");
    expect(src).toContain(
      "warmLight.color.setRGB(1, WARM_LIGHT_AMBER_G + i * WARM_LIGHT_AMBER_G_GAIN, WARM_LIGHT_AMBER_B + i * WARM_LIGHT_AMBER_B_GAIN)",
    );
    expect(src).not.toMatch(/WARM_LIGHT_AMBER_B \+ i \* 0\.1/);
    expect(src).toContain("export const WARM_LIGHT_DISTANCE_GAIN = 2.875");
    expect(src).toContain("export const WARM_LIGHT_DISTANCE_BASE = 7.475");
    expect(src).toContain(
      "warmLight.distance = WARM_LIGHT_DISTANCE_BASE + i * WARM_LIGHT_DISTANCE_GAIN",
    );
    expect(src).not.toMatch(/export const WARM_LIGHT_DISTANCE_GAIN = 2\.5/);
    expect(src).not.toMatch(
      /warmLight\.distance = WARM_LIGHT_DISTANCE_BASE \+ i \* 2\.5/,
    );
    expect(src).not.toMatch(/warmLight\.distance = 6\.5 \+ i \* 2\.5/);
    expect(src).toContain("export const WARM_LIGHT_Y = 1.7825");
    expect(src).toContain("warmLight.position.set(wx, WARM_LIGHT_Y, wy)");
    expect(src).not.toMatch(/export const WARM_LIGHT_Y = 1\.55/);
    expect(src).not.toMatch(/warmLight\.position\.set\(wx, 1\.55, wy\)/);
    expect(src).toContain("warmLight.position.set(0, 1.6, 0)");
    expect(src).toContain("export const WARM_LIGHT_INTENSITY_MUL = 1.7825");
    expect(src).toContain("warmLight.intensity = i * WARM_LIGHT_INTENSITY_MUL");
    expect(src).not.toMatch(/export const WARM_LIGHT_INTENSITY_MUL = 1\.55/);
    expect(src).not.toMatch(/warmLight\.intensity = i \* 1\.55/);
    expect(src).toContain("export const FLASHLIGHT_SPOT_DISTANCE_GAIN = 2.3");
    expect(src).toContain("export const FLASHLIGHT_SPOT_DISTANCE_EXTRA = 1.84");
    expect(src).toContain(
      "torchSpot.distance = FLASHLIGHT_CONE_LENGTH + FLASHLIGHT_SPOT_DISTANCE_EXTRA + i * FLASHLIGHT_SPOT_DISTANCE_GAIN",
    );
    expect(src).toContain("export const FLASHLIGHT_SPOT_Y = 1.7825");
    expect(src).toContain("torchSpot.position.set(0, FLASHLIGHT_SPOT_Y, 0)");
    expect(src).toContain("torchSpot.position.set(wx, FLASHLIGHT_SPOT_Y, wy)");
    expect(src).toContain("export const FLASHLIGHT_FILL_Y = 1.5525");
    expect(src).toContain("torchLight.position.set(0, FLASHLIGHT_FILL_Y, 0)");
    expect(src).toContain("torchLight.position.set(wx, FLASHLIGHT_FILL_Y, wy)");
    expect(src).toContain("export const FLASHLIGHT_FILL_DISTANCE_GAIN = 4.025");
    expect(src).toContain("export const FLASHLIGHT_FILL_DISTANCE_BASE = 8.05");
    expect(src).toContain(
      "torchLight.distance = FLASHLIGHT_FILL_DISTANCE_BASE + i * FLASHLIGHT_FILL_DISTANCE_GAIN",
    );
    expect(src).toContain("export const FLASHLIGHT_FILL_COLOR = 0xcaefff");
    expect(src).toContain("torchLight.color.setHex(FLASHLIGHT_FILL_COLOR)");
    expect(src).toContain("export const TRACER_COLOR = 0xffffb8");
    expect(src).toContain("color: TRACER_COLOR");
    expect(src).toContain("export const IMPACT_SPARK_COLOR = 0xffef93");
    expect(src).toContain("color: IMPACT_SPARK_COLOR");
    expect(src).toContain("export const MUZZLE_FLASH_COLOR = 0xffffdd");
    expect(src).toContain("color: MUZZLE_FLASH_COLOR");
    expect(src).toContain("export const WARM_LIGHT_COLOR = 0xffca81");
    expect(src).toContain("export const POSSESSED_HEAD_EMISSIVE = 0x30124a");
    expect(src).toContain("emissive: POSSESSED_HEAD_EMISSIVE");
    expect(src).toContain("color: POSSESSED_HEAD_COLOR");
    expect(src).toContain("export const POSSESSED_HEAD_COLOR = 0x8c469f");
    expect(src).toContain("emissive: POSSESSED_EMISSIVE");
    expect(src).toContain("color: POSSESSED_COLOR");
    expect(src).toContain("const POSSESSED_EMISSIVE = 0x1e0925");
    expect(src).toContain("emissiveIntensity: 0.22");
    expect(src).toContain("color: HOSTILE_COLOR");
    expect(src).toContain("color: PLAYER_COLOR");
    expect(src).toContain("color: PLAYER_HEAD_COLOR");
    expect(src).toContain("emissive: PLAYER_HEAD_EMISSIVE");
    expect(TRACER_FLASH_COLOR).toBe(0xffdd6e);
    expect(TRACER_FLASH_INTENSITY).toBe(2.76);
    expect(TRACER_FLASH_DISTANCE).toBe(3.68);
    expect(TRACER_FLASH_DECAY).toBe(1.74);
    expect(TRACER_FLASH_Y_OFFSET).toBe(0.1725);
    expect(TRACER_COLOR).toBe(0xffffb8);
    expect(IMPACT_SPARK_LIGHT_COLOR).toBe(0xffef93);
    expect(IMPACT_SPARK_COLOR).toBe(0xffef93);
    expect(IMPACT_SPARK_LIGHT_DECAY).toBe(1.74);
    expect(MUZZLE_LIGHT_COLOR).toBe(0xffffb8);
    expect(MUZZLE_LIGHT_DECAY).toBe(1.74);
    expect(MUZZLE_FLASH_COLOR).toBe(0xffffdd);
    expect(WARM_LIGHT_COLOR).toBe(0xffca81);
    expect(WARM_LIGHT_DECAY).toBe(1.74);
    expect(WARM_LIGHT_VISIBLE_EPS).toBe(0.0174);
    expect(WARM_LIGHT_AMBER_B_GAIN).toBe(0.115);
    expect(WARM_LIGHT_AMBER_B).toBe(0.437);
    expect(WARM_LIGHT_AMBER_G_GAIN).toBe(0.092);
    expect(WARM_LIGHT_AMBER_G).toBe(0.759);
    expect(WARM_LIGHT_DISTANCE_GAIN).toBe(2.875);
    expect(WARM_LIGHT_DISTANCE_BASE).toBe(7.475);
    expect(WARM_LIGHT_Y).toBe(1.7825);
    expect(WARM_LIGHT_INTENSITY_MUL).toBe(1.7825);
    expect(POSSESSED_HEAD_EMISSIVE).toBe(0x30124a);
    expect(POSSESSED_HEAD_COLOR).toBe(0x8c469f);
    expect(POSSESSED_COLOR).toBe(0x68347b);
    expect(HOSTILE_COLOR).toBe(0x7b1e1e);
    expect(PLAYER_COLOR).toBe(0x55a4f4);
    expect(PLAYER_HEAD_COLOR).toBe(0x91d1ff);
    expect(PLAYER_HEAD_EMISSIVE).toBe(0x122537);
    expect(MARKER_PALETTE.possessed.emissive).toBe(0x30124a);
    expect(MARKER_PALETTE.player.badge).toBe(0x91d1ff);
    expect(MARKER_PALETTE.player.emissive).toBe(0x1e4a6e);
    expect(MARKER_PALETTE.player.ring).toBe(0x4392f4);
    expect(MARKER_RING_OPACITY).toBe(0.6877);
    expect(muteBadgeY).toBe(2.3);
    expect(possessedBadgeY).toBe(2.3);
  });
});

describe("worldView hostile mute XZ scale", () => {
  test("hostile mute XZ scale 1.05 × 1.15 → 1.2075; hostile-head-base-y/hostile-body-base-y/player-head-base-y/player-body-base-y/marker-badge-tilt/marker-badge-radius/marker-icon-size/marker-icon-y/marker-ring-y/noise-ring-y/chevron-tilt/chevron-y/muzzle-forward/spot-target-y/y-offset/distance/intensity/decay/impact-spark-decay/muzzle-decay/spot-decay/fill-decay/warm-decay/visible-eps/amber-B-gain/amber-B/amber-G-gain/amber-G/gain/base/Y/intensity/color/spot-gain/extra/spot-Y/fill-Y/gain/base/flash/mesh/color/possessed/hostile/player/palette/badge-Y iguales", () => {
    expect(HOSTILE_MUTE_XZ_SCALE).toBe(1.2075);
    expect(HOSTILE_MUTE_XZ_SCALE).toBeCloseTo(1.05 * 1.15, 10);
    expect(HOSTILE_HEAD_BASE_Y).toBe(1.495);
    expect(HOSTILE_HEAD_BASE_Y).toBeCloseTo(1.3 * 1.15, 10);
    expect(HOSTILE_BODY_BASE_Y).toBe(0.644);
    expect(HOSTILE_BODY_BASE_Y).toBeCloseTo(0.56 * 1.15, 10);
    expect(PLAYER_HEAD_BASE_Y).toBe(1.518);
    expect(PLAYER_HEAD_BASE_Y).toBeCloseTo(1.32 * 1.15, 10);
    expect(PLAYER_BODY_BASE_Y).toBe(0.644);
    expect(PLAYER_BODY_BASE_Y).toBeCloseTo(0.56 * 1.15, 10);
    expect(MARKER_BADGE_TILT).toBe(2.99);
    expect(MARKER_BADGE_TILT).toBeCloseTo(2.6 * 1.15, 10);
    expect(MARKER_BADGE_RADIUS).toBe(0.184);
    expect(MARKER_BADGE_RADIUS).toBeCloseTo(0.16 * 1.15, 10);
    expect(MARKER_ICON_SIZE).toBe(0.207);
    expect(MARKER_ICON_SIZE).toBeCloseTo(0.18 * 1.15, 10);
    expect(MARKER_ICON_Y).toBe(0.023);
    expect(MARKER_ICON_Y).toBeCloseTo(0.02 * 1.15, 10);
    expect(MARKER_RING_Y).toBe(0.046);
    expect(MARKER_RING_Y).toBeCloseTo(0.04 * 1.15, 10);
    expect(NOISE_RING_Y).toBe(0.0575);
    expect(NOISE_RING_Y).toBeCloseTo(0.05 * 1.15, 10);
    expect(CHEVRON_TILT).toBe(-0.4025);
    expect(CHEVRON_Y).toBe(0.138);
    expect(MUZZLE_FORWARD).toBe(0.552);
    expect(FLASHLIGHT_SPOT_TARGET_Y).toBe(0.138);
    expect(TRACER_FLASH_Y_OFFSET).toBe(0.1725);
    expect(TRACER_FLASH_DISTANCE).toBe(3.68);
    expect(TRACER_FLASH_INTENSITY).toBe(2.76);
    expect(TRACER_FLASH_DECAY).toBe(1.74);
    expect(IMPACT_SPARK_LIGHT_DECAY).toBe(1.74);
    expect(MUZZLE_LIGHT_DECAY).toBe(1.74);
    expect(FLASHLIGHT_SPOT_DECAY).toBe(1.74);
    expect(FLASHLIGHT_FILL_DECAY).toBe(1.74);
    expect(WARM_LIGHT_DECAY).toBe(1.74);
    expect(WARM_LIGHT_VISIBLE_EPS).toBe(0.0174);
    expect(WARM_LIGHT_AMBER_B_GAIN).toBe(0.115);
    expect(WARM_LIGHT_AMBER_B).toBe(0.437);
    expect(WARM_LIGHT_AMBER_G_GAIN).toBe(0.092);
    expect(WARM_LIGHT_AMBER_G).toBe(0.759);
    expect(WARM_LIGHT_DISTANCE_GAIN).toBe(2.875);
    expect(WARM_LIGHT_DISTANCE_BASE).toBe(7.475);
    expect(WARM_LIGHT_Y).toBe(1.7825);
    expect(WARM_LIGHT_INTENSITY_MUL).toBe(1.7825);
    expect(FLASHLIGHT_SPOT_DISTANCE_GAIN).toBe(2.3);
    expect(FLASHLIGHT_SPOT_DISTANCE_EXTRA).toBe(1.84);
    expect(FLASHLIGHT_SPOT_Y).toBe(1.7825);
    expect(FLASHLIGHT_FILL_Y).toBe(1.5525);
    expect(FLASHLIGHT_FILL_DISTANCE_GAIN).toBe(4.025);
    expect(FLASHLIGHT_FILL_DISTANCE_BASE).toBe(8.05);
    expect(FLASHLIGHT_FILL_COLOR).toBe(0xcaefff);
    expect(MUZZLE_LIGHT_COLOR).toBe(0xffffb8);
    expect(IMPACT_SPARK_LIGHT_COLOR).toBe(0xffef93);
    expect(TRACER_FLASH_COLOR).toBe(0xffdd6e);
    const src = readFileSync(
      resolve(process.cwd(), "src/render/worldView.ts"),
      "utf8",
    );
    expect(src).toContain("export const HOSTILE_MUTE_XZ_SCALE = 1.2075");
    expect(src).toContain("mesh.scale.set(HOSTILE_MUTE_XZ_SCALE, 1, HOSTILE_MUTE_XZ_SCALE)");
    expect(src).not.toMatch(/mesh\.scale\.set\(1\.05, 1, 1\.05\)/);
    expect(src).not.toMatch(/const HOSTILE_MUTE_XZ_SCALE = 1\.05/);
    expect(src).not.toMatch(/export const HOSTILE_MUTE_XZ_SCALE = 1\.05/);
    expect(src).toContain("export const HOSTILE_HEAD_BASE_Y = 1.495");
    expect(src).toContain("head.position.y = HOSTILE_HEAD_BASE_Y");
    expect(src).not.toMatch(/head\.position\.y = 1\.3/);
    expect(src).not.toMatch(/const HOSTILE_HEAD_BASE_Y = 1\.3/);
    expect(src).not.toMatch(/export const HOSTILE_HEAD_BASE_Y = 1\.3/);
    expect(src).toContain("export const HOSTILE_BODY_BASE_Y = 0.644");
    expect(src).toContain("mesh.position.y = HOSTILE_BODY_BASE_Y");
    expect(src).toContain("body.position.y = HOSTILE_BODY_BASE_Y");
    expect(src).not.toMatch(/mesh\.position\.y = 0\.56/);
    expect(src).not.toMatch(/body\.position\.y = 0\.56/);
    expect(src).not.toMatch(/const HOSTILE_BODY_BASE_Y = 0\.56/);
    expect(src).not.toMatch(/export const HOSTILE_BODY_BASE_Y = 0\.56/);
    expect(src).toContain("export const PLAYER_HEAD_BASE_Y = 1.518");
    expect(src).toContain("playerHead.position.y = PLAYER_HEAD_BASE_Y");
    expect(src).not.toMatch(/playerHead\.position\.y = 1\.32/);
    expect(src).not.toMatch(/const PLAYER_HEAD_BASE_Y = 1\.32/);
    expect(src).not.toMatch(/export const PLAYER_HEAD_BASE_Y = 1\.32/);
    expect(src).toContain("export const PLAYER_BODY_BASE_Y = 0.644");
    expect(src).toContain("playerBody.position.y = PLAYER_BODY_BASE_Y");
    expect(src).not.toMatch(/playerBody\.position\.y = 0\.56/);
    expect(src).not.toMatch(/const PLAYER_BODY_BASE_Y = 0\.56/);
    expect(src).not.toMatch(/export const PLAYER_BODY_BASE_Y = 0\.56/);
    expect(src).toContain("export const MARKER_BADGE_TILT = 2.99");
    expect(src).toContain("disc.rotation.x = -Math.PI / MARKER_BADGE_TILT");
    expect(src).toContain("icon.rotation.x = -Math.PI / MARKER_BADGE_TILT");
    expect(src).not.toMatch(/disc\.rotation\.x = -Math\.PI \/ 2\.6/);
    expect(src).not.toMatch(/icon\.rotation\.x = -Math\.PI \/ 2\.6/);
    expect(src).not.toMatch(/const MARKER_BADGE_TILT = 2\.6/);
    expect(src).not.toMatch(/export const MARKER_BADGE_TILT = 2\.6/);
    expect(src).toContain("export const MARKER_BADGE_RADIUS = 0.184");
    expect(src).toContain("new THREE.CircleGeometry(MARKER_BADGE_RADIUS, 20)");
    expect(src).not.toMatch(/new THREE\.CircleGeometry\(0\.16, 20\)/);
    expect(src).not.toMatch(/const MARKER_BADGE_RADIUS = 0\.16/);
    expect(src).not.toMatch(/export const MARKER_BADGE_RADIUS = 0\.16/);
    expect(src).toContain("export const MARKER_ICON_SIZE = 0.207");
    expect(src).toContain("new THREE.PlaneGeometry(MARKER_ICON_SIZE, MARKER_ICON_SIZE)");
    expect(src).not.toMatch(/new THREE\.PlaneGeometry\(0\.18, 0\.18\)/);
    expect(src).not.toMatch(/const MARKER_ICON_SIZE = 0\.18/);
    expect(src).not.toMatch(/export const MARKER_ICON_SIZE = 0\.18/);
    expect(src).toContain("export const MARKER_ICON_Y = 0.023");
    expect(src).toContain("icon.position.y = MARKER_ICON_Y");
    expect(src).not.toMatch(/icon\.position\.y = 0\.02/);
    expect(src).not.toMatch(/const MARKER_ICON_Y = 0\.02;/);
    expect(src).not.toMatch(/export const MARKER_ICON_Y = 0\.02;/);
    expect(src).toContain("export const MARKER_RING_Y = 0.046");
    expect(src).toContain("ring.position.y = MARKER_RING_Y");
    expect(src).not.toMatch(/ring\.position\.y = 0\.04/);
    expect(src).not.toMatch(/const MARKER_RING_Y = 0\.04;/);
    expect(src).not.toMatch(/export const MARKER_RING_Y = 0\.04;/);
    expect(src).toContain("export const NOISE_RING_Y = 0.0575");
    expect(src).toContain("mesh.position.y = NOISE_RING_Y");
    expect(src).toContain("slot.mesh.position.set(state.x, NOISE_RING_Y, state.y)");
    expect(src).not.toMatch(/const NOISE_RING_Y = 0\.05;/);
    expect(src).not.toMatch(/export const NOISE_RING_Y = 0\.05;/);
    expect(src).toContain("export const CHEVRON_TILT = -0.4025");
    expect(src).toContain("chevronMesh.rotation.x = CHEVRON_TILT");
    expect(src).not.toMatch(/chevronMesh\.rotation\.x = -0\.35/);
    expect(src).not.toMatch(/const CHEVRON_TILT = -0\.35/);
    expect(src).not.toMatch(/export const CHEVRON_TILT = -0\.35/);
    expect(src).toContain("export const CHEVRON_Y = 0.138");
    expect(src).toContain("chevronMesh.position.set(x, CHEVRON_Y, z)");
    expect(src).not.toMatch(/const CHEVRON_Y = 0\.12/);
    expect(src).not.toMatch(/export const CHEVRON_Y = 0\.12/);
    expect(src).not.toMatch(/chevronMesh\.position\.set\(x, 0\.12, z\)/);
    expect(src).toContain("export const MUZZLE_FORWARD = 0.552");
    expect(src).toContain("Math.sin(playerGltfYaw) * MUZZLE_FORWARD");
    expect(src).toContain("Math.cos(playerGltfYaw) * MUZZLE_FORWARD");
    expect(src).toContain("muzzleMesh.position.set(ox, TRACER_HEIGHT, oz)");
    expect(src).toContain("muzzleLight.position.set(ox, TRACER_HEIGHT, oz)");
    expect(src).not.toMatch(/const MUZZLE_FORWARD = 0\.48/);
    expect(src).not.toMatch(/export const MUZZLE_FORWARD = 0\.48/);
    expect(src).toContain("export const FLASHLIGHT_SPOT_TARGET_Y = 0.138");
    expect(src).toContain("torchSpot.target.position.set(wx + tip.x, FLASHLIGHT_SPOT_TARGET_Y, wy + tip.z)");
    expect(src).not.toMatch(/torchSpot\.target\.position\.set\(wx \+ tip\.x, 0\.12, wy \+ tip\.z\)/);
    expect(src).not.toMatch(/export const FLASHLIGHT_SPOT_TARGET_Y = 0\.12/);
    expect(src).toContain("export const TRACER_FLASH_Y_OFFSET = 0.1725");
    expect(src).toContain("flash.position.set(from.x, TRACER_HEIGHT + TRACER_FLASH_Y_OFFSET, from.y)");
    expect(src).not.toMatch(/flash\.position\.set\(from\.x, TRACER_HEIGHT \+ 0\.15, from\.y\)/);
    expect(src).not.toMatch(/export const TRACER_FLASH_Y_OFFSET = 0\.15/);
    expect(src).toContain("export const TRACER_FLASH_DISTANCE = 3.68");
    expect(src).toContain("new THREE.PointLight(TRACER_FLASH_COLOR, TRACER_FLASH_INTENSITY, TRACER_FLASH_DISTANCE, TRACER_FLASH_DECAY)");
    expect(src).not.toMatch(/new THREE\.PointLight\(TRACER_FLASH_COLOR, TRACER_FLASH_INTENSITY, 3\.2, TRACER_FLASH_DECAY\)/);
    expect(src).not.toMatch(/new THREE\.PointLight\(TRACER_FLASH_COLOR, 2\.4, 3\.2, TRACER_FLASH_DECAY\)/);
    expect(src).not.toMatch(/new THREE\.PointLight\(TRACER_FLASH_COLOR, 2\.4, 3\.2, 2\)/);
    expect(src).not.toMatch(/export const TRACER_FLASH_DISTANCE = 3\.2/);
    expect(src).toContain("t.flash.intensity = TRACER_FLASH_INTENSITY * op");
    expect(src).not.toMatch(/t\.flash\.intensity = 2\.4 \* op/);
    expect(src).toContain("export const TRACER_FLASH_INTENSITY = 2.76");
    expect(src).toContain("export const TRACER_FLASH_DECAY = 1.74");
    expect(src).toContain("export const TRACER_FLASH_COLOR = 0xffdd6e");
    expect(src).toContain("export const IMPACT_SPARK_LIGHT_DECAY = 1.74");
    expect(src).toContain("IMPACT_SPARK_LIGHT_DISTANCE,\n    IMPACT_SPARK_LIGHT_DECAY,");
    expect(src).not.toMatch(/IMPACT_SPARK_LIGHT_DISTANCE,\s*\n\s*2,/);
    expect(src).not.toMatch(/export const IMPACT_SPARK_LIGHT_DECAY = 2/);
    expect(src).toContain("const IMPACT_SPARK_LIGHT_DISTANCE = 1.8");
    expect(src).toContain("export const IMPACT_SPARK_LIGHT_COLOR = 0xffef93");
    expect(src).toContain("new THREE.PointLight(\n    IMPACT_SPARK_LIGHT_COLOR,");
    expect(src).toContain("export const MUZZLE_LIGHT_DECAY = 1.74");
    expect(src).toContain("MUZZLE_LIGHT_DISTANCE,\n    MUZZLE_LIGHT_DECAY,");
    expect(src).not.toMatch(/MUZZLE_LIGHT_DISTANCE,\s*\n\s*2,/);
    expect(src).not.toMatch(/export const MUZZLE_LIGHT_DECAY = 2/);
    expect(src).toContain("const MUZZLE_LIGHT_DISTANCE = 2.6");
    expect(src).toContain("export const MUZZLE_LIGHT_COLOR = 0xffffb8");
    expect(src).toContain("new THREE.PointLight(\n    MUZZLE_LIGHT_COLOR,");
    expect(src).toContain("export const FLASHLIGHT_SPOT_DECAY = 1.74");
    expect(src).toContain("FLASHLIGHT_SPOT_PENUMBRA,\n    FLASHLIGHT_SPOT_DECAY,");
    expect(src).not.toMatch(/FLASHLIGHT_SPOT_PENUMBRA,\s*\n\s*2,/);
    expect(src).not.toMatch(/export const FLASHLIGHT_SPOT_DECAY = 2/);
    expect(src).toContain("FLASHLIGHT_CONE_LENGTH + 2.4");
    expect(src).toContain("export const FLASHLIGHT_FILL_DECAY = 1.74");
    expect(src).toContain("new THREE.PointLight(FLASHLIGHT_FILL_COLOR, 0, 10, FLASHLIGHT_FILL_DECAY)");
    expect(src).not.toMatch(/new THREE\.PointLight\(FLASHLIGHT_FILL_COLOR, 0, 10, 2\)/);
    expect(src).not.toMatch(/export const FLASHLIGHT_FILL_DECAY = 2/);
    expect(src).toContain("export const WARM_LIGHT_DECAY = 1.74");
    expect(src).toContain("new THREE.PointLight(WARM_LIGHT_COLOR, 0, 7.5, WARM_LIGHT_DECAY)");
    expect(src).not.toMatch(/new THREE\.PointLight\(WARM_LIGHT_COLOR, 0, 7\.5, 2\)/);
    expect(src).toContain("export const WARM_LIGHT_VISIBLE_EPS = 0.0174");
    expect(src).toContain("warmLight.visible = i > WARM_LIGHT_VISIBLE_EPS");
    expect(src).not.toMatch(/warmLight\.visible = i > 0\.02/);
    expect(src).not.toMatch(/export const WARM_LIGHT_VISIBLE_EPS = 0\.02/);
    expect(src).toContain("export const WARM_LIGHT_AMBER_B_GAIN = 0.115");
    expect(src).toContain("export const WARM_LIGHT_AMBER_B = 0.437");
    expect(src).toContain("export const WARM_LIGHT_AMBER_G_GAIN = 0.092");
    expect(src).toContain("export const WARM_LIGHT_AMBER_G = 0.759");
    expect(src).toContain(
      "warmLight.color.setRGB(1, WARM_LIGHT_AMBER_G + i * WARM_LIGHT_AMBER_G_GAIN, WARM_LIGHT_AMBER_B + i * WARM_LIGHT_AMBER_B_GAIN)",
    );
    expect(src).not.toMatch(/WARM_LIGHT_AMBER_B \+ i \* 0\.1/);
    expect(src).toContain("export const WARM_LIGHT_DISTANCE_GAIN = 2.875");
    expect(src).toContain("export const WARM_LIGHT_DISTANCE_BASE = 7.475");
    expect(src).toContain(
      "warmLight.distance = WARM_LIGHT_DISTANCE_BASE + i * WARM_LIGHT_DISTANCE_GAIN",
    );
    expect(src).not.toMatch(/export const WARM_LIGHT_DISTANCE_GAIN = 2\.5/);
    expect(src).not.toMatch(
      /warmLight\.distance = WARM_LIGHT_DISTANCE_BASE \+ i \* 2\.5/,
    );
    expect(src).not.toMatch(/warmLight\.distance = 6\.5 \+ i \* 2\.5/);
    expect(src).toContain("export const WARM_LIGHT_Y = 1.7825");
    expect(src).toContain("warmLight.position.set(wx, WARM_LIGHT_Y, wy)");
    expect(src).not.toMatch(/export const WARM_LIGHT_Y = 1\.55/);
    expect(src).not.toMatch(/warmLight\.position\.set\(wx, 1\.55, wy\)/);
    expect(src).toContain("warmLight.position.set(0, 1.6, 0)");
    expect(src).toContain("export const WARM_LIGHT_INTENSITY_MUL = 1.7825");
    expect(src).toContain("warmLight.intensity = i * WARM_LIGHT_INTENSITY_MUL");
    expect(src).not.toMatch(/export const WARM_LIGHT_INTENSITY_MUL = 1\.55/);
    expect(src).not.toMatch(/warmLight\.intensity = i \* 1\.55/);
    expect(src).toContain("export const FLASHLIGHT_SPOT_DISTANCE_GAIN = 2.3");
    expect(src).toContain("export const FLASHLIGHT_SPOT_DISTANCE_EXTRA = 1.84");
    expect(src).toContain(
      "torchSpot.distance = FLASHLIGHT_CONE_LENGTH + FLASHLIGHT_SPOT_DISTANCE_EXTRA + i * FLASHLIGHT_SPOT_DISTANCE_GAIN",
    );
    expect(src).toContain("export const FLASHLIGHT_SPOT_Y = 1.7825");
    expect(src).toContain("torchSpot.position.set(0, FLASHLIGHT_SPOT_Y, 0)");
    expect(src).toContain("torchSpot.position.set(wx, FLASHLIGHT_SPOT_Y, wy)");
    expect(src).toContain("export const FLASHLIGHT_FILL_Y = 1.5525");
    expect(src).toContain("torchLight.position.set(0, FLASHLIGHT_FILL_Y, 0)");
    expect(src).toContain("torchLight.position.set(wx, FLASHLIGHT_FILL_Y, wy)");
    expect(src).toContain("export const FLASHLIGHT_FILL_DISTANCE_GAIN = 4.025");
    expect(src).toContain("export const FLASHLIGHT_FILL_DISTANCE_BASE = 8.05");
    expect(src).toContain(
      "torchLight.distance = FLASHLIGHT_FILL_DISTANCE_BASE + i * FLASHLIGHT_FILL_DISTANCE_GAIN",
    );
    expect(src).toContain("export const FLASHLIGHT_FILL_COLOR = 0xcaefff");
    expect(src).toContain("torchLight.color.setHex(FLASHLIGHT_FILL_COLOR)");
    expect(src).toContain("export const TRACER_COLOR = 0xffffb8");
    expect(src).toContain("color: TRACER_COLOR");
    expect(src).toContain("export const IMPACT_SPARK_COLOR = 0xffef93");
    expect(src).toContain("color: IMPACT_SPARK_COLOR");
    expect(src).toContain("export const MUZZLE_FLASH_COLOR = 0xffffdd");
    expect(src).toContain("color: MUZZLE_FLASH_COLOR");
    expect(src).toContain("export const WARM_LIGHT_COLOR = 0xffca81");
    expect(src).toContain("export const POSSESSED_HEAD_EMISSIVE = 0x30124a");
    expect(src).toContain("emissive: POSSESSED_HEAD_EMISSIVE");
    expect(src).toContain("color: POSSESSED_HEAD_COLOR");
    expect(src).toContain("export const POSSESSED_HEAD_COLOR = 0x8c469f");
    expect(src).toContain("emissive: POSSESSED_EMISSIVE");
    expect(src).toContain("color: POSSESSED_COLOR");
    expect(src).toContain("const POSSESSED_EMISSIVE = 0x1e0925");
    expect(src).toContain("emissiveIntensity: 0.22");
    expect(src).toContain("color: HOSTILE_COLOR");
    expect(src).toContain("color: PLAYER_COLOR");
    expect(src).toContain("color: PLAYER_HEAD_COLOR");
    expect(src).toContain("emissive: PLAYER_HEAD_EMISSIVE");
    expect(TRACER_FLASH_COLOR).toBe(0xffdd6e);
    expect(TRACER_FLASH_INTENSITY).toBe(2.76);
    expect(TRACER_FLASH_DISTANCE).toBe(3.68);
    expect(TRACER_FLASH_DECAY).toBe(1.74);
    expect(TRACER_FLASH_Y_OFFSET).toBe(0.1725);
    expect(TRACER_COLOR).toBe(0xffffb8);
    expect(IMPACT_SPARK_LIGHT_COLOR).toBe(0xffef93);
    expect(IMPACT_SPARK_COLOR).toBe(0xffef93);
    expect(IMPACT_SPARK_LIGHT_DECAY).toBe(1.74);
    expect(MUZZLE_LIGHT_COLOR).toBe(0xffffb8);
    expect(MUZZLE_LIGHT_DECAY).toBe(1.74);
    expect(MUZZLE_FLASH_COLOR).toBe(0xffffdd);
    expect(WARM_LIGHT_COLOR).toBe(0xffca81);
    expect(WARM_LIGHT_DECAY).toBe(1.74);
    expect(WARM_LIGHT_VISIBLE_EPS).toBe(0.0174);
    expect(WARM_LIGHT_AMBER_B_GAIN).toBe(0.115);
    expect(WARM_LIGHT_AMBER_B).toBe(0.437);
    expect(WARM_LIGHT_AMBER_G_GAIN).toBe(0.092);
    expect(WARM_LIGHT_AMBER_G).toBe(0.759);
    expect(WARM_LIGHT_DISTANCE_GAIN).toBe(2.875);
    expect(WARM_LIGHT_DISTANCE_BASE).toBe(7.475);
    expect(WARM_LIGHT_Y).toBe(1.7825);
    expect(WARM_LIGHT_INTENSITY_MUL).toBe(1.7825);
    expect(POSSESSED_HEAD_EMISSIVE).toBe(0x30124a);
    expect(POSSESSED_HEAD_COLOR).toBe(0x8c469f);
    expect(POSSESSED_COLOR).toBe(0x68347b);
    expect(HOSTILE_COLOR).toBe(0x7b1e1e);
    expect(PLAYER_COLOR).toBe(0x55a4f4);
    expect(PLAYER_HEAD_COLOR).toBe(0x91d1ff);
    expect(PLAYER_HEAD_EMISSIVE).toBe(0x122537);
    expect(MARKER_PALETTE.possessed.emissive).toBe(0x30124a);
    expect(MARKER_PALETTE.player.badge).toBe(0x91d1ff);
    expect(MARKER_PALETTE.player.emissive).toBe(0x1e4a6e);
    expect(MARKER_PALETTE.player.ring).toBe(0x4392f4);
    expect(MARKER_RING_OPACITY).toBe(0.6877);
    expect(muteBadgeY).toBe(2.3);
    expect(possessedBadgeY).toBe(2.3);
  });
});
